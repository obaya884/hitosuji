# <!-- TODO(spec-init): プロダクト名 --> 要求定義書

- 作成日: <!-- TODO(spec-init): YYYY-MM-DD -->
- 本書の責務: **何を実現したいか**（要求）の一覧と優先度。どう実現するかは[要件定義書](./要件定義書.md)以下に書く

> 書き方: 各節の引用ブロックは執筆ガイド。初版完成時に削除してよい。

## 1. 背景

> なぜこのプロダクトを作るのか。現状の課題・既存手段で足りない点を3〜5行で。

## 2. 目的

> このプロダクトで達成したいことを1〜2文で。

### 2.1 成功基準

> 「何がどうなったら成功か」を測定可能な形で2〜3個。

## 3. 対象ユーザーと利用シーン

> 誰が・いつ・どこで使うか。シングルユーザーならその旨を明記（設計判断の多くがここに依存する）。

## 4. 前提知識・ドメイン概念

> 実装者（AI を含む）が知らないと要求を誤読するドメイン知識をここに書く。用語は §11 用語集へ。
> このプロダクトが特定のメソッド・業務・既存システムを前提にするなら、その概要を人に説明できる粒度で。

## 5. 重視する価値

> 機能横断で優先する価値を3〜5個（例: 操作の即応性 > 機能の多さ、記録の正確さ > 入力の手軽さ）。
> 個別要求で判断に迷ったとき立ち返る基準になる。

## 6. 機能要求

> 採番規約: `F-XXX`。百の位で領域を分ける（F-1XX=領域A, F-2XX=領域B, …）。
> 表には**要求文と優先度のみ**を書く（実現方式は要件定義書、理由は決定ログへ）。
> 優先度は MoSCoW（Must / Should / Could / Won't）。

### 6.1 <!-- TODO(spec-init): 領域A -->

| ID | 要求 | 優先度 |
|---|---|---|
| F-101 | | |

## 7. 非機能要求

> 採番規約: `N-XX`。性能・可用性・データ保全・セキュリティ・コスト等。

| ID | 要求 | 優先度 |
|---|---|---|
| N-01 | | |

## 8. スコープ外（現時点）

> **やらないと決めたこと**を理由付きで列挙する（不変条件5「決めない決定も記録」）。
> 将来の退路（データ設計だけ維持する等）があればここに1行残す。

## 9. 制約・前提

> 予算・期間・技術・運用体制の制約。

## 10. 決定ログ

要求の新設・優先度変更・スコープ判断の経緯は [log_要求定義書.md](./log_要求定義書.md) へ日付順に積む（本節は参照スタブ）。

## 11. 用語集

| 用語 | 定義 |
|---|---|
| | |
