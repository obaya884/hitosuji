# <!-- TODO(spec-init): プロダクト名 --> 要件定義書

- 作成日: <!-- TODO(spec-init): YYYY-MM-DD -->
- 上位文書: [要求定義書](./11_要求定義書.md)
- 本書の責務: **何がどうなるか**（要件・方式・現況）。どう見えるか・どう持つかは画面定義書・データモデル定義書に書き、本書は要約＋参照に留める

> 書き方: 各節の引用ブロックは執筆ガイド。初版完成時に削除してよい。

## 1. 本書の位置づけ

> 詳細設計書を別途作るか・作らないかをここで宣言する（作らない場合: 本書＋詳細層文書を挙動の契約とし、実装の形はコードに委ねる、と書く。この判断は決定ログへ）。

## 2. システム方式

### 2.1 全体構成

> 構成図（ブラウザ→認証→アプリ→DB 等）を1つ。

### 2.2 技術スタック選定

> 採用技術と選定理由を表で。理由が長くなるなら決定ログへ。

### 2.3 リポジトリ・環境

> リポジトリ、ローカル/本番環境、デプロイ方式。

## 3. 開発の現況

> いまどの段階か（初期構築中／実運用中）を**散文で**2〜3行。個別要求の状態一覧はここに持たず、[要件バックログ](../案件/22_要件バックログ.md)が正。
> （教訓: 本書にフェーズ表＝要求ID一覧を持つと、優先度変更のたびに台帳と二重更新になり追随漏れが起きる。一覧は台帳側の1か所に寄せる）

## 4. 画面一覧

> 採番規約: `S-XX`。画面の詳細は `13_画面定義書/` へ。UI が無いプロダクトならこの節をインターフェース一覧（API/CLI）に読み替える。

| ID | 画面 | 対応する画面定義書 |
|---|---|---|
| S-01 | | |

## 5. 機能要件

> 要求（F-XXX）ごとに「何がどうなるか」を1行以上。画面の詳細・データの持ち方は書かず参照する。
> 複数画面・複数機能にまたがる**生成規則・状態遷移**（例: 割り込み時の挙動）は本書のこの節が正になる。

### 5.1 <!-- TODO(spec-init): 領域A -->（S-XX）

## 6. 非機能要件の実現方式

> N-XX ごとに実現方式を1〜2行で。

## 7. トレーサビリティ（要求 → 実現先）

> すべての F/N-XX が §5/§6 のどこかに現れることを保証する表。抜けの検出に使う。

| 要求 | 実現先 |
|---|---|
| F-101 | §5.1 |

## 8. 決定ログ

方式・要件レベルの判断の経緯は [log_12_要件定義書.md](./log_12_要件定義書.md) へ日付順に積む（本節は参照スタブ）。
