# CLAUDE.md — Hitosuji 開発ガイド

## プロジェクト概要

タスクシュート時間術を実践するためのシングルユーザー向けタスク管理Webアプリ。オーナー本人だけが使う。仕様の一次情報は `docs/` 配下のドキュメント群。

## 最重要ルール: 仕様変更は docs/ 反映が先

挙動の契約はドキュメント群にある。実装中に挙動レベルの仕様判断が発生した場合、**コードより先に該当ドキュメントを更新する**。

- 要求: `docs/仕様/11_要求定義書.md`（F-XXX / N-XX の一覧）
- 要件・方式・現況: `docs/仕様/12_要件定義書.md`
- データ構造・主要ロジック: `docs/仕様/14_データモデル定義書.md`
- 画面・操作・ショートカット: `docs/仕様/13_画面定義書/*.md`（全画面共通の UI は `00_共通.md`）
- 挙動を変えない技術活動（技術負債の返済・リファクタ・依存追随・CI/ツール整備）: `docs/案件/23_技術改善バックログ.md`（**T-XX** で管理。上の要求軸とは別軸。挙動が絡むなら要求軸、絡まないならこちら、で振り分ける）

docs のファイル名は**全体で一意な2桁番号**を持つ（0X=画面定義書内 / 1X=仕様 / 2X=案件 / 3X=検討。新規文書は該当ブロックの次番号を使う）。`log_` / `closed_` / `guide_` の付属ファイルは親の番号を引き継ぐ（例: `log_23_技術改善バックログ.md`）。役目を終えて凍結した独立文書には `archive_` を前置する（例: `archive_24_実装計画.md`。番号はその文書自身のもの。ライブ台帳と対で追記され続ける `closed_` とは区別する）。

未実装要求の案件管理（何を・どの状態で・いつ着手するか）は `docs/案件/22_要件バックログ.md`（プロダクト軸のライブ台帳。決定状態＋着手トリガ）が持つ。挙動レベルの仕様判断は上の各定義書へ先に反映し、バックログはチェックと一行要約に留める。MVP 期のフェーズ運用の記録は `docs/案件/archive_24_実装計画.md`（史実アーカイブ）。

実運用で得たオーナーの気づきは `docs/案件/21_ユーザーフィードバック.md` に起票してから対応する。**着手時は `docs/案件/guide_21_ユーザーフィードバック.md` に従い、要求定義書 → 要件定義書 → 画面定義書 の順に上から判定する**（FB は画面の話として書かれるため、画面定義書だけ直して上位文書への反映を忘れやすい。既存の F-XXX / N-XX に収まらない FB は要求定義書に新しいIDを起こす）。

### 書き方の規約（2026-07-21 の再整理で確定）

同じ挙動が複数文書に全文で書かれ、改訂のたびに食い違う事故が起きたため、次を守る。

- **詳細は下位文書に1か所だけ書く**。上位文書は1〜2行の要約＋参照リンクに留める（要求定義書 = 何を実現したいか / 要件定義書 = 何がどうなるか / 画面定義書・データモデル定義書 = どう見えるか・どう持つか）。同じ規則を2か所に書きたくなったら、それは下位に寄せるべき合図
- **判断の経緯（「なぜそうしたか」）は本文に書かず、各文書と同階層の `log_<文書名>.md` へ日付順に積む**（本文側の「決定ログ」節は §番号を保った参照スタブ。画面定義書は `docs/仕様/13_画面定義書/log_NN_*.md`）。本文は現在の仕様だけを書く。将来再検討しうる前提だけ本文に1行残してよい。決定ログを持たない文書は最初のエントリが出た時点で同階層に `log_<文書名>.md` を作る
- 完了した事項に `(Phase 2)` のような予定表記を残さない。開発の現況は要件定義書 §3、未実装要求の状態・トリガは要件バックログが持つ（フェーズによる管理は MVP 完了で終了。フェーズ語は実装計画＝史実アーカイブにのみ残る）
- **台帳（FB・技術改善・要件バックログ）の完了・対応済みエントリは、状態更新と同じコミットで同階層の `closed_<文書名>.md` へ原文のまま移す**（ライブ台帳には未完了だけを残し、AI が日常的に読むファイルを小さく保つ）

## 技術スタック

Next.js 16 (App Router) + TypeScript / Tailwind CSS 4 / Drizzle ORM + node-postgres / PostgreSQL（ローカル: Docker(OrbStack)、本番: Neon 無料枠）/ Vercel 無料枠 / Basic認証（`proxy.ts`）

ランタイム（Node）とDB（PostgreSQL）の版は**本番（Vercel / Neon）が決めた版に他をそろえる**。追随関係と「追い越さない」「Dependabot は最新しか出さないので中間版は手動」の規則は `docs/仕様/15_アーキテクチャ定義書.md` §11 が正。

## アーキテクチャ

クリーンアーキテクチャ＋関数型DDD。**コード配置・依存方向・テスト戦略の契約は `docs/仕様/15_アーキテクチャ定義書.md` が正**。要点:

```
[ブラウザ] → HTTPS + Basic認証 (src/proxy.ts) → [Next.js App Router] → Drizzle → [PostgreSQL]

依存方向: domain ← usecases ← { infrastructure, presentation }（ESLint で強制）
```

### ディレクトリと責務

- `src/domain/` — 純粋な業務ロジック（採番・導出・展開判定等）。I/O・`new Date()` 禁止、すべて引数で受け取る。クラス不使用（`Readonly` type＋純関数）。業務的失敗は `Result`（`domain/shared/result.ts`）
- `src/usecases/` — ユースケース（1操作=1関数）＋ `ports/` にリポジトリIF。SQL を書かない。domain との判別基準は「リポジトリ（I/O）に触るか」
- `src/infrastructure/db/` — Drizzle スキーマ（データモデル定義書 §3 と1:1）・`repositories/` に Port 実装・migrations・seed・`testing/`（テストヘルパー）。トランザクション境界はリポジトリメソッド内
- `src/app/` — 画面（Server Component は表示日1日分＋マスタのみ取得）＋ Server Actions（更新の入口。合成ルートとしてリポジトリ実装をユースケースへ注入する）
- `src/proxy.ts` — Basic認証（Next.js 16 の middleware 相当）

### 設計原則（実装のたびに効く判断基準）

- **楽観的更新**（N-01）: 操作は即UIに反映 → Server Action で永続化。失敗時はトースト＋ロールバック。打刻・追加・並び替えは体感0msを目標
- **タスク状態は打刻から導出**（状態カラムなし）: 未実行 = started_at IS NULL / 実行中 = started_at あり・ended_at NULL / 完了 = ended_at あり
- **導出できる値はカラムに持たない**: セクション終了時刻・表示順（tasks.sort_order 以外）は導出。迷ったらデータモデル定義書 §1 の方針に従う
- **sort_order は間隔採番**: task_date ごとに独立、1000刻み、途中挿入は前後の中間値（データモデル定義書 §3.5）
- **ルーチン展開はデイリー表示時にサーバで冪等INSERT**: `ON CONFLICT (routine_id, task_date) WHERE routine_id IS NOT NULL DO NOTHING`。当日以降の日付のみ展開（§4.1）
- **実行中タスクは全体で最大1件**（アプリ層で保証）。割り込み・中断・複製の生成規則は要件定義書 §5.1/§5.2 が正
- **打刻はクライアントの現在時刻を送信**（サーバ時刻を使わない）
- **マスタは物理削除せずアーカイブ**（過去タスクからの参照を保つ）
- **task_date は保存された論理日付**。打刻時刻から導出しない（日界の将来導入に備える）

## サブエージェント運用（マルチエージェント体制）

実装サイクルの各段階で以下へ委譲し、メインセッションのコンテキストを実装そのものに集中させる。読み取り中心・出力が長くなる作業を委譲し、**単独作業ではコードの編集は常にメインセッションが行う**（複数タスクを並行させる場合は worktree ごとのワーカーが自分の worktree 内で編集し、ヘッドは編集しない。下の「並行開発（worktree）」参照）。

| 段階 | 委譲先 | 内容 |
|---|---|---|
| 着手前の調査 | 組み込み `Explore` | 既存コードの把握・影響範囲の特定（結論だけ受け取る） |
| 実装方式の設計 | 組み込み `Plan` | 実装方針の立案・トレードオフ整理（＝設計担当） |
| 実装の一区切り | `verifier` | lint / build / マイグレーション / シード / 疎通の機械的検証（＝テスト検証担当） |
| 実装の一区切り | `code-quality-reviewer` | 可読性・重複・命名・簡潔さの品質レビュー（verifier と並列起動可） |
| docs の条項に触れたとき | `spec-reviewer` | docs/ 仕様書群との整合＋設計原則チェック |
| テストファイルに触れたとき | `test-reviewer` | テスト観点の網羅性・正しさ・カバレッジのレビュー（spec-reviewer と並列起動可） |
| オーナー動作確認の直前 | `browser-checker` | 実ブラウザで UI を測り、機械的に真偽が決まることだけを判定（幾何・スクロール・キーナビ等） |
| 定期・節目のリファクタ棚卸し | `refactor-scout` | コードベースをリファクタ観点で診断し T-XX 案を起票（記入はメイン） |

- **挙動レベルの仕様判断は委譲しない**。オーナーとの対話で決め、docs/ を先に更新する（最重要ルール参照）。`Plan` に委譲するのは実装方式レベルの設計のみ
- **実装は原則メインセッションが行う**（会話で決めた文脈を最も必要とする工程のため）。例外: 仕様がドキュメントで完結している自己完結的な `lib/` の純粋ロジックは、仕様条項を指定して `general-purpose` に実装委譲してよい
- `verifier`・`code-quality-reviewer`・`spec-reviewer` は独立なので**バックグラウンドで並列起動**し、結果を待つ間に次の作業を進めてよい
- 軽微な変更（typo・文言・1ファイルの小修正）ではエージェントを起動せず、メインセッションで直接 lint/build する
- レビュー・検証エージェントの指摘への対処（修正）はメインセッションが行い、必要なら同じエージェントに再検証させる。**これは単独モードの規定**——並行モードではワーカーが自分の worktree で起動・対処する（下の「並行開発」参照）
- `code-quality-reviewer` の品質観点、`spec-reviewer` の仕様整合、`test-reviewer` のテスト観点は責務が異なる（品質＝内部コードの読みやすさ・重複・簡潔さ／仕様整合＝docs との一致／テスト観点＝観点の網羅・正しさ・カバレッジ）

### 起動のコスト

サブエージェントは1本ごとに独立したリクエストを持ち、利用枠を実装と同じ財布から使う。2026-07-26 の実測では消費の **33% が `general-purpose` 単独**で、レビュー系5本の合計（11%）の3倍だった（[T-66](./docs/案件/23_技術改善バックログ.md#t-66)）。

- **読み取り中心の調査は `Explore` を既定にする**。`general-purpose` は全ツール持ちかつ `model` 未指定＝セッションモデルを継承するため、「調べるだけ」に使うと最も高くつく。`general-purpose` を使うのは**編集を伴う自己完結タスク**に限る（上の実装委譲の例外・worktree ワーカー）
- **機械的な作業を `general-purpose` に投げるときはモデルを明示する**（継承任せにしない）
- **レビュー系のモデル指定は変えない**（`verifier`・`spec-reviewer` = sonnet ／ `code-quality-reviewer`・`test-reviewer`・`refactor-scout` = opus）。落としても消費はほとんど動かず、とくに `test-reviewer` は**見逃しが「指摘なし」として返り、正常な合格と区別できない**ため落とさない
- **タスクの切れ目で文脈を切る**。消費の7割が 150k 超のコンテキストから出ている

### 並行開発（worktree）

既定は常に単独モード（メインセッションが実装）。**オーナーが並行実行を明示的に指示したとき（例「これとこれを並行で」）だけ**、**ヘッド1＋ワーカーN** の体制に切り替える——AI が自律的に切り替える判断基準は設けない（体制・運用手順は `docs/仕様/16_git運用と並行開発体制定義書.md` §2〜§5 が正）。要点:

- ヘッド（本体ワークツリーのセッション）はタスク分配・`npm run wt:new` / `wt:rm`・マージ順序の調停・共有 docs（台帳・log_\*）への記入を担い、**コードを編集しない**。共有 docs にワーカーは触れない
- ワーカーは 1タスク＝1 worktree（`../hitosuji-wt/<ブランチ名>`）で、worktree の内側では本ガイドの運用をそのまま実行する。仕様質問はヘッド経由でオーナーに上げる（docs 先行の最重要ルールは不変）
- **レビューの起動と指摘への対処はワーカーが行う**（T-70）。起動の判定は上の「起動の契機」を worktree 内でそのまま引く。ただし**見送りたい指摘はワーカーが自分で決めず**、「対処した」「見送りたい＋理由」を分けて報告し**オーナーが裁定する**（自己レビューの利益相反への歯止め）。`spec-reviewer` が docs の条項に関わる指摘を出したら docs を編集せずエスカレーションする。完了報告は生のレビュー本文を送らず、指摘件数と要旨・対処内容・見送り希望・再検証結果の4点に畳む（仕様/16 §2・§4）
- 並行時のレビューの正は **PR の diff**（ワーカーの編集はヘッドのチャットに出ないため）。オーナーの動作確認はヘッド側でブランチを checkout して行う

### 起動の契機

上の表の「段階」列は概観。実際に各サブエージェントを起動する条件は次のとおり。**レビュー4本の起動は「作業の種類」ではなく「差分が何に触れたか」で引く**——機能実装・FB 対応・T-XX の内部作業を区別しない（「これは機能ではないから」と外す余地を残さないため。T-48 で実際に test-reviewer を取りこぼした）。

- **着手前** — 影響範囲が読み切れない／既存パターンを把握したいとき: `Explore`（結論だけ受け取る）
- **実装方式で迷うとき** — トレードオフのある設計判断: `Plan`（挙動レベルの仕様判断は委譲せず、オーナーと対話して docs/ を先に更新する）
- **実装が一区切りしたら（コミット前）** — 差分がまとまった時点で `verifier` と `code-quality-reviewer` を並列でバックグラウンド起動する。前者は機械的検証、後者は品質レビュー。軽微な変更では起動しない（上の例外基準と同じ）
- **差分にテストファイル（`*.test.*`）が含まれるとき** — `test-reviewer`（テスト観点の網羅・正しさ・カバレッジ）を起動する。追加・削除・移動のいずれでも対象。**テストの段や方針を判断したとき**（既存テストを削らずに据え置く、段を移す、等）も含む
- **差分が docs の条項に触れる／条項で定まる挙動を実装したとき** — `spec-reviewer`（docs 整合）を起動する。docs だけ直して実装が追随していない、の検出も守備範囲
- **オーナーの動作確認の直前（下のフローの手順2の手前）** — 差分が**幾何・スクロール・ポップオーバー・キーナビ**に触れたときだけ `browser-checker` を起動する。`npm run dev:check` の確認専用環境で実測し、機械判定できる分を先に潰す。**知覚の判断（色・折り返し・見やすさ）は測れない**ので、これでオーナーの動作確認を省略しない（T-37）
- **リファクタ棚卸しをするとき** — ①オーナーが「リファクタしたい／コードが大きくなった」と言ったとき、②機能をいくつか重ねた節目、③新しい負債の兆候に気づいたとき、のいずれかで `refactor-scout` を起動し、返ってきた T-XX 案をオーナー確認のうえメインが `docs/案件/23_技術改善バックログ.md` へ記入する。定常フローには載せず、**契機が来たときに回す**（毎コミットでは起動しない）

### 実装完了後のフロー（レビュー → 動作確認 → コミット/push）

実装が一区切りしたら次の順序を必ず踏む（機能実装に限らない。T-XX の内部作業も同じ）。**サブエージェントのレビューとオーナーの動作確認の両方を経るまで「完了」扱いにしない。**

1. **サブエージェントによるレビュー**: `verifier`（lint/build/型/テスト等の機械的検証）・`code-quality-reviewer`（可読性・重複・命名・簡潔さの品質レビュー）は常に、`test-reviewer`（テスト観点の網羅・正しさ・カバレッジ）と `spec-reviewer`（docs/ 仕様書群との整合）は**上の「起動の契機」の条件に当たれば**並列起動し、指摘があればメインセッションで対処する。**起動するかどうかは差分で判定する**（`git diff --name-only` にテストファイルがあるか、docs の条項に触れたか）
2. **オーナーによる動作確認**: メインセッションがローカル環境（`docker compose up -d` ＋ `npm run dev`）を立ち上げ、**確認手順を提示する**。オーナーが実機で挙動を確認する。差分が幾何・スクロール・ポップオーバー・キーナビに触れていれば、この手前で `browser-checker` を回して機械判定できる分を潰しておく（上の「起動の契機」。**これは動作確認の代わりにはならない**）
3. **コミット / push**: 上記2つを通してからコミットする。**push は本番デプロイになるため、必ずオーナーの合図を待つ**（動作確認前に push しない）

軽微な変更（typo・文言・1ファイルの小修正）は 1・2 を省略してよい（エージェント起動の例外基準と同じ）。並行開発時はこのフローを PR 単位に読み替える（レビューの正は PR の diff、動作確認はヘッド側でブランチを checkout）。

## git 運用（ブランチ・マージ・デプロイ）

**詳細（命名の文字集合・マージキュー・ブランチ削除の作法・並行時の規律）は `docs/仕様/16_git運用と並行開発体制定義書.md` §1 が正**。常時使う判断は次の2つ:

- **リスク別ルーティング**: docs・軽微（typo・文言・挙動に触れない小修正）→ `main` 直コミット（push＝本番デプロイはオーナーの合図でまとめて可）／コード・挙動の変更 → ブランチ → PR → CI 緑 → 動作確認 → **合図でマージ**／スキーマ変更 → さらに「本番へ migrate 適用 → マージ」の順（「本番マイグレーションの手順」節）。迷ったらブランチ＋PR 側に倒す
- **ブランチ命名**: `<タスクID>-<slug>`（例 `fb-54-section-jump`）。**1ブランチ＝1PR、マージ後は削除**。自動マージ・ブランチ保護は設けず、push・マージは常にオーナーの合図

## 開発コマンド

- `docker compose up -d` — ローカルDB起動（開発用 :5432 とテスト用 db-test :5433。OrbStack が必要）
- `npm run dev` — 開発サーバ（http://localhost:3000）
- `npm run dev:check` — ブラウザ実機確認用の dev サーバ（http://localhost:3100）。確認専用DB（`hitosuji_check`。db-test インスタンス内）を作り直してフィクスチャを入れ、そこへ向けて起動する。**実データを持つ開発DBには接続しない**ので破壊的操作を試してよい（T-37。`browser-checker` が使う）
- `npm test` — 全テスト / `test:unit` ユニットのみ / `test:component` コンポーネントのみ（jsdom）/ `test:int` 統合のみ（要 db-test）/ `test:watch`
- `npm run db:generate` — スキーマ変更からマイグレーション生成
- `npm run db:migrate` — マイグレーション適用
- `npm run db:seed` — 初期データ投入（冪等）
- `npm run db:studio` — Drizzle Studio（DB閲覧）
- `npm run build` / `npm run lint`
- `npm run wt:new -- <ブランチ名>` — 並行作業用 worktree の一括セットアップ（`../hitosuji-wt/` 配下に worktree＋ブランチ＋`npm ci`＋専用テストDB＋`.env.worktree`＋dev サーバ用の固有ポートと `.env.local` リンク）。起点は `origin` を fetch して検査し、**進んでいれば中断**・**遅れていれば警告**する（意図的なら `--allow-unpushed`・fetch を省くなら `--no-fetch`）
- `npm run wt:rm -- <ブランチ名>` — worktree の後片付け（worktree・ローカルブランチ・テストDBの削除）
- `npm run wt:overlap` — 稼働中ブランチの変更ファイルが重なっていないかを機械判定する（重なりありなら終了コード 1。仕様/16 §1.3）
- `npm run ledger:move -- <FB-XX|T-XX>` — 台帳21・23 のエントリ（§一覧の行と §詳細の節）を完了記録へ移す（手編集で壊さないための道具。仕様/16 §3.4）
- `npm run ledger:check` — 台帳の表構造を検査する（列数・エントリの混入・テーブル内の空行・台帳21/23 の一覧と詳細の対応・台帳22 の熟度タグとトリガの記入）

## このリポジトリは public

`obaya884/hitosuji` は公開リポジトリ（2026-07-20〜）。**到達手段・実データ・資格情報につながる記述はコミットしない**。

- **本番URL（Vercel のデプロイURL・独自ドメイン）を書かない** — README・docs・コメント・コミットメッセージのいずれにも。Neon のホスト名（`ep-*.neon.tech`）も同様
- **本番の実データを書かない** — タスク名・ルーチン名などオーナーの生活が読み取れる内容は、docs の例示にも使わない（`docs/案件/21_ユーザーフィードバック.md` は特に混入しやすい）
- **資格情報は値も所在も書かない** — 値は当然、「どのファイルにあるか」以上の詳細も残さない
- 一度 push した内容は履歴から消せない。迷ったら書かない

## 環境変数（`.env.local` / Vercel）

- `DATABASE_URL` — Postgres 接続文字列
- `BASIC_AUTH_USER` / `BASIC_AUTH_PASSWORD` — 両方設定すると Basic認証が有効。未設定なら素通し（ローカル開発用）。**リポジトリにコミットしない**

**`.env.production` / `.env.production.local` は作らない。** `npm run build` は `NODE_ENV=production` で走るため Next.js がこれらを自動読み込みし、ローカルのビルドが本番DBを向く。本番向けの値を手元に置く場合は、Next.js の読み込み対象（`.env` / `.env.local` / `.env.development*` / `.env.production*` / `.env.test*`）を外した名前を使う（`.env.migrate` と同じ流儀）。`.gitignore` の `.env*` でどの名前も git からは外れる。worktree 用の `.env.worktree`（`wt:new` が生成し vitest だけが読む `TEST_DATABASE_URL` 1行）も同じ流儀。

### 本番資格情報の控え（`.env.credentials`）

Vercel の環境変数は Sensitive 指定のため `vercel env pull` では `[SENSITIVE]` としか取れず、**手元で失うとローテーション以外に復旧手段がない**。Basic認証の資格情報は `.env.credentials` に控える。

```
BASIC_AUTH_USER='...'
BASIC_AUTH_PASSWORD='...'
```

`.env.local` には置かないこと（`proxy.ts` の条件が満たされ、ローカル開発でも Basic認証が有効になる）。値を変更したら Vercel 側は `vercel env rm` → `vercel env add` の後に**再デプロイが必要**（既存デプロイには新しい値が反映されない）。

## 本番マイグレーションの手順

スキーマ変更を含むデプロイは、**必ず「本番へ適用 → マージ（＝デプロイ）」の順**で行う（マージ＝本番デプロイ。逆順だと新コードが存在しないテーブルを参照して本番が壊れる）。

- **具体的な手順（リモート推奨／ローカル）は [README](./README.md)「スキーマ更新（本番マイグレーション）」が正**。方式・設計の背景は [docs/検討/32_スキーマ更新パイプライン検討.md](./docs/検討/32_スキーマ更新パイプライン検討.md)（T-22）
- 接続文字列を**平文で残さない**: ローカル適用は `.env.migrate`（`DATABASE_URL='...'` の1行）経由で渡しコマンドライン引数に直接書かない、リモート適用は `db-migrate` Environment の unpooled シークレットを使う。`.env.production` 系を使わない理由は「環境変数」節を参照

## 規約

- **コードの変更は必ず Edit / Write ツールで行う**。python や sed 等のスクリプトによる一括置換は使わない（変更内容がチャットに差分として表示されず、オーナーがレビューできなくなるため）。複数箇所の変更でも1つずつ Edit する
  - **変異テスト（コードを壊してテストが落ちるか確かめる）も同じ**。メインセッションが Edit で1か所だけ壊し、確認後に Edit で戻す。`verifier` には委譲しない（変異後のコマンド実行だけを頼む）。git で退避しない——`stash` / `reset` は未コミットの作業を消す。[T-67](./docs/案件/23_技術改善バックログ.md#t-67)
- UI文言・ドキュメント・コミットメッセージは日本語
- コミットは Conventional Commits（`feat:` `fix:` `docs:` `chore:` 等）＋日本語サマリ
- **Claude Code が作るコミットには必ず `Co-Authored-By: Claude <モデル名> <noreply@anthropic.com>` のトレーラを付ける**（メッセージ末尾・1行空けて）。public リポジトリなので、どのコミットが AI との作業かを履歴から追えるようにしておく。オーナー自身が手で打つコミットには付けない
- UIは装飾を排したシンプルなテーブル型リスト（N-05）。コンポーネントライブラリは使わない。モーダルは最小限、インライン編集を基本とする
- キーボードショートカットは画面定義書01 §6 が正。修飾キーは Shift のみ、preventDefault は最小化
- マイグレーションは Drizzle Kit で管理し、本番適用はデプロイ前に手動実行
- テストは古典学派（モック原則不使用）・テストピラミッド。コロケーション配置、ユニット `*.test.ts` / 統合 `*.int.test.ts`。domain のテスト名には対応する仕様条項を書く。詳細はアーキテクチャ定義書 §8
