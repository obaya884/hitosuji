# ローカル開発: docker compose up -d で起動する Postgres
# 本番 (Vercel): Neon の接続文字列を設定する
DATABASE_URL=postgresql://hitosuji:hitosuji@localhost:5432/hitosuji

# Basic認証 (N-03)。両方設定すると認証が有効になる。ローカル開発では未設定で良い
# BASIC_AUTH_USER=
# BASIC_AUTH_PASSWORD=
