# 要件バックログ完了記録

- 作成日: 2026-07-25
- 目的: 着手・実装が完了した要求を保管する。ライブ台帳は[要件バックログ](./22_要件バックログ.md)（未着手の3レーンのみを持つ）
- エントリは完了への状態更新と同じコミットで本書へ移す（並びは完了日の新しい順）。個別の反映先は[FB管理簿](./21_ユーザーフィードバック.md)、判断の経緯は各定義書の決定ログ、実装の変遷は git が正
- MVP 期（Phase 0〜2）の完了は[実装計画](./archive_24_実装計画.md)が史実アーカイブとして保持する（本書は運用モード移行後の完了を積む）

## F-125 タスク・ルーチンの参照先 URL（2026-09-23）

- レーン: 確定・着手待ち → 完了。トリガ「着手中」（起票と同日にオーナーと仕様を確定し、そのまま着手。台帳には載せず直接着手した）
- 対応: タスクとルーチンに**参照先 URL を1件**持たせ、**開始打刻を伴う操作（開始・割り込み開始・複製して開始）で新しいタブに開く**。行にはリンクの印（コメント印の右・⭐の左）を出し、クリックで開くだけ（打刻しない）。編集は行メニュー「URL」から、コメント行と同じ外殻の1行入力（編集中だけ開く）。受け付けるのは `http(s)` のみで空は未設定、形式違いは確定不可＋エラートースト。URL は名前と同じ経路すべて（ルーチン展開・中断/割り込みの再開・複製/複製して開始・ルーチン化）で写す。ルーチンはフォームに URL 入力（名前・分類の次）、一覧は名前の右に印。**`routines.url` / `tasks.url`（NULL 可の text）の追加を伴うためスキーマ変更**（本番 migrate 先行）
- 反映先: 要求定義書 F-125（新設）・F-302 ／要件定義書 §5.1 F-125（新設）・§5.3 F-302・§7 ／データモデル定義書 §3.4・§3.5・§4.1・§4.2・§4.6 ／画面定義書01 §3.3「URL」「ハイライト」・O-2・O-11・O-14・**O-18（新設）**・§4.1・§6・§8 ／画面定義書02 §3・§4。実装は `domain/shared/url.ts` の `validateUrl`（タスク・ルーチン共通の唯一の入口）と `app/_lib/open-url.ts` の `openInNewTab` を起点に、印は `app/_components/open-url-button.tsx`、2段目の行の外殻は `(daily)/_components/detail-row.tsx`（コメント行と URL 入力行が共有）
- **開くのを永続化の前に置いた**: ブラウザはユーザー操作の同期処理の中でしか新しいタブを開かせない（ポップアップ抑止）ため、サーバ応答を待ってからでは開けない。打刻が拒まれてもタブは閉じない。経緯は[log_12](../仕様/log_12_要件定義書.md) 2026-09-22
- **引き継ぎに契機ごとの例外を設けなかった**: URL は「そのタスクの入口」で名前と同じくタスクの一部。`highlighted`（再開のみ）や `bundle_id`（複製では写さない）のような区別を持つ理由が無い。経緯は[log_14](../仕様/log_14_データモデル定義書.md) 2026-09-22
- マージ前レビューで直したもの: **「永続化を待たずに開く」が1本も固定されていなかった**（応答後に開く変異で全緑）→ `hold` で解決前に `window.open` を見るテストを追加し変異で確認／URL 編集のロールバック・blur 確定・下線の譲り・中断/取り消しで開かないこと・ツールチップが未検査／コメント行と URL 入力行が同時に出るときの下線（コメント行が譲る）／2段目の行の外殻が2ファイルに丸写し（`DetailRow` へ引き上げ）／`window.open` の偽物が3流派（`_testing/window-open.ts` に一本化）／要件定義書・画面定義書01 が検証規則をデータモデル定義書と重ねて全文で持っていた（参照へ寄せた）

## F-123 取り残したタスクを今日へ移動（2026-09-21）

- レーン: 確定・着手待ち → 完了。トリガ「着手中」（起票と同日にオーナーと仕様を確定し、そのまま着手）。[FB-98](./closed_21_ユーザーフィードバック.md#fb-98)（未来日に積んだタスクを今日へ引き寄せる手段が無い）の対応を兼ねる
- 対応: 行メニューの日付移動（O-7）を「先送り」から一般化し、**行き先を表示日で決める**——今日を表示中なら翌日へ（先送り F-107）、そうでなければ今日へ引き寄せる（F-123）。**項目は1つのままで文言だけが `翌日へ先送り` / `今日へ移動` と切り替わる**（両方を並べて片方を非活性にはしない）。`postponed_count` は**行き先が元の日より後のときだけ +1**（過去日→今日は加算、未来日→今日は据え置き。減算はしない）。`initial_task_date` は動かさないので、引き寄せた行にも持ち越し（F-122）がそのまま出る。ルーチン由来の扱い（紐付けを外し元の日のスキップを記録）と移動先の置き場（移動先セクションの末尾）は従来どおりで、今日へ引き寄せた行は次の表示で F-113 規則 b が現在位置の直後へ繰り下げる。**スキーマ変更なし**
- 反映先: 要求定義書 F-123（新設）／要件定義書 §5.1 F-122・F-123・§5.3 F-301・§5.5・§5.6・§7 ／データモデル定義書 §3.5・§3.6・§4.8（「先送り」の記述を「日付移動」へ一般化）／画面定義書01 O-7（一般化）・**O-9 を「表示日の移動」へ改称**・§3.3・§5・§6・§7 ／画面定義書04 §3.4・§5。実装は `domain/task/date-move.ts` の `planDateMove`（行き先＋加算可否）を起点に、`postponeTask` → `moveTaskDate`、ポートの `postpone` → `moveToDate`（`countsAsPostpone` を受け、加算しないときは列に触れない）、Server Action は `moveTaskDateAction(id, now)` でサーバが日界を踏まえて今日を解く
- **`postponed_count` の意味を「先送りされた回数」から「後ろへ動かされた回数」へ改めた**: 同じ操作が前にも後ろにも動くようになったため、操作名では列の意味が定まらなくなった。押し戻した回数は別の事実なので同じ列に混ぜず、減算もしない（起きた事実を消さない）。日数ではなく回数のままにしたのは、引きずった長さを `initial_task_date`（F-122）が持つため。経緯は[log_14](../仕様/log_14_データモデル定義書.md) 2026-09-21
- **F-107 を一般化せず新しい要求 ID を起こした**: F-107 は Must で「先送りゼロ」の実践に直結する要求であり、意味を広げるとその Must が何を指すのか読めなくなる。実現したい価値も逆向き（送る／引き戻す）。UI としては1つの操作に畳むが、要求の層では分ける（[log_11](../仕様/log_11_要求定義書.md) 2026-09-21）
- マージ前レビューで直したもの: **O-9 の名称が既に「日付移動」だった**（同じ表に同名が2つ）／要件定義書3か所が「先送り」のまま実態より狭かった（F-301 のスキップ記録・§5.6 のバンドル伝播・F-122 の「差が 0」→「0 以下」）／**テストの穴2件**——盤面から行への `isToday` の配線が無検査（`true` 固定でも全緑）、送る現在時刻が `expect.any(Date)` で緩い（`new Date(0)` でも緑）／操作の union 型が3ファイルに重複（`RowOperation` に一本化）

## F-122 先送りの持ち越し表示（2026-09-21）

- レーン: 確定・着手待ち → 完了。トリガ「着手中」（起票と同日にオーナーと仕様を確定し、そのまま着手）
- 対応: 先送りで他日へ移ったタスクに `3日持ち越し（9/18から）` を弱色で出す。日数は `task_date − initial_task_date` で導出し、差が 0 以下の行には何も出さない。デイリー（タスク名の直後・セクション併記の左）とレビュー（実績ログ・先送り内訳）の両方に出す。**`tasks.initial_task_date`（生成時に `task_date` と同値を入れ、移動では変えない）の追加を伴うためスキーマ変更**（本番 migrate 先行）
- 反映先: 要求定義書 F-122（新設）／要件定義書 §5.1・§7 ／データモデル定義書 §3.5 ／画面定義書01 §3.3 ／画面定義書04 §3.3・§3.4。実装は `domain/task/task.ts` の `carryOverDays` と `_lib/format.ts` の `formatCarryOver` を起点に、列の充填は `repositories/new-task-row.ts` の `withInitialTaskDate` が全生成経路をまとめて担う
- **導出できるのに列を持った**: 先送りが翌日のみのあいだは `task_date − postponed_count` で求まるが、今日へ引き寄せる操作（F-123）が入ると等式が壊れ、壊れてからでは backfill できない。`created_at` でも代用できない（ルーチン展開は前日以前に生成されうる）。経緯は[log_14](../仕様/log_14_データモデル定義書.md) 2026-09-21
- マージ前レビューで直したもの: コード内コメントの `§` 参照に文書名の前置が無かった2件／`postpone` のテストがフィクスチャの同値により不変性を実証できていなかった（変異を入れても緑だった）

## F-119 関連ルーチンのバンドル（2026-08-10）

- レーン: 確定・着手待ち → 完了。トリガ「着手枠が空き次第」（起票時の熟度「設計済」＝[31_バンドル検討.md](../検討/31_バンドル検討.md) に方式があり、要件・データモデル・画面への展開を着手時に行った）
- 対応: 関連ルーチンを**バンドル**として束ね、展開された当日タスクをデイリー上で**行の最左端の縦帯（道）**として見せる。縦に隣り合う同一バンドルの行では帯がつながり、非メンバーが割り込むと途切れる。束ねの最中に非メンバーを開始すると「「〈バンドル名〉」が途中です（残り N 件）」を情報トーストで知らせる。**バンドルはスケジュールも順序も持たない集合モデルで、完全にプレゼンテーション層のオーバーレイ**——並び・採番・終了予定・残り時間・進捗の導出はいずれもバンドルを知らない。**`bundles` テーブルと `routines.bundle_id` / `tasks.bundle_id` の追加を伴うためスキーマ変更**（本番 migrate 先行）
- 反映先: 要求定義書 F-119・用語集 ／要件定義書 §4（S-05 追加）・**§5.6（新設）**・§6 N-11・§7 ／データモデル定義書 §2・§3.4・§3.5・**§3.7（新設）**・§4.1・§4.2・**§4.8（新設）** ／**画面定義書[05_バンドル管理](../仕様/13_画面定義書/05_バンドル管理.md)（新設）** ／画面定義書00 §1・§2.2・§4.1 ／01 §3.3・§4.1・**§4.4（新設）** ／02 §3・§3.1・§4 ／アーキテクチャ定義書 §2（裸の `§` の既定）・§5（集約の単位）
- **バンドルの管理を独立画面（S-05）にした**: マスタ管理のタブにも、ルーチン管理のタブにもしない。セクション・モード・プロジェクトが**タスクに貼るラベル**なのに対しバンドルは**複数のものを束ねる編成**で、S-03 の「シンプルなCRUDに徹する」性格と揃わない。ルーチンの下に置かなかったのは、バンドルの実体が「一緒に流れるタスク群」の識別子で、**`bundle_id` を付ける供給元がルーチン以外にも増えうる**ため（判断の骨子と今後の見取り図は[31_バンドル検討.md](../検討/31_バンドル検討.md) §7）
- **メンバーシップの伝播**（データモデル定義書 §4.8）: 展開で写す／中断・割り込みの再開タスクは引き継ぐ（外すとバンドルが永遠に完了しない）／複製・複製して開始は引き継がない（複製で未完了メンバーが増えると警告が鳴り続ける）／先送りは外す（`routine_id` を外す扱いと揃える）
- **割り込みの検知は状態を持たない**: 開始のたびに打刻の時系列から3条件を導出する。「一度離れると黙り、メンバーを開始し直すと再武装する」はフラグを持たずに条件から自然に成り立つ
- 見送った枝: **繰り返しが揃っていないメンバーへの印（⚠）**は詰めたうえで初期スコープから落とし、[31_バンドル検討.md](../検討/31_バンドル検討.md) §7.4 に再検討の契機とともに保留。**アドホック束ね（当日タスクを手で束ねる）**はオーナー判断で「やらない見込み」へ
- マージ前レビューで直したもの: テストの穴3件（`updateBundle` / `setBundleArchived` の成功経路が無検査・**割り込みでの警告テストが皆無**・アーカイブ済み除外のテストが自分でフィルタしていた）、保存境界の分裂、条項と実装の食い違い5件。経緯は各定義書の決定ログ（2026-08-10）

## F-118 その日注力したいタスクのハイライト（2026-08-01）

- レーン: 確定・着手待ち → 完了。トリガ「着手枠が空き次第」（起票時の熟度「未詰め」＝要求文しかなく、着手時にオーナーとの対話で仕様を確定した）
- 対応: その日注力するタスクに⭐を付けて強調する。**タスク1件ごとの手動トグルで本数の上限はなく、完了しても外れず、表示日も問わない**（過去日・未来日でも付け外しできる）。引き継ぎは「同じ仕事の続きには引き継ぎ、別の実施には引き継がない」——中断・割り込みの再開タスクは引き継ぎ、複製・複製して開始は引き継がない。先送りは同じ行が翌日へ移るので一緒に移り、ルーチン展開は `DEFAULT false` により常に OFF。デイリーは**淡黄の地色（新トークン `highlight`）＋タスク名セルのコメント印の右に置く⭐**（`H` / ⭐のクリック / 行メニューでトグル）で、**選択中と完了行では地色を出さず⭐が印を担う**。レビューは**タスク名の先頭に塗り⭐のみ**（読み取り専用）。**`tasks.highlighted` の追加を伴うためスキーマ変更**（本番 migrate 先行）
- 反映先: 要件定義書 §5.1・§7 ／データモデル定義書 §3.5・§4.2・§4.6 ／画面定義書01 §3.3・O-11・O-17・§6 ／画面定義書04 §3.3（要求定義書 F-118 は文言のまま）。実装は `domain/task/punch.ts` の `resumeTaskDraft` が引き継ぎ規則を持ち、`setTaskHighlight` → リポジトリの `updateHighlight` → Server Action → 楽観的更新まで既存の編集と同じ経路に乗せた。行の地色は `_lib/row-background.ts` に1本化してタスク行とコメント行で共有する
- 動作確認で覆した判断: ⭐の置き場所（セル右端の縦一列 → コメント印の右）、OFF の輪郭の出し方（ホバー＋選択 → 選択のみ・場所も空けない）、レビューでの位置（名前の右端 → 先頭）。経緯は画面定義書01・04 の決定ログ（2026-08-01）

## F-206 タスクへの一言コメント（2026-08-01）

- レーン: 確定・着手待ち → 完了。トリガ「着手枠が空き次第」（熟度「列済」＝`comment` 列は用意済みで UI のみ未実装だった）
- 対応: タスクに任意長のコメントを1つ持たせる。**デイリーで書き、レビューで読み返す**（用途は「実施の記録・気づき」とオーナーが確定）。コメントのある行にだけ印を出し、全文は選択行で行の下に開く。入力欄は同じ位置に開き（読む場所と書く場所を一致させる）、`Enter` 確定 / `Shift+Enter` 改行 / `Esc` 取消 / フォーカスアウトで確定。空で確定すれば消える。楽観的更新の対象。**`comment` 列は既存のためマイグレーション不要**
- 反映先: 要求定義書 F-206（文言改訂）／要件定義書 §5.2・§5.5 ／データモデル定義書 §3.5 ／画面定義書01 §3.3・O-16・§6 ／画面定義書04 §3.3。実装は `domain/task/edit.ts` の `normalizeComment`（trim・空は null）を起点に、`updateTaskComment` → リポジトリの `updateComment` → Server Action → 楽観的更新まで既存の編集と同じ経路に乗せた。表示は新規の `_components/comment-row.tsx`（判定 `showsCommentRow` は `_lib/editing.ts`）
- **F-207（長文ノート）を欠番にした**: 長さの制限を設けないと決めた時点で、長文かどうかで機能を分ける根拠が消えたため（旧 F-304 がスキップを F-301 へ吸収したのと同じ形。経緯は[要求定義書の決定ログ](../仕様/log_11_要求定義書.md)）
- **ショートカットを付け替えた**: `C` をコメント編集（Comment）に譲り、現在地ジャンプを `N`（Now）へ。`Shift+C` の予約は解除。`N` は FB-36 で `N`(New) → `A`(Add) へ移して以来の未使用キー
- 補足: 折り返し幅は**タスク名列に揃える**（行いっぱいに広げない）。オーナーの動作確認で「プロジェクト列の手前で折り返すイメージ」と指摘を受けた結果で、行いっぱいだとコメントの長さで右側の列の見え方が変わる。レビューも同じ規則に揃えたが、列構成が違うため実測の幅は一致しない（判断の経緯は[画面定義書01 の決定ログ](../仕様/13_画面定義書/log_01_デイリーリスト.md)）

## F-211 終了打刻後の選択送り（2026-08-01）

- レーン: 確定・着手待ち → 完了。トリガ「着手枠が空き次第」（本体は 2026-07-24 に実装済みで、失敗経路のみ残っていた）
- 対応: 終了打刻の選択送りを楽観的更新のロールバック対象に含める。`punch()` が `setSelectedId` を `run()` の外へ同期で置いていたため、サーバが拒むと行だけ実行中へ巻き戻り選択は次のタスクへ進んだままだった。送り先を決めて即座に選択を送るのは変えず（体感0ms）、`finishTaskAction` の結果が失敗なら送る前の選択へ戻す
- 反映先: 画面定義書01 §5（2026-07-27 に条項化済み。今回は実装の追随のみで docs の変更なし）／要件 N-01。実装は `daily-board.tsx` から切り出した `finish()`。回帰テストは `daily-board.test.tsx` に5本（失敗時のロールバック・第3の行を選んでいた場合・`Enter` 経路・確定待ちの選び直し・成功時に送り先へ残ること）を追加した
- 補足: §5 の「元の行」は**打刻した行**とした（行が実行中へ巻き戻る先と揃える）。打刻ボタンは `onSelect` → `onPunch` の順に呼ぶため、別の行を選んだまま ■ を押すと「打刻前の選択」は第3の行になり、そこへ戻すと選択だけ離れた場所へ飛ぶ。ロールバックは**送った選択がそのままのときだけ**行い（`setSelectedId` の関数形更新で現在値と送り先を照合）、確定を待つ間にユーザーが選び直していたらその操作を優先する。送り先が無い（`next === null`）ときは選択を送っていないので、この照合により自動的に no-op になる（専用分岐を持たせない。FB-78 と同じ流儀）

## F-121 現在セクションの見出し強調（2026-07-25）

- レーン: 確定・着手待ち → 完了。トリガ「着手枠が空き次第」（FB-52 で仕様確定済み）
- 対応: 現在時刻が属するセクションの見出し行だけ背景を新トークン `band-now`（`#ead9d0`。`band` を暖色側へ一段濃くした粘土寄り）にする。表示日が今日かつ現在時刻が時間帯内のときだけで、未分類とアーカイブ済みは対象外。新しい state・タイマーは持たず既存の `useNow` の毎分再描画に乗る
- 反映先: 要件定義書 F-121 / 画面定義書01 §3.2。実装は判定を `domain/section/section.ts` の `currentSectionId`（`isToday` ゲート込みで `sectionAt` をラップした純関数）へ切り出し、`daily-list.tsx` は1回だけ算出して各見出しに渡す
- 残課題: 帯の上でプログレスバーのトラック（`bg-line`）が見えにくくなる副作用は許容し、FB-54 として分離

## F-212 完了タスクを未実行へ戻す（2026-07-25）

- レーン: 確定・着手待ち → 完了。トリガ「着手枠が空き次第」（FB-41 で仕様確定済み）
- 対応: 完了タスクの `started_at`・`ended_at` をともにクリアして未実行へ戻す（`U`／完了を選択中）。表示日が今日なら現在位置（他に実行中タスクがあればその直後）へ並べ直し、今日でなければ打刻のクリアだけ。実績を破棄するため Undoトースト（5秒）で完了状態へ復帰でき、保留は削除（F-115）と共通の1スロット。スキーマ変更なし
- 反映先: 要件定義書 F-212 / 画面定義書01 O-15・O-13・O-8 / データモデル定義書 §4.7。実装は `relocationOnUndoStart` を `relocationOnUndoPunch` へ一般化して「現在位置」判定を `planCarryOver` と共有し、リポジトリ側は `writePunch` に統一して `updatePunch`・`undoStart`・`undoComplete` を薄いラッパ化

## F-120 未実行タスクの予想開始時刻（2026-07-25）

- レーン: 確定・着手待ち → 完了。トリガ「着手枠が空き次第」（FB-47 で仕様確定済み）
- 対応: 未実行行の実施時間列に予想開始時刻を `HH:MM–` 弱色で併記（列は増やさない）。値は終了予定（F-104）と同じ積み上げの途中経過で、現在時刻＋実行中タスクの残り＋前方の未実行タスクの見積もり和。セクションをまたいでもリセットせず、表示日が今日のときだけ出す。24:00 超過は論理日基準の折返し（`25:30–`）だが行単位では警告色にしない
- 反映先: 要件定義書 F-120 / 画面定義書01 §3.3 / データモデル定義書 §4.3。実装は `domain/task/projection.ts` に `projectedStartTimes` / `formatProjectedStart` を追加し、F-104 の `remainingMinutes` / `formatProjectedEnd` と `unfinishedMinutesOf` / `logicalClock` を共有

## F-117 日付表示クリック・`G` でカレンダー（datepicker）ジャンプ（2026-07-25）

- レーン: 候補・見極め中 → 完了。トリガ「遠距離の日付移動が煩わしくなったら」が実運用で顕在化（FB-39 起票）
- 対応: 日付表示クリック、またはショートカット `G`（Go to date）で月グリッドのカレンダーを開き、離れた日付へジャンプ。週の起点は月曜、月移動は前月/翌月のみ、カレンダー内は矢印/HJKL＋Enter/Esc で操作。前後日ナビ（F-106）の補完
- 反映先: 要件定義書 F-117 / 画面定義書01 §3.1・§6。実装はカレンダーグリッドを純関数 `domain/shared/month-grid.ts` に切り出し（FB-39）
