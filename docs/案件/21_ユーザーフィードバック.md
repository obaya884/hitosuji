# ユーザーフィードバック管理簿

- 作成日: 2026-07-20
- 目的: 実運用で得たオーナーの気づきを一元管理し、今後の開発の入力にする

## 運用ルール

1. 気づきはまず本書に**起票**する（粒度はバラバラで良い。会話で出たものは AI が転記する）
2. 起票時に**分類**する: `不具合` / `改善（軽微）` / `改善（要仕様整理）` / `要求（優先度変更含む）` / `開発環境`
3. 対応するときは通常の開発ルールに従う — **挙動レベルの変更は該当仕様書を先に更新**し（[guide_21_ユーザーフィードバック.md](./guide_21_ユーザーフィードバック.md)）、本書には対応結果と参照先だけを残す
4. 状態は `未対応` / `対応中` / `対応済み` / `見送り` の4つ。対応済み・見送りには日付と理由（または反映先）を書く
5. **対応済み・見送りになったエントリは、状態更新と同じコミットで[closed_21_ユーザーフィードバック.md](./closed_21_ユーザーフィードバック.md)へ移す**（本書には未対応・対応中だけを残す）

## 一覧

### 不具合

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

### 改善（軽微 — 仕様判断がほぼ不要で、画面定義書の更新＋実装で完結する）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

### 改善（要仕様整理 — 挙動・デザインの方針をオーナーと決めてから docs 反映 → 実装）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-48 | 2026-07-25 | デイリーのタスクリストで、プロジェクトをタスク名の横ではなく、モード（既存の列表示）のように独立した分かりやすい列で表示したい。現状はタスク名に併記されていて視認しづらい。列の位置（モード列との並び）・幅・未設定タスクの見え方・アーカイブ済みプロジェクトの扱い、テーブル型リスト（N-05）の範囲で列を増やす影響は着手時に詰める | 対応中 2026-07-25 → 要件定義書 F-402 / 画面定義書01 §2・§3.3。オーナー確定: 位置=タスク名の直後（モードの左）、未設定=薄色の `-`（**モード列の未設定も同じ表記に揃える**）、セクションの併記はタスク名セルに残す、幅=モードと同程度の固定幅＋切り詰め、アーカイブ済みプロジェクトも名前をそのまま表示。既存要求 F-402 の表示詳細のため要求定義書は変更なし。仕様確定済み・実装待ち |
| FB-51 | 2026-07-25 | セクション選択ポップオーバー（`S` / O-5）に「現在のセクションへ」を追加したい。現状は候補がセクション名の一覧だけで、いま自分がどの時間帯にいるかを頭で照合してから選ぶ必要がある。詰める点: 「現在」の定義（現在時刻を含むセクション／実行中タスクが属するセクション。§4.2 の「現在位置」と揃えるか）、候補一覧のどこに置くか（先頭固定か）と実セクション候補との重複の見せ方、表示日が今日でないときの扱い（残り時間 §3.2 と同じく出さないか）、移動先セクション内での挿入位置（末尾か・現在位置か）、`S` 以外（`Shift+J/K` の O-6）との役割分担。既存要求に収まらなければ要求定義書に新IDを起こす | 対応中 2026-07-25 → 要件定義書 F-112 / 画面定義書01 O-5・§4.3。オーナー確定: 見せ方=候補先頭に固定の特別項目（区切り線あり・実候補には印を付けない）、「現在」=現在時刻を含むセクション（§4.2 の現在位置とは揃えない）、挿入位置=移動先セクション末尾（現行どおり）、表示日が今日でなければ出さない。既存要求 F-112 の操作詳細のため要求定義書は変更なし（FB-46 と同じ扱い）。仕様確定済み・実装待ち |
| FB-52 | 2026-07-25 | デイリーのセクション見出し行（§3.2）で、現在時刻が属するセクションのヘッダー色を変えて一目で分かるようにしたい。詰める点: 強調に使う色（デザイン言語「紺の時間簿」のトークンから選ぶ。素の色は使わない）と強調の手段（背景／文字色／罫線）、「現在のセクション」の判定（残り時間 F-110 と同じく**表示日が今日**かつ現在時刻がそのセクションの時間帯内、の規律に揃えるか）、日界をまたぐ回転順（F-116）での判定、未分類グループは対象外でよいか、現在時刻の経過で強調が移る際の再描画契機 | 対応中 2026-07-25 → 要求定義書 F-121 / 要件定義書 F-121 / 画面定義書01 §3.2 / 要件バックログ（確定・着手待ち）。オーナー確定: 手段=背景色。ただし `accent-weak` は選択行と同色になるため流用せず、**一段濃い新トークン `band-now` を起こす**（開始値 `#dde5f2` 提案・実機で調整可）。判定=表示日が今日かつ現在時刻が時間帯内、未分類とアーカイブ済みセクションは対象外、再描画は既存の毎分更新に乗る。仕様確定済み・実装待ち |
| FB-53 | 2026-07-25 | ルーチンの週次に「平日」（月〜金）を追加したい。現状は曜日チェックボックスを5つ手で選ぶ必要がある。対象は**表示・追加・編集の3か所**（一覧の繰り返し要約表示＝画面定義書02 §3、追加フォーム、編集フォーム）。詰める点: プリセット（ボタンで月〜金を一括チェック）にするか独立した選択肢にするか、独立させる場合 `weekdays` ビットマスク（データモデル定義書 §3）を維持できるか＝列追加が要るか、要約表示で「週次(平日)」と出す条件（月〜金がちょうど全選択のときのみか。土日も含む場合の表記）、週間隔との組み合わせ表記（「隔週(平日)」等）、「土日」等の他プリセットまで広げるか。データモデルに触れずプリセットで済むなら軽微に落ちる見込み | 対応中 2026-07-25 → 画面定義書02 §3・§4 / 画面定義書01 §4.1。オーナー確定: 方式=プリセットボタン（繰り返し種別は増やさない）、プリセット=「平日」「土日」の2つ、要約表示=曜日がちょうど一致するときだけ「週次(平日)」「隔週(平日)」「週次(土日)」に置き換え（余分な曜日を含む場合は従来どおり列挙）。**データモデル定義書は無変更**（`weekdays` ビットマスクのまま・列追加なし）。挙動そのものは変わらない入力補助＋表記のため既存要求 F-301 の範囲内で、要求定義書・要件定義書とも変更なし。曜日チェックが出る場所には同じ補助を置く方針とし、デイリーのルーチン化ポップオーバーも対象に含めた（FB 本文の3か所＋1か所）。仕様確定済み・実装待ち |

### 要求（新規・優先度変更）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

### 開発環境

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-49 | 2026-07-25 | リポジトリのテストカバレッジをオーナーが簡単に確認できるようにしたい。現状は `npm run test:coverage` を手元で叩く必要があり、日常の開発や GitHub 上で数値を把握しづらい。実現方式（README バッジ／CI のジョブサマリ出力／アーティファクト保存／外部サービス連携等）と、public リポジトリでカバレッジ数値を公開してよいか（到達手段・実データにはつながらないが方針判断は要る）は着手時に詰める。挙動を変えないツール整備のため、方式が固まれば対応は技術改善バックログ（T-XX）へ流す | 起票（未着手） |

## 完了記録

対応済み・見送りの FB は[closed_21_ユーザーフィードバック.md](./closed_21_ユーザーフィードバック.md)に保管する。
