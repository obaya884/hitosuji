# ユーザーフィードバック管理簿

- 作成日: 2026-07-20
- 目的: 実運用で得たオーナーの気づきを一元管理し、今後の開発の入力にする

## 運用ルール

1. 気づきはまず本書に**起票**する（粒度はバラバラで良い。会話で出たものは AI が転記する）
2. 起票時に**分類**する: `不具合` / `改善（軽微）` / `改善（要仕様整理）` / `要求（優先度変更含む）` / `開発環境`
3. 対応するときは通常の開発ルールに従う — **挙動レベルの変更は該当仕様書を先に更新**し（[guide_21_ユーザーフィードバック.md](./guide_21_ユーザーフィードバック.md)）、本書には対応結果と参照先だけを残す
4. 状態は `未対応` / `対応中` / `対応済み` / `見送り` の4つ。対応済み・見送りには日付と理由（または反映先）を書く
5. **対応済み・見送りになったエントリは、状態更新と同じコミットで[closed_21_ユーザーフィードバック.md](./closed_21_ユーザーフィードバック.md)へ移す**（本書には未対応・対応中だけを残す）

## 一覧

### 不具合

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-68 | 2026-07-26 | **未打刻タスクで `B`／`F` を押すとキーボード操作がデッドロックする**。`use-daily-shortcuts.ts` の `case "b"/"f"` は打刻状態を見ずに `editing` を立てるが、`daily-list.tsx` の時刻入力欄は `task.startedAt !== null &&` の内側にあり**未打刻タスクでは描画されない**。一方フックは `if (editing !== null) return;` で**全ショートカットを止める**（画面定義書01 §6 / 00_共通 §3）。`editing` を解除できるのは `onEndEdit`（入力欄の blur・Esc かポップオーバーの `onClose`）だけなので、**発火源が存在しないまま無効状態が固定される**——`Esc` すら効かず、マウスで別のセルを開いて閉じるか再読み込みするまでキーボードが戻らない。T-39 第2バッチ②のテスト作成時に発見（レビューでの「`initialValue` の `""` 側は到達可能では」という指摘をワーカーが検算し、到達不能＝**死んだ枝がガードの裏返しとして生じている**ことから判明）。対応方針: `requestEdit("startedAt"/"endedAt")` の側で打刻の有無を見て、無ければ `editing` を立てない（あわせて `daily-list.tsx` の `initialValue` の到達不能分岐2つも消える）。**[FB-58](#)（`F` を実行中タスクで押すと空欄の終了時刻欄が開く）と修正箇所が同じ**なので一緒に決めて一緒に直すのが早い——ただし FB-58 は「入力欄は出るので Esc で戻れる」＝仕様の穴、本件は「戻れない」＝不具合、と害の大きさが違う | 未対応 |
| FB-64 | 2026-07-26 | **通信断で Server Action が reject したとき、トーストもロールバックも出ない**。[画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §8 は「永続化失敗（ネットワーク等）→ トースト『保存に失敗しました』＋UIロールバック」と定めるが、`daily-board.tsx` の `run` / `runSelectingCreated` / `operate` / `routinize` のいずれにも `try/catch` が無く、**Promise の拒否は transition の外へ抜ける**。実装が扱っているのは `ActionResult` が `{ ok: false }` を返す業務的失敗だけで、通信断・タイムアウト・サーバ 500 の経路は素通り。オフラインで打刻すると**画面上は成功したように見えたまま保存されていない**状態になりうる（N-01 の楽観的更新は「失敗したら戻す」が対で初めて成立する）。T-39 第2バッチ③のテスト作成時に発見——`{ok:false}` の失敗は12件のテストで固定できたが、reject 経路は実装が無いため書けなかった。**同型がマスタ管理にもある**——`masters/_lib/use-master-action.ts` も `startTransition` 内で `await action()` するだけで catch が無く、reject すると unhandled rejection になり `isPending` の戻りとエラー表示が不定になる（こちらは楽観的更新をしないぶんロールバックは不要だが、失敗が何も表示されない点は同じ）。対応時は共通の失敗処理を通す形（各画面の実行関数に集約するか、`ActionResult` へ落とす薄いラッパを噛ませるか）を決めてから docs 反映。**[T-42](./23_技術改善バックログ.md#t-42-server-action-のエラーメッセージ辞書が型で閉じていない)（未知のエラーコードでメッセージが `undefined` になる）と同じ「失敗がユーザーに伝わらない」系で、どちらも `actions.ts` 周辺を触るため一緒に着手すると効率的** | 未対応 |

### 改善（軽微 — 仕様判断がほぼ不要で、画面定義書の更新＋実装で完結する）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-60 | 2026-07-26 | **`Enter`（打刻）にボタンフォーカス時の除外があるのに条項へ書かれていない**。実装は `target.tagName === "BUTTON"` のとき打刻しない（打刻ボタンをクリックした直後に `Enter` が二重発火するのを防ぐため。根拠はコードコメントだけ）。[共通](../仕様/13_画面定義書/00_共通.md) §3 の除外規則は「テキスト入力中」までしか書かず、[画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §6 の一覧からも読み取れない。**挙動は妥当で変更不要**——docs 側に1行足すだけで閉じる。T-39 第2バッチ①のテスト作成時に発見（テストは現実装を条項として固定済み）。**FB-61・FB-65 と同じく「実装は正しく docs の条項が足りない」型なので、3件まとめて1回の docs 改訂で閉じられる** | 未対応 |
| FB-61 | 2026-07-26 | **`?` の説明が文書間で食い違う**。画面定義書01 §6 の表は「ショートカット一覧の**表示**」、画面に出る文言（`shortcut-help.tsx`）と実装は「表示・**非表示**」のトグル。実装と画面文言が一致しているので §6 の表記をトグルに寄せれば閉じる。T-39 第2バッチ①で発見。**FB-60・FB-65 とまとめて処理可**（同じ「実装は正しく docs が足りない」型） | 未対応 |
| FB-65 | 2026-07-26 | **「元に戻す」の有効期限が明文化されていない**。実装では undo トーストを × で閉じるか5秒で自動消去されると保留（`pendingUndo`）も破棄され、以後 `U` を押しても取り消せない＝**トーストの寿命がそのまま取り消せる期限**。[画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) O-8 / O-13 は「トーストのボタン、または続けての `U`」としか書かず期限が読み取れない。実装の挙動としては妥当（保留を無期限に持つと何を戻すのか曖昧になる）なので、**docs 側に1行足すだけで閉じる見込み**。[共通](../仕様/13_画面定義書/00_共通.md) §2.2 が undo トーストを5秒と定めているので、そこと結ぶ形が自然。T-39 第2バッチ③で発見（テストは現実装を固定済み）。**FB-60・FB-61 とまとめて処理可**（同じ「実装は正しく docs が足りない」型） | 未対応 |

### 改善（要仕様整理 — 挙動・デザインの方針をオーナーと決めてから docs 反映 → 実装）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-56 | 2026-07-25 | 週次で**全曜日（月〜日）が選択された場合は「毎日」として DB 登録**し、表示もすべて毎日に寄せたい。現状は同じ結果になるルーチンの作り方が2通りあり（繰り返し種別「毎日」と「週次＋全曜日」）、一覧の要約表示も「毎日」と「週次(月・火・水・木・金・土・日)」に分かれる。FB-53 の動作確認時にオーナーが発見（FB-53 自体は列挙のまま実装済み・確認済み）。**指摘済みの穴: `week_interval` が2以上のときは寄せられない**——`隔週(全曜日)` は「2週おきに、その週は7日連続」という別の意味で、間隔の概念を持たない `recurrence_type = daily` には表現できない（`3週ごと(全曜日)` も同様）。詰める点: ①正規化の条件を `週次 かつ 全曜日 かつ week_interval ∈ {NULL, 1}` に限定してよいか ②`weekdays` と `week_interval` を NULL に落とすか値を残すか ③正規化する場所（保存時に domain の入力検証か usecase へ寄せれば表示側は無変更で「毎日」と出るため `describeRecurrence` に分岐が増えない） ④フォームで7つめの曜日にチェックが入った瞬間に種別を「毎日」へ切り替えるか、黙って保存時に正規化するか（後者は編集フォームを開き直したとき曜日チェックが消えて驚きが出る） ⑤既存データに `週次＋全曜日＋week_interval=1` の行があるか、あればバックフィルするか次の保存時に寄るのを待つか。**スキーマは無変更の見込みだが書き込む値の規則が変わる**ため、データモデル定義書 §3.4（`weekdays` の意味づけ）と §4.1（展開判定）の確認が必要 | 未対応 |
| FB-57 | 2026-07-26 | 「未設定」を表す**語**が UI 内で4通りに割れている — セルの表記は薄色の `-`（FB-55 で統一済み）／デイリーの選択ポップオーバーの候補は「モードなし」「プロジェクトなし」／ルーチン編集フォームのセレクトは「なし」／レビュー集計のグループ行は「（未設定）」。[共通](../仕様/13_画面定義書/00_共通.md) §2.4 は**セルの表記だけを規定し、選択肢・グループ行の文言を規定していない**ため、同じ意味の語が画面と部品で割れたまま増える。FB-55 と同種の食い違いで、対処も同じ形（**上位文書で語を確定 → 各画面定義書はそこを参照 → その規定に沿って全画面を点検・整備**）。詰める点: ①規定する範囲（セルの記号・選択肢の文言・集計のグループ行ラベル・`aria-label` の読み上げ語のどこまでを §2.4 に含めるか）②選択肢の語を「なし」と「未設定」のどちらに揃えるか（「モードなし」のように属性名を冠するかも含む。冠すると候補一覧で語が長くなる一方、ポップオーバー単体では何の「なし」か読み取れる）③グループ行の「（未設定）」を選択肢の語と揃えるか、行ラベルとして別扱いのまま残すか（§2.4 は現状「別」としている）④点検の網羅（デイリー・ルーチン一覧/編集フォーム・レビュー・マスタ管理を一度に洗い出す）。FB-55 のレビューで `code-quality-reviewer` が発見 | 未対応 |
| FB-58 | 2026-07-26 | **`F`（終了時刻の修正）が実行中タスクでもガードされていない**。実行中タスク（`started_at` あり・`ended_at` なし）で `F` を押すと**空欄の終了時刻入力欄**が開き、そこへ入力すると実質的に「終了時刻だけ先に決める」ことになる。[画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §6 は `B`/`F` を「開始時刻 / 終了時刻のインライン修正」としか書かず、実行中に使えるのが意図かどうか読み取れない（`B` は開始済みなら常に意味を持つので同列に扱えない）。詰める点: ①実行中は `F` を無効にするか、押せるが「終了時刻を入れたら完了扱いにする」か、現状どおり空欄編集を許すか ②無効にするなら未実行タスクの `B` も同じ扱いにするか（未実行で開始時刻だけ入るのも同型の状態） ③打刻から状態を導出する設計（`ended_at` を入れた瞬間に完了になる）との整合。T-39 第2バッチ②のテスト作成時に発見。到達可能な状態のためテストは現実装を固定してある。**[FB-68](#不具合)（未打刻タスクの `B`／`F` でキーボードがデッドロックする不具合）と修正箇所が同じ**（`requestEdit` が打刻の有無を見ていない）ので一緒に決めて一緒に直す | 未対応 |
| FB-59 | 2026-07-26 | **マスタ管理のカラーピッカーが[共通](../仕様/13_画面定義書/00_共通.md) §2.1 のポップオーバー規約を満たしていない**。§2.1 は候補選択に「`J`/`K` で移動して Enter 確定・Esc 取消（開くと現在値をハイライト）」、表示位置に「下に入りきらず上の余白が広いときだけ上向きへ反転」を定めるが、実装はクリック選択と Esc／外側クリックのみで、位置も常に下（`use-flip-up` 未使用）。§2.1 が候補選択の詳細を S-01 §6（F-112）へ委譲しているため、**マスタ側のポップオーバーにも同じ規約が及ぶのかが読み取れない**のが問題の本体。詰める点: ①§2.1 の規約が全画面のポップオーバーに及ぶのか、デイリーの属性選択に限るのかを明記する ②及ぶなら `J`/`K`＋Enter と反転をカラーピッカーへ実装する（`shouldFlipUp` は T-39 第2バッチ⑥で純関数として切り出し済みなので反転は載せやすい） ③色は2次元に並ぶため `J`/`K` の1次元移動が自然かも要検討。T-39 第2バッチ④で発見。テストは実装どおりに留めてある | 未対応 |
| FB-66 | 2026-07-26 | **[共通](../仕様/13_画面定義書/00_共通.md) §2.3「保存中は同じ行の他のセルは編集できない」がデイリーに存在しない**。マスタ管理の各テーブルは `isPending` でセルを止めるが、`daily-board.tsx` の `editing` は「いまどのセルを編集しているか」だけを持ち保存中フラグが無いため、**保存の完了を待たずに同じ行の別セルを開ける**。デイリーは楽観的更新（N-01）で即座に画面へ反映するため、マスタ（保存完了を待って反映）とは前提が違い、§2.3 をそのまま当てるのが正しいかも自明でない。詰める点: ①§2.3 が楽観的更新の画面にも及ぶのか（及ぶなら保存中フラグの導入が要る／及ばないなら §2.3 に適用範囲を書く） ②同時編集で実際に壊れる組み合わせがあるか（例: 見積もりを保存中に開始時刻を編集）。**FB-63 と同じ条項の話なので一緒に決めるのが早い**。T-39 第2バッチ②③で発見 | 未対応 |
| FB-63 | 2026-07-26 | **保存中の編集抑止が画面ごとに不揃い**（[共通](../仕様/13_画面定義書/00_共通.md) §2.3 の適用範囲。**FB-66 と1回で決める**）。マスタ管理では、プロジェクト一覧の名前セルだけ `disabled={isPending}` が無く（モード・セクションには有る）、保存中に同じセルを開き直せる＝古い値を再送しうる。行内の編集可能セルが1つなので §2.3「保存中は同じ行の**他の**セルは編集できない」には形式上抵触しないが、条項の趣旨（保存中は編集を止める）からは外れる。**同型がルーチン一覧にもある**——`routines-table.tsx` は有効トグルと削除に `disabled={isPending}` があるのに「編集」ボタンだけ無い（画面定義書02 §1 は N-01 対象外＝保存完了まで待つと定めている）。詰める点は FB-66 と共通で、§2.3 を「保存中は同じ行の編集を止める」と書き換えて全画面を揃えるか、現状の文言のまま実装だけ揃えるか。**本件だけ先に実装しないこと**——FB-66（デイリーには保存中フラグ自体が無い）の判断が先に要り、また実装を3テーブルへ足すなら[T-44](./23_技術改善バックログ.md#t-44-マスタ管理3テーブルのインライン編集新規行が各自実装になっている)（部品の抽出）を先にする方が早い。T-39 第2バッチ④⑤で発見（テストは現状を「FB-63 で検討中」と明示して固定済み） | 未対応 |
| FB-67 | 2026-07-26 | **ルーチンの必須入力がフォームで抑止されず、サーバ応答のエラー帯で示される**。`routine-form.tsx` は名前空・見積空（`Number("")` → `0`）でもそのまま Server Action を呼び、`routinize-popover.tsx` の `submit` は時刻しか見ないため週次で全曜日を外すと `weekdays: 0` が渡る。いずれも domain の検証が弾いてメッセージを返すので**壊れはしない**が、画面定義書02 §4 の「必須○」表記と §5「見積もりは1分以上を必須（0分は登録できない）」・§4.1「曜日は1つ以上必須」を読むと「フォームで止まる（`required` が付く／ボタンが押せない）」と誤読しうる。[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4「業務ルール由来の検証は domain」とは整合しているので**設計の誤りではなく、docs の書き方か UX の判断の問題**。詰める点: ①現状（送信してからエラー帯）でよしとし、画面定義書02 に「検証は保存時。フォームでは抑止しない」と1行足すか ②送信前に止める（その場合どこまでを画面側で持つか——domain の検証関数を呼ぶのか、`required` 等の素朴な抑止に留めるのか。二重管理を避ける線引きが要る） ③ルーチン化ポップオーバー（デイリー側）も同じ扱いにするか。T-39 第2バッチ⑤と `test-reviewer` が発見 | 未対応 |
| FB-62 | 2026-07-26 | **画面定義書03 §3.1 の「日界ラベル」が実装に無い**。§3.1 はセクション一覧について「行内に『1日の開始』等のラベルで現在の日界を示す」と書くが、実装はラジオボタンの選択状態だけで、ラベル文字列は表示していない（操作用の `aria-label`「〜を1日の開始にする」はある）。詰める点: ①ラベルを表示する（docs どおりに実装を足す）か、②ラジオの選択状態で足りているとして §3.1 の記述を実態に合わせるか。日界はタスクの `task_date` の境界を決める重要な設定なので、視認性を上げる①側に倒す余地はある。T-39 第2バッチ④で発見 | 未対応 |

### 要求（新規・優先度変更）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

### 開発環境

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

## 完了記録

対応済み・見送りの FB は[closed_21_ユーザーフィードバック.md](./closed_21_ユーザーフィードバック.md)に保管する。
