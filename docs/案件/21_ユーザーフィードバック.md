# ユーザーフィードバック管理簿

- 作成日: 2026-07-20
- 目的: 実運用で得たオーナーの気づきを一元管理し、今後の開発の入力にする

## 運用ルール

1. 気づきはまず本書に**起票**する（粒度はバラバラで良い。会話で出たものは AI が転記する）
2. 起票時に**分類**する: `不具合` / `改善（軽微）` / `改善（要仕様整理）` / `要求（優先度変更含む）` / `開発環境`
3. 対応するときは通常の開発ルールに従う — **挙動レベルの変更は該当仕様書を先に更新**し（[guide_21_ユーザーフィードバック.md](./guide_21_ユーザーフィードバック.md)）、本書には対応結果と参照先だけを残す
4. 状態は `未対応` / `対応中` / `対応済み` / `見送り` の4つ。対応済み・見送りには日付と理由（または反映先）を書く
5. **対応済み・見送りになったエントリは、状態更新と同じコミットで[closed_21_ユーザーフィードバック.md](./closed_21_ユーザーフィードバック.md)へ移す**（本書には未対応・対応中だけを残す）

## 一覧

### 不具合

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

### 改善（軽微 — 仕様判断がほぼ不要で、画面定義書の更新＋実装で完結する）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

### 改善（要仕様整理 — 挙動・デザインの方針をオーナーと決めてから docs 反映 → 実装）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-51 | 2026-07-25 | セクション選択ポップオーバー（`S` / O-5）に「現在のセクションへ」を追加したい。現状は候補がセクション名の一覧だけで、いま自分がどの時間帯にいるかを頭で照合してから選ぶ必要がある。詰める点: 「現在」の定義（現在時刻を含むセクション／実行中タスクが属するセクション。§4.2 の「現在位置」と揃えるか）、候補一覧のどこに置くか（先頭固定か）と実セクション候補との重複の見せ方、表示日が今日でないときの扱い（残り時間 §3.2 と同じく出さないか）、移動先セクション内での挿入位置（末尾か・現在位置か）、`S` 以外（`Shift+J/K` の O-6）との役割分担。既存要求に収まらなければ要求定義書に新IDを起こす | 対応中 2026-07-25 → 要件定義書 F-112 / 画面定義書01 O-5・§4.3。オーナー確定: 見せ方=候補先頭に固定の特別項目（区切り線あり・実候補には印を付けない）、「現在」=現在時刻を含むセクション（§4.2 の現在位置とは揃えない）、挿入位置=移動先セクション末尾（現行どおり）、表示日が今日でなければ出さない。既存要求 F-112 の操作詳細のため要求定義書は変更なし（FB-46 と同じ扱い）。仕様確定済み・実装待ち |
| FB-55 | 2026-07-25 | 未設定の表記が画面ごとに食い違っている。デイリーリストは FB-48 で薄色の `-` に統一したが、**ルーチン一覧（画面定義書02 §3）とレビュー画面の実績ログ（同04 §3.3）のモード列・プロジェクト列は空欄のまま**で、同じ「未設定」が画面によって `-` と空欄に分かれる。FB-48 の動作確認時にワーカーが指摘し、オーナーが「すべての空欄は `-` で揃える」と判断 | 対応中 2026-07-25 → **[共通](../仕様/13_画面定義書/00_共通.md) §2.4 を新設**（画面定義書01 §3.3・02 §3・04 §3.3 はそこを参照する形に変更）。オーナー確定: 属性（モード・プロジェクト等マスタ参照値）の未設定は全画面で薄色の `-`。**時間の値（見積もり・実績・打刻時刻）の `--:--` は対象外**——時間には「未設定（0分）」と「未確定（実行中で実績が出ていない）」の2種があり、桁の形を保って不在を示す必要があるため意味が違う。集計の「（未設定）」グループ行（04 §3.5）も対象外（セルの不在ではなく行のラベル）。既存要求 F-401 / F-402 の表示詳細のため要求定義書・要件定義書とも変更なし。仕様確定済み・実装待ち（`routines-table.tsx` と `review-board.tsx`） |
| FB-56 | 2026-07-25 | 週次で**全曜日（月〜日）が選択された場合は「毎日」として DB 登録**し、表示もすべて毎日に寄せたい。現状は同じ結果になるルーチンの作り方が2通りあり（繰り返し種別「毎日」と「週次＋全曜日」）、一覧の要約表示も「毎日」と「週次(月・火・水・木・金・土・日)」に分かれる。FB-53 の動作確認時にオーナーが発見（FB-53 自体は列挙のまま実装済み・確認済み）。**指摘済みの穴: `week_interval` が2以上のときは寄せられない**——`隔週(全曜日)` は「2週おきに、その週は7日連続」という別の意味で、間隔の概念を持たない `recurrence_type = daily` には表現できない（`3週ごと(全曜日)` も同様）。詰める点: ①正規化の条件を `週次 かつ 全曜日 かつ week_interval ∈ {NULL, 1}` に限定してよいか ②`weekdays` と `week_interval` を NULL に落とすか値を残すか ③正規化する場所（保存時に domain の入力検証か usecase へ寄せれば表示側は無変更で「毎日」と出るため `describeRecurrence` に分岐が増えない） ④フォームで7つめの曜日にチェックが入った瞬間に種別を「毎日」へ切り替えるか、黙って保存時に正規化するか（後者は編集フォームを開き直したとき曜日チェックが消えて驚きが出る） ⑤既存データに `週次＋全曜日＋week_interval=1` の行があるか、あればバックフィルするか次の保存時に寄るのを待つか。**スキーマは無変更の見込みだが書き込む値の規則が変わる**ため、データモデル定義書 §3.4（`weekdays` の意味づけ）と §4.1（展開判定）の確認が必要 | 未対応 |

### 要求（新規・優先度変更）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

### 開発環境

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-49 | 2026-07-25 | リポジトリのテストカバレッジをオーナーが簡単に確認できるようにしたい。現状は `npm run test:coverage` を手元で叩く必要があり、日常の開発や GitHub 上で数値を把握しづらい。実現方式（README バッジ／CI のジョブサマリ出力／アーティファクト保存／外部サービス連携等）と、public リポジトリでカバレッジ数値を公開してよいか（到達手段・実データにはつながらないが方針判断は要る）は着手時に詰める。挙動を変えないツール整備のため、方式が固まれば対応は技術改善バックログ（T-XX）へ流す | 起票（未着手） |

## 完了記録

対応済み・見送りの FB は[closed_21_ユーザーフィードバック.md](./closed_21_ユーザーフィードバック.md)に保管する。
