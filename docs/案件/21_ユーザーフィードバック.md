# ユーザーフィードバック管理簿

- 作成日: 2026-07-20
- 目的: 実運用で得たオーナーの気づきを一元管理し、今後の開発の入力にする

## 運用ルール

1. 気づきはまず本書に**起票**する（粒度はバラバラで良い。会話で出たものは AI が転記する）
2. 起票時に**分類**する: `不具合` / `改善（軽微）` / `改善（要仕様整理）` / `要求（優先度変更含む）` / `開発環境`
3. 対応するときは通常の開発ルールに従う — **挙動レベルの変更は該当仕様書を先に更新**し（[guide_21_ユーザーフィードバック.md](./guide_21_ユーザーフィードバック.md)）、本書には対応結果と参照先だけを残す
4. 状態は `未対応` / `対応中` / `対応済み` / `見送り` の4つ。対応済み・見送りには日付と理由（または反映先）を書く
5. **対応済み・見送りになったエントリは、状態更新と同じコミットで[closed_21_ユーザーフィードバック.md](./closed_21_ユーザーフィードバック.md)へ移す**（本書には未対応・対応中だけを残す）

## 一覧

### 不具合

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

### 改善（軽微 — 仕様判断がほぼ不要で、画面定義書の更新＋実装で完結する）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-46 | 2026-07-25 | デイリーのセクション選択モーダル（タスクをどのセクションへ入れるか選ぶ場面）で、各セクション名の横にそのセクションの時間帯を付記したい。セクション名だけだと時間帯が思い出せず選びにくい。付記する時刻の粒度（開始時刻のみか範囲か＝終了時刻は導出）は着手時に決める | 起票（未着手） |

### 改善（要仕様整理 — 挙動・デザインの方針をオーナーと決めてから docs 反映 → 実装）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-43 | 2026-07-24 | `D`（Delete=削除）を、頭文字から Duplicate（複製）と混同し、複製のつもりで削除してしまいがち。削除は破壊操作のため誤爆の害が大きい（未実行は即削除＝Undoトースト、打刻済みは確認ダイアログはあるが、それでも混同する）。複製は現状 `Y`（Yank）。ニーモニックの衝突をどう解くか（削除・複製のキー割り当てを見直す／削除に一段確認を足す等）を詰める。ショートカット規約（画面定義書01 §6）と `?` ヘルプのニーモニック併記（§9）への波及も整理する | 起票（未着手） |
| FB-41 | 2026-07-24 | 完了タスク（終了打刻あり）を未実施に戻せるようにしたい。終了打刻の取り消しにあたる。現行の開始打刻取り消し（F-210 / O-13）は**実行中→未実行のみで完了は対象外**のため、これは別要求になる。戻す先（未実行か／実行中か＝`ended_at` のみクリアか `started_at` も消すか）、対象状態、戻したあとの表示位置、`U` キー・O-13 との切り分け、割り込み・中断（F-204）で生成した再開タスクがある場合の扱い、**この戻す操作自体を取り消せるか（誤って戻したときに元の完了状態へ復帰できるか＝undo の undo）**は着手時に詰める。既存要求に収まらなければ要求定義書に新IDを起こす | 起票（未着手） |
| FB-47 | 2026-07-25 | デイリーのタスクリストで、各タスクの予想開始時刻を表示したい。現在時刻を起点に、実行中タスクの経過ぶんと以降の未実行タスクの見積もり時間を積み上げて導出する。導出の起点（現在時刻か／セクション開始時刻か）、見積もりを持たないタスクの扱い、実行中・完了タスク行での表示可否、割り込み・中断（F-204）が絡む場合の再計算、楽観的更新との整合は着手時に詰める。既存要求に収まらなければ要求定義書に新IDを起こす | 起票（未着手） |
| FB-48 | 2026-07-25 | デイリーのタスクリストで、プロジェクトをタスク名の横ではなく、モード（既存の列表示）のように独立した分かりやすい列で表示したい。現状はタスク名に併記されていて視認しづらい。列の位置（モード列との並び）・幅・未設定タスクの見え方・アーカイブ済みプロジェクトの扱い、テーブル型リスト（N-05）の範囲で列を増やす影響は着手時に詰める | 起票（未着手） |

### 要求（新規・優先度変更）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|

### 開発環境

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-49 | 2026-07-25 | リポジトリのテストカバレッジをオーナーが簡単に確認できるようにしたい。現状は `npm run test:coverage` を手元で叩く必要があり、日常の開発や GitHub 上で数値を把握しづらい。実現方式（README バッジ／CI のジョブサマリ出力／アーティファクト保存／外部サービス連携等）と、public リポジトリでカバレッジ数値を公開してよいか（到達手段・実データにはつながらないが方針判断は要る）は着手時に詰める。挙動を変えないツール整備のため、方式が固まれば対応は技術改善バックログ（T-XX）へ流す | 起票（未着手） |

## 完了記録

対応済み・見送りの FB は[closed_21_ユーザーフィードバック.md](./closed_21_ユーザーフィードバック.md)に保管する。
