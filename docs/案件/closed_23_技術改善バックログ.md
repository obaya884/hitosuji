# 技術改善バックログ完了記録

- 作成日: 2026-07-24
- 目的: 完了した技術活動（T-XX）を原文のまま保管する。ライブ台帳は[技術改善バックログ](./23_技術改善バックログ.md)（未着手・進行中・保留のみを持つ）
- エントリは完了への状態更新と同じコミットで本書へ移す（並びは完了日の新しい順）。判断の経緯は従来どおり [log_23_技術改善バックログ.md](./log_23_技術改善バックログ.md) が持つ
- **書式は 2026-07-26 に変わった**。以後移すエントリは[一覧](#一覧)＋[詳細](#詳細)の新書式（`ID / タイトル / 種別 / 優先度 / 状態 / 詳細` の6列と `### T-XX` の詳細節）。それ以前に移した分は[旧書式の記録](#旧書式の記録2026-07-26-以前)に `## T-XX タイトル（完了日）` の形のまま凍結してある（過去の記録は書き換えない）

## 一覧

| ID | タイトル | 種別 | 優先度 | 状態 | 詳細 |
|---|---|---|---|---|---|
| T-48 | デイリーボードの純粋ロジックを `.ts` へ切り出す | 内部設計 | 低 | 完了 2026-07-26 | [詳細](#t-48) |
| T-43 | テストヘルパ・ファクトリを集約する | テスト | 中 | 完了（2026-07-26） | [詳細](#t-43) |
| T-66 | サブエージェントの起動方針が消費側から定まっていない | ツール整備 | 中 | 完了（2026-07-26） | [詳細](#t-66) |
| T-67 | `verifier` の変異テストが作業ツリーを壊す唯一の経路になっている | ツール整備 | 高 | 完了（2026-07-26） | [詳細](#t-67) |
| T-40 | worktree の dev サーバポートが並行時に衝突する | ツール整備 | 中 | 完了（2026-07-26） | [詳細](#t-40) |
| T-46 | `wt:new` が未 push のローカル main を起点にしても警告しない | ツール整備 | 中 | 完了（2026-07-26） | [詳細](#t-46) |
| T-58 | `test:coverage` が前回実行の残骸で異常終了することがある | ツール整備 | 低 | 完了（2026-07-26） | [詳細](#t-58) |
| T-56 | 常に成功する `Result` を返し、呼び出し側も検査していない | 型安全 | 低 | 完了（2026-07-26）→ 見極めの結果、挙動の案件と判断し FB-70 へ移管 | [詳細](#t-56) |
| T-42 | Server Action のエラーメッセージ辞書が型で閉じていない | 型安全 | 中 | 完了（2026-07-26）→ 辞書3本を直和型で閉じ #55。4本目は Partial でキー集合のみ拘束し文言は FB-71 へ | [詳細](#t-42) |
| T-51 | リポジトリ Port の付帯更新が「未指定・`null`・空配列」の3状態になっている | 型安全 | 中 | 完了（2026-07-26）→ Port を空配列1本へ正規化 #56。未検証だった3 guard に統合テスト7本 | [詳細](#t-51) |
| T-57 | モードのプリセット色が3か所の並行リストになっている | 内部設計 | 低 | 完了（2026-07-26）→ プリセット色を1本へ統合 #58。塗り色とシード色の未検証を解消 | [詳細](#t-57) |
| T-59 | `proxy.ts`（Basic認証）にテストが1本もない | テスト | 高 | 完了（2026-07-26）→ proxy.ts に28テスト #57。カバレッジ 0%→100%、派生で FB-73・T-71〜73 起票 | [詳細](#t-59) |
| T-70 | 並行時にレビューを誰が起動し誰が直すかが CLAUDE.md と仕様16 で食い違っている | ツール整備 | 高 | 完了（2026-07-26）→ 仕様16 §2・§4 と CLAUDE.md へ反映。初適用の4本で見送り13件すべてがエスカレーションされ独断処理ゼロ | [詳細](#t-70) |
| T-52 | Server Action 実行フックが routines で再実装されている | 内部設計 | 中 | 完了（2026-07-26）→ useServerAction として app/_lib へ引き上げ #59。テーブル単位の抑止を挙動不変と確認しテストで固定 | [詳細](#t-52) |
| T-64 | `applyOptimisticAction` の `remove` だけ domain へ委譲していない | 内部設計 | 低 | 完了（2026-07-26）→ withTaskRemoved へ委譲 #60。section/endTime の保持が全体で未検証だった穴も塞ぐ | [詳細](#t-64) |
| T-49 | エラーコード→表示文言の辞書が client/server・画面ごとに散在している | 内部設計 | 中 | 完了（2026-07-26）→ 共有辞書を app/_lib へ集約 #61。FB-72 の2件を反映し、変異6の生存から T-74 を起票 | [詳細](#t-49) |
| T-71 | Basic認証の分割処理が、評価順だけを頼りに認証バイパスを防いでいる | 内部設計 | 中 | 完了（2026-07-26）→ proxy.ts の解析・復号・除外パターンを整える #62 | [詳細](#t-71) |
| T-72 | Basic認証の base64 復号が latin1 で、扱える文字集合が実装都合で狭まっている | 内部設計 | 低 | 完了（2026-07-26）→ proxy.ts の解析・復号・除外パターンを整える #62 | [詳細](#t-72) |
| T-73 | 認証の除外パターンが正規表現として緩く、意図より広い範囲を対象外にしている | 内部設計 | 中 | 完了（2026-07-26）→ proxy.ts の解析・復号・除外パターンを整える #62 | [詳細](#t-73) |
| T-45 | 画面ショートカット用の共通キーガードが無い | 内部設計 | 低 | 完了（2026-07-26）→ 共通キーガードを2関数へ集約 #63。4か所の判定が同一でないことが判明し差を明示 | [詳細](#t-45) |
| T-50 | `sort_order` の採番・振り直し規則が domain 内で5か所に写っている | 内部設計 | 中 | 完了（2026-07-26）→ placeSortOrder へ集約し placement.ts を削除 #64。閾値の直上など生存変異3件を塞ぐ | [詳細](#t-50) |
| T-47 | 時刻の「表示は Asia/Tokyo 固定・入力解釈はローカル」が非対称 | 内部設計 | 低 | 完了（2026-07-26）→ 解釈・導出・表示を APP_TIME_ZONE 固定へ #68。domain はタイムゾーンを引数で受け取り atLocal を廃止 | [詳細](#t-47) |
| T-55 | `resolveToday` が presentation にありながらリポジトリを叩いている | 内部設計 | 低 | 完了（2026-07-26）→ usecases/section/resolve-today.ts へ移設 #68。todayLogicalDate は domain へ下げ now は presentation が渡す | [詳細](#t-55) |
| T-74 | Server Action がどの文言辞書を引くかの配線を、型もテストも検証していない | テスト | 中 | 完了（2026-07-26）→ 文言差を DUPLICATE_AND_START_MESSAGES へ隔離 #65。共有2辞書は取り違えても文言が変わらない。残りは T-76 | [詳細](#t-74) |
| T-75 | エラー文言辞書が画面配下と共有の2ファイルに分かれている | 内部設計 | 低 | 完了（2026-07-26）→ app/_lib/error-messages.ts 1ファイルへ統合 #65。残課題は T-77・T-78 | [詳細](#t-75) |
| T-53 | `daily-list.tsx` が1ファイル4コンポーネントで、テストが常に表全体を描く | 内部設計 | 低 | 完了（2026-07-26）→ TaskRow / GroupHeading / AssignCell を分離 #67。props 型を子から派生させ二重定義を解消 | [詳細](#t-53) |
| T-44 | マスタ管理3テーブルのインライン編集・新規行が各自実装になっている | 内部設計 | 中 | 完了（2026-07-27）→ 編集セル・新規行を部品へ抽出 #66。§2.3 の改訂に追随し保存中の抑止を3表＋ルーチンへ | [詳細](#t-44) |
| T-37 | 実装後のブラウザ実機確認を仕組み化する（Playwright・テストではない） | ツール整備 | 中 | 完了 | [詳細](#t-37) |
| T-69 | テストファイルの型エラーを誰も見ていない（`tsc --noEmit` が CI にない） | ツール整備 | 中 | 完了（2026-07-27） | [詳細](#t-69) |
| T-90 | Dependabot 依存追随（jsdom 30 マージ・`@types/node` 26 クローズ） | 依存追随 | 中 | 完了（2026-07-30） | [詳細](#t-90) |
| T-62 | CI でタイムゾーンを明示し、時刻の二重契約が壊れたら落ちるようにする | ツール整備 | 中 | 完了（2026-08-01）→ CI を UTC＋JST の2本回しに #85 | [詳細](#t-62) |
| T-41 | コード内コメントの仕様参照先が旧位置を指している | 内部設計 | 低 | 完了（2026-08-01）→ 委譲スタブ指しのコメント13箇所を実体へ #86 | [詳細](#t-41) |
| T-63 | 仮タスクIDの採番が同一ミリ秒で衝突しうる（テストでも固定できない） | 内部設計 | 低 | 完了（2026-08-01）→ 仮タスクIDを単調増加カウンタへ #88 | [詳細](#t-63) |
| T-77 | `ROUTINE_ERROR_MESSAGES` だけ辞書の命名軸が違う | 内部設計 | 低 | 完了（2026-08-01）→ ROUTINE_MESSAGES へ改名 #87 | [詳細](#t-77) |
| T-78 | マスタ管理の文言辞書に同一文言の別実装が残る | 内部設計 | 低 | 完了（2026-08-01）→ 置き場は寄せ辞書は畳まない #87 | [詳細](#t-78) |
| T-79 | マスタ管理3表の見出し・新規追加ボタン・エラー帯が逐語重複している | 内部設計 | 低 | 完了（2026-08-01）→ 3表の枠を master-table-frame へ抽出 #87 | [詳細](#t-79) |
| T-80 | `toOptions`（マスタ→候補の変換）が `daily-list.tsx` に残っている | 内部設計 | 低 | 完了（2026-08-01）→ master-options.ts へ切り出し #89 | [詳細](#t-80) |
| T-83 | 「モード未設定＝副次情報の色」の判定が3箇所に分散し、変数名も揃っていない | 内部設計 | 低 | 完了（2026-08-01）→ mode-appearance.ts へ集約 #89 | [詳細](#t-83) |
| T-89 | `sectionEndAt` が本番から参照されない export になっている | 内部設計 | 低 | 完了（2026-08-01）→ 削除しテストを sectionSlacks 経由へ #88 | [詳細](#t-89) |
| T-84 | shell スクリプトの db-test 起動待ちとポート保持者の表示が3か所目の重複になった | ツール整備 | 低 | 完了（2026-08-01）→ scripts/lib/ に db-test.sh・port.sh を切り出し | [詳細](#t-84) |
| T-91 | `dev:check` の生成物が `npm run typecheck` を落とす（tsconfig が `.next-check/` を拾う） | ツール整備 | 中 | 完了（2026-08-01）→ exclude に .next-check を追加 | [詳細](#t-91) |
| T-87 | 変異テストをいつ実施するかが明文化されていない（手続きだけがある） | 調査 | 中 | 完了（2026-08-01）→ テスト戦略定義書 §10 に発火条件を新設 | [詳細](#t-87) |
| T-92 | マスタ管理のテストに act 包みの click ヘルパが無く、assert を足すたびに待ちの罠を踏む | テスト | 中 | 完了（2026-08-01）→ `src/app/_testing/interactions.ts` の `click` に一本化 | [詳細](#t-92) |
| T-93 | 「失敗後に抑止が解ける」を見ているのがマスタ管理3表のうち modes だけ | テスト | 低 | 完了（2026-08-01）→ projects / sections にも解除側の assert を足した | [詳細](#t-93) |
| T-60 | usecases・infrastructure に「日常操作で最も通る経路」の未検証が残っている | テスト | 高 | 完了（2026-08-01）→ 統合 27 本・ユニット 2 本を追加。task リポジトリは 100% 到達 | [詳細](#t-60) |
| T-61 | コンポーネント段に残る穴（写像・境界・配線） | テスト | 中 | 完了（2026-08-02）→ 4項目を塞ぎ、差異 0 の表記を `0:00` へ変更 | [詳細](#t-61) |
| T-82 | テストフィクスチャのセクション定義が時間帯を手写しで持っている | テスト | 低 | 完了 | [詳細](#t-82) |
| T-76 | 「複製して開始」専用辞書は打刻系で取り違えても型・テストが検出しない | テスト | 低 | 完了 | [詳細](#t-76) |
| T-81 | `daily-board.test.tsx` が列位置のハードコードと独自フィクスチャで書かれている | テスト | 低 | 完了 | [詳細](#t-81) |
| T-96 | デイリーのテストが共有の click ヘルパに追随していない（ローカルの `clickAndSettle` が残る） | テスト | 低 | 完了 | [詳細](#t-96) |
| T-101 | `daily-board.test.tsx` が1938行・ファイルローカルヘルパ23本に膨らんでいる | テスト | 中 | 完了 | [詳細](#t-101) |
| T-98 | デイリーのショートカット定義が4か所の並行リストになっている | 内部設計 | 中 | 完了 | [詳細](#t-98) |
| T-107 | Server Action の結果変換が2流儀に分かれ、40本で逐語反復している | 内部設計 | 低 | 完了 | [詳細](#t-107) |
| T-99 | 実装済みの機能をコメントが「将来導入」「後続ステップ」と書いたままになっている | 内部設計 | 中 | 完了 | [詳細](#t-99) |
| T-104 | 判断の経緯と過去チケットの症状説明がコードコメントに残っている | 内部設計 | 低 | 完了 | [詳細](#t-104) |
| T-108 | `sortByStartTime` が自ファイル以外から参照されない export になっている | 内部設計 | 低 | 完了 | [詳細](#t-108) |
| T-95 | テスト戦略定義書 §7 のカバレッジ記述が実測と乖離している | テスト | 低 | 完了 | [詳細](#t-95) |
| T-102 | コード内の `§` 参照が文書名を持たず、同じ番号が文書ごとに別条項を指している | 内部設計 | 中 | 完了 | [詳細](#t-102) |
| T-110 | 見出しの「アーカイブ済みセクション＋タスクあり」がコンポーネント段で描かれていない | テスト | 低 | 完了 | [詳細](#t-110) |
| T-103 | 日付移動の URL 組み立てとキー処理が3か所に分かれている | 内部設計 | 低 | 完了 | [詳細](#t-103) |
| T-94 | マスタ管理3表から寄せた表の枠が、ルーチン管理では4本目の逐語コピーのまま残る | 内部設計 | 低 | 完了 | [詳細](#t-94) |
| T-106 | ID 型エイリアスの適用が domain の外で素の `number` に戻っている | 型安全 | 低 | 完了 | [詳細](#t-106) |

## 詳細

新書式で移したエントリの詳細節をここに積む（並び順は一覧と同じ）。

### T-48

- 背景: `daily-board.tsx` の `applyOptimisticAction` / `pendingUndoMessage` / `optimisticTask`（同ファイル 92〜187行）は**純関数**で、[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 の「presentation はまず純粋ロジックを `.ts` へ切り出してユニットテスト」に照らすと切り出す対象。`test-reviewer` の指摘
- 効果: 12種の楽観アクションの組合せを**ユニット段で安く網羅**でき、コンポーネント段（jsdom・描画あり）は代表経路だけで済む。現状は 60件のコンポーネントテストがこの組合せを担っており、実行時間と読みやすさの両方で割高
- 注意: 現在 `daily-board.tsx` は Stmts 97.47% / Lines 100% まで来ているので、**切り出しはカバレッジを埋めるためではなく段を適正化するための作業**。切り出し後もコンポーネント段の代表テストは残す（結線が壊れたら気づけなくなるため）
- 着手条件: `daily-board.tsx` を別件で触るとき、または楽観アクションの種類を増やすとき（そこで組合せが増えると段の割高さが効いてくる）
- 関連: [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 / `(daily)/_lib/popover-scroll.ts`・`_lib/flip-up.ts`（同じ流儀の先例） / T-39 第2バッチ③
- 結果（2026-07-26）: **2ファイルへ分けた**——`_lib/optimistic.ts`（`OptimisticAction` 型・`applyOptimisticAction`・`optimisticTask`）と `_lib/undo.ts`（`UndoTarget` / `PendingUndo` 型・`pendingUndoMessage`）。1ファイルにまとめなかったのは、前者が「グループ列をどう変えるか」、後者が「保留を何で持ちどう伝えるか」で関心が別だから（`_lib` の既存2ファイルも1関心1ファイル）。ユニットテストを追加し、`daily-board.tsx` は 650 → 541行。**T-48 が割高と見ていたコンポーネントテストは1件も削っていない**——上の「注意」のとおり、あれらが見ているのは組合せではなく Server Action との結線（楽観反映 → 確定 or ロールバック → トースト）で、ユニット段では代替できない（`test-reviewer` も据え置きを妥当と判定）。同じコミットで1件だけ減っているのは、直前の FB-65 で私が足した重複テストを上位互換の既存テストへ統合したもので、T-48 の対象ではない。エラー文言辞書（`PUNCH_EDIT_MESSAGES`）は [T-49](../案件/23_技術改善バックログ.md#t-49) の守備範囲なので手を付けていない

### T-43

- 背景: T-39 第2バッチで6ワーカーが**互いのコードを見ずに並行実装**したため、同じ道具が別々の流儀で複数生まれた。並行中は共有ファイルを凍結していた（衝突回避）ので、集約はマージ後のヘッドの仕事として先送りしてある
- 対応: 2段に分けて実施した（1PR だと30ファイル超でレビューの粒度が粗くなるため）。**本番コードは1行も変えていない**
  - **第1段（データ系）**: `task()` 24本 → `src/domain/task/testing/task.ts`、`routine()` 6本 → `src/domain/routine/testing/routine.ts`、`at()` 6定義・3シグネチャ → `src/domain/shared/testing/clock.ts` の `atJst()` / `atLocal()`、基準日3種 → `TEST_DATE`（2026-07-26）1つ、マスタ固定値のプリセット外の色 → `MODE_COLORS` 参照
  - **第2段（コンポーネントテストの作法系）**: `next/navigation` スタブ6ファイル・4流儀 → `src/app/_testing/setup.ts` が段全体で差し替え（実体は `next-navigation.ts`）、`rowOf` 2定義・`hasClass` 4定義＋`className.toContain` 20か所・色の表記変換2流儀 → `src/app/_testing/dom.ts` に集約、`vi.clearAllMocks()` 7ファイル → `setup.ts` の `afterEach`、`deferredAction`（masters 配下にあったが全画面共有の `ActionResult` を返す。routines に重複あり）→ `src/app/_testing/actions.ts`
- 結果: 置き場・既定値・呼び分けの規約は[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8（「偽物を置いてよい境界」と「テスト補助の置き場」）に、判断の経緯は[その決定ログ](../仕様/log_15_アーキテクチャ定義書.md)に残した
- **統合しないと決めたもの**（判断そのものが成果。詳細は決定ログ）:
  - **マスタ固定値 `MODES`・`PROJECTS`・`SECTIONS`** — 中身の違いは不揃いではなく、各シナリオが要る条件（アーカイブ済みを含むか・id 体系・時間帯の並び）そのもの
  - **jsdom の詰め物（`scrollIntoView`・`ResizeObserver`）** — 同じ API でもファイルごとに要る契約が違う（空実装の詰め物 / 呼び出しを検証する `vi.fn`）。一方に寄せると他方が嘘の緑になる。**「重複しているから寄せる」ではなく「契約が同じか」で判断する**という線引きがここで確定した
  - **マスタ3表の「インライン編集」「新規追加」ブロック** — パラメタライズすると FB-63 のような実装差の記録が消える（差そのものを無くすのは [T-44](./23_技術改善バックログ.md#t-44) の仕事）
  - `routine-form.test.tsx` の `DEFAULT_INPUT` と共有 `routine()` — 前者は「フォームの既定値（画面定義書02 §4 の仕様そのもの）」、後者は「既存ルーチンの雛形」で意味が違う
  - Server Action の成功既定（各ファイルの `beforeEach`）— どの action が成功既定かはファイルの前提として見えていた方が読める
- 副産物:
  - 既定値を中立にした結果、**暗黙の既定に寄りかかっていたテストが7件**表面化した（見積もり0では手前の `estimate_required` で落ちて曜日検証に届いていない、等）
  - 反面、レビューで**中立化が異常系の判別を1件消していた**ことも判明し（同一入力になった2本）、「既定を中立にする」規約は「そのテストが通す前提の値も呼び出し側に書く」まで含めて読む、と §8 に補った
  - **名前で呼び分けても取り違えは起きる**——`daily-list.test.tsx` の予想開始時刻テストが `atJst`（本来ローカル系）を使っており、しかもアサーションが書式（正規表現）しか見ていなかったため**どの TZ でも緑になっていた**。`atLocal` へ直し値の固定（`toBe("10:00–")`）に変えた。機械的に落とす検出側が要る、という [T-62](#t-62) の根拠
- 検証: 全段 1362件緑。**UTC / America_New_York / Australia_Sydney / Pacific_Auckland の4TZ**で全件緑（`atJst` / `atLocal` の呼び分けの実測）
- 関連: T-39（発生源） / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 / [T-47](./23_技術改善バックログ.md#t-47)（`at()` の同名別契約という症状は本 T で予防済み。根の非対称は未解決） / [T-57](./23_技術改善バックログ.md#t-57)（プリセット外の色は本 T で対応済み。並行リストの統合は未着手） / [T-62](#t-62)（TZ の検出側） / [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §2（並行時に共有ファイルを凍結する規律）

### T-66

- 背景: CLAUDE.md「サブエージェント運用」は**どのエージェントをどの契機で起動するか**を定めているが、**`general-purpose` をどう使うか**（そもそも起動するか・どのモデルで・どこまで読ませるか）は定めていない。結果、読み取り中心の調査まで全ツール持ちの `general-purpose` に投げており、これは `model` 未指定なのでセッションモデル（opus）を継承する
- 顕在化した経路: 2026-07-26 に消費の内訳を確認したところ、直近24hで **`general-purpose` が単独 33%**、レビュー系5本の合計は 11%（test-reviewer 5 / code-quality-reviewer 3 / spec-reviewer 1 / verifier 1 / refactor-scout 1）だった。あわせて「99% が subagent-heavy セッション」「77% が8時間以上のセッション」「72% が 150k コンテキスト超」も出ている
- 実害: 週・5時間の利用枠を実装以外で先に使い切る。T-39 第2バッチ以降サブエージェントの並列起動を常用するようになったため、放置すると悪化する
- 対応方針: ①読み取り中心の調査は `Explore`（読み取り専用・結論だけ返す）を既定にし、`general-purpose` は**編集を伴う自己完結タスクに限る** ②`general-purpose` を機械的な作業に使うときは起動時に `sonnet` を明示する ③タスクの切れ目で文脈を切る（150k 超が消費の7割）
- 見送った案: **レビュー系5本を sonnet へ落とす**。opus 指定の3本を全部落としても 9% しか動かず、節約が小さすぎて品質との取引が成立しない。とくに `test-reviewer`（レビュー中最大の 5%）は「実装をどう壊しても緑のままではないか」という反実仮想の推論が本体で、**見逃しが「指摘なし」と区別できない**沈黙する失敗になる。判断の経緯は[決定ログ](./log_23_技術改善バックログ.md)
- 対応: CLAUDE.md「サブエージェント運用」に **§起動のコスト**を新設し、上の4点を明文化した（2026-07-26）
- 関連: T-67（同じ日に同じ確認から出た別件） / [git運用と並行開発体制定義書](../仕様/16_git運用と並行開発体制定義書.md) §2〜§5（並行開発時のワーカー）

### T-67

- 背景: `verifier` は5本のサブエージェントで唯一 `Bash` による書き込み・実行の権限を持つ。2026-07-26 に**2度**、自分に課した制約を破って作業ツリーを壊した——①変異テストのために `git stash` → `git reset --hard` し、stash を戻さず終了（メインセッションの未コミット25ファイルが一時的に消失。`git stash pop` で復旧）②同日、変異を「Edit で1か所」ではなくスクリプトで一括適用し、停止時に戻さず `src/app/_testing/next-navigation.ts` に残した。**いずれも引き金は変異テスト（コードを壊してテストが落ちるか確かめる）を依頼したこと**
- 実害: メインセッションの未コミット作業の消失。①は気づかなければ数十ファイルを失っていた。②はツリーが汚れたまま次の作業に入る
- 対応方針: **`verifier` の責務から変異テストを外す**。禁止条項を積み増しても、破れる能力が残っている限り同じ事故が起きる（①のあとに条項を足したうえで②が起きた）。変異テストが要るときはメインセッションが Edit で行う——差分がチャットに出るので、戻し忘れもオーナーから見える（CLAUDE.md「規約」のスクリプト一括置換禁止と同じ理由）
- 優先度の根拠: 失うのが未コミットの作業で、復旧手段が `git reflog` / `git stash list` を思いつけるかに依存する。実際に2回踏んでいる
- 対応: `.claude/agents/verifier.md` の制約を「ファイルを一切書き換えない／変異テストは引き受けない」に置き換え、CLAUDE.md「規約」の Edit 必須ルールに変異テストの手順を追記した（2026-07-26）
- 積み残し: 委譲先そのものを作るかは [T-68](./23_技術改善バックログ.md#t-68) で検討する（本 T は安全側に倒しただけで、置き場を見つけたわけではない）
- 関連: T-66 / T-65（同じく `verifier` 運用から出た件） / [T-68](./23_技術改善バックログ.md#t-68)

### T-40

- 背景: `scripts/wt-new.sh` は dev ポートを **worktree 作成時点で 3001 から空きを探して** `.env.worktree` の `DEV_PORT` に焼き込む。dev サーバを立てずに worktree を続けて作ると全部が同じ 3001 になり、後から2つで `npm run dev` すると衝突する。2026-07-26 の T-39 第2バッチで worktree を6つ作り、**6つすべてが 3001** になって顕在化した——T-34 が言う「固有ポート」は、作成時点で他の dev サーバが動いていた場合にだけ固有になる
- 実害の程度: ワーカーは lint / build / test で完結し dev サーバを使わないため、並行実装そのものは妨げない。効くのは**オーナーが複数ブランチの画面を同時に確認したいとき**（片方を止めれば回避できる）
- 対応方針の候補（着手時に決める）:
  - **ブランチ名のハッシュから決める**（例 `3001 + hash % 90`）— 作成順に依存せず冪等で、`.env.worktree` へ焼き込む今の形をそのまま使える。衝突確率は残るが同時に扱う worktree が数個なら実用上足りる
  - **起動時に探す**（`scripts/dev.sh` が `DEV_PORT` を起点に空きを探す）— 実際に衝突しない保証は得られるが、表示ポートが起動のたびに変わり `wt:new` が案内した URL と食い違う
  - 併用（作成時はハッシュで焼き、起動時に埋まっていたらずらす）が素直かもしれない
- 着手条件: 2つ以上の worktree で dev サーバを同時に立てたくなったとき
- 関連: **[T-46](#t-46)（同じ `scripts/wt-new.sh` を触るので一緒に着手すると早い）** / [完了記録](./closed_23_技術改善バックログ.md) T-34（dev サーバの並列化） / T-37（ワーカーからの実機確認が絡む） / [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §2
- 結果（2026-07-26）: 候補2案（ハッシュ／起動時探索）はどちらも採らず、**既に `.env.worktree` に焼かれている割り当て済みポートを読んで避ける**形にした——衝突を確率ではなく事実として避けられ、`wt:new` が案内した URL も嘘にならない。`wt-new.sh` は 3001 から順に「他 worktree に割り当て済み」でも「使用中」でもない番号を選ぶ（3100 まで埋まっていればエラーで止める）。`dev.sh` は変更なし（割り当て外のプロセスが番号を掴んでいる場合の警告として今の形のまま残す）。あわせて `.env.worktree` の書き出しを `npm ci` の**前**へ移した——後だと選択から記録までの間に数分の窓ができ、`wt:new` を2本並行で走らせたときに互いの割り当てを見られず同じ番号を選ぶ（`code-quality-reviewer` の指摘）。判断の経緯は[定義書16の決定ログ](../仕様/log_16_git運用と並行開発体制定義書.md)、仕様は[定義書16](../仕様/16_git運用と並行開発体制定義書.md) §3.2

### T-46

- 背景: `wt-new.sh` の起点は既定でローカル `main`。**ローカルが origin より進んでいても検査せず worktree を作る**ため、未 push のコミットがそのまま全ブランチに複製される。2026-07-26 の T-39 第2バッチで実際に踏んだ——docs コミット1本（§8 の改訂）が未 push のまま6ブランチの起点になり、**6つの PR すべてに同じ docs 変更が写り込んだ**（GitHub は `origin/main` と比較するため）。ローカルで `main...branch` を見ると差分が出ないので、**ヘッドの検査では気づけずオーナーが PR 画面で発見した**
- 実害: 内容の巻き戻りは起きない（git は3方向マージなので、ブランチが変更していないファイルを古い版へ戻さない）。効くのは①PR の差分が汚れてレビューしづらい ②同じコミットが N 本の PR に重複して乗る ③マージ後にローカル main を rebase する際、既に適用済みのコミットの扱いで履歴が濁る
- 対応方針: `wt-new.sh` の起点解決時に `git rev-list --count origin/<base>..<base>` を見て、**0 でなければ警告して中断**する（`--allow-unpushed` 等で明示的に上書きできる逃げ道は残す）。スクリプト冒頭の「起点を最新にしたい場合は実行前に fetch / pull しておくこと」というコメントは**人間向けの注意で機械的な担保が無い**のが原因なので、そこを検査に変える
- あわせて検討: `origin/<base>` 自体が古い（fetch していない）場合も同様に危ういため、`git fetch` を内部で1回打つか、`--no-fetch` を既定にするかを着手時に決める
- 着手条件: 次に並行開発をするとき（同じ手順を踏むため）。単独モードでは worktree を作らないので急がない
- 関連: **[T-40](#t-40)（同じ `scripts/wt-new.sh` を触るので一緒に着手すると早い）** / [完了記録](./closed_23_技術改善バックログ.md) T-06・T-31〜36（worktree ツール一式） / [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §5 手順2
- 結果（2026-07-26）: `wt-new.sh` が起点解決の前に `git rev-list --count origin/<起点>..<起点>` を見て、0 でなければ**未 push のコミットを一覧表示して中断**する（`--allow-unpushed` で上書き可）。あわせて検討事項だった fetch は**既定で実行**に決めた（`--no-fetch` で省略可）——`origin/<起点>` が古いと検査そのものが意味を失うため。`origin/<起点>` が存在しない起点も同じ扱いで止める。**逆向き（起点が `origin` より遅れている）は警告のみで続行**する（`npm ci` の出力に流されるため最後の案内でも再掲）。手順の記述（[定義書16](../仕様/16_git運用と並行開発体制定義書.md) §5 手順2）から「実行前に fetch しておく」という人間向けの注意が消え、スクリプトの担保に置き換わった。中断と警告で扱いを分けた理由は[定義書16の決定ログ](../仕様/log_16_git運用と並行開発体制定義書.md)

### T-58

- 背景: `npm test` を実行した直後に `npm run test:coverage` を走らせると `Something removed the coverage directory "coverage/.tmp"` で異常終了することがある（T-39 の最終検証で `verifier` が遭遇。再実行で解消）。**[完了記録](./closed_23_技術改善バックログ.md) T-20 と同じ症状だが原因は別**——T-20 は並行実行による競合で `--no-file-parallelism` により発生条件を構造的に除去した。今回は**前回実行が残した `coverage/` を後続の run が引き継ごうとする**経路
- 対応方針: `test:coverage` の実行前に `coverage/` を掃除する（`rimraf` 相当を挟むか、vitest の `coverage.clean` 設定を確認する）。CI は毎回まっさらな環境なので影響はなく、**効くのはローカルで連続実行するとき**
- 着手条件: ローカルで再発したとき（現状は再実行で回避できる）
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-20（同じ症状・別原因）
- 結果（2026-07-26）: **上の「前回実行の残骸」という見立ては実測で否定された**——vitest は起動時に `coverage/` を消して `.tmp` を作り直す（`coverage.clean` の既定 true）ので残骸は元から掃除されており、`npm test` の直後に `test:coverage` を回しても再現しない。再現したのは**カバレッジ計測を2本重ねたとき**だけで、後発の掃除が先発の `.tmp` を消して先発が ENOENT で落ちる（vitest のエラー文言自体が「同じ `reportsDirectory` で複数の Vitest を同時に走らせていないか」と言っている）。したがって掃除を足す案は placebo と判断し、**同時実行を止める**方向へ倒した——`npm run test:coverage` を `scripts/test-coverage.sh` 経由にし、`node_modules/.cache` に排他ロック（`coverage/` の中に置くと vitest 自身に消される）を取る。2本目は理由を表示して即座に落ち、異常終了したロックは次回が奪い返す。ロックの実体は**シンボリックリンク（リンク先が保持者の PID）**で、作成と PID の記録が1操作で済むため「作った直後・PID を書く前」に別プロセスが残骸と誤認する隙が無い（`code-quality-reviewer` が mkdir＋PID ファイル版で実際に奪える経路を再現した）。残骸を奪い返す経路だけは `rm` と `ln` の2操作になるため、取得後に間を置いて**自分のリンクかを確かめてから進む**。EXIT での解放も**自分が保持している場合に限る**（奪われた後や手で消された後に、他の実行のロックを巻き添えにしないため）。仕様は[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8、経緯は[その決定ログ](../仕様/log_15_アーキテクチャ定義書.md)

### T-56

- 背景: `usecases/task/daily-list-usecases.ts` の `setTaskMode` / `setTaskProject` は `Promise<Result<TaskId, TaskEditError>>` を返すが `ok` しか返さず、`(daily)/actions.ts` の呼び出し側も返り値を検査せず `{ ok: true }` を返している。**型が存在しない失敗を宣言している**（不要な抽象）か、あるいは**「存在しない id への更新を検出しない」という設計判断が型に現れていない**か、どちらか
- 対応方針: 前者なら戻り値を `Promise<void>` / `TaskId` に落とすのが素直。後者（失敗を検出すべき）なら**挙動の話なので FB へ起票し直す**。着手時にどちらかを見極める
- 着手条件: `daily-list-usecases.ts` を別件で触るとき、または属性更新に失敗の概念を足すとき
- 関連: [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4（業務的失敗は `Result`）

### T-42

- 背景: `src/app/routines/actions.ts` の `MESSAGES[result.error]` は `Record<string, string>` へのインデックスアクセスのため、**未知のエラーコードが来ると `undefined` を返すのに型検査は通る**。`ActionResult.message: string` の契約を破りうる（画面には空のエラー帯が出る）。domain がエラーコードを増やしたとき、辞書へ追加し忘れても**コンパイルが通ってしまう**のが本質的な問題
- 対応方針: 辞書の型を `Record<string, string>` から**エラーコードの直和型**（例 `Record<RoutineError, string>`）へ絞る。追加漏れがコンパイルエラーになり、`undefined` の経路そのものが消える。**挙動は変わらない**（現状 `undefined` に到達する既知の経路は無い）
- 範囲: `routines/actions.ts` で発見したが、`(daily)/actions.ts`・`masters/{modes,projects,sections}/actions.ts` も同型の可能性がある。着手時に4本まとめて点検する
- 着手条件: domain のエラーコードを増やすとき（そのとき追加漏れが実害になる）、または上記いずれかの `actions.ts` を別件で触るついで
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-39 第2バッチ⑤（発見の契機） / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4（業務ルール由来の検証は domain）

### T-51

- 背景: `usecases/ports/task-repository.ts` は付帯更新（振り直し・自動セクション移動）を `relocation?: Relocations | null`・`renumber?: Renumber | null` で受ける。「なにもしない」を表す状態が**未指定・null・空配列の3通り**あるため、①実装側が毎回3通りを判定する ②呼び出し側が空配列を null へ詰め替える ③7メソッドが「付帯更新なし＝単文 / あり＝トランザクション」の**2経路を常に持つ**。`src/infrastructure` のカバレッジが 79.18% と相対的に低いのは、この片側経路が積み上がっているため
- 対応方針: Port を `relocations: Relocations`（空配列＝付帯更新なし）に正規化し、`?` と `null` を廃す。実装の判定は `length === 0` の1回に減り、詰め替えも消える。**トランザクションを常に張るかは別論点**（往復コストの判断が要る）なので、この T では分岐の条件式だけを1本化し、経路の統合は着手時に測って決める。**挙動は変えない**
- 着手条件: リポジトリに書き込みメソッドを追加するとき（同じ3状態判定をもう1つ書くことになる）、または infrastructure のカバレッジを埋めにいくとき
- 優先度の根拠: いま壊れてはいないが、「なにもしない」の表現が3通りある状態は**新しいメソッドを足すたびに再生産される**。修正は型定義と機械的な追随で、既存の統合テストがそのまま網になる
- 関連: T-50 / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §7（トランザクション境界）

### T-57

- 背景: `domain/mode/mode.ts` の `MODE_COLORS`（13値）と `MODE_COLOR_NAMES`（13値）が**添字で対応する並行リスト**で、片方だけ増やしても `modeColorName` が hex をそのまま返すフォールバックを持つため**静かに劣化する**。加えて `infrastructure/db/seed.ts` が hex を直書きしている（「プリセットから選ぶ」という自衛コメント付き＝定数を参照すればコメントごと不要）
- 対応方針: `[{ value, name }]` の1本にまとめ、`seed.ts` はそこを参照する。**挙動は変えない**
- 着手条件: プリセット色を増減するとき、または `mode.ts` を別件で触るとき
- 関連: [データモデル定義書](../仕様/14_データモデル定義書.md) / [T-43](./closed_23_技術改善バックログ.md#t-43)（テストの `MODES` フィクスチャがプリセット外の色を使っていた件は 2026-07-26 に `MODE_COLORS` 参照へ**対応済み**。本 T に残るのは並行リストそのものの統合）

### T-59

- 背景: カバレッジ 0%。**公開リポジトリにおける唯一のアクセス制御**（N-03）でありながら、`NextRequest → NextResponse` の純粋な関数なのに node 段のユニットテストが1本もない。§8 の「カバレッジの参照」は `proxy.ts` を「未着手の段に委ねた範囲」と書いてきたが、**これは E2E 固有ではなくユニットで測れる範囲**——T-39 で presentation の線引きを見直した結果、同じ誤りがここにも残っていたと分かった
- 未検証の分岐（すべてユニットで塞げる）: 環境変数が片方だけ／両方未設定なら素通し ／ `Authorization` なしで 401＋`WWW-Authenticate` ／ **不正 base64 で 500 ではなく 401**（コードのコメントが明示している防御そのもの） ／ ユーザ名だけ一致・パスワードだけ一致で 401 ／ `:` を含まない decoded で 401 ／ パスワードに `:` を含む場合に通ること
- 補足: 判定順が読みにくい箇所がある（`sep !== -1` をユーザ名一致より後に評価している）。テストで固定すべき
- 対応方針: `src/proxy.test.ts` を node 段で書く。**認証情報の値は絶対に書かない**（環境変数をテスト内で組み立てる）
- 着手条件: **すぐ**。優先度が高い理由は、①唯一のアクセス制御である ②修正コストが小さい（純粋関数） ③公開リポジトリのため防御の穴が外から観察されうる、の3点
- 関連: 要件定義書 N-03 / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8

### T-70

- 背景: CLAUDE.md「サブエージェント運用」は「レビュー・検証エージェントの指摘への対処（修正）はメインセッションが行い、必要なら同じエージェントに再検証させる」と書く。一方 [仕様16](../仕様/16_git運用と並行開発体制定義書.md) §2 はワーカーについて「worktree の内側では CLAUDE.md の運用をそのまま実行する（Explore/Plan → 実装 → verifier・code-quality-reviewer → spec-reviewer・test-reviewer）」と書いており、**並行時に誰がレビューを起動し誰が直すかが2文書で食い違っている**。CLAUDE.md の一文は単独モードを前提に書かれたが、並行モードにもそのまま読めてしまう
- 実害（2026-07-26 の並行4本で観測）: ①ヘッドがタスクカードに「検証コマンド → ローカルコミット」までしか書かなかったため、T-51 のワーカーが完了報告に「レビューは起動していない。PR の diff に対してヘッド側で起動してほしい」と書いて**完了状態のまま待機**し、その分だけ並行性が失われた ②指摘への対処に**ヘッド → ワーカーの往復**が要る。実装文脈を持つのはワーカー側なのに、指摘は一度ヘッドが全文を読み要約し直して送り返される（T-42 で実際に発生） ③4ブランチ × 最大4本 ＝ 最大16本のレビュー報告がすべてヘッドへ集まり、**最も希少なヘッドのコンテキストを食う**（[完了記録](./closed_23_技術改善バックログ.md#t-66) の実測で、消費の7割が 150k 超のコンテキストから出ている）
- 対応方針: **起動と対処はワーカー、裁定はヘッド**に分ける。①起動の判定は CLAUDE.md の「起動の契機」を worktree 内でそのまま引く（`git diff --name-only` で機械的に決まる） ②対処はワーカーが追加コミットで行う ③**見送りたい指摘をワーカーが自分で決めない**——「対処した」と「見送りたい（＋理由）」を分けて報告し、見送りの可否はオーナーが裁定する（自己レビューの利益相反への歯止め） ④`spec-reviewer` が docs の条項に関わる指摘を出したら **docs を編集せずエスカレーション**する（共有 docs はヘッドが一元記入＝§3.4／挙動レベルの仕様判断は委譲しない、の線は動かさない） ⑤完了報告の書式に「指摘件数と要旨・対処内容・見送り希望・再検証結果」を足し、**生のレビュー本文はヘッドへ流さない**
- 前提の変化: [T-67](./closed_23_技術改善バックログ.md#t-67) で `verifier` から変異テストの責務が外れ、**作業ツリーを壊す経路が消えた**。それ以前はワーカーに検証系を任せる案自体が採れなかった
- 残る懸念: `test-reviewer` の**沈黙する失敗**（見逃しが「指摘なし」として返り、正常な合格と区別できない。T-66）は、起動側と受領側が同一になると誰も範囲の妥当性を確認しなくなる。⑤で件数を必ず書かせることを最低限の観測点に置くが、実効は運用で見る
- 反映先: CLAUDE.md「サブエージェント運用」／仕様16 §2・§4
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-66（サブエージェントの起動コスト）・T-67（`verifier` の責務縮小）

### T-52

- 背景: `routines/routines-table.tsx` の `run()` は `masters/_lib/use-master-action.ts` の `run` と逐語同型（実行前に `setError(null)` → `startTransition` → 成功なら `onSuccess`、失敗なら `setError(result.message)`）。masters は3テーブルで共有するフックとして切り出し済みだが、routines は同じものを `useTransition` ごと自前で持っている。型も共通の `ActionResult`
- 傍証: routines 側は `isPending` を持ちながら**セルの `disabled` に使っていない**。[FB-63](./21_ユーザーフィードバック.md#fb-63)（プロジェクト一覧の名前セルだけ `disabled` 漏れ）と**同型の取りこぼしが起きる場所**が1つ増えている状態
- 対応方針: `use-master-action.ts` を `src/app/_lib/` へ引き上げ routines からも使う。**挙動は変えない**——`isPending` を routines のセルに適用するかは §2.3 の適用範囲の判断（FB-63 / FB-66）なので、この T では触らない
- 着手条件: **[T-44](#t-44) に着手するとき**（同じ作業の一部として片付く）、または routines の保存まわりを別件で触るとき
- 優先度の根拠: 単体では実質15行の移動だが、FB-63 と同型の食い違いを生む面を1つ減らせる
- 関連: T-44 / FB-63 / FB-66 / [共通](../仕様/13_画面定義書/00_共通.md) §2.3

### T-64

- 背景: `applyOptimisticAction` の12ケースのうち、`remove` だけが domain の `withTask*` に委譲せず `map`/`filter` を直書きしている（`(daily)/_lib/optimistic.ts`）。他11ケースとの非対称で、T-48 で `.ts` へ切り出したことで目立つようになった
- 対応方針: `domain/task/daily-list.ts` に `withTaskRemoved` を足して揃える。挙動は不変
- 着手条件: `daily-list.ts` を別件で触るとき、または楽観アクションを増やすとき
- 優先度の根拠: 実害はなく読みやすさのみ。ただし直書きの枝だけテストの置き場が違う（ユニット段の `optimistic.test.ts` が「居ないID」と不変性をこの枝で見ている。T-48）ため、揃えると観点も domain 側へ寄せられる
- 関連: [T-48](./closed_23_技術改善バックログ.md#t-48) / T-63

### T-49

- 背景: 同一のドメインエラーコードに対する表示文言が複数箇所に写っており、**うち2件は実際に文言が食い違っている**
  - `daily-board.tsx`（クライアント側の打刻修正）と `(daily)/actions.ts`（サーバ側）が同じ4コードを持ち、3件は逐語一致だが **`ended_before_started` だけ「終了時刻は開始時刻より後にしてください」と「終了時刻が開始時刻より前になります」で異なる**
  - `routines/actions.ts` と `(daily)/actions.ts` がルーチン入力の5コードを共有し、**`invalid_start_time` が「HH:MM 形式で入力してください」と「HH:MM で入力してください」で異なる**
  - 見積もりの文言も2か所に直書き
  - **masters だけは良い形**——`masters/_lib/action-result.ts` に1本化し `Record<MasterError, string>` で型を閉じ、`action-result.test.ts` が対応表そのものを固定している
- 対応方針: masters 方式に揃える。画面ごとに `_lib/` の1ファイルへ辞書を集約し、型は [T-42](#t-42) の方針（エラーコードの直和型）で閉じる。クライアント側とサーバ側は同じ辞書を参照する。**文言そのものは変えない**——上記2件の食い違いを「どちらに寄せるか」はユーザーから見える変更なのでオーナー判断が要り、**挙動を変える決着なら FB へ起票し直す**
- 着手条件: **T-42 に着手するとき**（対象ファイルが完全に一致する）。または打刻・ルーチンのエラー文言を1つでも直すとき
- 優先度の根拠: 実害は今のところ「latent な文言の不一致」だが、T-42 と同時なら追加コストがほぼゼロで、放置すると辞書が5本目に増える
- 関連: T-42（統合起票も可） / `masters/_lib/action-result.ts`（先例）

### T-71

- 背景: `src/proxy.ts` は decoded 文字列を `sep = indexOf(":")` で分割するが、**`sep === -1`（`:` を含まない）のときも先に分割を計算してから捨てている**——`reqUser = decoded.slice(0, sep)` は `decoded.slice(0, -1)`（末尾1文字を落とした文字列）、`reqPassword = decoded.slice(sep + 1)` は `decoded.slice(0)`（decoded 全体）になる。実際の可否は条件式の `sep !== -1` が先に評価されることだけで守られている
- 実害の形: このガードを失うと、**「パスワードがユーザ名＋任意1文字」という設定のとき、`:` を含まない1トークンを送るだけで認証を通せる**。2026-07-26 の [T-59](#t-59) で `sep !== -1` を削る変異を当てて実証済み（テストが1本落ちる）。**いま壊れてはいない**——テストで固定されたので、外せば赤くなる
- 対応方針: `sep === -1` の早期 return を先に置き、その後ろで分割する。評価順という暗黙の依存を消し、**読み手が条件式の順序を追わなくても安全と分かる形**にする。**挙動は変えない**
- 着手条件: `proxy.ts` を別件で触るとき。優先度を「高」にしないのは T-59 のテストが網として効いているため（防御は効いており、直すのは構造の話）
- 関連: [T-59](#t-59)（発見の契機。変異3で実証） / 要件定義書 N-03 / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8

### T-72

- 背景: `src/proxy.ts` は `Authorization` を `atob` で復号する。`atob` の戻り値は**バイト列を latin1 の文字列として解釈したもの**なので、ブラウザが UTF-8 で符号化して送った非 ASCII 文字は復号後に元の文字へ戻らない。結果として **`proxy.ts` が正しく照合できる文字集合が、意図した設計ではなく実装都合で ASCII に狭まっている**
- 対応方針: `Buffer.from(value, "base64").toString("utf8")` に替える。Node ランタイム（Fluid Compute）で動くので `Buffer` は使える。**照合できる文字集合が広がるだけで、ASCII の範囲では復号結果が完全に同一**（latin1 と UTF-8 は ASCII 領域で一致するため）
- 着手条件: `proxy.ts` を別件で触るとき（[T-71](#t-71) と同じファイルなので**一緒に直すのが早い**）
- 優先度の根拠: 低。狭まっていること自体が今すぐ何かを壊してはおらず、広げても ASCII 範囲の挙動は変わらない。ただし**実装都合の制約が仕様のどこにも書かれていない**状態は、後から理由の分からない不一致として現れうる（不一致は 401 になるだけで、原因が符号化にあるとは画面から読み取れない）
- 関連: [T-59](#t-59)（発見の契機。テスト作成時に判明。テストは ASCII の架空値のみを使うため現状の網には掛からない） / [T-71](#t-71)（同ファイル） / 要件定義書 N-03

### T-73

- 背景: `src/proxy.ts` の `config.matcher` が持つ除外パターンは、**`.` をエスケープしておらず、末尾の境界も無い**。正規表現の `.` は任意の1文字に一致するため `/faviconXico` が `/favicon.ico` の除外に掛かり、境界が無いため `/_next/staticX` も `/_next/static` の除外に掛かる。結果として、**唯一のアクセス制御（N-03）の除外リストが意図より広い範囲を対象外にしている**
- 実害の大きさ: **いま実害は無い**——該当する経路は実在しないため、素通ししても Next.js が 404 を返すだけで、認証すべき何かが露出しているわけではない。問題は「除外リストが意図と一致していない」こと自体で、**将来 `/favicon` や `/_next/static` に近い名前のルートを足したときに、静かに認証の外へ落ちる**
- 対応方針: `.` をエスケープし、除外の末尾に境界を置いてパターンを意図どおりに絞る。**認証を要する範囲が広がる向きの変更**で、既存の実在ルートの扱いは変わらない
- 着手条件: `proxy.ts` を別件で触るとき（[T-71](#t-71)・[T-72](#t-72) と同じファイルなので**まとめて片付けるのが早い**）、または `/favicon` や `/_next` に近い名前の公開ルートを足すとき
- 優先度の根拠: 実害が出ていないので高くはしないが、**気づける契機が無いまま将来の追加で穴になる**種類なので、放置より起票が安い。`proxy.test.ts` の matcher テストは現状「Next の突き合わせの近似」であり、この緩さは捉えない
- 関連: [T-59](#t-59)（発見の契機。`test-reviewer` が実装側の所見として報告） / [T-71](#t-71)・[T-72](#t-72)（同ファイル） / 要件定義書 N-03

### T-45

- 背景: `src/app/_lib/keyboard.ts` が提供するのは `inlineEditKeyHandler`（インライン編集中のキー判定）だけで、**画面全体のショートカットが必要とする共通ガード**——IME 変換中（`isComposing`）・修飾キー（Cmd/Ctrl/Alt）・入力欄（INPUT・TEXTAREA）へのフォーカス——は各所で個別に実装されている（`use-daily-shortcuts.ts`・`date-picker.tsx`・`review-board.tsx`）。T-39 第2バッチで各テストに「00_共通 §3 共通規約」ブロックが重複して現れたのはその写像
- 実害: 現時点では各実装とも規約を満たしている（テストで固定済み）。効くのは**次にショートカットを持つ画面が増えたとき**——ガードを1つ書き落としても気づけない（[共通](../仕様/13_画面定義書/00_共通.md) §3 は条項としては1か所にあるが、それを守る仕組みがコード側に無い）
- 対応方針: `keyboard.ts` に「このイベントはショートカットとして扱ってよいか」を判定する純関数を足し、3か所から使う。**挙動は変えない**（現在の各実装と同じ判定に揃える）
- 着手条件: ショートカットを持つ画面をもう1つ増やすとき、または上記3ファイルのいずれかを別件で触るついで
- 関連: [共通](../仕様/13_画面定義書/00_共通.md) §3（キーボード操作の共通規約） / T-39 第2バッチ①⑤⑥

### T-50

- 背景: 「挿入位置の前後から中間値を採り、尽きたらグループ全体を1000刻みで振り直す」（[データモデル定義書](../仕様/14_データモデル定義書.md) §3.5）という**1つの規則が5か所で個別に組まれている**（`domain/task/placement.ts`・`reorder.ts`・`relocation.ts` の3箇所）。いずれも `insertBetweenSortOrder` → `!ok` なら並びを作り直して `renumberSortOrders` → 差分だけ返す、という同じ手順で、返す型だけが違う
- 付随: 「セクション内を `sort_order` 昇順に並べる」も7か所に散っており、**うち2つ（`usecases/task/operations.ts`・`punch-usecases.ts`）は domain の並び順規則が usecases 側へ染み出している**（純粋な導出なので domain に属する）
- 対応方針: `domain/task/sort-order.ts` に「挿入位置から採番と（必要なら）振り直しを同時に返す」純関数を1つ置き、5か所をその上に載せ替える。並べ替えヘルパは domain の1関数に寄せ、usecases 側の2実装を消す。**挙動は変えない**（既存のユニットテストが規則を固定しているので変異で確認できる）
- 併せて片付ける: **振り直しの要素型（`taskId` と `sortOrder` の対）が `placement.ts`・`reorder.ts`・`usecases/ports/task-repository.ts` の3か所で完全一致している**。T-51 で3者とも「空配列＝振り直しなし」に揃った結果、型としても同一だと露見した（2026-07-26 のレビュー指摘。T-51 のスコープ外として送られた）。集約先は上と同じ `sort-order.ts` が自然
- 着手条件: §3.5 の採番規則を変更するとき、または**新しい挿入経路（タスクを生成・移動する新操作）を足すとき**——そこで6か所目を書くくらいなら先に寄せた方が早い
- 優先度の根拠: 現在は5実装とも正しくテストで固定されている。効くのは変更時で、**1か所落とすと「特定の密度でだけ並びが壊れる」再現しにくい欠陥**になる
- 関連: [データモデル定義書](../仕様/14_データモデル定義書.md) §3.5 / T-51

### T-47

- 背景: 表示側の `src/app/_lib/format.ts` は `APP_TIME_ZONE = "Asia/Tokyo"` を `Intl` に渡して**固定**で整形する一方、入力側の `applyClockTime`（`domain/task/punch-edit.ts`）は `Date#setHours` ＝**実行環境のローカルタイムゾーン**で解釈する。**JST で動く限り両者は一致するので実害は無い**が、前提が食い違っていること自体がコードから読み取れない
- 顕在化した経路: T-39 第2バッチ③のテストがこの非対称に足を取られ、UTC の CI で1件が反転して落ちた（JST では「開始 09:00 に対して 08:00 は前」、UTC では「開始 00:00 に対して 08:00 は後」）。テスト側は「表示を見るデータは JST 固定の壁時計・入力解釈を見るデータはローカルの壁時計」と使い分けて解決済み（②のワーカーは最初からこの使い分けができていた）
- **振り分けの決定（2026-07-26・オーナー判断）**: **①アプリ全体を Asia/Tokyo 固定に寄せる**。`applyClockTime` にタイムゾーンを渡し、入力の解釈も `APP_TIME_ZONE` に従わせる。**JST では現状と結果が同一なので挙動は変わらず**、本 T のまま実装に進める（②ブラウザのローカルに従う案は「海外で開くと現地時刻で表示される」＝挙動の変更で、`task_date` が保存された論理日付である設計・日界の設定とも整合を取り直す必要があるため採らない。将来必要になれば要求軸で新規に起こす）
- 対応方針: `applyClockTime`（`domain/task/punch-edit.ts`）が `Date#setHours` でローカル解釈しているのを、`APP_TIME_ZONE` を受け取る形へ変える。domain は `new Date()` 禁止・すべて引数で受け取る規約なので、**タイムゾーンも引数で渡す**（`format.ts` が定数を持ったままでよいか、共有の置き場へ出すかは実装時の判断）。既存テストの `atLocal()` 呼び分けは `atJst()` へ寄せられるはず
- **範囲（2026-07-26・オーナー判断）**: **`projection.ts` のローカル解釈も本 T に含める**。`dayStartAt`（`base.setHours(0,0,0,0)` と `now.getHours()`）はセクション終了時刻・予想開始時刻の土台なので、ここを残すと「表示は JST 固定・導出はローカル」の非対称が半分残る。`applyClockTime` だけに絞る案は採らない（[T-62](#t-62) の危険もこの導出側に residual として残るため）
- 着手条件: **解除済み**（振り分けが決まったので着手可能）。[T-55](#t-55) は本 T の裏面なので**一緒に片付ける**——タイムゾーンの持ち主が確定すれば `resolveToday` の配置も決まる
- 関連: [T-43](./closed_23_技術改善バックログ.md#t-43)（**症状はテスト側で予防済み**——`at()` が同名・別契約で2つ存在した件は 2026-07-26 に `atJst()` / `atLocal()` の呼び分けへ解消した。ただし**根の非対称は本 T で未解決**） / [データモデル定義書](../仕様/14_データモデル定義書.md)（`task_date` は打刻時刻から導出しない） / T-39 第2バッチ③
- **対応（2026-07-26・#68）**: `src/domain/shared/time-zone.ts` に `APP_TIME_ZONE` と壁時計の純関数（`zonedParts` / `fromZonedClock` / `withZonedClockTime`）を置き、`applyClockTime` / `editStartedAt` / `editEndedAt`（F-203）と `projection.ts` の論理日起点（F-116）をタイムゾーン引数へ変えた。表示（`formatClock`）も同じ純関数を通す。**domain は既定値を持たず、定数を束ねるのは usecases / presentation**。テストヘルパは `atLocal` を廃止し `atJst` 一本化。JST では結果が同一で挙動は変わらない。TZ 6種（UTC / New_York / +14 / −11 / London / Sydney）で全テスト緑、変異3件すべて赤。**運用タイムゾーンが Asia/Tokyo 固定である前提を[要件定義書](../仕様/12_要件定義書.md) §2.2 に明記**（どの仕様文書にも無かったことが `spec-reviewer` の指摘で判明）。夏時間に耐えるのは `fromZonedClock` 単体の契約で、上位の導出は「1日=24時間」前提のまま（Asia/Tokyo に夏時間が無いので成り立つ前提としてモジュール冒頭に明記）
- 残った分: [T-62](#t-62)（CI の TZ 宣言＝検出側。本 T で予防側は解消した）

### T-55

- 背景: `src/app/_lib/today.ts` の `resolveToday(sections, now)` は**リポジトリを引数に取り I/O を伴う**。[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) が示す domain / usecases の判別基準は「リポジトリ（I/O）に触るか」なので、基準上は usecases に属する。現状 presentation に置かれている理由はファイル冒頭に明記されている（タイムゾーン抽出が `Intl` 依存で presentation の都合）が、**複数の `page.tsx` と `actions.ts` から呼ばれる中核の導出**になっている
- 対応方針: 2026-07-26 に [T-47](#t-47) の振り分けが**「アプリ全体を Asia/Tokyo 固定に寄せる」**と決まった。タイムゾーンは domain が引数で受け取る形になるため `Intl` 依存を presentation に留め置く理由が薄れ、**`resolveToday` は usecases へ移せる**（層の判別基準「リポジトリ（I/O）に触るか」どおりの配置）。**T-47 と一緒に動かす**（単独で動かさない、は T-47 の判断待ちが理由だったので解消）
- 着手条件: **解除済み**。T-47 と同じブランチで片付ける
- 優先度の根拠: 実害はゼロ（正しく動きテストもある）。層の判別基準との食い違いが読み手を迷わせるだけなので、T-47 のついでが最も安い
- 関連: T-47 / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §3
- **対応（2026-07-26・#68）**: `resolveToday` / `todayFromSections` を `src/usecases/section/resolve-today.ts` へ移した（`expand.ts` と同じ「集約の非CRUD導出は独立ファイル」の流儀）。純粋な日付導出 `todayLogicalDate` は domain（`shared/logical-date.ts`）へ下げ、引数は `(now, timeZone, dayStartMinutes)`。**`now` の既定 `new Date()` は落として presentation（3つの page）が渡す形にした**（usecases に時計を持たせない）

### T-74

- 背景: `src/app/(daily)/actions.ts` の各 Server Action は「どのユースケースの失敗にどの辞書を当てるか」を手で対応づけているが、**その配線を型もテストも検証していない**。[T-49](#t-49) の変異6で実測——`startTaskAction` が引く辞書を `PUNCH_MESSAGES` から `OPERATION_MESSAGES` に差し替えても、**型検査も全テストも通った**
- なぜ型で防げないか: `PunchUsecaseError ⊆ TaskOperationError` という包含関係があるため、広い方の辞書で狭い方のコードを引いてもコンパイルが通る。実際に変わるのは `not_completed` の文言だけ（`OPERATION_MESSAGES` が「複製して開始できるのは完了タスクだけです」で上書きしている）で、**打刻の失敗にルーチン化まわりの文言が出る**という静かな取り違えになる
- なぜテストで防げないか: `actions.ts` は[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 の線引きで自動テストの対象外。コンポーネントテストは Server Action をスタブするため、**辞書の選択という presentation 側の分岐を通らない**
- **方針（2026-07-26・オーナー判断）**: **辞書間の文言差を「複製して開始」専用の辞書へ隔離する**。`OPERATION_MESSAGES` が `not_completed` を上書きしているのが差の全量なので、上書きを当該アクション専用の辞書へ出し、共有辞書は包含関係にあっても文言が完全一致する状態にする。**取り違えても文言が変わらない＝欠陥が構造的に消える**形で、差の再導入は `error-messages.test.ts` の不変条件テスト（共有コードで文言が一致すること）が検出する。挙動不変・[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 の境界は動かさない
- 採らなかった案: ①`actions.ts` に薄いテストを置く（§8 の境界を広げる docs 改訂＋合成ルートゆえ DB 前提のテストが要る。網は厚いが代償が大きい）②辞書の選択をユースケース側へ寄せる（表示文言＝presentation の都合を usecases へ持ち込み層の責務に反する）③ドメインエラー型を包含関係のない直和に分ける（意味的に共通な `task_not_found` 等を分ける不自然さが残り、全呼び出し元へ波及する）
- 着手条件: `actions.ts` の辞書対応を増やすとき、または [T-60](#t-60)・[T-61](#t-61) でテストの網を広げるとき（§8 の境界を触る作業がまとまる）
- 優先度の根拠: いま壊れてはいない（現在の配線は正しい）が、**取り違えても誰も気づけない**のが問題。文言の取り違えは実害が小さいので中に留める
- 関連: [T-49](#t-49)（発見の契機。変異6が生存） / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 / T-60・T-61
- **対応（2026-07-26・#65）**: `OPERATION_MESSAGES` から `not_completed` の上書きを外し、複製して開始（F-208）専用の `DUPLICATE_AND_START_MESSAGES` へ隔離した。`TaskOperationError` として `not_completed` を返すのは `duplicateAndStartTask`（`operations.ts`）だけ（`operations.ts` が import する打刻ドメイン関数は `canFinish` のみで、`not_completed` を返す `canUndoComplete` は import していない）なので**挙動不変**。T-49 の変異6（`startTaskAction` の辞書差し替え）を再実行し、**全5コードで文言が同一**であることを実測した。差の再導入・拡大は `error-messages.test.ts` の不変条件テスト2本が検出する（変異で実測）。あわせて `TaskOperationError` の重複メンバーを畳み `PunchUsecaseError | "not_postponable"` へ（集合は6コードで同一・網羅性検査が弱まらないことを変異で実測）。§8 の境界は動かしていない
- **但し書き（残存リスク → [T-76](./23_技術改善バックログ.md#t-76)）**: 「取り違えても文言が変わらない」が成り立つのは**共有2辞書の間だけ**。`DUPLICATE_AND_START_MESSAGES` は `Record<TaskOperationError, string>` で包含が残るため、**打刻系アクションで誤って引いても型が通る**（`undoCompleteAction` で変異させ、型検査も全テストも緑のまま文言が変わることを実測）。歯止めは「1アクション専用の自己説明的な名前」と辞書 doc の明記のみ

### T-75

- 背景: エラー文言辞書が `src/app/(daily)/_lib/error-messages.ts`（デイリーの6辞書＋`routineFromTaskErrorMessage`）と `src/app/_lib/routine-error-messages.ts`（`ROUTINE_ERROR_MESSAGES` の12件）の2ファイルに分かれている。[T-49](./closed_23_技術改善バックログ.md#t-49) で集約したときに**「1画面でしか使わないものは画面配下・画面をまたぐものは共有」**で置き場を分けた結果で、ルーチン文言はルーチン管理（画面定義書02 §4）とデイリーのルーチン化（画面定義書01 §4.1）の双方から引かれるため共有側へ置いた
- 分かれていることの読みにくさ: ①**名前の軸が揃っていない**——一方は領域名なしの `error-messages`、他方は領域名つきの `routine-error-messages`。ファイル名からは「デイリー用とルーチン用」に見えるが、実際の軸は「画面配下か共有か」 ②画面配下のファイルが共有側を import しており（`ROUTINE_FROM_TASK_MESSAGES` がルーチン文言を流用する）、**1つの文言を追うのに2ファイルを行き来する** ③文言を足すとき、どちらに書くかの判断が毎回必要になる
- 方針（2026-07-26・オーナー決定）: **全部 `src/app/_lib/` へ寄せる**。画面配下と共有で分けず、文言辞書は1か所に集める
- 対応方針: `(daily)/_lib/error-messages.ts` を `app/_lib/` へ移し、`routine-error-messages.ts` と名前の軸を揃える（1ファイルに統合するか `app/_lib/error-messages/` 配下へ領域ごとに置くかは着手時に決める）。テスト2本も一緒に動かす。import 元は3ファイル（`(daily)/actions.ts` / `(daily)/_components/daily-board.tsx` / `routines/actions.ts`）。**挙動は変えない**——文言・型・公開する名前を変えない純粋な移動
- 着手条件: すぐ（小さい）。[T-74](#t-74) の方針が「文言差を専用辞書へ隔離」に決まり移動先は変わらないため、**T-74 と同じブランチで片付ける**（同じ辞書ファイルを触るので分けると衝突する）
- 優先度の根拠: 実害は無く読み手の迷いだけ。ただし文言を足すたびに置き場の判断が発生するので、辞書を触る別件のついでに済ませたい
- 関連: [T-49](./closed_23_技術改善バックログ.md#t-49)（分割の由来）/ T-74（同じ辞書まわり）/ [FB-71](./21_ユーザーフィードバック.md#fb-71)（`ROUTINE_FROM_TASK_MESSAGES` の未記入コード。移動しても残る）
- **対応（2026-07-26・#65）**: 2ファイルを `src/app/_lib/error-messages.ts` **1ファイルに統合**（`error-messages/` 配下への領域分割は採らず——`ROUTINE_FROM_TASK_MESSAGES` が `ROUTINE_ERROR_MESSAGES` を参照しているため、分割すると「1文言を追うのに複数ファイル」が戻る）。テスト2本も統合し、旧ルーチン側の観点と条項参照（画面定義書02 §4）は `it` として保存。文言・型・公開名は無変更で、import 元3ファイルは指定子の差し替えのみ。**[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §2 に「エラー文言辞書はコロケーションの例外として `_lib/` に置く」を追記**（コロケーション条項との字面上の食い違いを `spec-reviewer` が指摘）
- 残課題: [T-77](./23_技術改善バックログ.md#t-77)（`ROUTINE_ERROR_MESSAGES` だけ命名軸が違う。公開名を変えない要件のため据え置き）/ [T-78](./23_技術改善バックログ.md#t-78)（`masters/_lib/action-result.ts` に同一文言の別辞書が残る）

### T-53

- 背景: `daily-list.tsx`（662行）は `DailyList`・`GroupHeading`・`TaskRow`・`AssignCell` の4コンポーネントを抱える。特に `TaskRow` は**プロパティが25個**で、その型定義は親の `Props` とほぼ同じ内容を2度書いている。結果として `daily-list.test.tsx` は1000行超の単一ファイルになり、**セル1つの挙動を見るにも毎回テーブル全体を描画する**（コロケーションの粒度が「画面」で止まっていて「部品」まで下りていない）
- 対応方針: `TaskRow`・`GroupHeading`・`AssignCell` を別ファイルへ出し、テストも部品ごとに分ける。props の型は共有の型から派生させて二重定義をやめる。**挙動は変えない**
- 着手条件: `daily-list.tsx` を別件で触るとき、または `daily-list.test.tsx` がこれ以上伸びるとき（すでに本番の1.6倍）
- 優先度の根拠: いま困っているのは主にテストの読みやすさで機能上の実害はない。[T-48](#t-48) と流儀が同じなので同時にやると段の整理として一貫する
- 関連: T-48 / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8
- **対応（2026-07-26・#67）**: `TaskRow`・`GroupHeading`・`AssignCell` を別ファイルへ分離（本体662行 → 171/358/94/57行）。**props の型は子を単一の真実にし**、`DailyListProps` を `Pick<TaskRowProps, …> & Pick<GroupHeadingProps, …> & Readonly<{ groups, selectedId, editing }>` として派生させて25個の二重定義を解消（子→親の向きにしたのは循環 import を作らないため。`Pick` なので子に props が増えると JSX 側で必ずコンパイルエラーになる）。編集状態の型は `_lib/editing.ts` へ。`AssignCell` の `dimmed: string` は `isDimmed: boolean` へ改めた
- テストも部品ごとに分割（1049行80本 → 317/665/152/69行）。**リストに残したのは「組み立てて渡す側」**（列の骨格・現在セクションの導出・マスタ解決・予想開始の積み上げ・選択行と編集セルの写像・セクション候補の配線）で、部品側は自分の描画・操作だけを見る。共有フィクスチャは `_testing/factories.ts`、表の DOM 読み取りは `_testing/table-helpers.ts` へ集約
- 挙動不変（本体は逐語移動でバイト一致を機械照合、class 出力も3状態で採取して前後一致を確認、変異テスト計12回）。レビューで観点の穴を追加テストで埋めた——経過の超過警告色（F-205）/ 予想開始のセクションまたぎ積み上げと日界の折り返し（F-116・F-120）/ 選択行の位置変化での再追従（§4.2・FB-20）/ ポップオーバー現在値の配線。**このうち日界のテストは最初に書いた形が `dayStartMinutes` を判別できておらず、変異テストで偽陽性を検出して組み直した**
- docs: [画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §3.3 に「モード未設定行の色」を条項として起こした（テストが実在しない条項を参照していたため。実装を正とした条項化で挙動は不変）
- 派生: [T-80](./23_技術改善バックログ.md#t-80)（`toOptions` の `_lib` 移設）/ [T-81](./23_技術改善バックログ.md#t-81)（`daily-board.test.tsx` の作法）/ [T-82](./23_技術改善バックログ.md#t-82)（フィクスチャの時間帯）/ [T-83](./23_技術改善バックログ.md#t-83)（未設定行の色判定が3箇所に分散）

### T-44

- 背景: `modes-table.tsx`（328行）・`sections-table.tsx`（272行）・`projects-table.tsx`（186行）は、共有しているのが `useMasterAction` / `ArchivedMasterSection` / `DeleteMasterButton` だけで、**インライン編集セルと新規追加行はそれぞれが自前で実装**している。T-39 第2バッチ④のテストが3テーブルで同型のシナリオ（インライン編集・新規追加・アーカイブ）を三重に書くことになったのは**テスト側の重複ではなく本番側の重複の写像**、というのが `code-quality-reviewer` の指摘
- 傍証: [FB-63](./21_ユーザーフィードバック.md#fb-63)（プロジェクト一覧の名前セルだけ `disabled={isPending}` が無い）は、まさに3つを別々に書いた結果として生じた取りこぼし。抽出すればこの種の食い違いは構造的に起きなくなる
- 対応方針: 編集セル（`inlineEditKeyHandler` ＋ blur 確定 ＋ 保存中の抑止）と新規追加行を部品として抽出する。**挙動は変えない**——変えるなら FB-63 の判断（[共通](../仕様/13_画面定義書/00_共通.md) §2.3 の適用範囲）を先に済ませる
- 着手条件: **解除済み**（2026-07-26 に §2.3 の適用範囲が確定した）。[共通](../仕様/13_画面定義書/00_共通.md) §2.3 は「保存中は同じ行は編集できない。適用は保存完了を待って反映する画面に限る」となり、**マスタ管理は適用対象**と確定。[FB-63](./21_ユーザーフィードバック.md#fb-63)（プロジェクト一覧の名前セルの `disabled` 漏れ）の実装を3テーブルへ足すことになるので、**先に本 T で抽出してから入れる方が早い**
- 関連: [FB-63](./closed_21_ユーザーフィードバック.md#fb-63)（本 T と同じ PR で対応済みになった） / FB-66（適用外と確定） / T-39 第2バッチ④
- **対応（2026-07-27・#66）**: 編集セルを `masters/_components/master-editable-cell.tsx`（表示/入力の切替・blur 1本の確定・Enter/Esc・変更なし判定・保存中の抑止）、新規追加行を `master-new-row.tsx`（保存/取消の並び・mousedown で blur 先取り・`data-field` からの値読み取り）へ抽出。列の並びは `renderCells` の render prop で表側に残す（`ArchivedMasterSection` と同じ流儀）。3表は 331/272/186行 → 273/202/134行。**modes / sections のテストは1行も変えずに緑**＝振る舞い保存の主な根拠。変異テスト計19か所で確認
- **条項の改訂を2度伴った**（[共通](../仕様/13_画面定義書/00_共通.md) §2.3。経緯は[log_00_共通](../仕様/13_画面定義書/log_00_共通.md)）: ①同じ行の編集・確定・取消をすべて止める（開いたままの入力欄からの再確定を含む）＋新規追加行の連打と取消 ②新規作成フォーム（インライン編集のセルに限らない）と「保存中に始める操作」（一覧の「新規追加」ボタン・送信せず表示だけを変えるその場の選択）。**いずれも3表すべてに元からあった持ち越しで、抽出が作った欠陥ではない**——部品が1か所に集まった直後が最も安く塞げるタイミングだったため同じ PR に含めた
- 副産物: 取消の2経路（ボタンと `Esc`）が同じ初期化をするようになった（従来は `Esc` だけカラーピッカーの開閉を残していた）
- 派生: [FB-76](../案件/21_ユーザーフィードバック.md#fb-76)（無効なボタンの見せ方が未定義） / [T-79](./23_技術改善バックログ.md#t-79)（3表の見出し・新規追加ボタン・エラー帯の重複）

### T-37

- 背景: presentation は自動テストの対象外（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8）のため、UI の挙動はオーナーの動作確認だけが網になっている。2026-07-25 に FB-51 で **Playwright による実機測定が2回とも決定的だった**——①「画面下部でポップオーバーが上向きに反転しない」の原因は、コードを読んで立てた仮説3つ（アンカーが想定外の祖先／実は反転不要／sticky ヘッダ未考慮）が**すべて外れ**で、測って初めて「アクティブ候補の `scrollIntoView({block:"nearest"})` が祖先＝ドキュメントまでスクロールし、反転判定が見る幾何情報を書き換えていた」と分かった ②修正後も測定で「ページのスクロール量0・パネル内だけが動く・上向きを維持」を確認できた。手作業で都度組むのは惜しい
- 方式: **サブエージェント**（CLAUDE.md のレビュアー表に1行増える形。`verifier` が機械的検証、`spec-reviewer` が docs 整合、という既存の責務の切り方と揃う）。スキルにするとメインセッションが手順を踏む形になり、長いブラウザ操作でコンテキストを消費する。**駆動は MCP のブラウザツール**で、`@playwright/test` をリポジトリに足さない（FB-51 の実測もこの形で行った）。以下4点は 2026-07-27 に確定:
  - **判定対象は機械判定できるもののホワイトリスト**: 幾何・スクロール量・クラストークン・フォーカス／キーナビの追従・コンソールおよびネットワークエラーの不在。「色が見づらい」「2行になって気になる」は知覚の判断で、エージェントの所見では代替できない（FB-52 の色・FB-51 のラベル折り返しはいずれもオーナーの実機知覚で決まった）。**報告には「測っていないこと」を必ず書かせる**——ここを曖昧にすると「確認したことになっている」状態を作る。本エージェントは**オーナーの動作確認を置き換えない**（その手前で機械判定できる分を潰す道具）
  - **確認専用DBを用意し、dev サーバをそちらへ向ける**: 開発DB（`db` / :5432）には `db:sync-masters` で**本番のマスタ・ルーチンが入る**ため、破壊的操作（打刻 `B`/`F`・削除 `D`・複製 `Y`）より**実名がエージェントの報告・スクリーンショット経由で public リポジトリへ流出する方が重い**。使い捨ての `db-test`（:5433・tmpfs）の中に統合テストとは**別のDB名**で確認用DBを作り、migrate → seed（架空マスタのみ）→ 確認用フィクスチャ投入 → `DATABASE_URL` と `DEV_PORT` を渡して `npm run dev` までを1本のスクリプトで行う。実データを持つインスタンスと物理的に別になり、`docker compose down` で消える
  - **自動テストと共有するのは環境だけ**: CI で回る自動テストとは目的が違い、駆動方法も別（本 T は MCP ブラウザ）。重なるのは**確認用DBと dev サーバの起動手順**だけなので、そこを共有物として切り出したうえで**本 T を先に軽く作る**。台帳が以前「片方を作ればもう片方がその上に乗る」と書いていた重なりは、実際には環境に限られる。**その環境を使うのは E2E（[T-85](./23_技術改善バックログ.md#t-85)）だけ**——[T-03](./23_技術改善バックログ.md#t-03)（ブラウザテスト段）は部品を実ブラウザに描くだけで DB もサーバも要らない（2026-07-27 に T-03 の主題を E2E から段3へ移した際の整理）
  - **起動の契機はオーナー動作確認の直前**（実装完了後フローの手順2の手前）。差分が**幾何・スクロール・ポップオーバー・キーナビに触れたときだけ**起動する。機能完成時に `spec-reviewer` / `test-reviewer` と並列にすると dev サーバとDBの起動を毎回伴い重い
  - **`db:seed` は厚くせず、確認用フィクスチャを別スクリプトで上乗せする**: `db:seed` は本番の初期投入にも使い、[データモデル定義書](../仕様/14_データモデル定義書.md) §5 が「ここが初期データの正」（sections 5件・modes 数個・**projects は空**）と定めている。確認用に要るデータ（画面下部までスクロールする程度のタスク・候補が十分あるマスタ）は**確認観点に従属して変わる**もので、§5 の安定した契約とは寿命が違う。分けておけばダミーが本番へ流れる事故も構造的に防げる
- 実装したもの（2026-07-27）:
  - `npm run dev:check`（`scripts/dev-check.sh`）— db-test インスタンス内に `hitosuji_check` を作り、migrate → フィクスチャ投入 → `DATABASE_URL` と `DEV_PORT` を渡して dev サーバ起動（:3100。worktree の割り当て 3001〜3099 の外側）まで1本で行う。統合テストの `hitosuji_test` とは別DBなので同時に走らせても干渉しない（dev:check 起動中に `test:int` が緑なのを実測）
  - `src/infrastructure/db/testing/browser-check-fixtures.ts` — 毎回 TRUNCATE して入れ直す。マスタは `seedMasters`（§5 の契約）をそのまま使い、確認用のプロジェクト2件・ルーチン2件・タスク29件を足す。タスクは `domain/*/testing/` の `task()` / `routine()` から組み、**id は serial に任せる**（明示すると sequence が追随せず、確認中に画面から追加した行が衝突する）
  - `.claude/agents/browser-checker.md` — 測ってよいもの（幾何・スクロール量・クラストークン・フォーカス／キーナビ・エラーの不在）と測ってはいけないもの（知覚の判断）を明記し、報告に「測っていないこと」を書かせる。確認専用環境以外へ接続しないことを制約に入れた
  - `next.config.ts` の `distDir`（`NEXT_DIST_DIR` で切り替え）— 確認用は `.next-check` へ出す。本体の dev サーバ（:3000）と `.next` を共有すると2つの `next dev` が同じ場所へ書き合うため。**オーナーの動作確認の直前に起動する運用なので、両方が同時に立つのが通常**。副作用として Next.js が `tsconfig.json` の `include` に `.next-check` の型パスを自動追記する（両方の出力先が載った状態で安定する）
  - CLAUDE.md（サブエージェント表・起動の契機・実装完了後フローの手順2・開発コマンド）／[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8（テスト補助の置き場・ブラウザ実機確認の位置づけ）／README
- **確認環境は機械全体で1本まで**: DB名（`hitosuji_check`）もポート（:3100）も固定で、worktree ごとに分かれるテストDBとは違う。実データと分離されている以上ワーカーからも実行してよいが、**2本目は1本目のデータを TRUNCATE で壊す**ため、起動前のポート確認（DB に触る前に走る）が唯一の歯止めになっている。並行開発でどちらが使うかはヘッドが調停する
- **未解明の事象**: 2026-07-27 の初回運用で `/masters/*` だけが 404 になった（トップレベルのルートは正常）。`.next` を消して起動し直すと解消。「直前の `npm run build` の出力が残っていたため」と考えて `dev-check.sh` に `.next` を作り直すガードを入れたが、**同じ順序（build → dev 起動）を単独で再現したところ 404 は起きず**、仮説が誤りと分かったのでガードは外した。再発したら `.next` を消して起動し直す（原因は未特定のまま）
- 関連: [T-85](./23_技術改善バックログ.md#t-85)（E2E。環境を共有する。上記） / [T-03](./23_技術改善バックログ.md#t-03)（ブラウザテスト段。環境は共有しない） / [完了記録](./closed_23_技術改善バックログ.md) T-34（dev サーバの並列化） / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 / [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §2・§3.2

### T-69

- 背景: `npx tsc --noEmit` を打つと**10件の型エラー**が出る（すべて `src/app/(daily)/_components/daily-board.test.tsx` の `gate.resolve({ ok: false, message: ... })`）。にもかかわらず lint・build・テストはすべて緑で、**誰も気づけない状態が続いていた**（2026-07-26 に T-40 の作業中、無関係な調べ物の副産物として発見）
- 検査が無い理由: `tsconfig.json` の `include` は `**/*.ts` / `**/*.tsx` なのでテストも型検査の対象だが、**その検査を走らせる経路が無い**。CI（`.github/workflows/ci.yml`）は `lint` → `build` → `test:coverage` の3本だけで、`next build` の型検査はアプリのビルドを通る範囲しか見ない（テストファイルは通らない）。vitest は型を検査せず実行するだけなので、テストの型は**書いた瞬間から誰の網にもかからない**
- 現に出ているエラーの原因: `hold<T>(settleOnCleanup: T)`（同ファイル内のローカルヘルパ）が引数から `T` を推論するが、渡している `const OK: DailyActionResult = { ok: true }` は**制御フロー解析で `{ ok: true }` に狭められている**ため、`T` から失敗側（`{ ok: false; message: string }`）が落ちる。そこへ `gate.resolve({ ok: false, ... })` を渡すので `Type 'false' is not assignable to type 'true'` になる。**実行時は正しく動く**（テストは緑）ので、型だけが嘘をついている状態
- 対応方針: ①`npm run typecheck`（`tsc --noEmit`）を足し、CI と `verifier` の手順に載せる ②現に出ている10件を直す（`hold` の呼び出しに明示型引数を与えるか、`resolve` を広い型で受ける形に変える）。**①を先に入れると CI が赤くなる**ので、同じ PR で②まで済ませる
- 着手条件: すぐ。検査が無い間は同種の型エラーが積み上がり、後で直す量が増える
- 優先度の根拠: 挙動は壊れていない（実行時は正しい）が、**テストの型が壊れても気づけない**状態はテストを増やすほど効いてくる。テスト整備（T-60・T-61）に着手する前に検査を入れておきたい
- 関連: [T-62](#t-62)（CI にタイムゾーンを明示・同じ「CI の網を足す」系） / T-60・T-61（テスト増強。先に検査を入れたい相手） / [テスト戦略定義書](../仕様/17_テスト戦略定義書.md)
- 結果（2026-07-27）: 方針どおり①②を同じ変更で入れた。①`npm run typecheck`（`tsc --noEmit`）を足し、CI（`ci.yml` の `lint` と `build` の間）・`verifier` の手順・CLAUDE.md「開発コマンド」・[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §9 に載せた。②10件は**すべて `hold` の呼び出しへ明示型引数を与えて解消**した（`hold<DailyActionResult>(OK)` 等の22箇所）。`resolve` を広い型で受ける案を採らなかったのは、**狭まった `T` は `promise` の型でもある**ため——`resolve` だけ広げると保留の promise が「成功しか返さない」と宣言したまま失敗を流すことになり、嘘の位置が変わるだけだった。narrowing が起きる理由（成功値定数が宣言型より狭い初期値を持つ）は `hold` の doc に残し、型引数を省略しないことを明記した。**テストの検証内容は変えていない**（ユニット815・コンポーネント670 が緑。統合テストは Docker の無い環境で作業したため CI 側で確認）。同ファイルの作法（列位置の直参照・独自フィクスチャ）は [T-81](./23_技術改善バックログ.md#t-81) に残す

### T-90

- 背景: Dependabot の version-update PR 2件（どちらも devDependencies のメジャー）。#81 = `jsdom` 29.1.1→30.0.1、#79 = `@types/node` 24.13.3→26.1.2。セキュリティアラート（Dependabot・code-scanning）は open 0件だった
- 対応: **#81 はマージ**。jsdom は vitest コンポーネントテストの DOM 環境（`vitest.config.ts` の `environment: "jsdom"`）にしか使われず本番の成果物に入らない。v30 の破壊的変更は Node 最低版（`^22.22.2 || ^24.15.0 || >=26.0.0`）のみで CI（Node 24）・ローカル（26.5.0）とも満たす。CI の `verify` は jsdom 30 の上でコンポーネントテスト込みの全テストを実行して緑＝この変更そのものを CI が検証済み。jsdom の `rgb()` 表記に期待値を合わせる `src/app/_testing/dom.ts` も緑で通過し、v30 の CSS 直列化修正の影響なしを実証。**#79 はクローズ**——本番 Vercel ランタイムが Node 24 系のため types は追い越さない（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §11）。現行 `^24` は追随済みで、[T-30](#t-30-typesnode-を本番ランタイムnode-24-系へ追随させる2026-07-25) のときと違い中間版へ上げる残作業は無い
- 結果: 挙動変更なし（lockfile とテスト環境のみ）。ローカル main は未 push の docs コミット1件を rebase で追随し `npm ci` で lockfile 整合。オープン Dependabot PR 0件・セキュリティアラート 0件

### T-62

- 背景: [T-47](./closed_23_技術改善バックログ.md#t-47) の非対称（表示は Asia/Tokyo 固定・入力解釈はローカル）により、テストヘルパの `at()` が **JST 固定とローカル壁時計の2契約**で併存している。最終レビューで6タイムゾーン（JST / UTC / NY / +14 / −11 / London）を実測し**全件緑**と確認できたが、**構造上の危険が残る**——`daily-list.test.tsx` の `renderList` の既定 `now` は JST 側で、この値は `formatClock`（JST 固定＝既定が正しい側）と `sectionEndAt`／予想開始時刻（ローカル壁時計＝既定が誤った側）の**両方**へ流れる。いま TZ 非依存なのは、projection を見るテストが `now` をローカルで明示上書きしているためで、**既定のまま projection の具体値を assert するテストを1本足した瞬間に CI だけ落ちる**
- 効き方の悪さ: 手元は JST・CI（ubuntu）は UTC で、`vitest.config.ts` にも `ci.yml` にも `TZ` 指定がない。この種の回帰は**「手元で再現しない CI 落ち」**として現れる（T-39 第2バッチ③で実際に1度踏んだ）
- 対応方針: CI で `TZ` を明示する。**UTC 固定にするか JST/UTC の2本回しにするか**は着手時に決める（2本回しなら二重契約が壊れた瞬間に落ちるが実行時間は倍）
- **予防側は [T-43](./closed_23_技術改善バックログ.md#t-43) で対処済み**（`atJst` / `atLocal` への改名で呼び分けを名前で強制した）。**本 T に残るのは検出側**——名前で分けても取り違えは起きうるし、実際 T-43 のレビューで「`atJst` を使うべきでない箇所」が1件見つかっている。それを機械的に落とすのが CI の TZ 指定
- **2026-07-26 の更新（T-47 の帰結）**: T-47 でアプリ全体が `APP_TIME_ZONE` 固定になり `atLocal` が消えたため、**本 T の「予防側の危険」（既定の `now` が JST 契約とローカル契約の両方へ流れる構造）は解消した**。残るのは検出側だけ——T-47 のレビューで `test-reviewer` が「`vitest.config.ts` にも `ci.yml` にも TZ 宣言がなく、**JST の手元ではローカル依存を再導入しても緑になる**」と改めて指摘している（T-47 自体は TZ 6種を手元で回して緑を確認した）
- 着手条件: CI で時刻起因の失敗が出たとき、または時刻まわりを別件で触るとき。**現状は手元で `TZ=... npm test` を回して確かめる運用**（T-43 では UTC・New_York・Sydney・Auckland の4TZ、T-47 では UTC・New_York・+14・−11・London・Sydney の6TZで確認した）
- 関連: [T-43](./closed_23_技術改善バックログ.md#t-43)（予防側） / T-47（根の非対称） / T-39 第2バッチ③
- 結果（2026-08-01）: CI のジョブに `TZ: UTC` を置き、テストだけ `TZ=Asia/Tokyo` でもう一度回す形にした（+13 秒）。契約は[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §9「実行タイムゾーン」、退けた案（極端タイムゾーン1本 / JST のみ）と選定の理由は[その決定ログ](../仕様/log_17_テスト戦略定義書.md)。着手時点で TZ 依存は残っておらず（4ゾーンで 1566＋63 本が全件緑）、足したのは網だけ。副次でフレークテストを1本直し、根本側を [T-92](./23_技術改善バックログ.md#t-92) へ起票した

### T-41

- 背景: 2026-07-21 の書き方の再整理（CLAUDE.md「書き方の規約」）で**詳細を下位文書1か所へ寄せ、上位文書は1〜2行の要約＋参照に留める**方針にしたが、**コード内のコメントが移動前の位置を指したまま**の箇所がある。判明分は `src/app/_lib/use-flip-up.ts` の JSDoc と `src/app/_components/date-picker.tsx` で、いずれも反転規則の出典を「画面定義書01 §7」と書くが、01 §7 は現在「表示位置と閉じ方は共通 §2.1 が正」と委譲する1行だけで、条項の実体は[共通](../仕様/13_画面定義書/00_共通.md) §2.1 にある
- 追加の判明分（2026-07-26・T-45 のレビューで発見）: `src/app/_lib/keyboard.ts` の `inlineEditKeyHandler` のコメントが出典を「画面定義書01 §6」と書くが、**この関数はマスタ管理でも使われており**、インライン編集の確定・取消は[共通](../仕様/13_画面定義書/00_共通.md) §2.3 の管轄。01 §6 はデイリーのショートカット一覧なので画面を限定した誤った参照になっている
- 実害: 参照をたどった人が「§7 には何も書いていない」と迷う。委譲の1行があるので最終的には辿り着けるため実害は小さいが、**同じ再整理を通った箇所は他にもある可能性が高い**（`docs/仕様/13_画面定義書/01〜04` から 00_共通 へ寄せた条項＝ポップオーバー・インライン編集・トースト・未設定表記）
- 対応方針: `src/` 内の「画面定義書」「§」を含むコメントを機械的に洗い出し、参照先が委譲スタブになっているものを実体側へ向け直す。**コメントの書き換えのみで挙動は変わらない**
- 着手条件: 同種の迷いが再発したとき、または該当ファイルを別件で触るついで
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-39 第2バッチ⑥（発見の契機） / CLAUDE.md「書き方の規約」
- 結果（2026-08-01）: `src/` の仕様参照コメントを機械抽出し（「画面定義書 / §」を含む481行・参照組38ユニーク）、委譲スタブを指していた13箇所を実体側へ向け直した（01 §7→共通 §2.1 / 01 §8→共通 §2.2 / 01 §6→共通 §2.3・§3 / **存在しない 01 §2.3**→共通 §2.3 / 要件定義書 §4→共通 §1 / 旧アーキテクチャ §8→テスト戦略定義書 §2・§4）。参照先に実体がある約390箇所は据え置き。並行作業中で触れなかった3箇所（`daily-board.tsx` のトースト3・`modes-table.tsx` の 03 §4・`review-board.tsx` の 04 §3.3）はマージ後に本体で処理した——うち 04 §3.3 は「S-01 と同じく薄色で `--:--`」を共通 §2.4 と二重に持っていたため、**先に画面定義書04 を委譲へ揃えてから**コメントを向け直している（[log_04](../仕様/13_画面定義書/log_04_レビュー.md)）。なお `modes-table.tsx` の同じ参照2箇所のうち直したのは1つで、もう一方（クリックで入力欄になる）は 03 §4 に実体があるため据え置いた

### T-63

- 背景: 楽観的更新で先に出す仮タスクの ID は `optimisticTask`（`(daily)/_lib/optimistic.ts`）が `-Date.now()` で作る。**同一ミリ秒内に2件追加すると同じIDになり**、`withTaskUpdated` や `remove` が2行に当たって片方のロールバックが他方を巻き添えにする
- 実害: 現時点では**無い**。クイック追加は手入力なので実操作の間隔がミリ秒を大きく超える。問題は再現条件が「1ms 未満の連続追加」で、将来の一括追加・貼り付け入力を足したときに静かに壊れること
- 対応方針: 採番をコンポーネント側の単調増加カウンタ（`useRef`。`pendingUndoSeq` に先例）へ移すか、`optimisticTask` の `now` 引数へそれを渡す。負のIDで「サーバ確定前」を表す規約自体は変えない
- 着手条件: タスクの一括追加・複数行貼り付けなど、**連続生成が起きる機能に着手するとき**。または仮タスクの取り違えを疑う不具合が出たとき
- 優先度の根拠: 実操作で踏めないため低。ただし踏んだときは「別の行が消える」という気づきにくい壊れ方をする
- 副次: **テストで固定できない**点も同時に解ける。コンポーネント段は `vi.useFakeTimers` で `Date.now()` が固定されるため、現状は「確定前に2件追加しても両方残る」を書くと必ず同一IDになり検証できない（`test-reviewer` の指摘。T-48）
- 関連: [T-48](./closed_23_技術改善バックログ.md#t-48)（切り出し元）/ T-64
- 結果（2026-08-01）: 採番を `daily-board` の単調増加カウンタ（`useRef`）へ移し、`optimisticTask` は `seq` を必須引数で受ける形にした。**負のIDで未確定を表す規約は不変**。副次の「テストで固定できない」も解け、偽時計のせいで書けなかった3件（確定前に2件追加しても両方残る／片方の編集・削除が他方を巻き添えにしない）を段2へ追加した（T-48 の指摘を回収）。あわせてクイック追加欄の `onKeyDown` を描画時からイベント時の組み立てへ変更し（`react-hooks/refs`）、その結線が守る IME 条項（[共通](../仕様/13_画面定義書/00_共通.md) §3）のテストも足した。変異（`-seq` → `-Date.now()` に戻す）で新規テストの teeth を実測済み

### T-77

- 背景: [T-75](./closed_23_技術改善バックログ.md#t-75) で辞書を `src/app/_lib/error-messages.ts` 1ファイルへ統合したが、`ROUTINE_ERROR_MESSAGES` だけ命名の軸が他と違う（他は `PUNCH_MESSAGES` / `OPERATION_MESSAGES` / `REORDER_MESSAGES` のように「領域＋MESSAGES」で、`ROUTINE_ERROR_MESSAGES` だけ `ERROR` が挟まる）。T-75 が「文言・型・公開する名前を変えない純粋な移動」を要件にしていたため据え置いた
- 対応方針: `ROUTINE_MESSAGES` 等へ改名し、参照元（`routines/actions.ts`・`_lib/error-messages.ts` 内の `ROUTINE_FROM_TASK_MESSAGES`・テスト）を追随させる。**挙動は変えない**
- 着手条件: ルーチン文言を別件で触るついで。単独で動かす価値は薄い
- 関連: [T-75](./closed_23_技術改善バックログ.md#t-75)（据え置きの由来）
- 結果（2026-08-01）: `ROUTINE_MESSAGES` へ改名し、参照元3ファイル（`routines/actions.ts`・`ROUTINE_FROM_TASK_MESSAGES`・テスト）を追随させた。他辞書と同じ「領域＋MESSAGES」に揃い、命名軸のずれは解消

### T-78

- 背景: `src/app/masters/_lib/action-result.ts` に、`name_required` など[T-75](./closed_23_技術改善バックログ.md#t-75) で集約した文言と**同一の文字列を持つ別の辞書**が残っている（計4写し）。T-75 の一本化はデイリー・ルーチンの辞書を対象にしており、マスタ管理側はその外側にあった
- 判断が要る点: 同じ文言が2か所にあるのを**重複として畳むか、画面ごとに独立した辞書として意図的に持つか**。畳むなら文言の変更が全画面に一度で効く代わりに、片方だけ変えたいときに分岐が要る
- 対応方針: 着手時に上の判断をしてから寄せる（寄せない結論なら「意図的な独立」として本項に記録して閉じる）
- 着手条件: マスタ管理の文言を触るとき、または同じ文言を2か所で直す事故が起きたとき
- 関連: [T-75](./closed_23_技術改善バックログ.md#t-75)（一本化の範囲外として残った分） / [T-44](./closed_23_技術改善バックログ.md#t-44)（マスタ管理3テーブルの重複。同じ領域）
- 結果（2026-08-01・**寄せたのは置き場、辞書は畳まない**）: 辞書を `masters/_lib/action-result.ts` から `_lib/error-messages.ts`（`MASTER_MESSAGES`）へ移し、[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §2 の例外条項に実装を追随させた。**2つの辞書は統合しない**——検証規則は共通（`domain/shared/master-name.ts` の `NameError`）でも、`invalid_start_time` をマスタは「開始時刻」・ルーチンは「開始想定時刻」と**意図的に違う文言**で出しており、キーで畳むと表示が変わる。取り違えは `MasterError` と `RoutineUsecaseError` に包含関係が無いため型が弾く（`ROUTINE_MESSAGES` を `MASTER_MESSAGES` に差し替える変異で `tsc` が落ちることを実測）ので、[T-74](./closed_23_技術改善バックログ.md#t-74) が共有 const を置いた動機（型で守れない）はここには働かない。この判断は doc コメントと不変条件テスト1本で残した

### T-79

- 背景: [T-44](./closed_23_技術改善バックログ.md#t-44) はマスタ管理3表のインライン編集セルと新規追加行を部品へ抽出したが、**見出し・「新規追加」ボタン・エラー帯（保存失敗の表示）は3表で逐語重複したまま**残っている（抽出の対象外だった）。`code-quality-reviewer` が T-44 のレビューで「次の抽出候補」として挙げた分
- 対応方針: 表の外枠（見出し＋新規追加ボタン＋エラー帯）を1部品へ寄せる。列の並びが表ごとに違う点は T-44 と同じく render prop で表側に残す。**挙動は変えない**
- 着手条件: 4つ目のマスタを足すとき、または3表のいずれかを別件で触るついで。T-44 で編集セル・新規行が寄ったので、残るのは枠だけ
- 優先度の根拠: 重複しているのが定型の枠で、[FB-63](./closed_21_ユーザーフィードバック.md#fb-63) のような取りこぼしを生む種類の重複ではない（分岐や状態を持たない）
- 関連: [T-44](./closed_23_技術改善バックログ.md#t-44)（抽出の第1弾） / [T-54](#t-54)（下層の三重化。寄せない判断済み）
- 結果（2026-08-01）: 見出し・「新規追加」ボタン・エラー帯を `masters/_components/master-table-frame.tsx` へ抽出し、列の並びと「新規追加」時の初期化は表側に残した（T-44 と同じ流儀）。`<table>` / `<thead>` は寄せない判断——`<th>` が3表で全部違い render prop が2〜3個要る割に、得るのはクラス文字列2本の重複解消だけ。抽出で生まれた「prop の渡し違い」を変異で洗い出したところ**3つの穴が見つかり**（説明文が sections でしか見られていない／`onAddNew` の `setError(null)` を落としても3表とも緑／エラー帯を `{children}` の後ろへ移しても全緑）、部品テスト8本と3表への追加5本で塞いだ。ルーチン管理に残る**4本目の逐語コピー**は [T-94](#t-94) へ起票

### T-80

- 背景: [T-53](./closed_23_技術改善バックログ.md#t-53) で `daily-list.tsx` を分割したあとも、`toOptions`（マスタ配列 → ポップオーバーの候補への変換）はリスト本体に残っている。同じ役目の `toSectionOptions` は既に `(daily)/_lib/section-options.ts` にあり、**対になるものが片方だけ `.tsx` の中**という状態
- 対応方針: `_lib/master-options.ts`（名前は着手時に決める）へ出し、ユニットテストを新設する（[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §6「純粋ロジックは `.ts` へ切り出してユニットテスト」）。**挙動は変えない**
- 着手条件: モード・プロジェクトの候補の作り方を変えるとき、または `daily-list.tsx` を別件で触るついで
- 優先度の根拠: 変換自体は数行で、コンポーネントテスト経由では検証されている。段が1つ下がるだけの整理
- 関連: [T-53](./closed_23_技術改善バックログ.md#t-53)（切り出し元） / [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §6
- 結果（2026-08-01）: `(daily)/_lib/master-options.ts` へ切り出し、boolean フラグ（`withColor`）はやめて `toModeOptions` / `toProjectOptions` の2関数に分け、対の `section-options.ts` と同じ形に揃えた。ユニットテスト6本を新設（アーカイブ除外・並び順の保存を変異で実測）。**本項の所在記述が誤っていた**——「`daily-list.tsx` に残っている」と書いていたが、実際には [T-53](./closed_23_技術改善バックログ.md#t-53) の分割時に `_components/task-row.tsx` へ移っており、着手時点の所在はそちらだった（実態に合わせて作業した）

### T-83

- 背景: 「モード未設定の行は副次情報の色（`text-ink-muted`）にする」（[画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §3.3。条項は 2026-07-26 に [T-53](./closed_23_技術改善バックログ.md#t-53) で起こした）の判定が `(daily)/_components/task-row.tsx`・`assign-cell.tsx`・`review/_components/review-board.tsx` の**3箇所に分散**している。`review-board.tsx` は変数名も `dimmed`（文字列）で、T-53 で `isDimmed`（真偽）＋`dimmedClass`（文字列）に整えたデイリー側と揃っていない
- 対応方針: 判定を共有の helper へ寄せ、変数名も揃える。**挙動は変えない**
- 着手条件: 未設定行の見せ方を変えるとき（3箇所を同時に直すことになる）、またはレビュー画面を別件で触るついで
- 優先度の根拠: `text-ink-muted` は `src` 全体に約70箇所ある設計トークンで、**トークンを直接使うこと自体は普通の書き方**（`code-quality-reviewer` も「抽出必須ではない」と述べている）。問題は判定（＝どの行を副次情報扱いにするか）が条項1つに対して3実装ある点だけ
- 関連: [T-53](./closed_23_技術改善バックログ.md#t-53)（発見の契機・デイリー側の整理） / [画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §3.3
- 結果（2026-08-01）: 判定を `src/app/_lib/mode-appearance.ts` へ集約し、3箇所（`task-row.tsx` / `assign-cell.tsx` / `review-board.tsx`）の変数名を `isDimmed`・`dimmedClass` に統一した。置き場が `(daily)/_lib` ではなく `app/_lib` なのは、デイリーとレビューの2画面が使うため（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §2「2つ目の利用者が現れた時点で1つ上の階層へ」）。**モード色の `style` も同じ helper が返す**形にした——`mode === undefined` が「グレーにするか」と「色を載せるか」で同一述語であり、判定だけ寄せると述語が呼び出し側に残るため。作業中に**レビュー画面のモード色の条項は 04 §3.3 ではなく 04 §2（レイアウト）**と判明し、コード側のコメントはそちらを引いている

### T-89

- 背景: `domain/task/projection.ts` の `sectionEndAt`（セクション枠の終了時刻）は export だが、**本番コードからの参照がゼロ**。FB-80 で `GroupHeading` が呼ぶのをやめ、`sectionSlacks` が内部の `sectionStartAt` / `sectionEndFrom` を使う形になった時点で production の呼び出し元が消え、`projection.test.ts` の8件だけがぶら下がっている
- 実害: 無い（挙動には影響しない）。ただし「テストのためだけに開いている export」は、次に触る人が公開 API と誤認する材料になる
- 対応方針: **単純削除はしない**——当該8件は日跨ぎ・タイムゾーン・日界（F-116）という枠導出の境界を実質的に守っており、消すと `sectionSlacks` の内部が無防備になる。取りうる形は2つで、①非 export へ降格し、テストを `sectionSlacks` の戻り値の `endAt` 経由へ付け替える（`DailyGroup` を1件手で組めば同じ境界を測れる）②公開 API として残す判断をし、その理由を JSDoc に書く。①が素直だが、テストが1段間接になるぶん読みやすさは落ちる
- 優先度の根拠: 挙動不変で急がない。ただし F-110 まわりは FB-80・FB-81 と短期間に2度触っており、次に触るときが畳みどき
- 関連: FB-80 / FB-81（この状態を作った変更。2026-07-28）/ [T-61](#t-61)（コンポーネント段に残る穴）。FB-81 のレビューで `code-quality-reviewer` が検出し、挙動を変えない整理なので技術改善軸へ回した
- 結果（2026-08-01・**①②のどちらでもなく削除**）: `sectionEndAt` を消し、8件のテストを `sectionSlacks` の `endAt` 経由（テスト内ヘルパ `endAtOf`）へ付け替えた。②（公開 API として残す）を採らないのは、`endAt` 単体を要する呼び出し元が無く（画面は `{ endAt, slackMinutes }` の対を使う）「なぜ公開のままか」の但し書きは古びても誰も消さないため。①（非 export へ降格）を採らないのは、固有ロジックを持たない2行の合成なので非 export のまま残すと死んだ関数になり、通す形に戻すには FB-80 が `sectionEndFrom` を切って避けた「`sectionStartAt` を2回計算する」構造へ巻き戻すしかないため。**本項の「単純削除はしない」は守っている**——境界（日跨ぎ・タイムゾーン・日界）を守っていた8件は1件も失われておらず、変異3本（日界加算・容量計算・TZ 伝播）で付け替え後も teeth があることを実測した。JSDoc の情報は `sectionStartAt`（枠の起点・日界）と `sectionSlacks`（表示日=今日の前提）へ分けて移設

### T-84

- 背景: [完了記録](./closed_23_技術改善バックログ.md) T-37 で `scripts/dev-check.sh` を足した結果、シェルの同じ塊が3か所目になった。①**db-test の起動と `pg_isready` 30回待ち＋DB の存在確認と `CREATE DATABASE`** の17行が `wt-new.sh` と変数名以外まったく同じ ②**ポートを掴んでいる PID を表示するループ**が `dev.sh` と同じ。待ち回数30・ポート5433 といった定数も各所に散っている
- 対応方針: `scripts/lib/db-test.sh` へ切り出し（`ensure_check_db <DB名>` / `print_port_holders <ポート>` 相当）、3つのスクリプトから読み込む。**挙動は変えない**
- 対応（2026-08-01）: `scripts/lib/` に **2本**置いた——`db-test.sh`（`ensure_db_test_database <DB名>`・`worktree_test_db_name <ブランチ名>`）と `port.sh`（`print_port_holders <ポート> <インデント>`）。`dev-check.sh`・`wt-new.sh`・`wt-rm.sh`・`dev.sh` が `. "$(dirname "$0")/lib/<名前>.sh"` で読み込む。背景が挙げた2つに加え、**worktree 用テストDB名の生成式**（`wt-new.sh` が作り `wt-rm.sh` が消す。ズレると別のDBを消しにいく）も同じ置き場へ寄せた。方針の1本ではなく2本にした理由・挙動を保つために採った措置・実走検証の内訳は[決定ログ](./log_23_技術改善バックログ.md) 2026-08-01
- 着手条件: 4か所目が生まれるとき、または db-test の起動・待ち方を変えるとき（3か所を同時に直すことになる）
- 優先度の根拠: シェルは自動テストの網が無く（種別「ツール整備」）、切り出しの検証は実行して確かめるしかない。いま動いているものを触る割に得るものが「重複の解消」だけなので、触る用事ができたときに合わせる方が安い。T-37 の PR では**範囲を超えるため見送ると決めた**（`code-quality-reviewer` の指摘・オーナー裁定）
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-37（発生の契機） / T-34（`dev.sh` を足した回） / [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §3.1・§3.2

### T-91

- 背景: `npm run dev:check`（ブラウザ実機確認用の確認専用環境）は生成物を `.next-check/` に出す。このディレクトリは `.gitignore` 済みだが、**`tsconfig.json` の `include` が `**/*.ts` で、`exclude` は `node_modules` だけ**なので、`npm run typecheck`（`tsc --noEmit`）が `.next-check/` 配下の生成 `.ts` まで読む
- 実害: **生成が途中で終わった型ファイルが残ると `typecheck` が落ちる**。2026-08-01 の FB-73 のレビューで実際に踏んだ（`.next-check/dev/types/validator.ts(101,1): error TS1128` — 行の途中で切れたファイルが残っていた）。原因が自分の差分に見えるため、**変更と無関係のエラーを追うことになる**。`dev:check` は `browser-checker` と `verifier` が使うので、レビューのたびに踏みうる
- 対応方針: `tsconfig.json` の `exclude` に `.next-check` を足す（1行）。`.next` が同じ問題を起こしていないのは、そちらの生成物が常に完成状態で残るため——**`exclude` に両方を並べるかは、`.next/types/**/*.ts` を `include` が明示的に拾っている構成と衝突しないか確かめてから決める**
- 対応（2026-08-01）: `tsconfig.json` の `exclude` に `.next-check` を足す1行のみ。`include` に並ぶ `.next-check` の2行は残し、両者が矛盾して見えないよう `exclude` 側に理由のコメントを置いた（`dev:check` のたび Next.js が書き戻すため消しても戻る）。`.next` は `exclude` に入れない。**上の背景の見立ては一部外れていた**——拾っていたのは `include` の `**/*.ts` ではなく明示行の方。詳細と検証は[決定ログ](./log_23_技術改善バックログ.md) 2026-08-01
- 優先度の根拠: 1行で塞げるうえ、放置すると「他人の差分のせいに見えるエラー」を毎回踏む。ただし挙動には影響しないため中
- トリガ: 次に `dev:check` を使う作業が来たとき（またはそれ以前でも、1行なので単独で片付けてよい）
- 関連: [T-37](./closed_23_技術改善バックログ.md#t-37)（`dev:check` の確認専用環境を用意した案件）。2026-08-01 の FB-73 のレビューで検出

### T-87

- 背景: 変異テストについて明文化されているのは**手続きと担当者だけ**で、**いつ実施するか**がどこにも無い。CLAUDE.md「規約」は「メインセッションが Edit で1か所だけ壊し、確認後に Edit で戻す。`verifier` には委譲しない。git で退避しない」（T-67）を定め、`verifier` 側にも裏返しの禁止条項がある。[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) にも発火条件は無い。本書の種別表が「内部設計」の網として「変異させて振る舞い保存を確かめる」と書いているのが、実施を求める唯一の記述で、しかも1種別に閉じている
- 実害: 実施するかどうかが毎回その場の判断になる。FB-68 の対応（2026-07-27）では2回実施したが、①新規テストがガードを見ているかの確認 ②`test-reviewer` が名指しした変異の再検証、という**判断ベース**で、条項に従ったわけではない。**やらなくても誰も気づかない**（テストが増えたことは差分に出るが、それが効いているかは出ない）
- 対応方針: 発火条件を1か所に書き、レビューの起動条件（CLAUDE.md「起動の契機」）と同じ**差分ベースの引き方**に揃える。案: **ガード・分岐・early return の追加/変更を主目的とする差分**では、その条件を外す変異を1回行い、狙ったテストが落ちることを確認する。書き場所はテスト戦略定義書（テストの契約の正）に条項を置き、CLAUDE.md の規約は手続きのまま参照に留めるのが、書き方の規約（詳細は下位に1か所）に沿う
- 詰める点: ①発火条件の粒度（「ガード・分岐の追加」で足りるか、種別「内部設計」の T-XX とテスト補強の T-XX も含めるか）②確認結果をどこに残すか（コミットメッセージに1行、が現状の慣習。条項化するか）③`test-reviewer` が「この変異は検出できない」と指摘した場合の扱い（今回はそのまま実施した。定型にするか）
- 優先度の根拠: 網が無いことによる劣化は静か（変異を怠っても緑のまま）で、テスト補強の T-XX（T-60・T-61）が控えているため、そこで効き始める。ただし挙動を変えない活動なので急ぎではない
- 関連: [T-68](./23_技術改善バックログ.md#t-68)（変異テスト専用サブエージェントの検討。**実施条件が決まると委譲先の要否も判断しやすくなる**ので、片方を決めるときにもう片方を見る）/ [完了記録](./closed_23_技術改善バックログ.md) T-67（変異テストをメインセッションへ戻した経緯）/ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md)（書き場所の候補）
- 結果（2026-08-01）: [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) に **§10「変異テスト」を新設**し、発火条件を差分ベースで2本（①テストを新規に足した差分で1本選んで壊す ②種別「内部設計」の T-XX で網が効くことを1点確認）に定めた。詰める点は3つとも決着——①の粒度は**対応方針案（「ガード・分岐・early return の追加/変更を主目的とする差分」）を採らず**、`git diff --name-only` で決まる「テストファイルを足したか」で引く形にした（本番コード側の性質で引くと該当判定が主観になり、レビューの起動条件と引き方が揃わない）。②結果はコミットメッセージ本文に1行（現状慣習の追認。台帳・決定ログには積まない）。③`test-reviewer` の「この変異は検出できない」指摘は**実施して赤くならないことを確かめる**を定型にした（穴の予告であって実施可否の話ではない）。CLAUDE.md「規約」は手続きのまま §10 への参照を足し、§10 は手続きを再掲に留めた（詳細は下位に1か所）。従属して [T-68](./23_技術改善バックログ.md#t-68) の「専用サブエージェントを置くか」も**置かない**で決着している。経緯は[テスト戦略定義書の決定ログ](../仕様/log_17_テスト戦略定義書.md)

### T-92

- 背景: `src/app/routines/routines-table.test.tsx` は操作を `await act(...)` で包む click ヘルパを通しているため、Server Action の応答が確定してから assert に進む。**マスタ管理（`src/app/masters/`）の3表には相当するヘルパが無く**、生の `fireEvent.click` を使っている。`src/app/masters/_testing/table-helpers.ts` はあるが、この役目を持っていない
- 実害: **待ちの扱いを assert ごとに書き手が判断することになる**。2026-08-01 の T-62 で実際にフレークを1本出した（`modes-table.test.tsx` の「追加の失敗は…」が20回中2回。メッセージの描画と `isPending` の解除が同じコミットで届くとは限らないのに同期で読んでいた）。その1本は `deferredAction` 形へ直したが、**同じ罠は assert を1つ足すたびに踏みうる**——1本ずつ直す限り面としては塞がらない
- 対応方針: act 包みの click をヘルパにして3表の `fireEvent.click` を差し替える。**置き換えの前後で各テストが同じ理由で落ちることを変異で確かめる**（待ちを足すと assert が弱くなる方向にずれやすいため）
- 置き場: この道具は masters と routines の両方が要るので、**画面をまたぐ道具の置き場（`src/app/_testing/`）が第一候補**（テスト戦略定義書 §4 の切り分けに従う）。`masters/_testing/table-helpers.ts` へ置くと routines 側の重複が残り、次に同じ判断をやり直すことになる
- 着手条件: マスタ管理のテストを別件で触るとき、または同種のフレークが2本目に出たとき
- 優先度の根拠: 実害は出ているが1本直せば当面は止まる。放置コストは「次に足す人が同じ判断を迫られる」こと
- 関連: [T-62](#t-62)（フレークを検出した案件）/ [T-93](#t-93)（同じレビューで出たマスタ管理3表の非対称）/ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §4
- 結果（2026-08-01）: 画面をまたぐ操作の置き場として `src/app/_testing/interactions.ts` を作り、押下から Server Action の解決までを `act` で流す `click` を1本だけ置いた。マスタ管理3表とルーチン管理の `fireEvent.click` はすべてこれを通し、ルーチン管理にあったローカルの同型ヘルパは消した。`masters/_testing/table-helpers.ts` の `startEditingCell` と sections の `startEditingStartTime` も内部で `click` を使う非同期関数にした。待ちの判断が要らなくなったぶん、click 起点の `waitFor` はすべて同期の assert になっている（blur・Enter 起点のものは今回の対象外なので残る）。条項は[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §4 に追加。**未追随は `daily-board.test.tsx` の1ファイル**（同型のローカルヘルパ `clickAndSettle` を持つ。[T-96](./23_技術改善バックログ.md#t-96) で管理）

### T-93

- 背景: 「保存に失敗したら抑止が解ける＝入力し直せる」（[00_共通](../仕様/13_画面定義書/00_共通.md) §2.3「失敗時」）を検証しているのは、マスタ管理3表のうち **modes だけ**。`projects-table.test.tsx` と `sections-table.test.tsx` の同名テストはメッセージの表示と行の残存しか見ていない
- 実害: `useServerAction` と `MasterNewRow` の**配線は表ごとに独立**（各表が自分で hook を呼んで `isPending` を渡す）ので、projects / sections で解除側の配線が壊れても緑のまま通る。同じ論理は `routines-table.test.tsx` のコメントが「新規と編集を別々に配線するから両方見る」として既に持っている。なお抑止側（保存中は押せない）は3表とも持っており、欠けているのは解除側だけ
- 対応方針: projects / sections の同名テストにも解除側の assert を足す（T-62 で modes に入れた `deferredAction` 形をそのまま写す）。[T-92](#t-92) のヘルパ化を先にやるなら、そのついでに済ませられる
- 着手条件: T-92 に着手するとき、またはマスタ管理の保存まわりを別件で触るとき
- 優先度の根拠: 壊れる確率は低い（3表とも同じ部品を使い、片方だけ配線を間違える経路が細い）。ただし壊れたときは「失敗後に何も操作できない」という詰みの出方をする
- 関連: [T-92](#t-92)（同じレビューで出た根本側）/ [T-62](#t-62)（検出の経緯）/ [T-61](./23_技術改善バックログ.md#t-61)（コンポーネント段に残る穴）
- 結果（2026-08-01）: projects / sections の「追加の失敗はメッセージで知らせ、新規行を残す」を modes と同じ `deferredAction` 形にし、失敗が返ったあとに「保存」「取消」の抑止が解けることを両表で見るようにした（T-92 と同じコミット）。新規行へ渡す `isPending` を `true` に固定する変異で、足した assert だけが落ちることを確かめている

### T-60

- 背景: T-39 で presentation を厚くした結果、相対的に下層の穴が見えるようになった。**統合テストはトランザクションを張る分岐だけを厚く検証しており、単純 UPDATE 経路が実DBに一度も当たっていない**
  - **Task リポジトリの単純経路**: `rename` / `updateEstimate` / `updateClassification` / `finish` / `findById`。**`create`（振り直しなし）・`start`（割り込みも再配置もない）・`move`（付帯更新なし）は 2026-07-26 に T-51 の統合テスト7本で解消済み**——`move` は Shift+J/K の通常経路、`create` はクイック追加の通常経路で毎操作走るのに、実DBに一度も当たっていなかった
  - **付帯更新の残る穴**（T-51 のレビューで判明、同 T のスコープ外として送られた分）: `suspend` の `renumber` が非空になる経路（F-204）が全段で未検証／`relocate([])` の早期 return が未到達
  - **`seedMasters` の冪等スキップ経路が未検証**（T-57 のレビューで判明、分岐カバレッジ 58.33%）。`seed.ts` は「空テーブルにのみ投入する冪等スクリプト」と宣言しているが、**2回目の呼び出しでスキップする側**にテストが無い
  - **`updateMode` / `updateProject` の成功パスが一度も実行されていない**。`describe("createMode / updateMode")` という名前で網羅しているように見えるが、update 側は異常系1本だけで、「更新経路でも名前が trim されるか」「更新経路でもプリセット外の色が弾かれるか」が未検証
  - **`RoutineRepository.listSkippedOn` が統合テスト未通過**。スキップ（F-301）は書き側だけ厚く、**展開の抑止が全面的に依存する読み出し側**が実DBに当たっていない
  - **DB 制約の境界**: `ck_tasks_time` の後半（`ended_at >= started_at`）が未固定。特に **`ended_at == started_at` の許容**は画面定義書01 §3.3「1分未満の実績は `0:00`」が成り立つ前提で、誤って拒否されると打刻直後の終了が落ちる。`ck_routines_recurrence_type` と `ck_routines_week_interval`（1〜53）の境界も DB 段では未検証
  - `tasks.section_id` / `mode_id` / `project_id` の **FK とマスタ物理削除時の DB 側挙動**が統合テストで一度も当たっていない（`routine_id` の SET NULL と `routine_skips` の CASCADE は検証済み）。F-405「参照0件のみ物理削除」はアプリ層で守る設計なので、**DB 側の最後の砦が未検証**という位置づけ
- 対応方針: 上から順に統合テスト／ユニットテストを足す。**挙動は変えない**
- 着手条件: **すぐ**（T-59 と同じ枠で片付けてよい）。優先度が高い理由は「日常操作で最も通る経路」と「制約の最後の砦」が対象だから
- 関連: [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §5・§6 / T-51（Port の3状態が分岐を倍にしている件と同じ領域）
- 結果（2026-08-01）: 統合 27 本・ユニット 2 本を足して列挙した穴をすべて塞いだ（挙動は変えていない）。`drizzle-task-repository.ts` は行・分岐とも 100% に到達。シードの冪等性は `seed.int.test.ts` を新設して見ている
  - **着手時に台帳の記述が2点ずれていた**: `suspend` の非空 renumber は usecases 段（`operations.test.ts`）に既にテストがあり、穴は統合段だけだった／`updateProject` の trim も既に見ており、テスト名が主張を表していなかっただけ（名前を直した）。残る「更新経路でプリセット外の色を弾く」は `updateMode` に無く、これは足した
  - **レビューで、埋めたはずの箇所に生き残る変異が見つかった**（`test-reviewer`）。`finish` と `updateClassification` は対象が1行しかないテストだったため **`.where()` を落としても緑**——どちらも隣の行を1つ足して固定した。`listSkippedOn` は `RESTART IDENTITY` でスキップ行の id とルーチンの id が揃い、返す列を取り違えても通っていたので、別日の記録を先に入れて id 空間をずらした。`suspend` にだけ無かったロールバックテストも足した
  - **テストで固定できない分岐は、その旨をコメントに書いて残した**: `relocate([])` の早期 return（外しても空トランザクションになるだけで観測差が出ない）と `week_interval IS NULL`（CHECK 式が NULL に評価されると PostgreSQL は満たされた扱いにする）
  - **シードの冪等契約が docs に無かったので[データモデル定義書](../仕様/14_データモデル定義書.md) §5 に明文化した**（空のテーブルにだけ投入する・`sections` と `modes` はそれぞれ独立に判定する）。テストが固定した契約の置き場を作るための追記で、挙動は変えていない
  - 隣接して `RoutineRepository.findById` の null 分岐（タスク側と同型の穴）も対にして塞いだ

### T-61

- 背景: T-39 第2バッチ後に残った、担当範囲の隙間に落ちていたもの
  - **予想開始時刻（F-120）の値が正規表現でしか見られていない** — 「未実行行にだけ弱色で出す」は担保できているが、**どの行にどの値が付くか**（積み上げの行への割り当て）は担保しておらず、行同士でラベルが入れ替わっても緑。計算そのものは domain のテストが持つが、**`groups` を平坦化して行へ配る写像は `DailyList` にしかない**
  - **レビューの差異 0（実績 == 見積）が未検証** — 画面定義書04 §3.3「超過は警告色」の**境界そのもの**。表記（`+0:00` / `-0:00` / `0:00` のどれか）と警告色が付かないことが未固定
  - **`stickyHeight` の board→list 配線がどの段にもない** — board は `ResizeObserver` で実測、list は prop → `scrollMarginTop` の写像を検証済みだが、**中間だけが空白**。スタブのコールバックを任意の値で1回呼べば jsdom で見られる（実測値そのものはブラウザ段送りで正しい）
  - `withTaskAppended` の未分類グループ不在分岐が未実行（domain の純関数なので1行で塞げる。塞がないなら「到達しない」ことを明示するコメントが要る）
- 対応方針: 上記4点をテストで塞ぐ。**挙動は変えない**
- 着手条件: 該当画面を別件で触るとき、または T-59 / T-60 を片付けた後の余力で
- 関連: T-39 / [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3
- 結果（2026-08-02）: 4項目を塞いだ（コンポーネント3本・ユニット4本。既存1本は挙動変更に合わせて更新）。**2つの表示が変わっている**——下記の差異 0 と、それに揃えたセクション残り時間 0
  - **予想開始時刻（F-120）は着手時に「既に担保済み」と判定したが、半分だけ正しかった**。行への配りとグループを**またぐ**順序は既存テストが固定していた（変異で確認）が、**同じグループの中の順序**はどのテストも見ておらず、`test-reviewer` が「グループ内を反転する変異が全スイートで生存する」と指摘した。1グループに未実行を2件並べるテストを足して塞いだ——**行の並びは正しいまま値だけ入れ替わる**形なので、1グループ1件で組んだ既存テストでは捕まらない
  - **差異 0（実績＝見積）の表記を `-0:00` から符号なしの `0:00` へ変えた**（オーナー裁定）。§3.3 は「超過は警告色」しか書いておらず符号の扱いを実装が決めていたため、固定する前に表記を決める必要があった。[画面定義書04](../仕様/13_画面定義書/04_レビュー.md) §3.3 → 実装（`formatSignedDuration`）→ テストの順で通し、経緯は[その決定ログ](../仕様/13_画面定義書/log_04_レビュー.md)。値の側（`estimateDiffMinutes` が 0 を返す＝null にしない）も domain 段で固定した
  - `stickyHeight` の board→list 配線は、`ResizeObserverStub` を「任意の高さでコールバックを発火できる」形にして塞いだ。**2回発火させて追従が続くことまで見る**（初回だけ計測する実装への退行を止める）
  - `withTaskAppended` の未分類グループ不在分岐は、**画面からは到達しない**（`groupTasksBySection` が必ず受け皿を作る）旨をコメントに明記したうえでテストで固定した。同じ形の未到達分岐がレビュー画面にも残っているので [T-97](#t-97) へ起票した
  - デイリーのセクション残り時間（画面定義書01 §3.2）も同じ「符号＋H:MM」の形で 0 を `+0:00` と出していた。当初は「差異＝差の向き／残り＝枠の余裕」で意味が違うため揃えない判断だったが、**オーナーが動作確認で揃える方を選んだ**ため同じ規則へ寄せた。符号の規則は共有ヘルパ `formatSignedDuration`（`src/app/_lib/format.ts`）へ置き、2画面が同じ実体を引く形にしている（`code-quality-reviewer` が「同じ形が2か所にあり 0 の規則だけ食い違う」と指摘していた重複も、これで解消した）

### T-82

- 背景: `(daily)/_testing/factories.ts` の `morning` / `forenoon` / `afternoon` は、セクションの時間帯（`endTime`）を**共有の `sectionRanges` から導出せず手写しで持っている**。`code-quality-reviewer` が導出化を提案し、ワーカーは「導出にすると時間帯が呼び出し側から読めなくなる」トレードオフを挙げて見送った（[T-53](./closed_23_技術改善バックログ.md#t-53)）
- 判断が要る点: [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §4 は「テストが依拠する値は**呼び出し側に書く**」と「基準日は `TEST_DATE` 1つ（リテラルで書くと手動同期になる）」の両方を持つ。時間帯がどちら側かは条項からは決まらないので、着手時に決めて同書 §4 へ1行足す
- 対応方針: 上の判断のうえで導出化するか、手写しのままにする理由を `factories.ts` の doc に残す（どちらでも「決めた」状態にする）
- 着手条件: セクションの時間帯を変えてテストが壊れたとき、またはフィクスチャを増やすとき
- 関連: [T-53](./closed_23_技術改善バックログ.md#t-53)（発見の契機） / [T-43](./closed_23_技術改善バックログ.md#t-43)（フィクスチャ集約の由来） / [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §4

### T-76

- 背景: [T-74](./closed_23_技術改善バックログ.md#t-74) は共有2辞書（`PUNCH_MESSAGES` / `OPERATION_MESSAGES`）の文言差を「複製して開始」専用の `DUPLICATE_AND_START_MESSAGES` へ隔離し、**共有辞書どうしは取り違えても文言が変わらない**状態にした。だが専用辞書の側は `Record<TaskOperationError, string>` で `PunchUsecaseError ⊆ TaskOperationError` の包含が残るため、**打刻系のアクションが誤ってこれを引いても型が通る**。`undoCompleteAction` で実測すると、`tsc --noEmit` も全 1467 テストも緑のまま「完了したタスクではありません」が「複製して開始できるのは完了タスクだけです」に化けた
- 現状の歯止め: 辞書の doc に「`duplicateAndStartTaskAction` 以外から引かないこと。この1本だけは型でもテストでも守れないため名前で示す」と明記してある。取り違え先が1アクション専用の自己説明的な名前1つに限定されているのが、いまの防護の全量
- 対応方針: **操作種別 → 辞書の選択を `src/app/_lib/` の関数へ出し、ユニットテストで対応表を固定する**（`test-reviewer` の代案）。[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §2 の「Server Action は同じ契約を満たす偽物へ差し替える」境界（＝ `actions.ts` 内の辞書選択はコンポーネントテストでは通らない）は動かさずに、残る1本を塞げる。却下済みの案③（ドメインエラー型の直和化）は採らない
- 着手条件: 専用辞書がもう1つ増えるとき（差の隔離先が2つ以上になると名前による歯止めが効きにくくなる）、または `actions.ts` の辞書対応を触る別件のついで
- 優先度の根拠: 実害は文言の取り違え1件分で小さく、いまの配線は正しい。ただし T-74 が「構造的に消した」と言えるのは共有辞書の間だけなので、**残りを塞がずに T-74 を閉じたことを台帳に残しておく**必要がある
- 関連: [T-74](./closed_23_技術改善バックログ.md#t-74)（切り出し元。但し書きの実体） / [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §2

### T-81

- 背景: `daily-board.test.tsx` は行の中のセルを **`td[1]` のような列位置の直参照**で取り、フィクスチャも共有の `_testing/factories.ts` を使わず独自に組んでいる。列を1つ挿すと無関係なテストが崩れ、フィクスチャの手動同期も要る（[T-53](./closed_23_技術改善バックログ.md#t-53) のレビューで `code-quality-reviewer` が指摘。同 T では**並行ブランチが同ファイルを編集中だったため触らない判断**をした）
- 対応方針: セルの取得を `_testing/table-helpers.ts`（T-53 で新設）の読み取りへ寄せ、フィクスチャを共有のものへ揃える。**テストの書き方だけを変え、検証内容は変えない**
- 着手条件: `daily-board` に列を足すとき、または同ファイルを別件で触るついで。**当初は「T-69 と同時に」を条件にしていたが、T-69（型エラー10件）は 2026-07-27 に単独で片付いた**——型は `hold` の呼び出しへ明示型引数を与えるだけで通り、本 T が扱う作法（列位置の直参照・独自フィクスチャ）とは独立だった
- 関連: [T-53](./closed_23_技術改善バックログ.md#t-53)（発見の契機・`table-helpers.ts` の由来） / [T-69](./closed_23_技術改善バックログ.md#t-69)（同一ファイル・先に片付いた分）

### T-96

- 背景: T-92 で画面をまたぐ操作を `src/app/_testing/interactions.ts` の `click` に一本化したが、`src/app/(daily)/_components/daily-board.test.tsx` は同型のローカルヘルパ（`clickAndSettle`）を持ったままで、素の `fireEvent.click` とも混在している。Server Action を直接呼ぶコンポーネントテストは5ファイルあり、追随していないのはここだけ
- 実害: [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §4 の条項（Server Action を呼ぶテストではクリックを `click` に通し、素の `fireEvent.click` を混ぜない）に反する状態が1ファイル残る。混在している限り「このクリックは待つ必要があるか」の判断が assert ごとに戻ってくるので、[完了記録](./closed_23_技術改善バックログ.md#t-62) T-62 と同型のフレークが出る余地がある
- 対応方針: `clickAndSettle` を共有の `click` へ置き換え、素の `fireEvent.click` も同じ道具に通す。**置き換えの前後で変異を1点確かめる**（楽観的更新のロールバックが絡み、待ちを足すと assert が弱くなる方向にずれやすい）
- ついでに検討する: **`src/app/_testing/setup.ts` で `console.error` をテスト失敗にするか**。型付き lint を入れていないため `click` の `await` 落ちは型でも lint でも捕まらず、React の警告（`act` 未包み・スコープ未クローズ）が出るだけで緑になりうる。落とす形にすればこの手の取りこぼしを機械検出できるが、既存テストが出している警告を先に洗う必要がある（T-92 の `test-reviewer` が提案）
- 着手条件: デイリーのテストを別件で触るとき、または同種のフレークが出たとき
- 優先度の根拠: T-92 で分けたのは、1881行のファイルを同じコミットに入れると差分が倍になりレビューの目が届かなくなるため。既にローカルで act 包みの道具を使い分けているので、素の `fireEvent.click` しか無かった3表よりは危険が小さい
- 関連: [完了記録](./closed_23_技術改善バックログ.md#t-92) T-92（一本化した本体）/ [T-81](#t-81)（同じファイルの別の負債）

### T-101

- 背景: `src/app/(daily)/_components/daily-board.test.tsx` は1938行・9 describe・ファイルローカルヘルパ23本で、テストファイル中で突出している（2位の `drizzle-task-repository.int.test.ts` が1374行、コンポーネント段の3位は885行）。ローカルヘルパのうち少なくとも3本は共有版と重複する——`hold`（220-253行）は `src/app/_testing/actions.ts` の `deferredAction` の再実装（他5ファイルは共有版を使う）、`clickAndSettle`（313-317行）は `_testing/interactions.ts` の `click`（[T-96](#t-96) が扱う）、行・セルの取得（256-295行）は `_testing/table-helpers.ts`（[T-81](#t-81) が扱う）。**`hold` だけは共有版に無い価値を持つ**（`act` 包みと `afterEach` での解決漏れ保険）ので、寄せる方向は共有版→`hold` 側
- 対応方針: 2段で進める。①`hold` を `_testing/actions.ts` へ引き上げ（`deferredAction` を置き換えるか併存させるかは着手時に決める）②9つの describe を主題ごとにファイル分割する（楽観的更新／打刻と取り消し／クイック追加とインライン編集／ショートカット結線と割り当て、など）。**[T-81](#t-81)・[T-96](#t-96) は同じファイルの別の負債なので、分割の前後どちらで消化するかを着手時にまとめて決める**（分割後に3件を各ファイルへ配ると差分が読めなくなる）
- 着手条件: デイリーのテストを別件で触るとき。T-81・T-96 のいずれかに着手する時点で本 T の順序決めを先に済ませる
- 優先度の根拠: 1938行は「どこに何があるか」を毎回読み直すコストが常時かかる規模で、[T-96](#t-96) が「1881行のファイルを同じコミットに入れるとレビューの目が届かない」と書いた状態がそのまま続いている。分割はテストコードだけに閉じるので回帰の心配がない
- 関連: [T-81](#t-81) / [T-96](#t-96)（同一ファイルの別の負債）/ [T-92](./closed_23_技術改善バックログ.md#t-92)（共有ヘルパの由来）/ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §4
- 結果（2026-08-02）: 着手時に実測すると**2002行・12 describe・114テスト**で、背景の「1938行・9 describe」は起票時から古びていた。**6ファイルへ分割**（`daily-board.{optimistic,punch,editing,shortcuts,row-operations,display}.test.tsx`）し、テスト本文は1件も書き換えていない（`it` 114・`describe` 12→13 は分割時の再編ぶん、本数は不変）。共有部分は `(daily)/_testing/board-helpers.tsx`（描画・前後処理 `setupBoard`・保留 `hold`・盤面のフィクスチャ）と `action-mocks.ts`（`vi.mock` のファクトリ。ホイスト対策で独立モジュール）へ。**対応方針①の「`hold` を共有 `_testing/actions.ts` へ引き上げ」は採らず `(daily)/_testing/` に据え置いた**（オーナー裁定。理由は[決定ログ](./log_23_技術改善バックログ.md) 同日）。分割の軸・行ヘルパーの再配置・条項の改訂も同ログが持つ。反映先の条項は[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §2・§3・§4

### T-98

- 背景: 画面定義書01 §6 のキー集合が、機械的な結び付きのないまま4か所に写っている。①ディスパッチャ `src/app/(daily)/_components/use-daily-shortcuts.ts:92-198` の switch ②ヘルプ一覧 `src/app/(daily)/_components/shortcut-help.tsx:9-42` の `SHORTCUTS`（20行）③`use-daily-shortcuts.test.tsx:25-49` の `ALL_SHORTCUT_KEYS`（26件。「IME変換中・入力中は無効」の除外規則をこの一覧に対して回す）④`shortcut-help.test.tsx:22-42` のキー列の期待値配列。**ディスパッチャにだけキーを足すと、ヘルプに載らないうえ ③ に入らないため除外規則の検証対象からも外れ、全テストが緑のまま通る**
- 対応方針: キーと説明・ニーモニックを1つの表（`_lib/` の const）に持たせ、ヘルプ一覧はそこから描き、テストの `ALL_SHORTCUT_KEYS` もそこから導く。ディスパッチャの switch は挙動の分岐なので統合しないが、**表に載っているキーが全部ディスパッチャで拾われることを1本のテストで固定する**。挙動は変えない
- 着手条件: 次にショートカットを増減するとき、または `use-daily-shortcuts.ts` を別件で触るついで
- 優先度の根拠: [T-57](./closed_23_技術改善バックログ.md#t-57)（プリセット色の3並行リスト）と同型だが、あちらが表示の食い違いで済むのに対しこちらは**テストの網の穴を静かに増やす**。既に一覧が4本あり、5本目が増える前に畳みたい
- 関連: [T-45](./closed_23_技術改善バックログ.md#t-45)（共通キーガードの集約）/ [T-57](./closed_23_技術改善バックログ.md#t-57)（並行リストの前例）/ 画面定義書01 §6

### T-107

- 背景: Server Action 40本すべてが「ユースケースを呼ぶ → `!result.ok` なら辞書を引いて失敗を返す → `revalidatePath` → `{ ok: true }`」の同じ4行で、しかも失敗の作り方が2流儀に割れている——マスタ管理14本は `failure(result.error)`（`src/app/masters/_lib/action-result.ts:9`）、デイリー22本とルーチン4本はインラインの `return { ok: false, message: DICT[result.error] }`（計26箇所）。**辞書をどれと組み合わせるかがこの26箇所に散っている**ことが、[T-76](#t-76) の「打刻系が `DUPLICATE_AND_START_MESSAGES` を引いても型が通る」の温床にもなっている。あわせて失敗後の後始末も2実装ある（`src/app/_lib/use-server-action.ts:26-28` と `src/app/(daily)/_components/daily-board.tsx:163-166` が、`setError` ＋「サーバ失敗のときだけ `router.refresh()`」を逐語で持つ）
- 対応方針: `toActionResult(result, MESSAGES)` 相当を `src/app/_lib/action-result.ts` に置き、40本をそこへ通す。**辞書の選択がこの1関数の引数に集約されるので、[T-76](#t-76) が求める「操作種別 → 辞書の対応表をユニットテストで固定する」がそのまま乗る**。失敗後の後始末は共通関数へ寄せ、楽観的更新のロールバックだけを呼び出し側に残す
- 着手条件: **[T-76](#t-76) と同時に判断する**（T-76 の対応方針と実体が重なるため、統合するか従属関係を明示するかを決める）。単独で回すなら `actions.ts` を別件で触るついで
- 優先度の根拠: いまの40本は正しく書かれており、[T-54](#t-54) が「読めば分かる定型は寄せない」と決めた対象に近い。ただし T-54 が扱う3層×3集約の重複と違って、こちらは**寄せると型・テストの穴（T-76）が同時に塞がる**ぶん元が取れる
- 関連: [T-76](./closed_23_技術改善バックログ.md#t-76)（統合候補）/ [T-54](#t-54)（寄せない判断の前例）/ [T-52](./closed_23_技術改善バックログ.md#t-52)（実行フックの共通化）
- 結果（2026-08-02）: **対応方針の `toActionResult(result, MESSAGES)` は作らなかった**。辞書を引数で受ける形は、[T-76](./closed_23_技術改善バックログ.md#t-76) が非公開にした専用辞書への到達路を再び開くため。代わりに `failure(文言)` を `src/app/_lib/action-result.ts` へ置き、**辞書引きは40箇所に残したまま返し方だけを1流儀に揃えた**（マスタ管理のローカル `failure(code)` は畳み、`masters/_lib/` ごと消滅）。「辞書選択を1関数へ集約」は未達だが、それが塞ぐはずだった穴は T-76 が単独完了時に塞いでおり実害はない——**起票時の「寄せると T-76 の穴が同時に塞がるぶん元が取れる」という根拠は着手時点で失効していた**。失敗後の後始末の共通化（`handleActionFailure`）は方針どおり達成。**着手後に副作用が1つ表に出た**: 返し方を関数呼び出しに変えたことで、`return` を落としても tsc・lint・全テストが緑のまま通る状態が生まれた（変更前はオブジェクトリテラルが文の位置に来て構文エラーだった。40本中35本が該当）。オーナー提案により**早期 return をやめて if/else へ揃える**ことで型が塞ぎ（`TS2366`）、作法を[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4「Server Action の形」として条項化した。経緯は[同書の決定ログ](../仕様/log_15_アーキテクチャ定義書.md) 同日。**型が塞ぐ範囲の境界**: 効くのは **if/else 形と戻り値型の注釈を保つ限り**で、早期 return へ戻す・注釈を外すのいずれでも検出は消える（3形とも実測）。いま網を支えているのは §4 の規約だけなので、テストの段で裏打ちする案を [T-112](#t-112) として起票した

### T-99

- 背景: 実装が済んだ事柄を「まだ先」と書いたコメントが4か所残る。①`src/app/(daily)/_components/daily-list.tsx:53`「打刻・並び替えは後続ステップ」——打刻（F-201）も並び替え（O-6）も実装済み ②`src/infrastructure/db/schema.ts:119`・③`src/domain/shared/logical-date.ts:2`「日界の将来導入に備える」——F-116 は実装済みで、要件バックログにも残っていない ④`eslint.config.mjs:5`「docs/アーキテクチャ定義書.md §3」——実在しないパス（正は `docs/仕様/15_アーキテクチャ定義書.md`）。②③は**最も引用されやすいスキーマと日付ユーティリティの冒頭**にあり、読んだ人が「日界は未実装」と受け取る
- 対応方針: ①は削除（残す情報が無い）。②③は「保存された論理日付。打刻時刻から導出しない（F-116）」の形へ直し、将来形をやめる。④はパスを現行へ直す。あわせて `src/domain/task/projection.ts:100,145` の「従来どおり／従来挙動に一致する」も、日界導入前との対比ではなく「`dayStartMinutes = 0` なら暦日 0:00 起点」という現在の契約の言い方へ寄せる
- 着手条件: 単独で回してよい（コメントのみで挙動に触れない）。**同じ文言が CLAUDE.md「設計原則」の task_date の行にもある**ので、直すなら同じコミットで揃える
- 優先度の根拠: 誤読が実装判断に直結する種類（「日界は未実装だから考えなくてよい」）。修正コストは数行で、[T-41](./closed_23_技術改善バックログ.md#t-41)（旧参照の追随）で一度通した作業の取りこぼしにあたる
- 関連: [T-41](./closed_23_技術改善バックログ.md#t-41)（コメントの仕様参照追随）/ CLAUDE.md「書き方の規約」（旧参照の追随義務）
- 結果（2026-08-02）: 対応方針どおり①削除・②③「日界 F-116」の現在形へ・④パス修正・`projection.ts` の「従来どおり／従来挙動に一致する」2件を現在の契約の言い方へ寄せた。CLAUDE.md「設計原則」の task_date 行も同じコミットで揃えた。**起票時の4か所に加えて5か所目が出た**——「将来／後続／従来」を全文検索したところ `src/app/layout.tsx:35`「本文は**従来どおり**中央寄せ・最大幅」（Navigation Rail 導入前との対比）が同種として残っており、あわせて落とした。棚卸しが列挙で拾い切れない種類の指摘であることの実例なので、次に同型の T を起こすときは**着手時に全文検索で母集団を取り直す**

### T-104

- 背景: docs の「経緯は `log_` へ、本文は現在だけ」の規律がコードコメントに適用されておらず、判断の経緯・却下案・過去チケットの症状説明が本文に残っている。①`src/app/_components/date-nav.tsx:28-31`——「2026-07-20 オーナー判断: …FB-07」という決定ログそのもの ②`src/app/_lib/error-messages.ts:68-93,123-134`——T-74 の動機（「消したかった症状そのもの」）と T-78 で2辞書を畳まなかった理由が長文で入る ③`src/app/(daily)/_lib/optimistic.ts:90-96`——「時刻由来（`-Date.now()`）だと同一ミリ秒内に…（T-63）」という直した後の症状説明 ④`src/app/(daily)/_components/use-daily-shortcuts.ts:11-13`——「daily-board 本体に埋め込まれていた useEffect と同一で、可読性のためフックへ切り出しただけ」 ⑤`src/app/(daily)/_components/daily-board.tsx:486`「配線はフックへ切り出し（挙動は不変・T-14）」 ⑥`src/app/masters/_lib/action-result.ts:6`「従来どおり import できる（T-08）」
- 対応方針: **「型で表せない契約・不変条件」だけを残し、そこへ至った経緯は削る**。②の「`duplicateAndStartTaskAction` 以外から引かないこと。型でもテストでも守れないため名前で示す」は契約なので残し、T-74 / T-78 の判断過程は落として [log_23](./log_23_技術改善バックログ.md) 側の参照1行に替える。③は「`seq` はボードが生きている間ずっと重複しない正の整数」の契約だけ残す。①④⑤⑥は削除してよい（現在の形の説明になっていない）
- 着手条件: 該当ファイルを別件で触るついで。単独で回すなら [T-99](#t-99) と同じコミットにまとめる
- 優先度の根拠: 誤りではないので実害は読む時間だけ。ただし件数が増えると「コードの隣に決定ログがある」状態が既定になり、docs の規律との二重管理が始まる。いま6箇所のうちに揃えたい
- 関連: [T-99](#t-99)（同じくコメント整理。まとめて回せる）/ CLAUDE.md「書き方の規約」（経緯は `log_` へ）
- 結果（2026-08-02）: [T-99](./closed_23_技術改善バックログ.md#t-99) と同じコミットで実施（着手条件どおり）。**⑥は着手時点で既に解消済み**——`src/app/masters/_lib/action-result.ts` は [T-107](./closed_23_技術改善バックログ.md#t-107) がファイルごと消している。**①は「削除してよい」だったが、画面定義書01 §3.1 / 04 §3.1 への参照1行だけ残した**——落としたのは FB-07 の判断過程で、規則そのものは §3.1 が持つため転記もしない（残すと二重管理になる）。**②の対象行は台帳の行番号（68-93 / 123-134）から動いており**、実体は `OPERATION_MESSAGES` / `DUPLICATE_AND_START_MESSAGES` / `MASTER_MESSAGES` の3つの doc だった（T-76 がこのファイルを書き換えたため）。契約（文言の一致・`taskActionErrorMessage` 経由・非公開・畳まない）は残し、T-74 / T-76 / T-78 の判断過程は落として**ファイル冒頭の参照**に替えた（T-74 / T-76 の経緯は log_23、T-78 は本書の結果節にあるため両方を指す）。③④⑤は方針どおり。**起票時の6箇所に加えて5箇所目以降が出た**——`code-quality-reviewer` に同種の残骸を掃かせたところ `global-nav.tsx:7`（日付入りの決定ログ。①と完全に同型）・`nav-tabs.tsx:9`（過去形の移動履歴）・`daily-list.tsx:91`（FB-14 の症状説明）・`shared/testing/clock.ts:4`（削除済み関数の来歴）・`(daily)/_testing/table-helpers.ts:49`（過去実装との対比）の5件が残っていた。うち `daily-list.tsx` と `table-helpers.ts` は**削除ではなく現在形への書き換え**（前者は「`table-auto` にしない」、後者は「この引き方が何を主張するか」がいずれも契約にあたる）。[T-99](./closed_23_技術改善バックログ.md#t-99) と同じく**列挙をそのまま作業リストにすると取りこぼす**型で、こちらは機械検索では母集団が出ないぶん掃き役のレビュアーが要る

### T-108

- 背景: `src/domain/section/section.ts:48` の `sortByStartTime` は export されているが、**同じファイルの `activeSections`(53行) からしか呼ばれておらず、テストからも直接は参照されていない**（`npx knip` と全文検索の両方で確認）。export の必要がない内部ヘルパが公開面に出ている
- 対応方針: export を外して file-local にする。`activeSections` 経由のテストで振る舞いは既に押さえられているので、テストの追加は不要
- 着手条件: `section.ts` を別件で触るついで。**単独では回さない**（1語の削除で PR を立てる値打ちが無い）
- 優先度の根拠: 実害は無い。ただし [T-89](./closed_23_技術改善バックログ.md#t-89)（`sectionEndAt` の未使用 export）で同じものを1件片付けた直後に、同じファイルにもう1件残っていた形なので、そのとき見落としたことを台帳に残しておく
- 関連: [T-89](./closed_23_技術改善バックログ.md#t-89)（同型・同一ファイル）
- 結果（2026-08-02）: 方針どおり `export` を外した（テスト追加なし）。着手条件は「単独では回さない」だったので [T-99](./closed_23_技術改善バックログ.md#t-99) / [T-104](./closed_23_技術改善バックログ.md#t-104) と同じ PR に載せた。`npx knip` は指摘ゼロで、`sortByStartTime` の参照は同ファイル内の `activeSections` だけであることを再確認。3件のうち唯一コメント以外に触れる差分なので、[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §10 の変異テストもこの箇所で実施した（**結果は §10 の定めどおりコミットメッセージ本文に残し、台帳には積まない**）

### T-95

- 背景: [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §7 は「presentation を含めた結果として**全体値は5割前後になる**が、数値ゲートが無いので支障はない」と書くが、その後テストを厚くした結果**実測は 87.83%** で乖離している（T-63 のレビューで `test-reviewer` が検出）
- 判断が要る点: 数字を実測に合わせて書き換えるか、**そもそも本文に数値を書かない形にするか**。カバレッジの全体値はテストを足すたびに動くので、書けば必ず古びる。§7 自身が「全体値ではなく層ごとの数字で読む」と定めており、全体値の水準を本文が持つ必要があるのかから見直す余地がある
- 対応方針: 上の判断をしてから §7 を直す。**数値ゲート・合格基準を設けない方針そのものは変えない**
- 優先度の根拠: 数値ゲートが無いため実害は無く、誤読の余地も小さい（層ごとに読む指示が同じ節にある）。ただし「本文の数字が実態と違う」状態は、次に読む人が他の記述も疑う材料になる
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-58（カバレッジ計測の排他。同じ §7） / CLAUDE.md「書き方の規約」（本文は現在の仕様だけを書く）
- 結果（2026-08-02）: **「そもそも本文に数値を書かない形にする」を採った**。判断の材料は着手時の測り直しで、起票時の実測 87.83% が **88.52% へ動いていた**——数日で動く値なので、実測へ書き換えても同じ乖離が再発する。§7 自身が「全体値ではなく層ごとの数字で読む」「全体値はテスト密度の指標にならない」と定めており、本文が全体値の水準を持つ理由が無い。数値ゲート・合格基準を設けない方針は方針どおり不変。経緯は[同書の決定ログ](../仕様/log_17_テスト戦略定義書.md) 同日

### T-102

- 背景: コード内の `§` 参照483箇所のうち大半が文書名を持たず、**同じ番号が文書によって別条項を指す状態**になっている。実例: `src/infrastructure/db/repositories/drizzle-task-repository.ts:40` は1つのコメントの中で「§4.2-c」（画面定義書01＝開始時刻修正に伴う移動）と「§4.5 / §4.7」（データモデル定義書＝開始打刻の取り消し／完了の取り消し）を文書名なしで並べている。さらに **`§4.2-a` は データモデル定義書では「明示的な中断での再開タスクの配置」（`src/usecases/task/operations.ts:42` が指す先）、画面定義書01 では「開始タスク自身の自動セクション移動」（同ファイル:147 が指す先）** と別物で、どちらも裸で書かれている。`src/app/(daily)/_lib/optimistic.ts` も 61・64行が データモデル、107行が 画面定義書01 を指す
- 対応方針: 表記の規約を1つ決めて[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md)（またはテスト戦略定義書 §4 と同格の場所）へ1行置く。案は「**画面配下のファイルは裸の `§` をその画面の定義書とみなし、他文書を指すときは必ず文書名を書く。`src/domain`・`src/usecases`・`src/infrastructure` では裸の `§` を書かない**」。そのうえで、規約に反する箇所（domain / usecases / infrastructure の裸参照と、画面配下で他文書を指す裸参照）だけを補記する。**全483箇所を機械的に書き換えない**
- 着手条件: 文書の節を移動・改番したとき（そのとき grep で追随できないのが実害として出る）、または上記3層のコメントを別件でまとめて触るとき
- 優先度の根拠: いま誤りではないが、`§4.2-a` のように**同じ字面が2つの正解を持つ**状態は、次に節を動かしたときの追随を機械的に不可能にする。CLAUDE.md の「再編時は grep で旧参照を洗い出す」作法が前提にしている検索可能性が、裸参照のぶんだけ効かない
- 関連: [T-41](./closed_23_技術改善バックログ.md#t-41)（旧参照の追随）/ [T-86](#t-86)（docs の機械検査。コード内参照まで広げるかは本 T の判断が先）
- 結果（2026-08-02）: 規約を[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §2「コメントの仕様参照」として置いた。**対応方針の案（下位3層では裸の `§` を書かない）はオーナー裁定で採らず**、「1ファイルが2つ以上の文書を指すときだけ各参照に文書名を書く」にした——着手時に全88件を特定したところ**下位3層76件の大半は「1ファイル＝1文書」**で（`sort-order.ts` の5件は全部データモデル §3.5、`review.ts` の6件は全部 画面定義書04）、そこへ文書名を足しても曖昧さが1つも減らないため。経緯は[同書の決定ログ](../仕様/log_15_アーキテクチャ定義書.md) 同日。**補記した実数は34箇所**（下位3層の混在ファイル22＋`src/app` の違反12）。`src/app` の既定は画面ディレクトリで決め、配下の `_components` / `_lib` / `_testing` に継がせた。**起票時の実例3つはいずれも現存し、全部塞いだ**——`drizzle-task-repository.ts:40` と `:169`・`in-memory-repository.ts:123` は1行の中に2文書が混在、`operations.ts` は同一ファイル内で `§4.2-a` が2文書を指す状態、`punch-usecases.ts:74` は直上行が「データモデル定義書 §4.2」なのに当該参照は画面定義書01（最も誤誘導が強い1件）。**件別の調査リストだけでは母集団が閉じなかった**——`Explore` の76件表に載らない混在ファイルが5つあり（`mode.ts` `edit.ts` `projection.ts` `reorder.ts` `relocation-usecases.ts`）、「文書名の種類数 > 1 かつ裸の参照あり」を数える機械検算で初めて 0 に落ちた。この検算は [T-86](./23_技術改善バックログ.md#t-86)（docs の機械検査の拡張）へ持ち込める形。**テストファイルは母集団から外した**（20ファイル・約150参照で量が別物のため。[T-113](./23_技術改善バックログ.md#t-113) へ送った）が、**本体を直したせいでかえって誤読しやすくなった2ファイルだけは同じコミットで直した**——`operations.test.ts` は本体が「画面定義書01 §4.2-a」と明記した一方でテスト側が裸のまま残り、しかもそのファイルが名指しする文書はデータモデル定義書だけだった（データモデル §4.2-a は別条項）。`error-messages.test.ts` は `§4.1` だけで3文書を指していた。**その2ファイルで補記を1件間違えた**——`operations.test.ts` の「有効セクションが1つも無いときの退避」を画面定義書01 §4.2 と書いたが、正はデータモデル定義書 §4.6（`test-reviewer` が検出）。**誤った断定は裸より悪い**ので、補記は必ず条項本文の突き合わせを経る

### T-110

- 背景: `src/app/(daily)/_components/group-heading.tsx` は「セクションあり・枠なし（`endTime === null`）」＝**アーカイブ済みセクションにタスクが残っている**組を分岐として持つ（26-29行で枠の長さが `null` になり、45行の時間帯表示が開始時刻だけになる）。だが `group-heading.test.tsx` が渡すグループは `morning`（有効セクション）と `unclassifiedGroup`（セクションなし）の2通りだけで、この組をコンポーネント段で一度も描いていない
- 実害: 未分類（`section === null`）と枠なし（`endTime === null`）は**枠の長さが `null` になる点だけが同じで、セクション名と開始時刻を出すかどうかが違う**。分岐を片方に寄せる書き換えをしても、未分類のテストが緑のまま通る
- 対応方針: アーカイブ済みセクションのグループを1つ組んで、見出しが「名前＋開始時刻のみ・枠に依る表示なし」で描かれることを1件見る。**フィクスチャは `sectionGroup` へ直に渡す**（`sectionRanges` はアーカイブ済みを落とすので、[T-82](./closed_23_技術改善バックログ.md#t-82) で導出化した `morning` 系には乗らない）
- 着手条件: 単独で回してよい（テスト1件の追加）。またはデイリーの見出しを別件で触るついで
- 優先度の根拠: アーカイブ済みセクションにタスクが残る状態自体が例外的で、出るとしても表示の欠けに留まる。ただし**塞ぐコストがテスト1件**と小さい
- 関連: [T-82](./closed_23_技術改善バックログ.md#t-82)（検出の契機。同 T の `test-reviewer` が範囲外として指摘した）/ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3
- 結果（2026-08-02）: アーカイブ済みセクションのグループを `sectionGroup` へ直に組んで見出しを描くテストを1件足した（フィクスチャの `sectionGroup` は `endTime` に `null` を取れるよう `DailyGroup` と同じ型へ広げた）。合計は `合計 0:20` の全文一致で見る——分母と一緒に分子まで落とす書き換えを通さないため（`test-reviewer` の指摘で当初の「合計の語が出る」assert から変更）。変異は2点確認: ①開始時刻の表示を枠ありに限る ②枠なしの分子を 0 にする、のいずれでも足したテストだけが落ちる。あわせて **`spec-reviewer` が見つけた画面定義書01 §3.2 の記述漏れ**（アーカイブ済みは開始時刻だけ・分母なし、が画面側の条項に書かれていなかった）を同じコミットで埋めた

### T-103

- 背景: 「前日 / 翌日 / 今日へ」は画面定義書04 §5 が「日付移動のみ S-01 と揃える」と明記した共有規則だが、URL の組み立てが3か所にある——`src/app/_components/date-nav.tsx:39,74,82`（ボタン）、`src/app/(daily)/_components/use-daily-shortcuts.ts:106,109,164`（Shift+H / Shift+L / T）、`src/app/review/_components/review-board.tsx:39,40,43`（同じ3キーの再実装）。`DateNav` は既に `basePath` を prop で受けているのに、キーボード側だけが `/` と `/review` を各画面でリテラルで持つ
- 対応方針: `addDays` と basePath から href を作る小さな関数を `src/app/_lib/` に置き、3か所ともそこを通す。キーボード側は「basePath を受け取る日付移動フック」まで寄せるかを着手時に決める（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §2「2つ目の利用者が現れた時点で1つ上の階層へ」に照らすと、利用者は既に2つある）。挙動は変えない
- 着手条件: 3画面目で日付移動が要るとき、またはレビュー画面かデイリーのショートカットを別件で触るついで
- 優先度の根拠: 重複しているのは分岐を持たない定型3行で、事故は起きていない。ただし [T-94](#t-94) と同じく「共有規則だと文書に書いてあるのに実装が分かれている」形なので、片方だけ直す事故の余地はある
- 関連: [T-94](#t-94)（同型の未寄せ）/ 画面定義書04 §5
- 結果（2026-08-02）: `src/app/_lib/date-href.ts` に `dateHref(basePath, date, offsetDays)` と画面パスの定数（`DAILY_PATH` / `REVIEW_PATH`）を置き、背景が挙げた3か所に加えて**同じ形式を持っていた `stale-running-banner.tsx`（§8 のバナー内リンク）と実績ログ行のリンク（04 O-2）も通した**（計5ファイル・4用途）。**キーボード側は共有フックまで寄せない**——デイリーは編集中・datepicker のガードを持ちレビューは持たない非対称があり、日付移動だけ別リスナーへ切り出すと登録順とガードの二重管理が生まれる。残る重複は「Shift+H/L と `T`」というキー割り当ての一致だけで、これは 04 §5 が「S-01 と揃える」と定めた仕様の重複でコードの重複ではない（`code-quality-reviewer` も同判定）
- 副産物（2026-08-02）: `test-reviewer` が「`daily-board.tsx` の `basePath` の配線を固定するテストが無い」と指摘。**指摘どおり `basePath` を `/review` に壊すとコンポーネント段835件が全緑のまま通る**ことを確かめ（デイリーの日付ナビが丸ごとレビュー画面へ飛ぶ回帰が素通りする）、S-04 側の対（`review-board.test.tsx`「日付ナビの移動先は S-04 に閉じる」）と同じ形のテストを `daily-board.display.test.tsx` に足して塞いだ。あわせて `spec-reviewer` が見つけた「今日へはクエリを付けない」の未記載を画面定義書01 §3.1 に条項として書いた。変異は `dateHref` の `addDays(date, offsetDays)` を `-offsetDays` に壊す1点も実施し、3画面の既存テスト7件が落ちることを確認している

### T-94

- 背景: T-79 でマスタ管理3表の見出し・「新規追加」ボタン・エラー帯を `masters/_components/master-table-frame.tsx` へ寄せたが、`src/app/routines/routines-table.tsx` に**同じ外枠の4本目の逐語コピー**が残っている（差はボタン文言が「新規ルーチン」であることと表の余白が `mt-3` である点のみ）。T-79 のワーカーが申し送りとして検出
- 対応方針: 寄せるなら[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §2「2つ目の利用者が現れた時点で1つ上の階層へ」に従い、部品を `src/app/_components/` へ移してルーチン管理からも使う。**挙動は変えない**
- 着手条件: ルーチン管理の見出し周りを別件で触るついで、または**5本目が現れたとき**
- 優先度の根拠: T-79 の時点で寄せなかったのは、移設するとルーチン管理のテストも巻き込みスコープが倍になるため。重複しているのは分岐も状態も持たない定型の枠で、[T-79](./closed_23_技術改善バックログ.md#t-79) と同じく取りこぼしを生む種類の重複ではない
- 関連: [T-79](./closed_23_技術改善バックログ.md#t-79)（抽出の第2弾・切り出し元） / [T-44](./closed_23_技術改善バックログ.md#t-44)（第1弾）
- 結果（2026-08-02）: 部品を `src/app/_components/table-frame.tsx` の **`TableFrame`** へ移し（マスタ専用ではなくなったので `Master` を外した）、ルーチン管理を含む4か所が使う形にした。ボタン文言の差は `addLabel`（既定「新規追加」）で吸収し、表の余白（`mt-2` / `mt-3`）は元から children 側の先頭要素が持つので枠には持たせない。旧名 `MasterTableFrame` を指していたマスタ3表のテストコメント5か所も追随させた
- 副産物（2026-08-02）: `test-reviewer` が「T-79 が3表で潰した穴の4本目が残っている」と検出。①`onAddNew` の `setError(null)`（新規ルーチンを押すと前の失敗が消える）②説明文の渡し違え——のどちらもルーチン側には主張するテストが無く、落としても緑だった。2件とも足し、変異で個別に落ちることを確かめている。**エラー帯の見た目（`text-danger`）を表段から落として部品段へ寄せる指摘は見送った**——「部品段が持つ観点と表段が持つ配線」の分担は [T-100](./23_技術改善バックログ.md#t-100) が未決の主題で、そこを決める前に4本目だけ動かすと分担がさらに割れるため

### T-106

- 背景: `TaskId` / `SectionId` / `ModeId` / `ProjectId` / `RoutineId` の型エイリアスがあり、`src/domain/task/task.ts:9-25` と `src/usecases/ports/task-repository.ts` は一貫して使う。だが適用が層をまたぐと途切れる——①同じ `Task` の中で `routineId: number | null`（task.ts:22）だけがエイリアスを使っていない ②presentation は素の `number` に戻る（`src/app/(daily)/actions.ts` の40本近い `id: number` / `sectionId: number | null`、`src/app/(daily)/_lib/optimistic.ts:20-36` の全 `id: number`、`use-daily-shortcuts.ts:21,23` の `selectedId` / `currentSectionId`）。一方で `daily-list.tsx:191,212` や `section-options.ts:17` は `SectionId` / `TaskId` を使っており、**同一ディレクトリ内で流儀が割れている**
- 対応方針: どちらかに決める。(a) 全層で ID エイリアスを通す（`number` の別名なので安全性は増えないが、読み手にとって「これはどの ID か」が型で分かる）(b) 「エイリアスは domain と Port の契約に限る」と決めて presentation は `number` に統一する。**決めたことを[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4 に1行残す**（決めずに直すと次に同じ揺れが出る）。`routineId` の不揃いはどちらの結論でも直す
- 着手条件: presentation の props 型を別件で触るとき、またはブランド型の導入を検討するとき
- 優先度の根拠: 素の別名なので取り違えを型が捕まえるわけではなく、実害は読みやすさに留まる。ただし「どこまで適用するか」が未決のまま両方が増え続けている
- 関連: [T-51](./closed_23_技術改善バックログ.md#t-51)（Port の型を正規化した案件）/ [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4
- 結果（2026-08-03）: オーナー裁定は **(a) 全層でエイリアスを通す**。domain・usecases・Port・infrastructure・presentation の全層で素の `number` の ID をエイリアスへ置き換え、背景が挙げた `Task.routineId` の不揃いも直した。方針は[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4「ID型」の行に明記した。**例外は ID の種類が静的に決まらない箇所**（`field` で種類が決まる割り当てハンドラ、マスタ種別を跨いで共通化した部品＝選択ポップオーバー・アーカイブ済み一覧・名前の引き当て）で、理由は共通部品側（`select-popover.tsx`）に1か所だけ書いた
- 経過（2026-08-03）: 最初の実装は presentation だけを直して §4 に「全層」と書いたため、**規則と実装が逆向きに食い違った**（domain・usecases・Port に36箇所が残り、同じ入力オブジェクトが層をまたぐと流儀が変わる状態）。`code-quality-reviewer` と `spec-reviewer` が独立に同じ指摘をしたため、同じコミットで下層まで揃えた。`daily-board` の `useState<number | null>`（選択行 ID の供給元）とマスタ3表の `deletableIds` / `editingId` も取りこぼしていた

## 旧書式の記録（2026-07-26 以前）

## T-33 カバレッジ計測に `.tsx` を含める（2026-07-26）

- 種別: 調査 / 優先度: 様子見
- 背景: `include: ["src/**/*.ts"]` により `.tsx` を対象外にしていた。当初は presentation を自動テストの対象外に置く方針と一貫した設定だったが、F-120・F-121 の両方で `test-reviewer` が独立に「presentation の実装がカバレッジに現れない」と指摘しており、外れていること自体が定期的に疑問として上がっていた
- 前提の訂正: 当初の「**E2E を入れない限り数字は埋まらない**」は誤りだった。Client Component（22ファイル・約3,600行＝`.tsx` の大半）はコンポーネントテスト（jsdom）で測れる。E2E が要るのは async Server Component（13ファイル・約450行）だけ。この切り分けは本書 T-39 で実装し、4段の呼び名と境界は[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 に記録
- 結論: **含める**（`include: ["src/**/*.{ts,tsx}"]`）。「全テスト済みになってから含める」は採らなかった——残る Client Component は20ファイル・約3,500行で終わりが定義できず、待つ間は T-39 の各バッチの成果もUI変更の未カバー箇所も数字に出ない。**カバレッジは T-39 の成果であって前提ではない**（計器なしで作業を進めることになる）
- 決め手: 数値ゲートが無い（§8）ため全体値が下がっても支障がない。層ごとの表があるので `src/domain` 等は従来どおり読める。折衷案（変更箇所カバレッジだけ `.tsx` を含め、全体・層は `.ts` のまま）は分母が2種類になり後で混乱するため採らない
- 結果: 挙動不変（計測設定のみ）。全体 Stmts 75.76% → **50.55%**、`src/app` 21.46% → **7.83%**（分母は 1675 → 2528 文）。**含めることで初めて UI 変更の PR で未カバー行が表示される**ようになった
- 併せて `test-reviewer` の観点に4段の線引きを追記した（`.claude/agents/test-reviewer.md`）。段2で幾何を検証する偽の緑・全画面共通の契約（00_共通）の取りこぼしを毎回見る
- 関連: [23](./23_技術改善バックログ.md) T-39（コンポーネントテスト導入）/ T-85（E2E。起票当時は T-03 が E2E を抱えていた）/ 本書 T-38（可視化の仕組み）/ [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8

## T-38 カバレッジを GitHub 上で確認できるようにする（2026-07-26）

- 種別: ツール整備 / 優先度: 中
- 背景: [FB-49](./closed_21_ユーザーフィードバック.md)。カバレッジは手元で `npm run test:coverage` を叩かないと見えず、日常の開発でも GitHub 上でも把握しづらい
- 対応: `scripts/coverage-summary.sh` を新設し、CI が毎回 `test:coverage` を回して結果を **PR コメント（実行ごとに同じコメントを更新）と Actions のジョブサマリの両方**へ出す。内容は①全体値（`text-summary` reporter と同じ見た目・桁数）②層（`src` 直下2階層＝[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §2 の依存方向の層）ごとの表 ③PR のときは**変更箇所のカバレッジ**（変更行のうち計測対象の実行状況と未カバーの行番号）。reporter は `json-summary`（層の集計）と `lcovonly`（行データ。`lcov` は HTML ツリーも作るので使わない）
- 決めたこと: ①public リポジトリで数値を公開してよい ②単一のバッジ・全体値のみは採らない ③外部サービス（Codecov 等）は使わない ④計測対象は変えず本書 T-33（`.tsx` を含めるか）と独立を保つ。理由は[決定ログ](./log_23_技術改善バックログ.md)
- 実装上の要点: 割合は istanbul と同じ**切り捨て**（四捨五入すると手元の出力と 1/100 ずれ、同じ数字か判断できない）。コメント更新の目印は本文1行目の HTML コメントで、ci.yml 側には書き写さない（片方だけ変えるとコメントが静かに増える）。`actions/checkout` に `fetch-depth: 0` が要る。Dependabot の PR は `GITHUB_TOKEN` が読み取り専用に降格するため投稿ステップは `continue-on-error`
- 結果: 挙動不変（CI とスクリプトのみ）。PR #42 で実機確認——コメント投稿、**5回 push してもコメントは1件のまま**（id 不変・内容のみ更新）、ジョブサマリ出力、数値が手元の `text-summary` と4項目完全一致。変更箇所カバレッジは過去コミットを base に3パターン検証（20行/20行・126行中111行＋未カバー行の範囲圧縮・対象なし）
- 残す注意: 判定対象は `src/**/*.ts` のみで、**`.tsx`・docs・scripts だけの PR は常に「計測対象なし」になる**（直近40コミットで `.ts` を含むのは7件）。これを埋めるかは T-33 の判断次第
- 関連: [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 / [23](./23_技術改善バックログ.md) T-33 / 本書 T-17（カバレッジ導入）・T-20（`.tmp` 競合）・T-24（ワークフロー権限の最小化）

## T-35 並行タスク間のファイル衝突を機械判定する（2026-07-26）

- 種別: ツール整備 / 優先度: 中
- 背景: 並行度4本以上を目標にした（[定義書16](../仕様/16_git運用と並行開発体制定義書.md) §2）が、タスク分割時の衝突判定が**ヘッドの目視**に依っている。2026-07-25 の並行4本では `Explore` の調査で FB-48 と FB-51 が `daily-list.tsx` の同じ関数（`TaskRow` 内の10行以内）で衝突することを見つけて直列化できたが、本数が増えれば見落とす。見落とすと「自動マージは通るが意味的に壊れる」形になり、CI では検出できない
- 対応: `npm run wt:overlap`（`scripts/wt-overlap.sh`）を新設。稼働中の worktree のブランチ（または引数指定のブランチ）を総当たりし、`git diff --name-only <base>...<branch>` の積集合を出す。空のペアは「中間 rebase を省略してよい」、空でないペアは重なるファイルを列挙する。**積集合が空のペアには `git merge-tree` を重ねて掛ける**——ディレクトリとファイルの取り違えなど、ファイル名の突き合わせだけでは見逃す衝突があるため（§1.3 が両方を求めている理由）。終了コードは 0＝重なりなし / 1＝重なりあり / 2＝判定できなかった
- 当初あった方針①（着手前に変更予定ファイルを宣言させて交差を取る）は**作らない**。宣言という不確実な入力を機械化するより、各ブランチに最初のコミットが乗った時点で②を掛け直して実差分で検算するほうが確実なため。着手前の見積りは `Explore` の目視のままとし、この検算を §5 手順1 に明記した
- 結果: 挙動不変（開発ツールのみ）。使い捨てブランチで実測し、同一ファイルの重なり・交わらないペア・ディレクトリとファイルの衝突（積集合は空だが merge-tree が検出）・同一ブランチの重複指定・規約外のブランチ名・日本語パス・異常系の終了コードを確認した
- 残す注意: **意味的な衝突（別ファイルだが前提が壊れる）は原理的に検出できない**。分割の妥当性そのものは人が見る。この但し書きはスクリプトのヘッダと §1.3 の両方に置いた
- 関連: [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §1.3・§5（判定の正）/ 本書 T-34・T-36（同じ `scripts/` 配下の整備）

## T-34 worktree ごとに dev サーバを立てられるようにする（2026-07-25）

- 種別: ツール整備 / 優先度: 中
- 背景: dev サーバが本体ワークツリーの1本だけで、動作確認のたびにヘッドが `git checkout --detach` で切り替えるため、PR が複数あると確認が直列化する。2026-07-25 の並行4本（FB-48・FB-53・FB-51・FB-54）で明確なボトルネックになった
- 対応: `wt-new.sh` が 3001 から空きポートを探し `.env.worktree` に `DEV_PORT` を書く。`npm run dev` を `scripts/dev.sh` 経由にし、そこで `DEV_PORT` を読んで `next dev -p` に渡す（`next dev` のポートはプロセス起動時のシェル環境か `-p` でしか決まらず、Next.js の env ファイル読み込みはポート決定後に走るため、`.env.worktree` に書くだけでは効かない）。本体には `.env.worktree` がないため従来どおり :3000 で後方互換。`.env.local` は**コピーではなく本体へのシンボリックリンク**（資格情報の実体を1か所に保つ。`.gitignore` の `.env*` でリンクも git から外れる）。**開発DB（:5432）は本体と共有**——worktree ごとの複製は `createdb -T` が本体の接続中に失敗し `pg_dump` は遅く、実データの厚みも失うため採らない。起動はオーナーのターミナルが持つ（AI のバックグラウンドから立てたプロセスは30分前後で回収されると実測。約32分・約29分・約29分の3回）
- 結果: 挙動不変（開発ツールのみ）。本体 :3000 と worktree :3001 の**2本が同時に HTTP 200** を返すことを実証（`/` はサーバコンポーネントがDBを読むため、200＝リンクした `.env.local` の `DATABASE_URL` が効いている証拠）
- 残す注意: 開発DBを共有するため、2本で同時に打刻・削除すると互いに干渉する。データを触る確認は1つずつ行う（干渉が実害になったらそのとき分離を足す）
- 関連: [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §3.2（方式の正）・§2（並行度4本以上の条件①）/ 本書 T-32・T-36（同じ整備で一括実施）

## T-32 `wt:rm` が worktree ディレクトリを消し残す（2026-07-25）

- 種別: ツール整備 / 優先度: 中
- 背景: `git worktree remove` が "Directory not empty" で落ちることがあり、**実測で7本中4本が失敗**（当初「再現性が不定」と書いたが失敗率はほぼ半分）。失敗すると手前のテストDB削除と registration 解除までは通っているためディレクトリだけが残り、ローカルブランチの削除も手作業になる。残骸は `node_modules` を欠いた状態でエディタの型チェック対象に残り続け大量の偽エラーを出す
- 対応: **`--force` を無条件に付ける形は採らない**（dirty 判定の防護を迂回しないという既存判断の維持）。代わりに ①`node_modules`・`.next` を先に削除する（ファイル数の大半を占め、gitignore 対象なので clean 判定に影響しない）②失敗したら2秒待って再試行 ③それでも失敗したら登録解除＋明示削除へフォールバック（dirty の防護はスクリプト自身の `git status --porcelain` が既に果たしているため、ここで git の安全装置を迂回しても防護は落ちない）。あわせてブランチ削除を、`git branch -d` 失敗時に**リモートブランチの不在**（GitHub のマージ時自動削除）を確認して `-D` する形にし、squash マージ運用でも一連で通り切るようにした。`.env.local` のリンクは実体を消さないよう `-L` で確かめてから外す
- 結果: 挙動不変（開発ツールのみ）。worktree の作成 → 削除を通しで実行し、テストDB・ブランチ・ディレクトリまで残骸ゼロで完走
- 関連: [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §5-6 / 本書 T-06（worktree 体制の整備）・T-34（同じ整備で一括実施）

## T-36 台帳の行操作をスクリプト化し、表構造を機械検査する（2026-07-25）

- 種別: ツール整備 / 優先度: 中
- 背景: 2026-07-25 の1日で、ヘッドが `21_ユーザーフィードバック.md` の markdown テーブルを**3回壊した**——①行の削除で「途中から次の行頭まで」をアンカーにして行を切り落とし次のエントリを連結 ②同じ誤りを別の行で再演 ③行の追加で「テーブルと節見出しを区切る空行」の下に挿入しテーブルが3つに分断（オーナーがプレビュー崩れとして発見）。いずれも判断のミスではなく**1行が千文字を超えるテーブル行を手で編集したこと**が原因で、並行度が上がりヘッドの文脈切替が増えるほど起きやすい
- 対応: **本命は検査ではなく操作の自動化**。`npm run ledger:move -- <FB-XX> [--status '<新しい状態列>']` を追加し、行の切り出しは **ID の完全一致**（破損2件の原因は部分文字列アンカー）、挿入は**テーブルの内側＝最終行の直後**（3件目の原因）を機械で守る。移動先の節は完全一致で探し、見つからなければ補足（`— …` 以降）の差異だけを許し、それでも一致しなければ推測せず失敗させる。網として `npm run ledger:check` を併設し、台帳3冊と `closed_*` の①列数②エントリの混入③テーブル内の空行を検査する。22（チェックリスト）・23（`## T-XX` 節）は構造が違い1行が短く壊れにくいため移動用スクリプトは持たず検査だけを掛ける
- 結果: 挙動不変（開発ツールのみ）。3種の破損を仕込んだ複製に対し検査が全件検出することを確認。`ledger:move` は**実装中に節の誤爆（「改善（軽微）」と「改善（要仕様整理）」を先頭語で照合して取り違え）を自ら踏み**、完全一致を第一候補にする形へ修正した——手作業なら黙って間違えていた種類の誤りで、スクリプト化の価値がその場で出た
- 関連: [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §3.4（文案はワーカー・記入はヘッド、記入はスクリプト経由）・§2（並行度4本以上の条件④）/ 本書 T-27（案件台帳の完了分をアーカイブへ分離）

## T-31 `updated_at` 比較に依存する統合テストの flaky 解消（2026-07-25）

- 種別: 改善（テスト補強）/ 優先度: 中
- 背景: `drizzle-section-repository.int.test.ts` の更新系テストが低頻度で落ちる。原因は**時刻源が2つある**こと——挿入時の `updated_at` は DB の `now()`、更新時はアプリの `new Date()` で、コンテナとホストの時計が数ミリ秒前後しうる。テスト内のコメントは「同一ミリ秒で等値になりうる」ケースだけを想定し、逆方向のずれ（更新後が挿入時より前）を見落としていた
- 対応: 大小関係の検証をやめ、「更新時に現在時刻で埋め直されている」ことだけを見る形へ（`Date.now()` との差が60秒未満）。時刻源を片方へ寄せる案は挙動に触るため範囲外。テスト名も実態に合わせて「updated_at を巻き戻さない」→「updated_at を現在時刻で埋める」に改めた
- 結果: 挙動不変（テストのみ）。3回連続の全実行で 48ファイル・705テスト全緑。**着手の直前に実際の失敗を踏んだ**（`expected 1784990153096 to be greater than or equal to 1784990153107` ＝ 11ms の逆行）ため、想定した機序がそのまま観測された
- 規模: 1ファイル1行。`updated_at` の大小比較を持つ統合テストは他の4リポジトリには無く、起票時に書いた「横断して洗う」は空振りだった
- 発見: F-212 実装時の `test-reviewer` レビュー（2026-07-25）。F-212 の差分とは無関係の既存問題
- 関連: [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8（テスト戦略）

## T-06 worktree を活用した AI 並行開発体制（2026-07-25）

- 種別: 改善（開発体制）/ 優先度: 中
- 背景: 単一ワークツリー・単一セッションでの作業は、別タスクが並行すると変更が交錯する。実際、別セッションで進行中だった作業（`docs/案件/21_ユーザーフィードバック.md` ほかの WIP）を中断・除外して本作業を進める必要が生じた（2026-07-22 起票の動機）
- 対応: **ヘッド1＋ワーカーN の2階層**（ヘッドはコードを編集しない／ワーカーは worktree の外に出ない）。統合は PR＋CI（T-01）に乗せ、マージ順序はヘッドが逐次調停。通信機構はサブエージェント（worktree 分離＋SendMessage 継続）を採用し Agent Teams は不採用。整備＝CLAUDE.md 改訂（「git 運用」節・「並行開発（worktree）」節）／`scripts/wt-new.sh`・`wt-rm.sh`（worktree＋ブランチ＋テストDBの一括セットアップ・後片付け）／`vitest.config.ts` の `.env.worktree` 読み込み（worktree ごとのテストDB分離）／permissions 追記。**体制の契約は [16_git運用と並行開発体制定義書](../仕様/16_git運用と並行開発体制定義書.md) が正**（検討ドキュメント34から昇格。T-26 の git 運用ルールと統合）。並行モードへの切替はオーナーの明示指示のみを契機とし、当初構想の Phase 3（ヘッド運用の型化・Agent Teams 再検討）は設けないことにした
- 結果: 挙動不変（開発体制・テスト設定のみ）。worktree 作成 → 専用テストDBへのマイグレーション適用 → 本体と worktree の統合テスト同時実行で両方緑（TRUNCATE の消し合いなし）を実証
- 関連: 本書 T-26（git 運用ルール。一括実施）・T-01（CI 基盤）/ [CLAUDE.md](../../CLAUDE.md)「サブエージェント運用」

## T-26 git/ブランチ戦略の全体設計（T-06 と統合・2026-07-25）

- 種別: 調査 → 対応 / 優先度: 中
- 背景: git/ブランチ運用に明文規定がなく、実態は `main` 直コミットと PR（#16/#27 等）が混在。`main` への push＝本番デプロイ（Vercel）という制約の下で、変更の種類ごとの扱いを一貫させたい。並行開発（複数 worktree）まで見据えると、マージ順序の調停・スキーマ変更の直列化を扱う T-06「AI並行開発体制」と不可分であり、両者を1つの設計として束ねる
- 対応: 設計を [16_git運用と並行開発体制定義書.md](../仕様/16_git運用と並行開発体制定義書.md) §1「git 運用ルール」として確定（T-06 と統合。当初は検討ドキュメント34、のち仕様/16 へ昇格）。(1) リスク別ルーティング＝docs・軽微は `main` 直コミット／コード・挙動はブランチ→PR→CI→オーナー合図でマージ／スキーマは migrate 先行（T-22）、(2) push・マージは常にオーナー合図（自動マージ・ブランチ保護なし＝T-01 方針の継承）、(3) 並行化の運用＝ブランチ命名 `<タスクID>-<slug>`（worktree ディレクトリ名・テストDB名と機械的に対応）・1ブランチ1PR・マージ後削除・スキーマ変更ブランチは常に最大1本・マージキューで逐次調停。CLAUDE.md に「git 運用」節を新設（要約＋参照）
- 結果: 挙動不変（docs・運用ルールのみ）。本ルールの初回実践として、実装自体を `t-26-git-strategy` ブランチ＋PR で実施。残置していたマージ済みリモートブランチ（`claude/*` 4本）はマージ後に削除
- 関連: 本書 T-06（並行開発体制。Phase 1〜2 を同時実施）/ 本書 T-01（CI 基盤）・T-22（本番スキーマ更新パイプライン）/ [CLAUDE.md](../../CLAUDE.md)「git 運用」

## T-19 Shift+J/K のアーカイブ済みセクション境界で楽観更新とサーバ確定がずれうる（2026-07-25）

- 種別: 調査 → 対応 / 優先度: 中
- 背景: T-11 のレビュー（spec-reviewer）で判明した既存の潜在論点。Shift+J/K の移動先算出で、サーバ `moveTaskByOneStep` の `sectionOrder` は `activeSections`（アーカイブ済み除外）だが、presentation `daily-board.tsx` は `optimisticGroups` 由来で当日タスクが属するアーカイブ済みセクションを含みうる。その境界を跨ぐ Shift+J/K で、client は移動先ありと楽観更新するがサーバは `indexOf === -1` で「動かさない」と判定し楽観更新が巻き戻る（データ不整合はなく視覚的な巻き戻りのみ・エッジケース）
- 調査結果: 再現条件（当日タスクを持つアーカイブ済みセクションの境界を跨ぐ Shift+J/K）と実害（視覚的巻き戻りのみ・sort_order は常に妥当でデータ不整合なし）を確定。ローカルでの間欠再現は不要な範囲だが、コード追跡で client/server の `sectionOrder` 定義差を実害の原因と特定
- オーナー判断: 「表示されているものは跨げる」＝当日タスク付きアーカイブ済みセクションも Shift+J/K の移動先に含める（O-5 の割り当てがアーカイブ済みを除外するのとは役割が異なる）。docs 先行で画面定義書01 O-6 を更新（`log_01_デイリーリスト.md` に決定エントリ）
- 対応: 移動先セクション順を表示順に統一。`src/domain/task/daily-list.ts` に `displaySectionOrder(tasks, sections)`（＝`groupTasksBySection` の表示順から ID 列を作る純関数）を追加し、`moveTaskByOneStep`（サーバ）がこれを使用（従来の `activeSections` 版を置換）。presentation は同じ規則を `optimisticGroups` から再現。両者とも `groupTasksBySection` に由来し定義の二重実装が解消
- 結果: 挙動変更（アーカイブ済み境界を跨げるようになった）。docs 反映済み。lint / build / 全テスト（unit 570・int 49）緑。domain・usecases に O-6/T-19 テストを追加（混在ソートの挿入位置・跨ぎの両方向・リスト端 no-move を含む）
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-11（移動先算出の domain 一本化）/ 画面定義書01 O-6

## T-30 `@types/node` を本番ランタイム（Node 24 系）へ追随させる（2026-07-25）

- 種別: 負債返済 / 優先度: 中
- 背景: `package.json` が `@types/node: ^20` のままで、本番 Vercel・CI・db-migrate のランタイム（Node 24 系）に型定義が追随していなかった。Dependabot PR #3（20 → 26）を「ランタイムを追い越すため見送り」でクローズした際に追随先の 24 へ上げる作業が残り、Dependabot は最新版（26）しか PR にしないため放置では埋まらない状態だった（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §11）
- 対応: `@types/node` を `^24` に上げ `npm install`（2パッケージ更新）。lint / build / 全テスト（46ファイル・610件）で回帰なしを確認。メジャー4つ分の型更新だが型エラーは発生しなかった
- 結果: 挙動不変（型定義のみ）。本番ランタイムと型定義のメジャーが一致
- 関連: [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §11（版の追随関係は同節が正）

## T-20 `npm run test:coverage` の `.tmp` 削除競合で Unhandled Rejection（2026-07-25）

- 種別: 負債返済 / 優先度: 様子見
- 背景: T-11/T-12 の検証（verifier）で判明した設定負債。`test:coverage` が unit と integration の2プロジェクトを同一 `coverage.reportsDirectory` で並行実行し、片方のクリーンアップがもう片方の `coverage/.tmp` を削除する競合で `ENOENT`（Unhandled Rejection）がログに出た。exit code は 0 で CI は成功扱いだがログにエラーが残る。vitest 4.1.10（最新4系）は upstream 修正（[PR #9378](https://github.com/vitest-dev/vitest/pull/9378)）未取り込みのため設定側で対処
- 対応: `test:coverage` スクリプトに `--no-file-parallelism` を付与（`vitest run --coverage --no-file-parallelism`）。全ファイルを直列実行することで共有 `coverage/.tmp` への並行アクセス自体が構造的に消える。バックログが挙げた2案（出力先のプロジェクト分離／逐次2回run）はいずれも合算カバレッジ像を失う（分離＝2レポートに割れて誤読を招く／逐次＝後のrunが前を上書き）ため採らず、単一の合算レポートを保てる本方式を選んだ。coverage は随時実行の補助指標で `test:unit`/`test:int`/`watch` は無影響
- 結果: 挙動（プロダクト）不変。`test:coverage` は exit 0・610件成功・合算レポート維持（Statements 71.78%）・ENOENT なしを確認（所要 ~11.7s）。※元の競合はローカルで再現しなかったため「失敗ケースの消失」実証はできていないが、直列化で競合の発生条件（並行アクセス）自体を除去した
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-17（カバレッジ導入）

## T-28 docs 全文書への一意採番の導入（2026-07-24）

- 種別: 改善（docs 構造）/ 優先度: 中
- 背景: T-27 の台帳分離に続く docs 構造の整備。文書ファイルに順序・識別の番号がなく（画面定義書 00〜04 のみ採番済み）、一覧性と参照の安定性に欠けていた
- 対応: **docs 全体で一意な2桁ブロック採番**を導入（0X=画面定義書内〔現状維持〕/ 1X=仕様 / 2X=案件 / 3X=検討）。並びは仕様=FB着手の判定順（11 要求 → 12 要件 → 13 画面 → 14 データモデル → 15 アーキテクチャ）、案件=流入順（21 FB → 22 要件BL → 23 技術改善BL → 24 実装計画）、検討=作成順（31 バンドル → 32 スキーマ更新 → 33 バックアップ → 34 AI並行開発）。`log_` / `closed_` / `guide_` は親の番号を引き継ぐ。全19ファイル/ディレクトリを `git mv` し、docs・README・CLAUDE.md・`.claude/`（agents 4本・dependabot-triage）の参照を全追随。体系は CLAUDE.md「最重要ルール」に明文化
- 結果: 挙動不変（docs・エージェント定義のみ）。文中の「画面定義書01」呼称・H1 タイトル・FB/T/F/N の ID は不変。git は全ファイルを rename として認識し履歴が連続
- 関連: 本書 T-27（台帳分離）/ [CLAUDE.md](../../CLAUDE.md)「最重要ルール」

## T-27 案件台帳の完了分をアーカイブへ分離（2026-07-24）

- 種別: 改善（docs 構造）/ 優先度: 中
- 背景: ユーザーフィードバック管理簿（43件中38件が対応済み）と技術改善バックログ（26件中18件が完了）は完了エントリが本文に線形蓄積する構造で、AI が起票・着手のたびに開くファイルなのに、ライブに読みたい未完了分が完了分のノイズに埋もれていた
- 対応: 既存の「ライブ台帳 vs 完了アーカイブ」パターン（要件バックログ／実装計画）を両台帳へ適用。`closed_<文書名>.md` を隣接新設して完了エントリを**原文のまま**移し、ライブ台帳は未完了のみに（FB管理簿の着手手順も `guide_21_ユーザーフィードバック.md` へ分離）。「完了・対応済みへの状態更新と同じコミットでアーカイブへ移す」を運用ルール・CLAUDE.md に明文化
- 結果: 挙動不変（docs のみ）。FB-XX / T-XX の番号・記述は温存し、完了 T を名指しする既存リンクは本書へ付け替え
- 関連: [ユーザーフィードバック管理簿](./21_ユーザーフィードバック.md) / [要件バックログ](./22_要件バックログ.md) / [CLAUDE.md](../../CLAUDE.md)「書き方の規約」

## T-22 本番スキーマ更新をリモートで実行するワークフロー（案A 採用・2026-07-23）

- 種別: ツール整備（当初は調査）/ 優先度: 中
- 背景: 本番マイグレーションは手元での手動実行だった（[CLAUDE.md](../../CLAUDE.md)「本番マイグレーションの手順」）。これをリモート（GitHub Actions）で完結させ、接続情報を Environment シークレットに置きたかった
- 方式判断: 実現方式 A（実行のリモート化・`workflow_dispatch`）→ B（マージ契機の自動 migrate→deploy）→ C（expand/contract）のうち、**個人開発の規模では A で必要十分**と判断し A を採用。B・C は過剰につき見送り（将来の two-way door として検討ドキュメント §3 に記録）。難所は migrate 実行そのものではなく (1) Vercel 自動デプロイとの順序保証、(2) public リポジトリでのシークレット露出、の2点。**詳細は [32_スキーマ更新パイプライン検討.md](../検討/32_スキーマ更新パイプライン検討.md) が正**
- 対応: ①`.github/workflows/db-migrate.yml`（`workflow_dispatch` ＋ migrate 専用 `db-migrate` Environment の承認ゲートで `npm run db:migrate`。新規マイグレーションを持つ PR ブランチから実行 → 成功後マージ＝デプロイ。Vercel の "Production" 環境とは分離）②`label-schema-migration.yml` を拡張し、スキーマ更新 PR に migrate 実行を促す注意コメント（`[!WARNING]`＋Run workflow 直リンク＋ブランチ名＋`gh` コマンド、sticky upsert）③README・CLAUDE.md を整理（手順は README が正・重複解消）
- 結果: 実働確認済み。PR #16（F-116・マイグレーション 0002）で「ラベル → 注意コメント → リモート migrate（`db-migrate` 承認ゲート経由で本番へ 0002 適用）→ マージ（＝デプロイ）」を通しで確認。挙動（プロダクト）不変
- 関連: 本書 T-01（CI 基盤）/ T-21（スキーマ更新ラベル）/ [CLAUDE.md](../../CLAUDE.md)「本番マイグレーションの手順」

## T-24 CI ワークフローの GITHUB_TOKEN を最小権限に絞る（2026-07-23）

- 種別: ツール整備 / 優先度: 中
- 背景: CodeQL（GitHub default setup）の code-scanning アラート（medium、`actions/missing-workflow-permissions`）。`.github/workflows/ci.yml` に `permissions:` ブロックが無く、GITHUB_TOKEN が既定の広い権限で発行されていた。verify ジョブは checkout と npm の lint/build/test のみで書き込み権限を要さない
- 対応: workflow レベルに `permissions: contents: read` を追加し、コード取得に必要な最小権限のみへ絞る。挙動（プロダクト・CI の検証内容）は不変。マージ後、default setup の再スキャンでアラートは自動クローズする
- 関連: 本書 T-01（CI 基盤）/ [.claude/skills/dependabot-triage](../../.claude/skills/dependabot-triage/SKILL.md) §7

## T-23 sharp（libvips 継承）の high 脆弱性を overrides で解消（2026-07-23）

- 種別: 負債返済 / 優先度: 中
- 背景: Dependabot セキュリティアラート（high、`sharp` <0.35.0）。上流 libvips 由来の脆弱性 4件（CVE-2026-33327 ほか、untrusted な GIF/TIFF/VIPS 画像のデコード時に影響）。`next@16.2.11` が transitive に同梱する `sharp@0.34.5` が原因。本アプリは `next/image`（`<Image>`）を使わず画像アップロード機能もないため sharp は実行経路に乗らず**到達不能**だが、上流に修正版（0.35.x）があるため dismiss ではなく根治を選んだ
- 対応: `package.json` の `overrides` で `sharp` を `^0.35.0` に固定。ツリーが `sharp@0.35.3`（libvips 8.18.3 同梱）へ dedupe され、アラートは修正版採用で自動クローズ。0.34→0.35 は minor（prebuilt バイナリ差し替えのみ）で API 破壊なし。CI の build で回帰なしを確認
- 関連: 本書 T-04（postcss overrides の同型対応）

## T-21 スキーマ更新が必要な PR に「スキーマ更新」ラベルを自動付与（2026-07-22）

- 種別: ツール整備 / 優先度: 中
- 背景: マージ＝本番デプロイであり、スキーマ変更を含む PR は**マージ前に本番マイグレーションを手動で流す**必要がある（[CLAUDE.md](../../CLAUDE.md)「本番マイグレーションの手順」）。この前提が PR 上で可視化されておらず、流し忘れがデプロイ事故につながりうる
- 対応: `.github/workflows/label-schema-migration.yml` を新設。`pull_request`（opened / synchronize / reopened）で PR 差分を見て、**新規追加**のマイグレーション（`src/infrastructure/db/migrations/*.sql`・`listFiles` の `status === 'added'`）を含む PR に「スキーマ更新」ラベルを付与し、含まなくなれば外す。`actions/github-script` でラベルの取得／生成／付与／除去を行う（ラベルは初回実行時に workflow が自動生成）
- 方針: 検出条件は**新規マイグレーション SQL の追加の有無に一本化**（`schema.ts` 変更のみでマイグレーション未生成のケースは別軸の課題として扱わない）。ラベルの付与・除去のみで、**CI は fail させず・マージもブロックしない**（T-01 の「検証のみ・手動マージ」方針と揃え、注意喚起に徹する）
- 結果: 挙動（プロダクト）不変。PR 上でスキーマ更新の要否が可視化され、本番マイグレーションの流し忘れを防ぐ注意喚起になる
- 関連: 本書 T-01（CI 基盤）/ [CLAUDE.md](../../CLAUDE.md)「本番マイグレーションの手順」

## T-09 マスタ表の Action ラッパとアーカイブ済みブロックを共通化（2026-07-22）

- 種別: 改善 / 優先度: 中
- 背景: modes/projects/sections の3表が、`run`（transition＋エラー表示）とアーカイブ済み `<details>` ブロック（復元／削除行）をほぼ同一構造で持っていた
- 対応: `useMasterAction`（`_lib/use-master-action.ts`＝`run`/`error`/`setError`/`isPending`。3表で完全一致だった）と `ArchivedMasterSection`（`_components/archived-master-section.tsx`。ラベル列は表ごとに違うため `renderCells` で受ける）を切り出し3表から使う。`commit`/`newRow` の骨格や各表固有のインライン編集（modes=カラーピッカー／sections=フィールド単位編集＋終了時刻導出）は差分として各表に残した
- 結果: 挙動不変。lint / build / test 緑

## T-10 ポップオーバーの「外側クリック＋Esc で閉じる」を共通フック化（2026-07-22）

- 種別: 改善 / 優先度: 中
- 背景: `mousedown` 外側クリック＋`keydown` Escape で閉じる同型の `useEffect` が5箇所（row-menu・select-popover・routinize-popover・modes-table の ColorPickerPopover・shortcut-help）に手書きコピーされていた
- 対応: `src/app/_lib/use-dismiss.ts` に `useDismiss(ref, onClose, { enabled, escape })` を新設（`use-flip-up` と同じ全画面共有 `_lib`）。ref=null で外側クリックを監視しない（shortcut-help＝スクリム側で処理）、`escape:false` で Escape を呼び出し側に残す（select-popover＝IME判定込みのキーナビと融合）、`enabled` で常時マウントの購読制御（row-menu）に対応
- 結果: 挙動不変（mousedown＋Escape）。lint / build / test 緑

## T-14 daily-board のキーボードショートカットをフックへ抽出（2026-07-22）

- 種別: 改善 / 優先度: 中
- 背景: `daily-board.tsx` 後半のグローバルショートカット処理（`useEffect` 約110行）が本体に埋め込まれ可読性を下げていた（挙動は画面定義書01 §6 が正）
- 対応: `use-daily-shortcuts.ts` の `useDailyShortcuts` フックへ抽出。状態・操作ハンドラを引数で受ける配線層で、依存配列を持たず毎レンダー登録し直して最新クロージャを拾う点も従来どおり
- 結果: 挙動不変。lint / build / test 緑

## T-16 生成物へ選択を移す非楽観パターンの共通化（2026-07-22）

- 種別: 改善 / 優先度: 低
- 背景: `daily-board.tsx` で「採番をサーバが決めるため楽観更新せず、成功で生成物の `createdId` へ選択移動・失敗で `setError`」という同型処理が複製・複製して開始・クイック追加の3箇所にあった
- 対応: `runSelectingCreated(action, optimistic?)` ヘルパーへ括り出し3箇所から使う。クイック追加は `optimistic` 引数で確定前の楽観 append を挟む。`operate` の "duplicate" 分岐を共有 transition から切り出し本ヘルパーへ寄せた
- 結果: 挙動不変。lint / build / test 緑

## T-11 Shift+J/K の移動先算出を domain 純関数へ一本化（2026-07-22）

- 種別: 負債返済 / 優先度: 中
- 背景: 選択タスクを1段動かす移動先（グループ内±1、端で隣セクションの先頭／末尾へ）の規則が、domain の `moveTaskByStep`（`reorder.ts`）と、楽観更新のため presentation の `moveByStep`（`daily-board.tsx`）に別実装で存在した（唯一のレイヤ責務違反）。片方だけ直すと楽観更新とサーバ確定がずれる
- 対応: 移動先だけを返す純関数 `stepMoveDestination(tasks, taskId, step, sectionOrder): { sectionId, index } | null` を `src/domain/task/reorder.ts` に切り出し、`moveTaskByStep` と `daily-board` の `moveByStep`（`orderedTasks`＋`optimisticGroups` 由来の sectionOrder を渡す）の双方が使う。移動しないときは `null`。`reorder.test.ts` に `stepMoveDestination` 直接のユニットを追加（端・空セクションまたぎ・未分類またぎ・不在）
- 結果: 挙動不変（同じ移動先を返す）。業務規則が domain 1箇所に集約。lint / build / test 緑

## T-12 `domain/routine/routine.ts` のコロケーションテストを補完（2026-07-22）

- 種別: 改善（テスト補強）/ 優先度: 様子見
- 背景: `describeRecurrence`・`toggleWeekday` は T-XX（PR #10 のテスト補完）で `routine.test.ts` に追加済みだったが、`weekdayBitOf`（日曜=0→月曜=0 のビット変換。off-by-one を起こしやすい）と `hasWeekday` の直接テストが無かった
- 対応: 既存 `src/domain/routine/routine.test.ts` に `weekdayBitOf`（月=bit0・日=bit6 の全曜日対応）と `hasWeekday`（月/日/複数曜日の該当判定、日曜 bit6 の off-by-one 確認）の直接テストを追記
- 結果: 挙動不変。対象4関数すべてに直接の仕様条項テストが揃った

## T-08 Server Action 結果型を共通型へ一本化（型のみ・2026-07-22）

- 種別: 改善 / 優先度: 中
- 背景: 構造が同一の結果型 `Readonly<{ ok: true } | { ok: false; message: string }>` が3箇所（daily `DailyActionResult`／routines `RoutineActionResult`／masters `ActionResult`）に別名で並立していた
- 対応: 正となる `ActionResult` を `src/app/_lib/action-result.ts` に新設し、3ファイルは再エクスポート/エイリアスに寄せた（`DailyActionResult = ActionResult` 等）。**型名は温存**したため消費側 `.tsx` は不変。**低リスクな型の一本化のみ先行**し、`revalidatePath` 込みの定型ラッパ化・`MESSAGES` 統合（文言差があり挙動＝表示変化になる）・`createdId`/carry-over 等の固有処理は据え置き（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §1 の過剰抽象回避）
- 結果: 挙動不変。型定義が1箇所に集約。lint / build / test 緑

## T-17 カバレッジ計測（`@vitest/coverage-v8`）の導入を確認（2026-07-22）

- 種別: ツール整備 / 優先度: 中
- 背景: `test-reviewer` 導入に合わせカバレッジを数値で参照できるようにする項目
- 対応: `@vitest/coverage-v8`（devDependency）・`test:coverage` スクリプト・`vitest.config.ts` の `coverage` 設定（provider v8 / reporter text,text-summary / include・exclude）は既に対応方針どおり整備済みであることを確認。コード変更なし。`npm run test:coverage`（unit）で行/分岐カバレッジの実数値が出力されることを確認した（統合分は db-test 起動時に併走）
- 結果: **数値ゲートは設けない**方針は維持（§8 の主指標は「仕様条項に対応するテストがあるか」のまま、カバレッジは補助指標）

## T-18 draft から `NewTask` を組み立てる写経の共通化（2026-07-22）

- 種別: 改善 / 優先度: 低
- 背景: draft（`duplicateDraft`／`resumeTaskDraft` の戻り）から `NewTask` を1列ずつ写して組み立てるブロックが、`operations.ts`（`suspendTask`・`duplicateTask`・`duplicateAndStartTask` の newTask/resumeTask）と `punch-usecases.ts`（`startTask` の割り込み再開）で計5箇所に増殖していた（`sectionId`/`sortOrder`/`splitParentId` の供給元だけが違う）
- 対応: `src/usecases/task/from-draft.ts` に `newTaskFromDraft(draft, { taskDate, sectionId, sortOrder, splitParentId })` を新設し5箇所を置換。複製系は `splitParentId` を省略（＝null）、再開系は `draft.splitParentId` を渡す。ファイル名は §2 命名規約（ディレクトリ名 `task` を繰り返さない）に沿い `from-draft.ts` とした。`operations.ts` で不要になった `NewTask` の import を整理
- 結果: 挙動不変。フィールド対応は現状と1:1で、既存の `operations.test.ts`・`punch-usecases.test.ts` が緑

## T-07 統合テストの `updated_at` ミリ秒衝突フレークを解消（2026-07-22）

- 種別: 改善（テスト補強）/ 優先度: 様子見
- 背景: `drizzle-section-repository.int.test.ts`「update は名前と開始時刻を書き換え、updated_at を進める」が、更新の前後が同一ミリ秒に収まると `updatedAt` が変化せず `not.toBe` で偽陽性で落ちた。実装（アプリ層が `updated_at` を設定する契約）ではなくテスト側の時刻粒度の問題
- 対応: assertion を `not.toBe` → `toBeGreaterThanOrEqual`（巻き戻らない、または同値）に緩和。DB既定値（挿入時）とアプリ時刻（更新時）は同一クロックのため後退しないので `≧` で十分。テスト名を「updated_at を巻き戻さない」に、コメントも意図に整合させた
- 結果: 挙動不変。同一ミリ秒衝突でも偽陽性で落ちなくなった

## T-15 Dependabot 依存追随（next セキュリティ patch＋Actions v7）（2026-07-22）

- 種別: 依存追随 / 優先度: 中
- 背景: Dependabot の version-update PR 2件。#8 = npm minor-and-patch グループ（`next` 16.2.10→16.2.11・`react`/`react-dom` 19.2.7→19.2.8・`eslint-config-next` 16.2.10→16.2.11）。`next` 16.2.11 は DoS/SSRF/Middleware bypass 等の advisory を含むセキュリティ修正リリース。#7 = GitHub Actions グループ（`actions/checkout` v4→v7・`actions/setup-node` v4→v7、major）
- 対応: 両 PR とも `verify` ジョブ（lint/build/test）が緑を確認のうえ squash マージ。#7 の major 破壊的変更（fork PR の `pull_request_target` チェックアウト制限）は自リポの push/PR CI に無関係で、変更は `.github/workflows/ci.yml` のみ・本番ビルド内容に影響なし。ローカル main は rebase で追随、`npm ci` で lockfile 整合
- 結果: 挙動変更なし。オープン Dependabot PR 0件・セキュリティアラート 0件

## T-13 domain/usecases のファイル名を概念名へ統一（2026-07-22）

- 種別: 改善 / 優先度: 中
- 背景: ドメイン別ディレクトリに分けたうえで、ファイル名の命名規約が混在していた（`task/` に `task-edit.ts` と `punch.ts` が同居、`routine/` に `routine-order.ts` と `expansion.ts` が同居、各ドメインの `{obj}-usecases.ts` がディレクトリ名と重複）。新規追加のたびに「prefix を付けるか」を都度判断する揺れの元だった。オーナーの気づき（動詞単独ではドメインが読み取れない）を起点に対話で診断し、ディレクトリで文脈が付く前提で「オブジェクト名を繰り返さない」方向に倒すと決めた
- 対応: [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §2「ファイル命名規約」に明文化。ディレクトリ名と重複する prefix を外した（`task-edit.ts`→`edit.ts`、`routine-order.ts`→`order.ts`、`routine-input.ts`→`input.ts`、`routine-from-task.ts`→`from-task.ts`、`expand-routines.ts`→`expand.ts`、`task-operations.ts`→`operations.ts`、`testing/in-memory-*-repository.ts`→`in-memory-repository.ts`）。**据え置き**: 集約の代表型（`task.ts` 等）・横断ディレクトリ（`ports/` `shared/` `repositories/`）・機能別 usecase（`punch-usecases.ts` 等）・集約操作をまとめた `{ドメイン}-usecases.ts`（domain の集約代表と同名衝突するため `-usecases` を維持）。シンボル名（関数・型）の Task/Routine 接頭辞の揺れは今回対象外
- 結果: lint / build / test（461件）グリーン。git は全ファイルを rename として認識し履歴・blame が連続する

## T-04 postcss XSS アラートを overrides で解消（2026-07-22）

- 種別: 負債返済 / 優先度: 中
- 背景: Dependabot セキュリティアラート（medium、postcss <8.5.10 の stringify XSS）。`next@16.2.10` 同梱の古い postcss が原因（最上位の Tailwind 側は既に修正版）。自前CSS＋Tailwind のため実害は乏しいが解消しておく
- 対応: `package.json` の `overrides` で `postcss` を `^8.5.10` に固定。ツリー全体が 8.5.x（8.5.21）へ dedupe され、`npm audit` から postcss が消えた。CI の build（Tailwind/postcss）で回帰なしを確認

## T-01 CI 整備（lint / build / test の自動実行）（2026-07-22）

- 種別: ツール整備 / 優先度: 高
- 背景: 依存更新（Dependabot）の検証を手動（ブランチ取得 → `npm ci` → lint/build/test）で行っていた。定期的に発生するため機械化したかった。従来の自動チェックは Vercel ビルド（`next build`＝型チェックのみ）だけで、`npm run lint` と `npm test` は走っていなかった
- 対応: `.github/workflows/ci.yml` を新設。`pull_request` と main への `push` で `npm ci` → `lint` → `build` → `test` を実行。統合テストは Postgres サービスコンテナ（`postgres:17-alpine`、DB=`hitosuji_test`）を建て、`TEST_DATABASE_URL` を渡す（マイグレーションは vitest の globalSetup が自動適用）。Node は本番に合わせて 24
- 方針: CI は**検証のみ**。Dependabot PR がグリーンでもマージ（＝main への push ＝本番デプロイ）は手動で、オーナーの合図を待つ。自動マージ・CI 必須チェック化（ブランチ保護）はしない

## T-39 コンポーネントテスト（jsdom）の導入

- 種別: ツール整備 / 優先度: 中 / 状態: **完了（2026-07-26）**
- **結果（第2バッチ完了時・`dcf49cf`）**: テスト **752 → 1343件**（80ファイル）。カバレッジは全体 Stmts **50.55% → 86.39%**、`src/app` **7.83% → 78.79%**。`src/domain` 99.69% / `src/usecases` 98.02% / `src/infrastructure` 79.18% は据え置き。本番コードの変更は `_lib/flip-up.ts`（新規）と `_lib/use-flip-up.ts`（判定式の委譲）のみで、**挙動は変えていない**
- **完了と判断した根拠**: 統合後の main に対して `verifier`・`test-reviewer`・`code-quality-reviewer`・`spec-reviewer`・`refactor-scout` の5本をリポジトリ全体へ掛け、①全項目グリーン（JST・UTC 両方で 1343件、`test:component` 10連続緑）②統合起因の致命的な取り違えなし（6タイムゾーンで実測）③§8「偽物を置いてよい境界」を全26テストファイルが遵守 ④依存方向の違反ゼロ、を確認した。**残る作業は独立した T へ切り出した**（下記）ので本 T に残作業はない
- **本 T から切り出した後続**: [T-43](#t-43)（テストヘルパの集約・完了）／T-44・T-52・T-54（マスタ3集約の重複）／T-45（共通キーガード）／T-48・T-53（大きいファイルの分割）／T-49（エラー文言辞書）／**T-59（`proxy.ts` のテスト・高）**／**T-60（usecases・infrastructure の未検証経路・高）**／T-61（コンポーネント段の残る穴）／T-62（CI の TZ 明示）。挙動が絡むものは FB-58〜FB-69 へ
- 背景: presentation を「自動テスト対象外」と一括りにしていたが、実測すると `.tsx` の大半（22ファイル・約3,600行）は Client Component で、jsdom 上のコンポーネントテストで普通に測れる。さらに **hooks は `.ts` なので既に計測対象の内側にいて 0%** という状態だった（`use-daily-shortcuts.ts` 188行・`use-dismiss.ts`・`use-now.ts`・`keyboard.ts`）。段の呼び名と境界は[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 が正
- 対応方針: jsdom + `@testing-library/react` を入れ、vitest の project を `unit` / `component` / `integration` の3本にする。テストは `*.test.tsx`（jsdom を要するものは対象が `.ts` でも `.tsx` に置く）
- 済（第1バッチ / 2026-07-26・PR #43 / b374c41）: 環境（jsdom＋`@testing-library/react`、project を unit / component / integration の3本、`test:component`）と、`use-now`・`use-dismiss`・`use-flip-up`（幾何に依存しないリセット分岐のみ）・`keyboard`・`TaskProgress`・`Toast` のテスト47件。カバレッジは `src/app` 8.96% → 21.46%（Stmts）、全体 72.59% → 75.76%
- 第1バッチで判明: **`coverage.include` は「未実行ファイルを走査する範囲」しか制御せず、テストが読み込んだファイルは include に関係なく計上される**。そのため `.tsx` を2つテストした時点で「その2ファイルだけが 100% で分母に入り、未テストの33ファイルは見えない」非対称集計になった
- 計測対象は `.tsx` を含む（T-33 で決着）。**各バッチの成果と未カバー箇所が PR コメントに出る**ので、次に何をテストすべきかは変更箇所カバレッジを見て決められる
- Server Action を直 import するコンポーネント（`daily-board.tsx`・masters の各テーブル）は**素の jsdom では描画できない**（`actions.ts` の先が `pg.Pool` と `revalidatePath` に届く）。差し替えの可否と目的は[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8「偽物を置いてよい境界」が正
- **第2バッチ（完了 / 2026-07-26・6並行・PR #45〜#50）**: 残る段2の対象を**ディレクトリ境界で6つに分割**し、worktree ごとのワーカーで並行実装した（体制は[git運用と並行開発体制定義書](../仕様/16_git運用と並行開発体制定義書.md) §2〜§5）。分割は新規テストファイルしか作らないため衝突面が実質ゼロ——例外は `use-flip-up.ts` の判定式抽出（唯一の本番コード変更）で、担当を⑥に限定した

  | # | ブランチ | 対象（Stmts） |
  |---|---|---|
  | ① | `t-39-daily-shortcuts` | `use-daily-shortcuts.ts`（93）。画面定義書01 §6 は20キー超＋除外規則を持つ最大の条項表で、jsdom 段の本命 |
  | ② | `t-39-daily-list` | `daily-list.tsx`（82）・`select-popover.tsx`（50）・`routinize-popover.tsx`（43）・`row-menu.tsx`（14）・`daily-summary.tsx`（7）・`shortcut-help.tsx`（6）・`stale-running-banner.tsx`（1） |
  | ③ | `t-39-daily-board` | `daily-board.tsx`（198）。**N-01 の楽観的更新とロールバック**・undo キュー・ショートカットとの結線 |
  | ④ | `t-39-masters` | `masters/**`（216）＝3テーブル（modes 76・sections 63・projects 47）＋`use-master-action`・`delete-master-button`・`archived-master-section`・`master-tabs`・`_lib/action-result` |
  | ⑤ | `t-39-routines-review` | `routines-table.tsx`（44）・`review-board.tsx`（39）・`routine-form.tsx`（35） |
  | ⑥ | `t-39-shared-components` | `_components/date-picker.tsx`（50）・`date-nav.tsx`（9）・`icons.tsx`（8）・`global-nav.tsx`（6）・`nav-tabs.tsx`（4）・`unset-mark.tsx`（1）＋`_lib/use-flip-up.ts` から純関数 `shouldFlipUp` を抽出 |

  段2で到達しうるのは計 906 文と見積もり、`src/app` の分母 1295 文のうち残り 276 文は段外（`actions.ts` 4本＝216／`page.tsx`・`layout.tsx`・`icon.tsx`＝42／`proxy.ts`＝18）とした。**`proxy.ts` を段外としたのは誤りで、ユニットで測れる**——完了時のレビューで判明し T-59 として起票した
- **第2バッチで得た教訓（次に並行でテストを書くとき効く）**:
  - **段をまたいで重複を消すと、合成側でしか見えない配線が落ちる**。ヘッドが「日付移動のテストはフック側と重複」と判断して board 段から削らせたが、フック側は「与えられた `date` から `push` の引数を組む」ことしか主張せず、**フックへ何を渡すか（`date` と `today` の取り違え・`isToday` の配線）は board 段でしか固定できない**とレビューで判明し差し戻した。重複判定は「同じ関数を2回呼んでいるか」ではなく「**どちらの段でしか観測できない契約か**」で行う
  - **盾が二重にあるとき、合成側のテストは弱い方の盾に隠れる**。FB-42（確定の `Enter` が背後の打刻へ抜ける）は `select-popover` の `stopPropagation` が防いでいるが、`daily-board` 経由では `setEditing(null)` が React の自動バッチで同一 dispatch 中に反映されず、`use-daily-shortcuts` の `editing` ガードだけで防げてしまう——**`stopPropagation` を外しても緑のまま**（変異で実測）。盾ごとに観測可能な段へテストを置く。段3への申し送りは T-03 に記録
  - **カバレッジ 100% でも観点は抜ける**。`date-picker.tsx` のカーソル表示（`ring-1 ring-accent`）は丸ごと消しても全テストが緑で、**v8 の行カバレッジも 100% のまま**だった。数値は「実行されたか」しか語らない
  - **エラー表示と `isPending` の解除は別のタイミングで届く**。マスタ管理のテストで「文言が出た瞬間に他行のセルを `click` すると、まだ `disabled` のままで無視される」flake を踏んだ（約2割の頻度で赤。実装は正しくテスト側の競合）。**エラー文言の出現だけを待って次の操作へ進まない**——押せる状態に戻るまで待つ
  - **レビューは2巡で十分だった**。2巡目の新規指摘は「修正で入り込んだもの」ではなく「元からあった対の欠落」に集中し、**修正ループ自体の欠陥生成率はほぼゼロ**だった。一方で**ブランチをまたいだ統合の罠**（同名別契約の `at()`、既定の食い違う `task()`）は per-branch レビューでは原理的に見えず、マージ後に見るしかない（T-43）
  - **presentation を厚くすると下層の穴が見える**。完了時のレビューで `proxy.ts` の 0%（T-59）と、統合テストが「トランザクションを張る分岐だけを厚く検証し、日常操作で最も通る単純 UPDATE 経路に実DBが一度も当たっていない」こと（T-60）が判明した
- **共有ファイルは誰も触らない**（並行時の規律）: `src/app/_testing/setup.ts`・`vitest.config.ts`・`package.json`・各台帳と `log_*`。jsdom に無い API はテストファイル内で局所的に補い、全体で要るならヘッドがマージ後に集約する。依存追加（`@testing-library/user-event` 等）も禁止——6ブランチが同じ lock を書き換えて必ず衝突するため、操作は既存の `fireEvent` に揃える
- 範囲外: async Server Component は E2E（T-85。起票当時は T-03 が E2E を抱えていた）。**幾何依存はブラウザテスト（T-03）へ**——ただし線引きは**ファイル単位ではなく「DOM の実測値を読む箇所」単位**。`use-flip-up.ts` の判定式は純粋算術なので `popover-scroll.ts` と同じ流儀で切り出せばユニット段に落とせる、と第1バッチ時点で見立てていた——**第2バッチ⑥で実際に `_lib/flip-up.ts` の `shouldFlipUp` として切り出し済み**で、00_共通 §2.1 の3条件と「上下どちらも狭い同点の場合は下のまま」という境界も `flip-up.test.ts` がユニット段で固定した。フックに残るのは実測値そのものを読む3行（`offsetHeight`・`getBoundingClientRect()`・`innerHeight`）だけで、そこが段3の担当
- 検証の作法: 「テストが通る」だけでなく**実装を一時的に壊して赤くなることを確かめる**。第1バッチでは `Toast` の依存配列を `[]` から `[variant, onClose]` に変える変異で該当テストが落ちることを実測した（コメントと eslint-disable だけが防波堤だった契約をテストで固定できている証拠）。第2バッチでは6ワーカーとも全ての主要テストで変異検証を行い、**ヘッドの指示（FB-42 の住所・重複判定）を2件否定した**
- 関連: T-33（`.tsx` を計測対象に含める決着。本 T の計器になった）/ T-03（FB-42 の再現条件の特定を宿題として引き継いだ）/ T-37（実機確認の仕組み化。ブラウザテストと機構が重なる）
