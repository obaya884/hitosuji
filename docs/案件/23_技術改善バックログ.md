# 技術改善バックログ

- 作成日: 2026-07-22（改称: 2026-07-23、旧「技術改善計画」）
- 目的: 技術負債の返済・リファクタリング・開発ツール整備など、**プロダクト挙動を変えない技術活動**を、要求（F-XXX / N-XX）とは別軸で管理・計画する
- 関連文書: [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md)（設計・テスト戦略）/ [要件バックログ](./22_要件バックログ.md)（プロダクト軸の台帳）

## この文書の守備範囲

プロダクトの**挙動**に関わる変更は扱わない（それは要求 → 要件 → 画面/データモデルの軸で、着手前に該当文書を先に直す）。ここに書くのは、ユーザーから見た挙動を変えずに**開発の健全性を上げる活動**——依存追随・CI/ツール整備・内部設計の改善・テスト補強・調査など。

- 採番は **T-XX**（Technical）。1系列で通し、種別で区別する
- 種別: `負債返済` / `改善` / `ツール整備` / `調査`
- 優先度: `高` / `中` / `様子見`
- 状態: `未着手` / `進行中` / `完了` / `保留`
- **完了になったエントリは、状態更新と同じコミットで[closed_23_技術改善バックログ.md](./closed_23_技術改善バックログ.md)へ移す**（本書には未着手・進行中・保留だけを残す）
- 判断の経緯は本文に書かず、[log_23_技術改善バックログ.md](./log_23_技術改善バックログ.md) へ日付順に積む（CLAUDE.md「書き方の規約」に従う）
- リファクタ観点の棚卸しは `refactor-scout`（読み取り専用サブエージェント）が T-XX 案のドラフトを起票し、オーナー確認のうえメインセッションが本書へ記入する（CLAUDE.md「起動の契機」参照）

---

# これからの計画

## T-02 eslint 10 への追随（保留）

- 種別: 負債返済 / 優先度: 様子見 / 状態: 保留
- 背景: `eslint-config-next` 同梱の `eslint-plugin-react` が ESLint 10 で削除された context API（`getFilename`）を使っており、eslint 10 では lint がクラッシュして起動しない（依存方向を守らせる `no-restricted-imports` すら実行不可）。Dependabot PR #4 はこの理由でクローズした
- 対応方針: `eslint-config-next` が ESLint 10 対応版を出したら再挑戦する。それまでは 9 系を維持。次に Dependabot が eslint の新版 PR を出したときに状況を再確認する

## T-03 E2E テスト（Playwright）導入の検討

- 種別: 改善（テスト補強）/ 優先度: 様子見 / 状態: 未着手
- 背景: テストピラミッドの頂点。現状は手動の実運用確認で代替している（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8）。プロダクト要求（F-XXX / N-XX）には乗らない技術活動のため、実装計画 Phase 3 に置かれたまま行き場がなかったのを本書へ移した
- 対応方針: 必要性を運用で見極めてから、導入するなら Playwright。手動確認では追えない回帰が出てきた等を着手条件にする

## T-05 esbuild dev-serve アラート（dismiss 済み・追跡）

- 種別: 負債返済 / 優先度: 様子見 / 状態: 保留（dismiss 済み）
- 背景: Dependabot セキュリティアラート（medium）。`drizzle-kit@0.31.10（最新）→ @esbuild-kit/esm-loader → esbuild@0.18` 経由。脆弱箇所は esbuild の dev serve が任意オリジンからの要求に応じる件だが、drizzle-kit は @esbuild-kit を設定ファイル変換にしか使わず dev serve を起動しない**到達不能パス**。最新 drizzle-kit でも @esbuild-kit を同梱しており上流にクリーンな修正が無い
- 対応: 理由 `not_used` でアラートを dismiss（`overrides` で esbuild を 0.25+ に上げると @esbuild-kit（0.18 前提）が壊れ、かつ CI で検出できないため見送り）
- 根治条件: drizzle-kit が @esbuild-kit を外す（または esbuild を上げる）版を出したとき。次に esbuild 系のアラート／更新が来たら再確認する

## T-25 データバックアップの自動化（暗号化確定・保存先2案は保留）

- 種別: ツール整備（当初は調査）/ 優先度: 中 / 状態: 未着手（暗号化方式は確定・保存先は保留）
- 背景: N-06 のバックアップ取得は手動（週次目安）で、取り忘れた期間はどこにも退避が無く Neon の PITR/履歴保持の窓を超えると復元不能になる。GitHub Actions cron で定期取得を自動化し取得漏れをなくしたい（README・要件定義書 §6 とも「運用の負担を見て判断」に留めていた）
- 方式: 難所は「取得の実行」ではなく **public リポジトリで実データを含むダンプをどこへどう置くか**。これを (1) 暗号化 (2) 保存先 に分解し、**`age` 非対称暗号（公開鍵はコミット可・復号鍵は CI に載せない）で秘匿を保存先に依存させない**設計に**確定**。保存先はクラウドオブジェクトストレージを前提に **案B（Cloudflare R2 / Backblaze B2・静的キー）と案C（AWS S3 / GCP GCS・OIDC/WIF で静的キーレス）を併記し、採用は保留**（両クラウド未保有につき、課金アカウントを作る段で B/C を確定）。差は CI 認証とセットアップの重さで機能・コスト差は極小データではほぼ無い。**詳細は [33_データバックアップ自動化検討.md](../検討/33_データバックアップ自動化検討.md) が正**
- 実装: 未着手（本 T は方式決定まで）。着手する場合は age 鍵ペア・`BACKUP_DATABASE_URL`（unpooled）シークレット・保存先（案B＝バケット＋静的キー／案C＝バケット＋OIDC/WIF 設定）・lifecycle rule・`.github/workflows/db-backup.yml`（`schedule`＋`workflow_dispatch`／`pg_dump`→age→upload／失敗通知）を用意する（検討ドキュメント §5・§7）。定期実行のため**承認ゲートは付けず**「非 PR イベント限定＋最小権限＋失敗通知」で担保する
- 着手条件: 手動運用の取得漏れが実害になり始めた／週次取得の手間を恒常的に感じるとき
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-22（スキーマ更新のリモート化・同型の作業場）/ [README](../../README.md)「バックアップ（N-06）」/ [データモデル定義書](../仕様/14_データモデル定義書.md) §6

## T-37 実装後のブラウザ実機確認を仕組み化する（Playwright・テストではない）

- 種別: ツール整備 / 優先度: 中 / 状態: 未着手
- 背景: presentation は自動テストの対象外（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8）のため、UI の挙動はオーナーの動作確認だけが網になっている。2026-07-25 に FB-51 で **Playwright による実機測定が2回とも決定的だった**——①「画面下部でポップオーバーが上向きに反転しない」の原因は、コードを読んで立てた仮説3つ（アンカーが想定外の祖先／実は反転不要／sticky ヘッダ未考慮）が**すべて外れ**で、測って初めて「アクティブ候補の `scrollIntoView({block:"nearest"})` が祖先＝ドキュメントまでスクロールし、反転判定が見る幾何情報を書き換えていた」と分かった ②修正後も測定で「ページのスクロール量0・パネル内だけが動く・上向きを維持」を確認できた。手作業で都度組むのは惜しい
- 方式の推し: **サブエージェント**（CLAUDE.md のレビュアー表に1行増える形。`verifier` が機械的検証、`spec-reviewer` が docs 整合、という既存の責務の切り方と揃う）。スキルにするとメインセッションが手順を踏む形になり、長いブラウザ操作でコンテキストを消費する
- 先に決めるべき論点:
  - **判定対象の線引き**: 幾何・スクロール量・クラスの有無・キーナビの追従・コンソールエラーの不在など**機械的に真偽が決まるもの**に限る。「色が見づらい」「2行になって気になる」は知覚の判断で代替できない（FB-52 の色・FB-51 のラベル折り返しはいずれもオーナーの実機知覚で決まった）。ここを曖昧にすると「確認したことになっている」状態を作る
  - **実データを壊さない担保**: 破壊的操作（打刻 `B`/`F`・削除 `D`・複製 `Y`）を禁じるか、専用の開発DBに向けるか。後者は **T-34 の「どのDBを向けるか」と同じ論点**に合流する
  - **T-03 との重複整理**: [T-03](#t-03-e2e-テストplaywright導入の検討)（E2E テスト導入の検討）と機構が大きく重なる。本 T は「オンデマンドの確認パス」、T-03 は「CI で回る自動テスト」で目的が違うが、**片方を作ればもう片方はその上に乗る**。別々に進めると二重投資になるため、着手時にどちらを土台にするかを決める
  - 起動の契機: 機能完成時（`spec-reviewer` / `test-reviewer` と並列）か、オーナー動作確認の直前か
- 前提: dev サーバと開発DBが必要。T-34（完了）で worktree でも `npm run dev` が本体（:3000）とは別のポートで立つようになったため、**ワーカーからも実行しうる**（ただし開発DBは本体と共有なので、下の「実データを壊さない担保」が未決のうちはヘッドに限るのが安全。worktree 同士のポートは重複しうる＝[T-40](#t-40-worktree-の-dev-サーバポートが並行時に衝突する)）
- 着手条件: UI の回帰を実機確認で取りこぼした事例が出たとき。または T-03 を検討するとき（機構が重なるため）
- 関連: T-03 / [完了記録](./closed_23_技術改善バックログ.md) T-34（dev サーバの並列化・DBは共有） / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 / [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §2・§3.2

## T-39 コンポーネントテスト（jsdom）の導入

- 種別: ツール整備 / 優先度: 中 / 状態: 進行中
- 背景: presentation を「自動テスト対象外」と一括りにしていたが、実測すると `.tsx` の大半（22ファイル・約3,600行）は Client Component で、jsdom 上のコンポーネントテストで普通に測れる。さらに **hooks は `.ts` なので既に計測対象の内側にいて 0%** という状態だった（`use-daily-shortcuts.ts` 188行・`use-dismiss.ts`・`use-now.ts`・`keyboard.ts`）。段の呼び名と境界は[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 が正
- 対応方針: jsdom + `@testing-library/react` を入れ、vitest の project を `unit` / `component` / `integration` の3本にする。テストは `*.test.tsx`（jsdom を要するものは対象が `.ts` でも `.tsx` に置く）
- 済（第1バッチ / 2026-07-26・PR #43 / b374c41）: 環境（jsdom＋`@testing-library/react`、project を unit / component / integration の3本、`test:component`）と、`use-now`・`use-dismiss`・`use-flip-up`（幾何に依存しないリセット分岐のみ）・`keyboard`・`TaskProgress`・`Toast` のテスト47件。カバレッジは `src/app` 8.96% → 21.46%（Stmts）、全体 72.59% → 75.76%
- 第1バッチで判明: **`coverage.include` は「未実行ファイルを走査する範囲」しか制御せず、テストが読み込んだファイルは include に関係なく計上される**。そのため `.tsx` を2つテストした時点で「その2ファイルだけが 100% で分母に入り、未テストの33ファイルは見えない」非対称集計になった
- 計測対象は `.tsx` を含む（[完了記録](./closed_23_技術改善バックログ.md) T-33 で決着）。**各バッチの成果と未カバー箇所が PR コメントに出る**ので、次に何をテストすべきかは変更箇所カバレッジを見て決められる
- Server Action を直 import するコンポーネント（`daily-board.tsx`・masters の各テーブル）は**素の jsdom では描画できない**（`actions.ts` の先が `pg.Pool` と `revalidatePath` に届く）。差し替えの可否と目的は[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8「偽物を置いてよい境界」が正
- **第2バッチ（進行中 / 2026-07-26・6並行）**: 残る段2の対象を**ディレクトリ境界で6つに分割**し、worktree ごとのワーカーで並行実装する（体制は[git運用と並行開発体制定義書](../仕様/16_git運用と並行開発体制定義書.md) §2〜§5）。分割は新規テストファイルしか作らないため衝突面が実質ゼロ——例外は `use-flip-up.ts` の判定式抽出（唯一の本番コード変更）で、担当を⑥に限定した

  | # | ブランチ | 対象（Stmts） |
  |---|---|---|
  | ① | `t-39-daily-shortcuts` | `use-daily-shortcuts.ts`（93）。画面定義書01 §6 は20キー超＋除外規則を持つ最大の条項表で、jsdom 段の本命。`U` の切り分け順〈O-13〉／`editing !== null` で全ショートカット無効〈00_共通 §3〉／`pickerOpen` 中に行操作キーを背後へ流さない／`Shift+J/K` と `J/K` の判別 |
  | ② | `t-39-daily-list` | `daily-list.tsx`（82）・`select-popover.tsx`（50）・`routinize-popover.tsx`（43）・`row-menu.tsx`（14）・`daily-summary.tsx`（7）・`shortcut-help.tsx`（6）・`stale-running-banner.tsx`（1）。インライン編集の残り（00_共通 §2.3 の blur 確定・変更なしは送信しない・保存中は同行の他セル編集不可・失敗時は編集状態を維持。`keyboard.ts` はキー判定だけを担う） |
  | ③ | `t-39-daily-board` | `daily-board.tsx`（198）。**N-01 の楽観的更新とロールバック**・undo キュー・ショートカットとの結線 |
  | ④ | `t-39-masters` | `masters/**`（216）＝3テーブル（modes 76・sections 63・projects 47）＋`use-master-action`・`delete-master-button`・`archived-master-section`・`master-tabs`・`_lib/action-result` |
  | ⑤ | `t-39-routines-review` | `routines-table.tsx`（44）・`review-board.tsx`（39）・`routine-form.tsx`（35） |
  | ⑥ | `t-39-shared-components` | `_components/date-picker.tsx`（50）・`date-nav.tsx`（9）・`icons.tsx`（8）・`global-nav.tsx`（6）・`nav-tabs.tsx`（4）・`unset-mark.tsx`（1）＋`_lib/use-flip-up.ts` から純関数 `shouldFlipUp` を抽出（フックの外形シグネチャは変えない） |

  段2で到達しうるのは計 906 文で、`src/app` の分母 1295 文のうち残り 276 文は段外（`actions.ts` 4本＝216／`page.tsx`・`layout.tsx`・`icon.tsx`＝42／`proxy.ts`＝18）
- **共有ファイルは誰も触らない**（並行時の規律）: `src/app/_testing/setup.ts`・`vitest.config.ts`・`package.json`・各台帳と `log_*`。jsdom に無い API はテストファイル内で局所的に補い、全体で要るならヘッドがマージ後に集約する。依存追加（`@testing-library/user-event` 等）も禁止——6ブランチが同じ lock を書き換えて必ず衝突するため、操作は既存の `fireEvent` に揃える
- 範囲外: async Server Component は E2E（T-03）。**幾何依存はブラウザテストへ**——ただし線引きは**ファイル単位ではなく「DOM の実測値を読む箇所」単位**。`use-flip-up.ts` の判定式（`panel.offsetHeight > spaceBelow && spaceAbove > spaceBelow`）は純粋算術なので `popover-scroll.ts` と同じ流儀で切り出せばユニット段に落とせる（00_共通 §2.1 の3条件、特に**上下どちらも狭い同点の場合は下のまま**という境界が現在どの段でも未検証）。この抽出は本体コードの変更になるため次バッチ以降に分ける
- 検証の作法: 「テストが通る」だけでなく**実装を一時的に壊して赤くなることを確かめる**。第1バッチでは `Toast` の依存配列を `[]` から `[variant, onClose]` に変える変異で該当テストが落ちることを実測した（コメントと eslint-disable だけが防波堤だった契約をテストで固定できている証拠）
- 関連: T-33（`.tsx` を計測対象に含めるか。本 T の後に実数で判断する）/ T-03 / T-37（実機確認の仕組み化。ブラウザテストと機構が重なる）

## T-40 worktree の dev サーバポートが並行時に衝突する

- 種別: ツール整備 / 優先度: 中 / 状態: 未着手
- 背景: `scripts/wt-new.sh` は dev ポートを **worktree 作成時点で 3001 から空きを探して** `.env.worktree` の `DEV_PORT` に焼き込む。dev サーバを立てずに worktree を続けて作ると全部が同じ 3001 になり、後から2つで `npm run dev` すると衝突する。2026-07-26 の T-39 第2バッチで worktree を6つ作り、**6つすべてが 3001** になって顕在化した——T-34 が言う「固有ポート」は、作成時点で他の dev サーバが動いていた場合にだけ固有になる
- 実害の程度: ワーカーは lint / build / test で完結し dev サーバを使わないため、並行実装そのものは妨げない。効くのは**オーナーが複数ブランチの画面を同時に確認したいとき**（片方を止めれば回避できる）
- 対応方針の候補（着手時に決める）:
  - **ブランチ名のハッシュから決める**（例 `3001 + hash % 90`）— 作成順に依存せず冪等で、`.env.worktree` へ焼き込む今の形をそのまま使える。衝突確率は残るが同時に扱う worktree が数個なら実用上足りる
  - **起動時に探す**（`scripts/dev.sh` が `DEV_PORT` を起点に空きを探す）— 実際に衝突しない保証は得られるが、表示ポートが起動のたびに変わり `wt:new` が案内した URL と食い違う
  - 併用（作成時はハッシュで焼き、起動時に埋まっていたらずらす）が素直かもしれない
- 着手条件: 2つ以上の worktree で dev サーバを同時に立てたくなったとき
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-34（dev サーバの並列化） / T-37（ワーカーからの実機確認が絡む） / [定義書16](../仕様/16_git運用と並行開発体制定義書.md) §2

## T-41 コード内コメントの仕様参照先が旧位置を指している

- 種別: 保守性 / 優先度: 低 / 状態: 未着手
- 背景: 2026-07-21 の書き方の再整理（CLAUDE.md「書き方の規約」）で**詳細を下位文書1か所へ寄せ、上位文書は1〜2行の要約＋参照に留める**方針にしたが、**コード内のコメントが移動前の位置を指したまま**の箇所がある。判明分は `src/app/_lib/use-flip-up.ts` の JSDoc と `src/app/_components/date-picker.tsx` で、いずれも反転規則の出典を「画面定義書01 §7」と書くが、01 §7 は現在「表示位置と閉じ方は共通 §2.1 が正」と委譲する1行だけで、条項の実体は[共通](../仕様/13_画面定義書/00_共通.md) §2.1 にある
- 実害: 参照をたどった人が「§7 には何も書いていない」と迷う。委譲の1行があるので最終的には辿り着けるため実害は小さいが、**同じ再整理を通った箇所は他にもある可能性が高い**（`docs/仕様/13_画面定義書/01〜04` から 00_共通 へ寄せた条項＝ポップオーバー・インライン編集・トースト・未設定表記）
- 対応方針: `src/` 内の「画面定義書」「§」を含むコメントを機械的に洗い出し、参照先が委譲スタブになっているものを実体側へ向け直す。**コメントの書き換えのみで挙動は変わらない**
- 着手条件: 同種の迷いが再発したとき、または該当ファイルを別件で触るついで
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-39 第2バッチ⑥（発見の契機） / CLAUDE.md「書き方の規約」

## T-42 Server Action のエラーメッセージ辞書が型で閉じていない

- 種別: 型安全 / 優先度: 中 / 状態: 未着手
- 背景: `src/app/routines/actions.ts` の `MESSAGES[result.error]` は `Record<string, string>` へのインデックスアクセスのため、**未知のエラーコードが来ると `undefined` を返すのに型検査は通る**。`ActionResult.message: string` の契約を破りうる（画面には空のエラー帯が出る）。domain がエラーコードを増やしたとき、辞書へ追加し忘れても**コンパイルが通ってしまう**のが本質的な問題
- 対応方針: 辞書の型を `Record<string, string>` から**エラーコードの直和型**（例 `Record<RoutineError, string>`）へ絞る。追加漏れがコンパイルエラーになり、`undefined` の経路そのものが消える。**挙動は変わらない**（現状 `undefined` に到達する既知の経路は無い）
- 範囲: `routines/actions.ts` で発見したが、`(daily)/actions.ts`・`masters/{modes,projects,sections}/actions.ts` も同型の可能性がある。着手時に4本まとめて点検する
- 着手条件: domain のエラーコードを増やすとき（そのとき追加漏れが実害になる）、または上記いずれかの `actions.ts` を別件で触るついで
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-39 第2バッチ⑤（発見の契機） / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4（業務ルール由来の検証は domain）

---

# 完了記録

完了した T-XX は[closed_23_技術改善バックログ.md](./closed_23_技術改善バックログ.md)に保管する。

---

# 決定ログ

技術活動（T-XX）に関する判断は[log_23_技術改善バックログ.md](./log_23_技術改善バックログ.md)に日付順で積む。
