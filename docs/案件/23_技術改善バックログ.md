# 技術改善バックログ

- 作成日: 2026-07-22（改称: 2026-07-23、旧「技術改善計画」）
- 目的: 技術負債の返済・リファクタリング・開発ツール整備など、**プロダクト挙動を変えない技術活動**を、要求（F-XXX / N-XX）とは別軸で管理・計画する
- 関連文書: [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md)（設計）/ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md)（テスト）/ [要件バックログ](./22_要件バックログ.md)（プロダクト軸の台帳）

## この文書の守備範囲

プロダクトの**挙動**に関わる変更は扱わない（それは要求 → 要件 → 画面/データモデルの軸で、着手前に該当文書を先に直す）。ここに書くのは、ユーザーから見た挙動を変えずに**開発の健全性を上げる活動**——依存追随・CI/ツール整備・内部設計の改善・テスト補強・調査など。

- 採番は **T-XX**（Technical）。1系列で通し、種別で区別する
- 種別: **何に触るか＝着手時に何が網になるか**で分ける（下表）。「負債返済」「改善」のような**本書の全件に当てはまる語は使わない**（種別として情報量がない）
- 優先度: `高` / `中` / `低` / `様子見`
- 状態: `未着手` / `進行中` / `完了` / `保留`
- **1エントリ ＝ [一覧](#一覧)の1行 ＋ [詳細](#詳細)の1節**（台帳21 と同じ書式）。一覧は索引に徹し `ID / タイトル / 種別 / 優先度 / 状態 / 詳細` の6列を持つ
  - **見出しは `### T-XX` の ID のみ**。タイトルを重ねると改題のたびにアンカーが壊れ、一覧との二重管理にもなる
  - **セル内に `|` を書かない**（`ledger:move` / `docs:check` が列を数えられなくなる）。型を示すときは「未指定・`null`・空配列」のように語で書く
  - **種別・優先度・状態を詳細節に書かない**。一覧が正（二重に持つと必ず食い違う）
  - 詳細節の項目は `背景`（必須）に続けて `対応方針` / `着手条件` / `優先度の根拠` / `関連` を必要なだけ。エントリ固有の項目（`方式` `実害` `範囲` など）を足してよいが、上の5語を優先して使う
  - 詳細節の並び順は一覧と同じにする
- **完了になったエントリは、状態更新と同じコミットで[closed_23_技術改善バックログ.md](./closed_23_技術改善バックログ.md)へ移す**（本書には未着手・進行中・保留だけを残す）。**一覧の行と詳細節はセットで移す** — `npm run ledger:move -- T-XX` が両方を移す
- 判断の経緯は本文に書かず、[log_23_技術改善バックログ.md](./log_23_技術改善バックログ.md) へ日付順に積む（CLAUDE.md「書き方の規約」に従う）
- リファクタ観点の棚卸しは `refactor-scout`（読み取り専用サブエージェント）が T-XX 案のドラフトを起票し、オーナー確認のうえメインセッションが本書へ記入する（CLAUDE.md「起動の契機」参照）

### 種別

| 種別 | 何に触るか | 着手時に何が網になるか |
|---|---|---|
| 内部設計 | `src/` の構造（挙動不変） | 既存テスト。**変異させて振る舞い保存を確かめる** |
| 型安全 | 型定義（挙動不変） | コンパイラ。追加漏れがビルドエラーになる形へ寄せるのが目的 |
| テスト | テストコードのみ（本番コード不変。補強も整理も含む） | 本番コードに触れないので回帰の心配がない |
| ツール整備 | `scripts` / CI / 開発環境 | **自動テストの網が無い**。実行して確かめる |
| 依存追随 | 依存パッケージの更新・脆弱性対応 | 上流待ちが多く自分では動かせない。**再確認の契機を必ず書く** |
| 調査 | 何も変えない。判断を記録する | 成果物は決定そのもの |

## 一覧

| ID | タイトル | 種別 | 優先度 | 状態 | 詳細 |
|---|---|---|---|---|---|
| T-02 | eslint 10 への追随 | 依存追随 | 様子見 | 保留 | [詳細](#t-02) |
| T-05 | esbuild dev-serve アラートの追跡 | 依存追随 | 様子見 | 保留（dismiss 済み） | [詳細](#t-05) |
| T-25 | データバックアップの自動化 | ツール整備 | 低 | 未着手（暗号化方式は確定・保存先は保留） | [詳細](#t-25) |
| T-54 | マスタ3集約が repo / usecase / actions の3層でも三重化している（当面は寄せない決定の記録） | 調査 | 様子見 | 未着手 | [詳細](#t-54) |
| T-65 | 統合テストの同時実行が互いの DB を壊す（偽の赤） | ツール整備 | 中 | 未着手 | [詳細](#t-65) |
| T-68 | 網羅的な変異テストの道具（Stryker）を評価する | ツール整備 | 様子見 | 未着手 | [詳細](#t-68) |
| T-85 | E2E テストの導入（async Server Component の検証手段が無い） | テスト | 様子見 | 未着手 | [詳細](#t-85) |
| T-88 | `dayStartMinutes` の board→子 の配線がテストで無検証（前提が変わったため穴の有無は要再確認） | テスト | 低 | 未着手 | [詳細](#t-88) |
| T-109 | `TaskRow` と `DailyList` の props が25個・23個に達している（当面は畳まない判断の記録） | 調査 | 様子見 | 未着手 | [詳細](#t-109) |
| T-111 | React の警告がテストを緑のまま通すので、`click` の `await` 落ちを機械検出できない | ツール整備 | 中 | 未着手 | [詳細](#t-111) |
| T-112 | Server Action 40本にテストの段が無く、形の規約だけが型の網を支えている | テスト | 中 | 未着手 | [詳細](#t-112) |
| T-113 | テストファイルの `§` 参照が T-102 の規約の外に残っている | テスト | 低 | 未着手 | [詳細](#t-113) |
| T-114 | 本質的に複雑な仕様（楽観更新・打刻）の定義を、オーナーの理解と実装が揃う形に整理する | 調査 | 中 | 未着手 | [詳細](#t-114) |
| T-118 | CI の `verify` が 2分台から 3分台へ伸びた（全テストを2周している） | ツール整備 | 中 | 未着手 | [詳細](#t-118) |
| T-120 | 文字サイズだけがトークンの規律の外にあり、3段の外のサイズを書けてしまう | 内部設計 | 中 | 未着手 | [詳細](#t-120) |
| T-124 | 画面定義書がエラー文言そのものを書いており、辞書と二重管理になっている箇所が残る | 調査 | 低 | 未着手 | [詳細](#t-124) |
| T-125 | `isToday` が prop なのに board 内で導出でき、テストで矛盾した日付の組を作れてしまう | 内部設計 | 低 | 未着手 | [詳細](#t-125) |
| T-126 | PR コメントが変更行数を分けないので、docs の増分に埋もれて `src` の変更量が読めない | ツール整備 | 中 | 未着手 | [詳細](#t-126) |
| T-127 | デイリーの行フォーカスの送り先が操作ごとに散っており、条項の無い操作が残る | 調査 | 中 | 未着手 | [詳細](#t-127) |
| T-129 | pg v9 で `sslmode` の解釈が変わり、接続文字列を変えないまま TLS 検証が弱くなる | 依存追随 | 中 | 未着手 | [詳細](#t-129) |
| T-130 | `pg.Pool` の `idleTimeoutMillis` が既定10秒で、実行環境が温かくても DB 接続を張り直している | 内部設計 | 低 | 未着手 | [詳細](#t-130) |
| T-134 | ホバー・無効のクラスを直書きできる余地が残り、条項（00_共通 §2.5）から外れても機械で気づけない | ツール整備 | 低 | 未着手 | [詳細](#t-134) |
| T-136 | 現在地の探索が「現在セクション ＋ 表示順」を2つの位置引数で受け取っている | 内部設計 | 様子見 | 未着手 | [詳細](#t-136) |
| T-138 | 実測でしか固定できない条項が3つ、ブラウザ段の外に残っている | テスト | 中 | 未着手 | [詳細](#t-138) |
| T-139 | `setSelectedId` が `TaskId` を prop の境界で素の `number` へ広げ直している | 型安全 | 低 | 未着手 | [詳細](#t-139) |

## 詳細

### T-02

- 背景: `eslint-config-next` 同梱の `eslint-plugin-react` が ESLint 10 で削除された context API（`getFilename`）を使っており、eslint 10 では lint がクラッシュして起動しない（依存方向を守らせる `no-restricted-imports` すら実行不可）。Dependabot PR #4 はこの理由でクローズした
- 対応方針: `eslint-config-next` が ESLint 10 対応版を出したら再挑戦する。それまでは 9 系を維持。次に Dependabot が eslint の新版 PR を出したときに状況を再確認する
- **再確認（2026-07-27・eslint 10.8.0 の PR #73）**: 状況は変わらずクローズした。CI の `npm run lint` が同じ `getFilename is not a function` で落ちる。上流も未対応で、`eslint-plugin-react` は最新 7.37.5 でも peer が `^9.7` 止まり、`eslint-config-next` も最新 16.2.12 が `eslint-plugin-react@^7.37.0` に依存している
- **再確認（2026-08-24・eslint 10.8.1 の PR #136）**: **上流は1か月動いていない**。`eslint-plugin-react` は 7.37.5 のまま（peer は依然 `^9.7`）、`eslint-config-next` は 16.3.2 まで進んだが `eslint-plugin-react@^7.37.0` への依存は据え置き。CI も同じ `getFilename is not a function` で赤のためクローズした。判断が3回とも同じなので、次回は**まず `eslint-plugin-react` の peer に `^10` が入ったかだけ**を見れば足りる

### T-05

- 背景: Dependabot セキュリティアラート（medium）。`drizzle-kit@0.31.10（最新）→ @esbuild-kit/esm-loader → esbuild@0.18` 経由。脆弱箇所は esbuild の dev serve が任意オリジンからの要求に応じる件だが、drizzle-kit は @esbuild-kit を設定ファイル変換にしか使わず dev serve を起動しない**到達不能パス**。最新 drizzle-kit でも @esbuild-kit を同梱しており上流にクリーンな修正が無い
- 対応: 理由 `not_used` でアラートを dismiss（`overrides` で esbuild を 0.25+ に上げると @esbuild-kit（0.18 前提）が壊れ、かつ CI で検出できないため見送り）
- 根治条件: drizzle-kit が @esbuild-kit を外す（または esbuild を上げる）版を出したとき。次に esbuild 系のアラート／更新が来たら再確認する

### T-25

- 背景: N-06 のバックアップ取得は手動（週次目安）で、取り忘れた期間はどこにも退避が無く Neon の PITR/履歴保持の窓を超えると復元不能になる。GitHub Actions cron で定期取得を自動化し取得漏れをなくしたい（README・要件定義書 §6 とも「運用の負担を見て判断」に留めていた）
- 方式: 難所は「取得の実行」ではなく **public リポジトリで実データを含むダンプをどこへどう置くか**。これを (1) 暗号化 (2) 保存先 に分解し、**`age` 非対称暗号（公開鍵はコミット可・復号鍵は CI に載せない）で秘匿を保存先に依存させない**設計に**確定**。保存先はクラウドオブジェクトストレージを前提に **案B（Cloudflare R2 / Backblaze B2・静的キー）と案C（AWS S3 / GCP GCS・OIDC/WIF で静的キーレス）を併記し、採用は保留**（両クラウド未保有につき、課金アカウントを作る段で B/C を確定）。差は CI 認証とセットアップの重さで機能・コスト差は極小データではほぼ無い。**詳細は [33_データバックアップ自動化検討.md](../検討/33_データバックアップ自動化検討.md) が正**
- 実装: 未着手（本 T は方式決定まで）。着手する場合は age 鍵ペア・`BACKUP_DATABASE_URL`（unpooled）シークレット・保存先（案B＝バケット＋静的キー／案C＝バケット＋OIDC/WIF 設定）・lifecycle rule・`.github/workflows/db-backup.yml`（`schedule`＋`workflow_dispatch`／`pg_dump`→age→upload／失敗通知）を用意する（検討ドキュメント §5・§7）。定期実行のため**承認ゲートは付けず**「非 PR イベント限定＋最小権限＋失敗通知」で担保する
- **前提の変化（2026-08-13）**: 起票時の背景「Neon の履歴保持の窓（無料枠は短い）を超えると復元不能」は、**Neon を Launch プランへ移す判断（[34_初回応答の遅延とNeonプラン検討.md](../検討/34_初回応答の遅延とNeonプラン検討.md) §6）に伴い復元ウィンドウを 6時間から7日へ延ばせる**ため、取り忘れの猶予が大きく広がる。これを受けて優先度を 中 → 低 に下げた。**ただし本 T が不要になるわけではない**——7日の窓は Neon の中だけの話で、N-06② のロックイン回避（他の Postgres へそのまま戻せる控えを外に持つ）は満たさない。プラン変更が未実施の間は猶予も 6時間のままなので、**優先度を戻す条件は「Launch への移行を取りやめたとき」**
- 着手条件: 手動運用の取得漏れが実害になり始めた／週次取得の手間を恒常的に感じるとき
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-22（スキーマ更新のリモート化・同型の作業場）/ [README](../../README.md)「バックアップ（N-06）」/ [データモデル定義書](../仕様/14_データモデル定義書.md) §6 / [34_初回応答の遅延とNeonプラン検討.md](../検討/34_初回応答の遅延とNeonプラン検討.md)（復元ウィンドウが変わる側）

### T-54

- 背景: [T-44](./closed_23_技術改善バックログ.md#t-44) は presentation の三重化を扱うが、**同じ三重化が下の層にもそのまま続いている**。`drizzle-mode-repository.ts` と `drizzle-project-repository.ts` は識別子を機械的に置換すると **`toDomain` の1行（`color` の有無）以外が完全一致**（各69行）。`mode-usecases.ts` と `project-usecases.ts` も入力型と変数名以外は一致。`masters/{modes,projects,sections}/actions.ts` も同型。**`refactor-scout` と `code-quality-reviewer` が独立に同じ指摘をした**
- 方針: **現時点では寄せない**。テーブルもドメイン型も違うものを総称化すると Drizzle の型が効かなくなるか総称パラメータが増えて読みにくくなる（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §1「過剰抽象の禁止」）。ただし**「気づいていて見送った」ことを台帳に残す**——判断を残さないまま放置するのが一番まずい形
- 着手条件（再検討のトリガ）: ①**4つ目のマスタを足すとき** ②アーカイブ／物理削除の規則を変えるとき——どちらも同じ変更を3層×3集約に入れることになり、そこで初めて抽象化の元が取れる
- 優先度の根拠: 現在の重複は「読めば分かる定型」で事故を起こしていない。逆に先に抽象化すると、オーナー1人のアプリで層のための層を作ることになる
- 関連: T-44 / [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §1

### T-65

- 背景: 統合テストは全件が同一の `db-test`（:5433）を共有し、テストごとに TRUNCATE でリセットする（[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §5）。**1回の vitest 実行の中では `fileParallelism: false` で直列化しているが、別プロセスからの同時実行は何も防いでいない**
- 顕在化した経路: 2026-07-26 の T-43 第2段で、レビューエージェント4本とメインセッションが並行して `npm test` を走らせ、`drizzle-task-repository.int.test.ts` が `sort_order` の重複で7件落ちた。**単独で再実行すると全件緑**で、原因の切り分けに時間を取られた。サブエージェントを並列起動する運用（CLAUDE.md「起動の契機」）を常用するようになった以上、再発は避けられない
- 実害: 偽の赤。落ちたのが自分の変更のせいなのか同時実行のせいなのかが読めず、**「再実行したら通ったので大丈夫」という危うい判断**を誘発する
- 対応方針: 排他を機械的に効かせる。①`test:int` の実行前にロックを取る（既に走っていれば待つか明示的に落とす）②実行ごとに使い捨てのスキーマ／DB名を切る（worktree 分離＝T-06 と同じ発想をプロセス単位へ広げる）のどちらか。②なら待ち時間が消える代わりにマイグレーション適用のコストが毎回かかる
- 暫定の回避: レビューエージェントの定義に「統合テストが落ちたら単独で再実行して再現を確かめてから報告する」を入れた（2026-07-26）
- 着手条件: 同じ切り分けにもう一度時間を取られたとき、または統合テストを増やすとき
- 関連: [完了記録](./closed_23_技術改善バックログ.md) T-06（worktree ごとのテストDB分離・同じ問題のブランチ単位版） / [T-58](./closed_23_技術改善バックログ.md#t-58)（**同じ「別プロセスからの同時実行」がカバレッジ計測で出た件。対応方針①のロックを `scripts/test-coverage.sh` で実装済みなので、着手時はその形をそのまま広げられる**） / [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §5

### T-68

- 背景: もとは T-67（変異テストをメインセッションへ戻した件）の積み残しとして「**専用サブエージェントを置くか**」を抱えていたが、2026-08-01 に**置かないと決めた**——[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §10 で実施条件が「差分あたり1回」に定まり、対象ファイルだけを回せば出力は数行、長ければ実行だけ `verifier` に頼めるため、当時の動機（メインセッションのコンテキスト圧迫）が起動コスト（[T-66](./closed_23_技術改善バックログ.md#t-66)）に見合わない。当時の論点①（worktree 分離で事故を防ぐ）③（`verifier` と別に立てる意味）も同時に不要になった
- 残る論点: 当時の論点②「**どこを壊せば意味があるかごと委譲する**」＝網羅的な変異だけは別の価値。ただしこれはエージェントより **Stryker Mutator**（vitest ランナー対応）の領分で、決定論的に動くので T-67 のような戻し忘れの事故が構造的に起きない
- 対応方針: 導入するなら**対象を `src/domain` に絞る**（変異体ごとにテスト一式を回すため遅い）。CI には載せず、節目に手元で回す道具として評価する
- 着手条件: テスト補強の T-XX（[T-60](./closed_23_技術改善バックログ.md#t-60)・[T-61](./closed_23_技術改善バックログ.md#t-61)）で「どこに穴があるか」を人手で数えるのが辛くなったとき
- 優先度の根拠: [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §10 の1点確認で当面の網は張れている。網羅は「あると嬉しい」段階
- 関連: [完了記録](./closed_23_技術改善バックログ.md#t-87) T-87（実施条件を決めた案件。本 T の判定はその従属） / [完了記録](./closed_23_技術改善バックログ.md#t-67) T-67（経緯） / [T-66](./closed_23_技術改善バックログ.md#t-66)（サブエージェントの起動コスト） / [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §10

### T-85

- 背景: テストピラミッドの頂点。**async Server Component は Testing Library で描画できない**ため、`page.tsx` 系（13ファイル・約450行）の検証手段がここにしか無い（[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3）。現状は手動の実運用確認で代替している。もとは [T-03](./closed_23_技術改善バックログ.md#t-03)（当時の主題は E2E）が抱えていたが、2026-07-27 に T-03 をブラウザテスト段へ移した際に分けた
- 対応方針: 導入するなら Playwright。サーバ・DB を通すので、[完了記録](./closed_23_技術改善バックログ.md) **T-37 が作った確認用DB（`hitosuji_check`）と `npm run dev:check` の起動手順をそのまま土台にできる**（T-37 検討時に「T-03 と共有するのは環境だけ」と整理したのは、いま思えばこの E2E との関係だった）。CI で回すなら Postgres サービスコンテナとブラウザバイナリの用意が要る
- 着手条件: 手動確認では追えない回帰が出たとき。**先に入れるべき段3（[T-03](./closed_23_技術改善バックログ.md#t-03)）は 2026-08-29 に導入済み**——段3 は DB もサーバも要らず、幾何の回帰という当面の実害はそちらで取れるので、本 T は「サーバ・DB を通さないと捉えられない回帰」が出るまで動かない
- 優先度の根拠: 対象の `page.tsx` は大半が薄く（データを取ってコンポーネントへ渡すだけ）、壊れれば画面が出ないので手動確認で必ず気づく。段3 が守る幾何の不具合（気づきにくい）とは性質が違う
- 関連: [T-03](./closed_23_技術改善バックログ.md#t-03)（先に入れる段） / [完了記録](./closed_23_技術改善バックログ.md) T-37（環境の土台） / [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3・§8

### T-88

- 背景: `daily-board.tsx` は日界（F-116）から `dayStartMinutes` を導出して `DailySummary` とセクション見出しへ配るが、起票時（2026-07-28）は `daily-board.test.tsx` が持つ独自の `SECTIONS` フィクスチャに `isDayStart` を持つセクションが無く、`startMinutes(dayStartTimeOf(sections))` が常に 0 を返していた。子側テストの既定値も 0 なので、**board の `dayStartMinutes` を 0 にハードコードしても全テストが緑**になる
- **前提の変化（2026-08-02）**: [T-81](./closed_23_技術改善バックログ.md#t-81) で盤面のフィクスチャを共有の `(daily)/_testing/factories.ts` へ寄せた結果、`SECTIONS` は「朝 06:00・`isDayStart: true`」を持つようになり、**盤面テストの `dayStartMinutes` は 0 ではなく 360 を返す**。背景が書いた「常に 0」はもう成り立たない。**穴（配線を壊しても緑）が残っているかは着手時に再確認する**——日界を動かした結果を主張するテストが無ければ、値が 360 でもハードコードは検出できない
- 実害: F-116（日界を 0:00 以外にすると終了予定・セクション残り時間の起点が変わる）の配線が壊れても検出されない。導出関数そのもの（`dayStartTimeOf` / `offsetFromDayStart`）は domain 段で押さえてあるので、抜けているのは**導出と描画のあいだ**だけ
- 対応方針: board のフィクスチャに日界セクション（例: 朝 06:00 を `isDayStart`）を持つ組を用意し、それを渡したときにサマリの終了予定またはセクション見出しの残り時間が既定（日界 00:00）と変わることを board 段で1件見る。既定の `SECTIONS` を差し替えると既存テストの前提（見出し2つ・現在セクション＝午前）が動くので、**その1件だけが使う別フィクスチャ**にするのが安全
- 優先度の根拠: 同型の配線漏れ（`currentSectionId`）は FB-78 の対応で塞いだが、こちらは日界を既定の 00:00 から動かさない限り実害が出ない。オーナーが日界を設定した時点で優先度が上がる
- 関連: FB-78 対応（2026-07-28）の `test-reviewer` が「前回指摘と同型の残り」として検出（範囲外として起票）/ [T-61](./closed_23_技術改善バックログ.md#t-61)（コンポーネント段に残る穴。配線の未検証という点で同族）/ [T-82](./closed_23_技術改善バックログ.md#t-82)（テストフィクスチャのセクション定義）

### T-109

- 背景: デイリーの表は `DailyBoard`(8 props) → `DailyList`(23 props) → `TaskRow`(25 props) のバケツリレーになっており、`DailyList` の23のうち16は素通し（`src/app/(daily)/_components/daily-list.tsx:19-51,135-161`、`task-row.tsx:27-58`）。ハンドラだけで12本ある（`onRename` / `onEstimate` / `onComment` / `onPunch` / `onEditPunch` / `onAssign` / `onOperate` / `onToggleHighlight` / `onRoutinize` / `onSelect` / `onBeginEdit` / `onEndEdit`）。[T-53](./closed_23_技術改善バックログ.md#t-53) で `DailyListProps` を `TaskRowProps` から派生させたので**型の二重定義は既に無い**が、数そのものは残っている
- 方針: **現時点では畳まない**。Context やハンドラのオブジェクト化で数は減るが、[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §1「過剰抽象の禁止」に照らすと、オーナー1人のアプリで**「どの操作がどこから来るか」を props で追えなくする代償**が大きい。とくにコンポーネントテストは props を直接組み立てて描くので、Context を挟むと `TaskRow` 単体のテストにプロバイダの用意が要る（[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3 のコンポーネント段の作りやすさが落ちる）。**「気づいていて見送った」ことを台帳に残す**
- 着手条件（再検討のトリガ）: ①`TaskRow` に4段目の子が生まれ、リレーが3段を超えるとき ②同じハンドラ束を必要とする2つ目の表（レビューの実績ログなど）が編集操作を持つとき——どちらも「素通しのためだけの引数」が2倍になり、そこで初めて集約の元が取れる
- 優先度の根拠: 25 props は数として多いが、内訳は「表示に要る値」と「操作の入口」で、分岐も状態も持たない。T-53 で型の二重管理は解消済みなので、いま残っているのは読みづらさだけ
- 関連: [T-53](./closed_23_技術改善バックログ.md#t-53)（props 型の派生化）/ [T-54](#t-54)（同じ「見送りを記録する」形の調査エントリ）/ [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §1

### T-111

- 背景: [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §4 は Server Action を呼ぶクリックを `click` に通すと定めるが、**`click` の `await` 落ちは型でも lint でも捕まらない**（`click` の doc が「必ず `await` する」と名前で示しているだけ）。落とすと `act` のスコープが閉じないまま後続へ流れ、React が警告を出すだけでテストは緑になる。`src/app/_testing/setup.ts` は `console.error` を失敗として扱わないため、**取りこぼしを機械検出する手段が無い**
- 対応方針: `setup.ts` で `console.error`（少なくとも React の `act` 未包み・スコープ未クローズの警告）をテスト失敗にする。**既存テストが出している警告を先に洗う**必要があり、そこが本 T の実体（数と種類が読めないまま倒すと全段が赤くなる）。特定の警告だけを対象にするか全部を落とすかも着手時に決める
- 着手条件: 単独で回してよい。または `await` 落ちに起因するフレークが実際に出たとき
- 優先度の根拠: [T-62](./closed_23_技術改善バックログ.md#t-62) で実際にフレークを1本出しており、`click` の適用範囲が5ファイル＋共有ヘルパへ広がった（T-96）ぶん取りこぼしの機会も増えた。一方で現状の網（レビューと変異）でも見つかってはいる
- 関連: [T-96](./closed_23_技術改善バックログ.md#t-96)（「ついでに検討する」として抱えていた項目の切り出し元）/ [T-62](./closed_23_技術改善バックログ.md#t-62)（フレークの実績）/ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §4

### T-112

- 背景: `actions.ts` 5ファイル・40本はどの段からも実行されず**カバレッジ 0%** のまま。コンポーネント段は Server Action を偽物へ差し替える（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8）ので通らず、[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §6 は async Server Component の担保は書いているが**Server Action については何も書いていない**。[T-107](./closed_23_技術改善バックログ.md#t-107) が `TS2366` による `return` 落ちの検出を入れたが、これが効くのは**if/else 形と戻り値型の注釈を保つ限り**で、早期 return で書かれた41本目は tsc・lint・全テストが緑のまま穴に落ちる（3形とも実測済み。同 T の結果節）。いま規約（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4「Server Action の形」）だけが型の網を支えている
- 対応方針: **失敗経路だけを統合段（`*.int.test.ts`）から直接叩く**。T-107 で `revalidatePath` が成功枝の内側へ移り**失敗枝が副作用ゼロと確定した**ので、Next のリクエストコンテキスト無しに実DBから呼べる（成功枝は `revalidatePath` がワークストア不在で投げるため同じ手では測れない）。辞書ファミリごとに1本（TASK_EDIT / PUNCH / OPERATION / REORDER / MASTER / ROUTINE / ルーチン化＝7本程度）置くと、**条件反転・早期 return への回帰・辞書の取り違えが同時に落ちる**。あわせてテスト戦略定義書 §6 に Server Action の担保を明記する。成功枝を型に委ねる線引きも同時に書く
- 着手条件: 41本目の Server Action を足すとき、または `actions.ts` を別件でまとめて触るとき。**新しいアクションを足す前に入れる方が安い**（穴が開くのはそのときのため）
- 優先度の根拠: 現状の40本は正しく、規約も条項化済みなので即時の実害は無い。ただし守っているのが規約だけという状態は、[T-98](./closed_23_技術改善バックログ.md#t-98) が「静かにテストの網に穴を増やす」として片付けたのと同型。失敗が成功として返る事故は、楽観的更新が巻き戻らず「保存したのに次のリロードで消える」形で出るため気づきにくい
- 関連: [T-107](./closed_23_技術改善バックログ.md#t-107)（穴と型の網の出どころ）/ [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4「Server Action の形」・§8（偽物を置いてよい境界）/ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §6

### T-113

- 背景: [T-102](./closed_23_技術改善バックログ.md#t-102) は規約を[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §2 に置き、本番コード側の違反34箇所を塞いだが、**テストファイルを母集団から外した**（着手時の判断で、記録は T-102 の結果節）。残っているのは20ファイル・約150参照で、混在の度合いは本番コードより高い——`src/app/_lib/error-messages.test.ts` は **`§4.1` だけで3文書（画面定義書01 のルーチン化・03 の物理削除・00_共通 の通信失敗）を指す**、`src/app/(daily)/_components/task-row.test.tsx` は裸35件、`daily-list.test.tsx` は裸24件
- 対応方針: 本番コードと同じ規約をそのまま適用する（`src/app` 配下は自画面が既定・下位3層は混在ファイルのみ各参照に文書名）。**テストファイル固有の規約は作らない**。判定は「文書名の種類数 > 1 かつ裸の参照あり」の機械検算で行い、**その検算を [T-86](./closed_23_技術改善バックログ.md#t-86) の拡張検査に載せるところまでを1件にする**（人手で数えると今回のように取りこぼす）
- 着手条件: テストファイルをまとめて触るとき。**検算の実装先は用意できた**（T-86 完了。`scripts/docs-check.sh` に1検査を足す形になる）ので、あとは150参照を直す側だけが残っている
- 優先度の根拠: テストの `it` 名の `§` は仕様条項との対応を示す索引で（[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) の domain テスト命名規則）、誤読しても実装は壊れない。ただし**次に節を移動・改番したとき grep で追随できない量が本番コードより多い**のが実害で、T-102 が守ろうとした検索可能性がテスト側だけ穴のまま残る
- 関連: [T-102](./closed_23_技術改善バックログ.md#t-102)（本体。規約と本番コード側の適用）/ [T-86](./closed_23_技術改善バックログ.md#t-86)（機械検査の実装先）/ [T-41](./closed_23_技術改善バックログ.md#t-41)（旧参照の追随）

### T-114

- 背景: **性質上どうしても複雑になる領域**——楽観的更新（N-01）と打刻（F-201 / F-203 / F-204 / F-210 / F-212）——で、**オーナーの理解と実装の食い違いが繰り返し表面化している**。挙動の正は docs にあるのに、規則が複数の文書・複数の節に分かれて置かれているため、全体像を頭の中で組み直さないと「この操作で何が起きるか」を追えない。実例:
  - [FB-83](./closed_21_ユーザーフィードバック.md#fb-83)（2026-08-03）— 打刻修正が日界（F-116）をまたぐ場合の解釈が**どの文書にも書かれていなかった**。未定義のまま実装だけが存在し、オーナーが実運用で踏むまで誰も気づかなかった
  - [FB-69](./closed_21_ユーザーフィードバック.md#fb-69)（2026-08-08 対応済み）— 割り込み時に実行中タスクが一瞬2件に見えることが N-01 から読み取れなかった（実装はコメントだけが根拠だった）
  - [FB-79](./21_ユーザーフィードバック.md#fb-79) / [FB-58](./closed_21_ユーザーフィードバック.md#fb-58) — 「実行中に終了時刻を入れたら完了扱いにするか」が、打刻から状態を導出する設計との関係で毎回議論になる
- 対応方針（案。着手時に決める）: **既存の条項を書き換えるのではなく、散っている規則を1枚に集めて突き合わせる**ことを主眼にする。取りうる形は3つで、①該当領域の**状態遷移と不変条件を1か所に列挙した節**を設ける（打刻2列と状態の対応・実行中は最大1件・楽観更新の対象と除外の一覧）②オーナー向けの読み物（`guide_` 付属ファイル）を起こす ③既存の節構成のままで**相互参照だけを張り直す**。①②は文書を増やすので、CLAUDE.md「詳細は下位文書に1か所だけ書く」との整合を先に確かめる
- 追加の材料（2026-08-06、[FB-86](./closed_21_ユーザーフィードバック.md#fb-86)・[FB-87](./closed_21_ユーザーフィードバック.md#fb-87) の対応で判明）: **同じ節（画面定義書01 §4.2）の中で規則どうしが食い違っていた**——規則 a だけが「セクションの末尾へ積む」で規則 c は「開始時刻順」、対象日の条件は規則 a / b の都合（現在時刻が要る）で決めた制限が規則 c にも一律で掛かっていた。どちらも節の冒頭が掲げる C-7 と突き合わせれば気づけたはずで、**上位の狙いと個々の規則を並べて読む形が無い**ことが露呈した。整理の形を決めるとき、この「狙い ↔ 規則の対応表」を持てるかを判断材料にする
- 追加の材料（2026-08-08、[FB-69](./closed_21_ユーザーフィードバック.md#fb-69) の対応で判明）: N-01 の除外が**操作の列挙**（中断・複製・先送り）で書かれていたため、**操作まるごと対象か対象外かの二値にしか読めなかった**。実際には開始（O-2）のように打刻は即時・生成タスクの採番だけ確定後という**部分的な操作**があり、その線引きはコード内コメントにしか無かった。FB-69 では列挙をやめて**除外の理由（サーバが採番・配置を決める副作用）**を書く形へ改めたが、同じ理由づけがクイック追加（O-1、負IDの仮タスクで先出し）には当たっておらず、**除外の根拠自体が一貫していない**ことが [FB-94](./21_ユーザーフィードバック.md#fb-94) で表面化した。整理の形を決めるとき、「操作 × どこまで即時か × なぜそこで切るか」の対応表を持てるかを判断材料にする
- 着手条件: 打刻・楽観更新まわりの FB が**次に出たとき**（そのとき何が読み取れなかったかが最良の入力になる。2026-08-06 に FB-86・FB-87、2026-08-08 に FB-69・FB-94 で発火済み——上の材料を確保したうえで、着手枠はオーナー判断で別に取る）。または未対応の [FB-94](./21_ユーザーフィードバック.md#fb-94) / [FB-79](./21_ユーザーフィードバック.md#fb-79) に着手するとき（どちらもこの整理と同じ材料を扱うので、まとめて片付ける。とくに FB-94 は N-01 の除外の線引きそのものが論点で、単独で決めると同じ整理を二度やることになる）
- 優先度の根拠: 挙動は現に正しく動いており、直近の実害は「オーナーが仕様を確認するのに時間がかかる」「未定義の隙間が残る」の2点。急がないが、**この領域の FB は毎回コストが高い**（FB-83 は起票から対応まで一連のレビューを要した）ので、放置すると同じ形の取りこぼしが再発する
- 関連: [要件定義書](../仕様/12_要件定義書.md) §5.2・§6（N-01）/ [画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §3.3・§4.2・§8・O-4・O-13・O-14・O-15 / [データモデル定義書](../仕様/14_データモデル定義書.md) §3.5・§4.2・§4.5・§4.7 / [FB-94](./21_ユーザーフィードバック.md#fb-94) / [FB-79](./21_ユーザーフィードバック.md#fb-79) / [FB-69](./closed_21_ユーザーフィードバック.md#fb-69)（対応済み。上の材料）

### T-118

- 背景: CI の `verify` ジョブが**2026-07 末の 2.0〜2.5分から 2026-08-07 時点で 2.8〜3.6分へ伸びた**（成功実行の実測。同期間にテスト本数が増え続けている）。PR と main への push の両方で毎回走るため、待ち時間がそのまま開発の往復に乗る
- 内訳（2026-08-07 の実行・計3分35秒）: `test:coverage` 96秒 / JST での2周目 43秒 / コンテナ初期化 19秒 / `build` 15秒 / `npm ci` 14秒 / `lint` 8秒 / `typecheck` 6秒。**テスト実行だけで 139秒＝全体の約2/3**を占める
- 効き方: 同じ全テストを**2周している**（1周目 = UTC＋カバレッジ計測、2周目 = JST。[T-62](./closed_23_技術改善バックログ.md#t-62) で「TZ だけを変えた2本目。段では分けない」と決めた）。テスト本数に比例して2倍の速度で伸びる構造なので、放置すると増え続ける
- **`ci.yml` のコメントが実測と合わなくなっている**: `test:coverage` に「`--no-file-parallelism` による直列化で数秒増えるだけ」とあるが、実測は 96秒 対 43秒（同じテストを並列・非計測で回した2周目）で**+53秒**。当時は正しかった記述が本数増で陳腐化している。着手時にコメントも直す
- 対応方針（案。着手時に決める）: ①**2周目を TZ 依存のテストだけに絞る**（最大の削減幅。ただし T-62 の「段では分けない」を覆すことになるので、[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §9 と合わせて判断する）②**カバレッジ計測を main への push だけにする**（PR では並列・非計測で回す。ただし変更箇所カバレッジ〔T-38〕は PR でこそ効くので、可視化の価値と引き換え）③ジョブを分割して `lint` / `typecheck` / `build` とテストを並列に走らせる（`npm ci` とコンテナ初期化が二重になる分、削減幅は正味 30秒弱）④カバレッジ計測の直列化を見直す
- 着手条件: `verify` が 5分を超えたとき、または CI の待ちが実際に手を止めていると感じたとき。**テストを増やす作業（T-112 など）の直後は所要を測り直す**
- 優先度の根拠: 3分台はまだ実害が小さいが、**伸びが本数に連動する構造**なので放置すると必ず悪化する。一方で削減案①②はいずれも過去の決定（T-62・T-38）とのトレードオフを含み、速度だけを見て倒すと検査の網が減る。急がず、しかし条件が来たら必ず倒す
- 関連: [完了記録](./closed_23_技術改善バックログ.md)の T-01（CI 基盤）・T-38（カバレッジ計測と PR コメント）/ [T-62](./closed_23_技術改善バックログ.md#t-62)（UTC と JST の2本回し）/ [T-65](#t-65)（統合テストの同時実行）/ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §9

### T-120

- 背景: `globals.css` は色・影・角丸のトークンを `@theme` で `initial` に潰し、**素のパレット（`gray-*` 等）を書けない形で設計言語を強制**している。一方 `--text-*` は Tailwind 既定のまま残っており、`text-xs` から `text-9xl` までどれでも書ける。[FB-91](./closed_21_ユーザーフィードバック.md#fb-91) で文字サイズが主・従・メタの3段という**意味を持つ軸**になったため、色と同じ規律が要る状態になった
- 実害: 3段の外のサイズ（`text-lg` 等）を書いてもビルドもレビューも止まらない。加えて**主段が「クラス指定なし＝既定を継承」で表される**ため、①指定なしが「主に置いた」のか「決めていない」のか読み分けられない ②祖先に1つサイズが付くと配下の主が黙って動く ③`grep` で主段を数えられない。実際 FB-91 の作業中に、表から `text-sm` を落としたことで `inputBase` を使う入力欄が主段へ引き上がる退行を起こした（レビューで検出）
- 対応方針（案。着手時に決める）: ① `@theme` で `--text-*: initial` とし `--text-main` / `--text-sub` / `--text-meta` の3つだけを定義する（色と同じ流儀。段外のサイズはビルドで生成されないので機械的に防げる）② `ui.ts` に3段の定数を置き、`TaskProgress` の `textSize?: string` のような受け口を3値の直和型で閉じる ③主段も明示する（継承に頼らない）。①と③は組みで効く
- 着手条件: 画面を1つ増やすとき、または3段の割り当てを動かす FB が次に来たとき。**いま急ぐ必要はない**——`ui.test.ts` が「部品は自分のサイズを持つ」を固定したので、FB-91 で実際に起きた退行の再発は防げている
- 優先度の根拠: 中。仕組みで防げていない規律が1本残っているが、直近の退行経路は塞いだ。色トークンの規律が効いている実績があるので方式に迷いは無く、着手すれば確実に効く
- 関連: [FB-91](./closed_21_ユーザーフィードバック.md#fb-91)（3段を定めた側）/ [画面定義書00_共通](../仕様/13_画面定義書/00_共通.md) §1.1（規則の正）/ `src/app/globals.css`（色・影・角丸を `initial` で潰している実体）/ `src/app/_lib/ui.ts`・`src/app/_lib/ui.test.ts`（部品側の受け皿と、退行を止めている検査）

### T-124

- 背景: [FB-72](./closed_21_ユーザーフィードバック.md#fb-72) ② で「**文言カタログは画面定義書に持たせない。集約後の辞書とその対応表を固定するテストを単一の正とする**」と決めたが、画面定義書に文言そのものを書いた箇所が残っている。確認できているのは[画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §4.1「見積もり0分（未設定）の扱い」行の「見積もりを入力してからルーチン化してください」で、これは `ROUTINE_FROM_TASK_MESSAGES.estimate_required`（`src/app/_lib/error-messages.ts`）と同じ文字列の写し
- 実害: 小さいが FB-75 と同じ形。**片方だけ直すと静かに割れる**——辞書を改訂しても docs は誰も直さず、docs を読んで実装を確かめるレビューが古い文言を正だと判断しうる。挙動は変わらない（コードは辞書を引いている）
- 対応方針: ①docs 全体を走査して同型を洗い出す（`docs/仕様/13_画面定義書/` に文言リテラルが何件あるか）②残す・落とすの基準を決める——**文言が条項の意味を担っている箇所**（例「未設定」の語のように、その語自体が仕様）と、**単に辞書の写しになっている箇所**は分けて扱う ③落とす側は「〜の旨を表示する」のように意味だけを書き、文言は辞書とそのテストに委ねる
- 着手条件: docs の品質監査を回すとき（CLAUDE.md「起動の契機」の docs 監査）に合わせる。**単独で急ぐ必要はない**
- 優先度の根拠: 低。現に食い違ってはおらず（FB-71 の対応時に文言を変えていない）、割れるのは辞書を次に改訂したときから
- 発見: FB-75 の対応中。ポップオーバー側の直書きを辞書へ寄せる作業で §4.1 を読み直していて、同じ節の別の行に文言そのものが書かれていることに気づいた
- 関連: [完了記録](./closed_21_ユーザーフィードバック.md#fb-72) FB-72 ②（方針を決めた側）/ [完了記録](./closed_21_ユーザーフィードバック.md#fb-75) FB-75（コード側の同型を閉じた側）/ `src/app/_lib/error-messages.ts`

### T-125

- 背景: `DailyBoard` は `date`（表示日）と `today`（日界考慮済み）を両方 prop で受け取っているのに、そこから導ける `isToday` も**別の prop として**受け取っている（`src/app/(daily)/page.tsx` が `isToday={date === today}` を組んで渡す）。同じ事実を2通りの形で持っている
- 実害: 小さいが、**テストで矛盾した組を作れる**。`_testing/board-helpers.tsx` の `boardProps` は3つを独立に受けるので、`date > today` なのに `isToday: true` のような本番では起こりえない盤面を組める。[FB-85](./closed_21_ユーザーフィードバック.md#fb-85) で足した未来日のテストが `{ date: NEXT_TEST_DATE, today: TEST_DATE, isToday: false }` と3つを手で整合させているのは、この冗長さの現れ
- 対応方針: `isToday` prop を落とし、board 内で `date === today` として導出する（FB-85 で足した `isFutureDate = date > today` と同じ形になる）。`page.tsx` の組み立てと `boardProps` の1項目が消え、矛盾した組は型の上で作れなくなる
- 着手条件: デイリー盤面の props 周りを別件で触るとき。**単独で急ぐ必要はない**
- 優先度の根拠: 低。現に矛盾した組を持つテストは無く、挙動も変わらない。`isToday` は `DailyList` 側でも受けている（セクション残り時間・予想開始の出し分け）ので、そちらまで含めてどこで導出するかを決める必要があり、prop を1つ消すだけでは済まない
- 発見: FB-85 の対応（2026-08-09）で `code-quality-reviewer` が指摘。「同種の値なのに `isFutureDate` は board 内導出・`isToday` は prop、と扱いが割れている。board が両方の材料を持つ以上、導出側が正しい」
- 関連: `src/app/(daily)/_components/daily-board.tsx` / `src/app/(daily)/page.tsx` / `src/app/(daily)/_testing/board-helpers.tsx` / [T-109](#t-109)（`TaskRow` と `DailyList` の props 過多。props を減らす話として地続き）/ [完了記録](./closed_21_ユーザーフィードバック.md#fb-85) FB-85（発見元）

### T-126

- 背景: 本リポジトリは docs 先行の運用（CLAUDE.md「最重要ルール」）のため、**1つの案件で docs の増分がコードの数倍になることが常態**。PR の変更行数は GitHub 上でも1つの合計として出るので、「今回コードは実際どれだけ動いたのか」がその数字からは読めない。現状 CI が PR へ出しているのはカバレッジ表（[完了記録](./closed_23_技術改善バックログ.md)の T-38）だけで、変更量の内訳は誰も出していない
- 対応方針: `verify` ジョブの既存の PR コメント（`scripts/coverage-summary.sh` が組み立て、`.github/workflows/ci.yml` が投稿・更新する）に**変更行数の内訳表を1つ足す**。`git diff --numstat "$BASE_SHA"...HEAD` の各行を次の4区分へ振り分け、追加・削除を区分ごとに合計する
  - `docs` — `docs/` 配下＋リポジトリ直下の `*.md`（CLAUDE.md・README.md・AGENTS.md）
  - `src`（本体） — `src/` 配下から下のテスト分を除いたもの
  - `src`（テスト） — `src/` 配下の `*.test.*` と、テストヘルパーの置き場（`src/app` 配下は `_testing/`、それ以外の層は `testing/`。[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §4）。**`testing/` を落とすと `src/domain/**/testing/`・`src/infrastructure/db/testing/` が本体側に混ざり、区分の目的を果たせない**
  - その他 — 上のいずれでもない（`scripts/` `.github/` 設定ファイル等）
- 着手条件: なし（他の作業を待たない）。**PR コメントの本文を組む箇所は T-38 の資産をそのまま使えるので、投稿まわりを作り直す必要はない**
- 優先度の根拠: 中。挙動には関わらないが、レビューの当たりを付ける材料として毎 PR で効く。区分の粒度は**テストを本体から割る4区分**で確定済み（テストの厚みが本体の変更量を覆い隠さないようにするのが要点。本リポジトリはテストを `src/` にコロケーションするため、2区分だとテスト行が `src` に混ざる）
- 発見: オーナーの要望（2026-08-11）。「docs は増える一方なので src の変更の実情が掴みにくい」
- 関連: `.github/workflows/ci.yml`（`verify` ジョブの PR コメント）/ `scripts/coverage-summary.sh` / [完了記録](./closed_23_技術改善バックログ.md)の T-38（コメント投稿の仕組みを作った側）/ [T-118](#t-118)（同じ `verify` ジョブを触る話）

### T-127

- 背景: **「その操作のあと選択行（行フォーカス）がどこへ行くか」を定める条項が散らばっており、書かれていない操作がある**。[画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §5 が持つのは初期選択・`N`・終了打刻後の送り（F-211）・並び替えの追従・スクロール追従で、残りは各操作行が個別に書いている（O-11 は複製行へ移す／O-14 は複製タスクへ／O-17 は⭐のクリックで押した行へ／O-2・O-3 の打刻ボタンも押した行へ）。**書かれていない操作が残っている**——完了の取り消し（O-15）・開始の取り消し（O-13）の後にどの行を選ぶか、中断（O-4）で再開タスクが生まれたときにどちらを選ぶかは、いまも条項が無い（[FB-106](./closed_21_ユーザーフィードバック.md#fb-106) で削除・先送りの送り先だけは §5 に条項化した）
- 実例（毎回オーナーが実運用で踏んでから分かっている）: [FB-106](./closed_21_ユーザーフィードバック.md#fb-106)（削除後に選択が先頭へ飛ぶ。条項が無いまま `keepSelection` が現在地を採り直していた）/ [FB-50](./closed_21_ユーザーフィードバック.md#旧書式の記録2026-07-26-以前)（並び替えで選択が別タスクへ飛んだ）/ [FB-78](./closed_21_ユーザーフィードバック.md#fb-78)（`N` の送り先が未分類に吸われた）/ [決定ログ](../仕様/13_画面定義書/log_01_デイリーリスト.md) 2026-08-02（終了打刻で送り先が無いときの正が読めなかった）
- 対応方針（案。着手時に決める）: **§5 に「操作 × 選択の送り先 × 根拠」の対応表を1つ置き、各操作行（O-XX）はそこを参照する形に畳む**。表を作る過程で条項の無い操作が穴として並ぶので、**埋めるべき穴の一覧**がそのまま成果物になる。表の形は T-114 が検討する「狙い ↔ 規則の対応表」と同型なので、**先に決まった方の形に揃える**
- **守備範囲**: 本書は挙動を変えない活動を扱うので、ここでやるのは**集約と洗い出しまで**。穴を埋める＝送り先を決める判断は挙動そのものなので、洗い出した穴は FB に起こしてオーナーと決め、決まった結果を §5 の表へ入れる（削除・先送りが FB-106 でその形を通った）
- 着手条件: なし（他の作業を待たない）。**選択まわりの FB が次に出たときはこの整理ごと片付ける**——[FB-106](./closed_21_ユーザーフィードバック.md#fb-106) は送り先1つを条項化しただけで、表そのものはまだ無い
- 優先度の根拠: 挙動そのものは動いており、実害は「未定義の隙間をオーナーが踏むまで気づけない」こと（FB-106 が実際にそうだった）。T-114 と同じ性質だが、対象が選択に限られるぶん範囲は小さい
- 関連: [FB-106](./closed_21_ユーザーフィードバック.md#fb-106)（**削除・先送りの送り先を条項化した側**。残りの穴は未着手）/ [T-114](#t-114)（打刻・楽観更新の規則の整理。**同じ「散った規則を1枚にまとめる」形**で、選択の送り先は F-211 経由で打刻とも接する）/ [画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §5・O-2・O-3・O-7・O-8・O-11・O-13・O-14・O-15・O-17 / 実装は `src/domain/task/selection.ts`・`src/app/(daily)/_components/daily-board.tsx`

### T-129

- 背景: 本番のランタイムログに `pg` の警告が出続けている。「SSL modes `prefer` / `require` / `verify-ca` は現在 `verify-full` の別名として扱っているが、`pg-connection-string` v3.0.0・`pg` v9.0.0 で libpq 標準の意味（検証がより弱い）へ変わる」という予告。Neon が配る接続文字列は `sslmode=require` を含むため該当する（本番の値は Vercel の Sensitive 変数で直接は読めないが、警告が出ている事実がそれを示している）
- 実害: **いまは無い**。現行の pg は強い側（`verify-full` 相当）で接続している。問題は次の major に上げたとき——**接続文字列を1文字も変えていないのに TLS の検証が黙って弱くなる**。しかも Dependabot の major PR をマージした瞬間に起き、**lint・typecheck・build・全テストは緑のまま**（ローカルDBもテストDBも TLS を使わないので、この差はどの段にも現れない）
- 対応方針: 現在の挙動を接続文字列側で明示する。本番（Vercel の `DATABASE_URL`）と、本番 Neon を指す手元の接続文字列（`.env.migrate` 等）の `sslmode=require` を `sslmode=verify-full` へ変える。意味は変わらず（現行では同義）、将来の解釈変更に巻き込まれなくなり、警告も消える。libpq 互換の緩い側へ倒す選択肢（`uselibpqcompat=true&sslmode=require`）は**採らない**——本番DBへの接続で検証を弱める理由が無い
- 再確認の契機: Dependabot が `pg` または `pg-connection-string` の major を出したとき。**その PR をマージする前に必ず本項を見る**（先に接続文字列を直しておけば major 追随はただの更新になる）
- 優先度の根拠: 中。放置しても現時点では壊れないが、**壊れ方が「静かに securityが下がる」形**で、CI もレビューも検出できない。対処は環境変数の1語の書き換えで済み、コストが極端に低い
- 発見: 2026-08-13、本番のコールドスタート計測（[34_初回応答の遅延とNeonプラン検討.md](../検討/34_初回応答の遅延とNeonプラン検討.md)）で Vercel のランタイムログを読んでいて気づいた
- 関連: `src/infrastructure/db/index.ts`（`pg.Pool` の生成箇所）/ [CLAUDE.md](../../CLAUDE.md)「環境変数」「本番マイグレーションの手順」/ [T-130](#t-130)（同じ計測で見つかった `pg.Pool` の設定の件）/ [34_初回応答の遅延とNeonプラン検討.md](../検討/34_初回応答の遅延とNeonプラン検討.md)（発見元の計測）

### T-130

- 背景: `src/infrastructure/db/index.ts` の `new Pool({ connectionString, max: 5 })` は `idleTimeoutMillis` を既定（10秒）のままにしている。Vercel の実行インスタンスが温まっていても、**10秒アイドルすると接続が閉じ、次のリクエストが TCP・TLS・認証を張り直す**
- 観測: 2026-08-13 の本番計測で、接続確立時にだけ出る `pg` の警告（[T-129](#t-129)）が**「全部冷」と「Neon だけ冷（6分アイドル後）」の2リクエストにだけ**現れ、その直後に連続して投げた温リクエストでは出なかった。実行環境が温かいまま接続だけを張り直している経路があることの傍証
- 実害: 小さい。温まった状態のサーバ処理 0.25秒（東京のエッジからシンガポールまでの往復 80ms 前後を含む）のうち、接続確立分が乗っている可能性がある。**コールドスタートの主因ではない**——同計測での 2.05秒のペナルティは Neon のコンピュート起動が 1.67秒、Vercel の関数起動が 0.39秒だった
- 対応方針（案。着手時に決める）: `idleTimeoutMillis` を延ばして接続を保つ。ただし**サーバレスでは実行インスタンスが凍結されるため、長く張った接続が復帰時には死んでいることがある**（その場合エラーになるか、張り直しのぶん逆に遅くなる）。Neon の pooler 側のアイドルタイムアウトとも突き合わせが要る。**効果と危険の両方を実測してから決める**
- 着手条件: 温まった状態の応答が気になったとき。**Neon のプランを変える（サスペンドの待ち時間を延ばす）なら、その後に測り直すほうが切り分けが楽**——コンピュート起動の 1.67秒が消えて、接続確立分だけが残るため
- 優先度の根拠: 低。挙動は変わらず、削れるのは温状態の一部だけ。一方で対処は設定1行なので、体感に効くと分かれば即座に入る
- 関連: `src/infrastructure/db/index.ts` / [T-129](#t-129)（同じ計測で見つかった件。警告そのものの扱い）/ [34_初回応答の遅延とNeonプラン検討.md](../検討/34_初回応答の遅延とNeonプラン検討.md)（計測の方法と数値の正）

### T-134

- 背景: [FB-100 / FB-97](./closed_21_ユーザーフィードバック.md#fb-100) で[共通](../仕様/13_画面定義書/00_共通.md) §2.5 を新設し、**語のリンクと面のボタン、恒久的な無効の薄さは `src/app/_lib/ui.ts` の定数に集約**した。定数に載ったものは `ui.test.ts` が守る（`link*` / `btn*` を名前の接頭辞で引くので、新しい定数は自動で検査に載る）
- 課題: **定数を経由せず直書きされたクラスは誰も見ていない**。現に §2.5 に沿ってはいるが直書きの箇所が残る——列見出しの `hover:underline`（`routines-table.tsx`）、候補の面の `hover:bg-accent-weak disabled:hover:bg-transparent`（`select-popover.tsx` / `row-menu.tsx` / `bundle-members-table.tsx` に同文）、条項が**例外**として認めたナビの `hover:text-ink`（`global-nav.tsx` / `nav-tabs.tsx`）とトーストの `hover:no-underline`。**規則側は定数テストが守るのに例外側だけ無防備**という非対称もある
- 対応方針（案。着手時に決める）: ①これらを `ui.ts` の定数へ寄せて `ui.test.ts` の規則を自動的に効かせる ②[§2.4](../仕様/13_画面定義書/00_共通.md) が `unset.ts` の直書きを `no-restricted-syntax` で禁じている前例に倣い、`hover:text-` を許可箇所（ナビ・アイコン）以外で禁じる／`disabled:opacity-` を全面禁止する lint を置く。**N か所にクラス主張テストを撒くのは採らない**——この条項は「書き手ごとに割れる」ことへの対策なので、割れを検出したい層はファイル横断であり、テストは1ファイルの中しか見ない
- 着手条件: 直書きが再び条項から割れたとき、または UI 部品を新しく足すとき（増えるほど揃え直しが高くつく）
- 優先度の根拠: 低。いま実装は条項に沿っており、直すべき割れは無い。**次に割れたときの検出手段**を用意する話なので、割れの兆候が出るまで待てる
- 関連: [完了記録](./closed_21_ユーザーフィードバック.md#fb-100) FB-100 / [FB-97](./closed_21_ユーザーフィードバック.md#fb-97)（条項を立てた側）/ [共通](../仕様/13_画面定義書/00_共通.md) §2.5・§2.4（前例の lint）/ `src/app/_lib/ui.ts`・`src/app/_lib/ui.test.ts`

### T-136

- 背景: 現在地の探索（[画面定義書01](../仕様/13_画面定義書/01_デイリーリスト.md) §5）は `currentSectionId`（現在時刻を含むセクション）と `sectionOrder`（表示順のセクション列）を**常に一緒に動く2つの位置引数**として受け取る。[FB-109](./closed_21_ユーザーフィードバック.md#fb-109) で後者が増えた結果、`keepSelection` は4引数になった
- 対応方針: 2つを `Readonly<{ currentSectionId; sectionOrder }>` の1引数に束ねる。[T-135](./closed_23_技術改善バックログ.md#t-135) の型名を先に付けておくと、束ねた型に説明を集約できる
- 着手条件: **同じ組にもう1つ引数が増えたとき**。いまは束ねない——配列と `number` なので取り違えは型が捕まえ、`reorder.ts` の位置引数の慣習からも外れる（[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §1 の過剰抽象の禁止）
- 優先度の根拠: 様子見。`code-quality-reviewer` 自身が「今回は据え置きでも許容」と判定した項目で、実害が出るのは引数がもう1段増えたとき
- 関連: [T-135](./closed_23_技術改善バックログ.md#t-135)（**先に入れる側**）/ [完了記録](./closed_21_ユーザーフィードバック.md#fb-109) FB-109（4引数になった契機）/ `src/domain/task/selection.ts`

### T-138

- 背景: [T-03](./closed_23_技術改善バックログ.md#t-03) でブラウザ段が入り、実測に依る条項を固定できるようになったが、**同じ性質の箇所が3つ手つかずで残っている**（T-03 の `test-reviewer` レビューで洗い出したもの）。いずれも jsdom では `getBoundingClientRect()` も `scrollTop` も 0 なので、いま書けるのは「呼んだか」までで、**条項が言う結果は誰も確かめていない**
  - **ポップオーバー内のスクロール**（`select-popover.tsx` の `panel.scrollTop = revealedScrollTop(...)`）— 計算そのものは純関数としてユニット段が網羅済み（`popover-scroll.test.ts`）。未測定なのは**適用**と、`popover-scroll.ts` 冒頭が理由まで書いている「**ドキュメントを動かさない**」（`scrollIntoView` を使わない理由。動かすと `useFlipUp` の反転判定が狂う）
  - **選択行のスクロール追従**（`task-row.tsx` の `scrollIntoView({ block: "nearest" })` ＋ `scrollMarginTop`）— 画面定義書01 §5 と 00_共通 §2 の「選択行が固定見出しの裏に隠れない」。[FB-77](./closed_21_ユーザーフィードバック.md#fb-77) で見出しを固定したときに入れた条項で、**隠れないことは実測でしか言えない**
  - **固定領域の高さの積み上げ**（`use-element-height.ts`）— 同じく FB-77 由来。jsdom では `offsetHeight` が常に 0 なので、積み上げた値そのものは未検証
- 対応方針: 3つとも `*.browser.test.tsx` で書く。**まとめて1本の T にしてあるのは、3つが「FB-77 とポップオーバーの幾何」という同じ塊で、着手時に同じ盤面（固定見出しのある実 DailyBoard）を組むことになるため**——別々に着手すると同じ土台を3回作る
- 着手条件: **上の3つのいずれかに関わる不具合が出たとき**（実害が出てからで足りる。T-03 の着手条件と同じ引き方）、または `use-element-height.ts` / `task-row.tsx` の追従まわりを別件で触るついで
- 優先度の根拠: 中。段が無くて書けなかった時期と違い、**いまは書けるのに書いていない**状態で、FB-77（見出しの固定）と FB-51（反転）という**実際に不具合を出した領域**そのものを覆う。一方で現に動いてはいるので、急ぎではない
- 関連: [完了記録](./closed_23_技術改善バックログ.md#t-03) T-03（段の導入。書き方の契約は[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3）/ [完了記録](./closed_21_ユーザーフィードバック.md#fb-77) FB-77・[FB-51](./closed_21_ユーザーフィードバック.md)（この領域で実際に出した不具合）/ `src/app/(daily)/_lib/popover-scroll.ts`・`src/app/_lib/use-element-height.ts`

### T-139

- 背景: `daily-board.tsx` は選択行の state を `useState<TaskId | null>` と**正しい型で持っている**のに、それを `use-daily-shortcuts.ts` へ渡す prop の型が `setSelectedId: Dispatch<SetStateAction<number | null>>` と素の `number` に広がっている（同ファイル31行目）。[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4 は ID 型エイリアスの「**適用は全層**——presentation の props・Server Action の引数・`useState` の型でも」と明記しており、そこから外れた1箇所
- 実害: 無い（`TaskId` は `number` のエイリアスなので安全性は変わらない）。**規約が守ろうとしている「これはどの ID か」が型で読めない**という一点だけ
- 対応方針: prop の型を `Dispatch<SetStateAction<TaskId | null>>` にする。1行
- **これは掃き出し済みの1件**（2026-09-06）: `useState` / `Dispatch` / `SetStateAction` と ID らしい prop 名を機械的に洗い、素の `number` が残っていたのは本件のみ。`select-popover.tsx` と `assign-cell.tsx` の `selectedId: number | null` は §4 が明記する**例外**（マスタ種別を跨いで共通化した部品）なので対象外
- 着手条件: `use-daily-shortcuts.ts` かデイリー盤面の props を別件で触るとき。**単独で急ぐ必要はない**
- 優先度の根拠: 低。挙動にも安全性にも影響せず、直しても1行。ただし取りこぼしを放置すると「例外なのか漏れなのか」が次に読む人に判別できなくなる（§4 の例外リストが効かなくなる）ので、印は残しておく
- 発見: [完了記録](./closed_23_技術改善バックログ.md#t-135) T-135 のレビューで `code-quality-reviewer` が差分外の既存分として指摘した。**型が広がる形は [T-121](./closed_23_技術改善バックログ.md#t-121) と同型**（絞った型を境界で広げ直している）だが、あちらと違って到達不能分岐は生まれていない
- 関連: `src/app/(daily)/_components/use-daily-shortcuts.ts`（31行目）/ [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4（ID 型の規約と例外）

## 完了記録

完了した T-XX は[closed_23_技術改善バックログ.md](./closed_23_技術改善バックログ.md)に保管する。

## 決定ログ

技術活動（T-XX）に関する判断は[log_23_技術改善バックログ.md](./log_23_技術改善バックログ.md)に日付順で積む。
