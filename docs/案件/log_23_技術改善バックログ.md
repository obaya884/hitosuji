# 技術改善バックログ 決定ログ

技術活動（T-XX）に関する判断を日付順に残す。台帳そのものは[技術改善バックログ](./23_技術改善バックログ.md)が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-22 | 本文書を新設し、技術活動を T-XX として要求軸と分けて管理する | リファクタ・負債返済・ツール整備の置き場が docs/ に無く、プロダクト要求（F/N）に混ぜると軸が濁るため |
| 2026-07-22 | CI（T-01）はグリーンでも自動マージ・必須チェック化をせず手動マージとする | マージ＝main への push＝本番デプロイであり、デプロイはオーナーの合図を前提とする運用（CLAUDE.md）を保つため。必須チェック化は本番マイグレーションの main 直 push 運用を要 PR 化してしまう |
| 2026-07-22 | eslint 10 への追随（T-02）を保留する | `eslint-config-next` 同梱の `eslint-plugin-react` が ESLint 10 未対応で lint がクラッシュするため。上流対応を待つ |
| 2026-07-22 | E2E（Playwright）導入検討（T-03）を実装計画から本書へ移設 | F/N に乗らない技術活動で、実装計画（プロダクト機能の計画）では宙吊りだったため |
| 2026-07-22 | postcss 脆弱性（T-04）を overrides で解消・esbuild 脆弱性（T-05）は not_used で dismiss | postcss は安全に根治可能。esbuild は上流修正が無く到達不能パスのため、コードを壊すリスクを取らず dismiss＋追跡とした |
| 2026-07-22 | Dependabot を daily 化し github-actions・docker エコシステムを追加 | CI で自動検証できるようになり頻度を上げられる。ワークフローの action と compose のイメージも追随対象に含める（docker のメジャー更新は手動トリアージ） |
| 2026-07-22 | 統合テストの `updated_at` フレーク（T-07）を起票する | セッション中に把握したが docs に反映されず宙に浮いていたため。挙動を変えないテスト側の問題で要求軸に載らない。修正は様子見（まれな再現・偽陽性のみ） |
| 2026-07-22 | `refactor-scout` の診断から T-08〜T-12 を起票する | 初回のリファクタ棚卸し。負債は presentation のコロケーション運用に集中（結果型の並立・ポップオーバー effect の重複・マスタ表の重複）。唯一のレイヤ責務違反は移動先算出の二重実装（T-11）で最優先。いずれも挙動を変えない |
| 2026-07-22 | domain/usecases のファイル命名を「ディレクトリ名を繰り返さない」方向で統一する（T-13） | ドメイン別ディレクトリが集約を表すため、`task/task-reorder.ts` のような prefix 重複（stutter）を避ける。オーナーは当初オブジェクト名 prefix での統一を想定したが、対話で stutter コストとディレクトリ文脈を検討し、prefix を外す方向に倒した。既存の「domain に動詞ファイルが並ぶ」前提（アーキ定義書 §2）とも整合 |
| 2026-07-22 | usecase の集約操作ファイル（`mode-usecases.ts` 等）は `-usecases` を維持する（T-13 の訂正） | いったん `usecases.ts` に改名したが、`usecases/mode/usecases.ts` はレイヤ名の重複（stutter）になり、かつ命名規約「domain 集約代表 `mode.ts` と同名衝突するため `-usecases` を付ける」にも反するため戻した。`punch.ts`⇔`punch-usecases.ts` と同じ理屈 |
| 2026-07-22 | daily-board（625行）のうちキーボードショートカット部分のみ抽出対象として T-14 に起票 | 独立性が高く可読性への効果が明確な塊に絞った。楽観更新 reducer・操作ハンドラは凝集が高く、`TaskRow` 分割は props 増で効果が限定的なため対象外とした |
| 2026-07-22 | Dependabot PR #8（next セキュリティ patch 群）・#7（Actions v7）をマージし T-15 に記録 | どちらも `verify` 緑で挙動変更なし。#8 は next のセキュリティ修正を含むため早期に取り込む価値があり、#7 の major は Actions のみで本番ビルドに無関係。オーナー合図のうえマージ（＝本番デプロイ） |
| 2026-07-22 | F-208 実装時の品質レビュー指摘（生成物へ選択を移す非楽観パターンの重複）を T-16 として起票 | F-208 単体で対処すると `operate`/`add` を巻き込む小リファクタになり差分が膨らむため、挙動を変えない改善として別軸（T-XX）へ切り出した。優先度は低 |
| 2026-07-22 | テスト観点レビュアー `test-reviewer` の導入に合わせカバレッジ計測（`@vitest/coverage-v8`）を T-17 として補助指標で導入する | 観点の網羅・正しさを専門に見る役が不在だった。カバレッジは数値ゲートにせず補助指標に留める（§8 の主指標は「仕様条項に対応するテストがあるか」のまま）。数値化は既存 review 群と重複しない `test-reviewer` の判断材料 |
| 2026-07-22 | 技術改善計画から「簡単に取り組める」低リスク項目として T-18・T-07 を先行実施する | 差分が小さく挙動不変・既存テストで回帰を担保できる2件に絞った。横断・中リスクの T-08〜T-11・T-14・T-16 は据え置き |
| 2026-07-22 | T-18 のヘルパー配置ファイルを `from-draft.ts` とする | §2 命名規約「ディレクトリと同じオブジェクト名を繰り返さない」に厳密に沿い、`task` を含めない。`NewTask` は port の型名だが `new-task.ts` はディレクトリ名との重複気味のため避けた |
| 2026-07-22 | バッチA（T-11・T-12・T-08・T-17）を実施。T-09・T-10・T-14・T-16 は据え置き | 低リスク・ファイル非共有で並列しやすい4項目に絞った。daily-board.tsx を共有する T-14・T-16、modes-table.tsx を共有する T-09・T-10 は順序制御が要るため別ラウンドへ。着手時に main を同期したところ T-18・T-07 は PR #11 でマージ済み・T-12 は PR #10 で大半が実装済みと判明し、ブランチを最新 main から作り直した |
| 2026-07-22 | T-08 は「型の一本化」のみ実施し定型ラッパ化・MESSAGES 統合は見送る | 型の集約は再エクスポートで消費側 .tsx を触らず低リスク。MESSAGES 統合は箇所ごとに文言差（「開始時刻を…」/「開始想定時刻を…」）があり、統合＝表示文言の変更になるため docs 先行が必要（挙動軸）。ラッパ化はオーナー1人のアプリで過剰抽象になりうるため効果を見て判断 |
| 2026-07-22 | T-08 の共通結果型は `src/app/_lib/action-result.ts`（全画面共有）に置く | 消費は daily/routines/masters の3画面にまたがるため、特定画面配下でなく全画面共有の `_lib` が一貫（`use-flip-up` 等と同位置）。masters の `failure`・`MESSAGES` はマスタ固有なので現状の `masters/_lib/action-result.ts` に残す |
| 2026-07-22 | バッチAのレビューで判明した既存の潜在事項を T-19（アーカイブ済み境界の楽観更新ずれ）・T-20（coverage の .tmp 競合）として起票 | いずれも今回の変更が生んだものではなく main でも再現する既存事項。T-19 は修正が挙動を変えるため種別を「調査」とし、着手時は要求軸を先に判断すると明記。T-20 はツール整備で挙動に無関係 |
| 2026-07-22 | 残りの実施項目 T-09・T-10・T-14・T-16 を順序制御して実施（同ブランチに積む） | ファイル共有を踏まえ daily-board 共有の T-16→T-14、modes-table 共有の T-10→T-09 の順で直列実施。T-16・T-14 は同一ファイルに交錯するため1コミットにまとめた。いずれも挙動不変の可読性/重複リファクタ。残る未実施は保留/外部要因待ち（T-02/T-03/T-05/T-06）と調査（T-19/T-20）のみ |
| 2026-07-22 | スキーマ更新が必要な PR に「スキーマ更新」ラベルを付ける仕組み（T-21）を導入し、検出を「新規マイグレーション SQL の有無」に一本化する | マージ＝本番デプロイ前の手動マイグレーション流し忘れを防ぐ注意喚起が狙い。本番適用の要否と 1:1 で対応するのは `migrations/*.sql` の追加であり、`schema.ts` 変更検出はコメント修正やマイグレーション未生成でも誤検出しやすいため採らない。ラベルの付与・除去のみで CI を fail させず・マージもブロックしないのは、T-01 の「検証のみ・手動マージ」方針と揃えるため |
| 2026-07-23 | 本番スキーマ更新をリモート実行するパイプラインを T-22（調査）として起票し、詳細は専用ドキュメント `32_スキーマ更新パイプライン検討.md` に置く | 方式 A/B/C の段階的分析で本文が長くなるため、PR #17 の `31_バンドル検討.md` と同じく検討専用ドキュメントに分離し、技術改善計画本体は要約＋参照に留める（CLAUDE.md「詳細は下位文書に1か所」）。難所は migrate 実行そのものでなく Vercel デプロイとの順序保証・public リポジトリのシークレット露出の2点と整理し、今回は調査までとした |
| 2026-07-23 | T-22 全体を再スコープ。**案A（手動 migrate ワークフロー）を最終形として採用し、案B（マージ契機の自動 migrate→deploy）・案C（expand/contract）は個人開発の規模では過剰につき見送り**（将来の選択肢として検討ドキュメント §3 に記録）。代わりに運用の明瞭化として「スキーマ更新 PR への migrate 実行を促す注意コメント」（T-21 の label workflow 拡張）と「README への運用記載」を足す。ラベル CI＋注意コメント＋README で、順序の自動保証（案B）まで作らずとも事故は起きにくいと判断 |
| 2026-07-23 | T-22 の案A（手動 migrate ワークフロー）を実装。`workflow_dispatch` ＋ migrate 専用 `db-migrate` Environment 承認ゲート＋確認入力で `npm run db:migrate` を本番へ適用する `.github/workflows/db-migrate.yml` を新設 | 「実行の場所」だけをローカルからリモートへ移す最小の一歩。デプロイは従来どおり手動に据え置くため順序リスクは現行と同じ（増やさない）。接続文字列は unpooled 専用の `MIGRATE_DATABASE_URL` に分離し（CLAUDE.md の手順どおり）、`pull_request` では走らせず fork PR にシークレットを渡さない。承認ゲート用の環境は Vercel の "Production" を再利用せず専用 `db-migrate` を新設（関心の分離）。Environment・シークレット・reviewers の設定はオーナーの手動作業として docs に明記した |
| 2026-07-23 | T-06 の体制構想を検討し、詳細は専用ドキュメント `34_AI並行開発体制検討.md` に置く（本書 T-06 は要約＋参照に改訂） | T-22/T-25 と同じく検討専用ドキュメントに分離（CLAUDE.md「詳細は下位文書に1か所」）。体制は「ヘッド1＋ワーカーN」（ヘッドは編集しない／統合は PR＋CI）、通信機構は Agent Teams（実験的・CLI 限定・in-process メイトのバックグラウンド起動不可）でなく**サブエージェント（worktree 分離＋SendMessage 継続）を採用**と整理。決め手は機能差でなく「通信を最小化する体制設計」が本命であること。実装は未着手のまま、着手条件を「並行させたいタスクが実際に複数たまったとき」とした |
| 2026-07-23 | T-22 を完了に移す。`db-migrate` Environment 設定 → PR #16（マイグレーション 0002）でリモート migrate を承認ゲート経由で本番適用 → マージ、を通しで実働確認できたため | ラベル→注意コメント→リモート migrate→マージの一連が動くことを実PRで確認。案A を最終形として確定（B・C は個人開発では過剰につき見送り継続）。以後スキーマ変更は README「スキーマ更新（本番マイグレーション）」の手順で回す |
| 2026-07-24 | 案件台帳の完了分を `closed_<文書名>.md` へ分離する（T-27）。エントリは1行要約への圧縮でなく**原文のまま**移す | FB管理簿・技術改善バックログとも完了分が本文の過半を占め、AI が起票・着手のたびに読むライブ情報がノイズに埋もれていた。既存の「ライブ台帳 vs 完了アーカイブ」（要件バックログ／実装計画）パターンを両台帳へ適用。圧縮しないのは、完了 T の方式判断・FB の反映先が再発時の参照価値を持ち、アーカイブは日常的に読み込まれないためサイズが害にならないから。FB-XX / T-XX は docs 内 200 箇所超・コミット約 50 件が参照する安定キーのため番号・記述を温存し、移動だけを行った |
| 2026-07-24 | docs の全文書に**全体で一意な2桁ブロック採番**を導入する（T-28）。0X=画面定義書内 / 1X=仕様 / 2X=案件 / 3X=検討 | オーナーの要望は「全体で一意な番号」。ディレクトリごとに 01 から振り直す案は 00〜04（画面定義書）と衝突して一意にならず、全 docs 通し連番は既存の画面番号と文中の「画面定義書01」呼称（数十箇所）の振り直しを強いる。2桁ブロックなら画面定義書の既存採番と呼称を温存したまま全体一意が成立し、各ブロックに将来の空き番号も残る。並びは仕様=FB着手の判定順・案件=流入順・検討=作成順とオーナーが確定 |
| 2026-07-24 | 凍結文書の接頭辞 `archive_` を導入し 24_実装計画.md を `archive_24_実装計画.md` へ改名（T-28 の補完） | オーナーの「実装計画に closed_ を付けられないか」の検討から。closed_ は「同番号のライブ台帳と対で、完了のたび追記され続ける受け皿」であり、親を持たず凍結された実装計画に流用すると接頭辞の意味が濁る。凍結（追記されない・記録のためだけに残る）は別役割として `archive_` を新設し、直交を保った。番号は文書自身のもの（親の引き継ぎではない）。検討/ の完了ステータス文書（32 等）は今も「方式・設計は本書が正」のライブな参照先のため archive_ の対象外、と線引きした |
| 2026-07-24 | Dependabot PR #29（compose の postgres 17→18）をクローズし、追随は T-29 として保留する | 本番 Neon が 17 系のため、ローカル／統合テストDBだけ 18 に上げると本番と別メジャーでテストが通る状態になる。`verify` は緑だがこの変更を検証していない（CI は `ci.yml` 側の 17 ピンで走り compose を使わない）＝緑を根拠にできない。加えて 18 系イメージは既定 PGDATA が `/var/lib/postgresql/18/docker` へ変わり既存 `db-data` ボリュームをそのまま使えず、版指定も compose/ci.yml/db-backup.sh/README の4か所に散るため、上げるなら Neon 追随のタイミングで一括に倒した |
| 2026-07-24 | T-29 の着手条件を「Neon のメジャーが上がったとき」から「**18 が preview を抜け、かつ別件で本番DBを作り直す機会が来たとき**」へ改める | 前者は「Neon が勝手に上げてくれる」前提で循環していた。実際は Neon がメジャーの in-place アップグレード（`pg_upgrade` 含む）に未対応で、上げるには 18 の新プロジェクトを作って dump/restore する＝**こちらが移行を決断したときが唯一の契機**。18 は preview 扱いで本番投入は時期尚早、18 の新機能（skip scan・非同期I/O・uuidv7・仮想生成列）も単一ユーザー数千行規模の本アプリでは効かず、単体で踏む動機がない。一方 PG17 のコミュニティ EOL は 2029-11 で期限も遠い。よって「どうせ作り直す機会」に相乗りする条件に置き換え、移行手順（N-06 のバックアップ資産を使う4ステップ）を対応方針に明記した |
| 2026-07-24 | DBメジャー移行の知識は専用の検討ドキュメント（35）を立てず、**T-29 に「外部事情（確認日つき）＋再確認の観点＋出典リンク」として残す** | 保存価値があるのは減衰しない判断軸（何を確認すれば判断できるか）で、外部事実そのもの（preview 状況・in-place 可否）は Neon 側の都合で腐り、腐った記述は「確認済み」と誤読されるため確認日と再確認指示を必ず添える形にした。検討/ への分離を今回見送ったのは、既存 32〜34 の分離基準が「方式 A/B/C の比較で本文が長くなること」であるのに対し、本件は Neon の制約で方式が dump/restore 一択＝比較軸が無く、35 を立てても T-29 の焼き直しになり二重管理の乖離リスクだけが増えるから。**着手が現実になった時点**（判断メモでなく手順書になる）、または**同型の「外部事業者都合で決まる追随」が複数たまった時点**で 35 へ昇格させる、と昇格条件を T-29 に明記した |
| 2026-07-24 | 版の追随関係を[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §11「実行環境とバージョン整合」として新設し、未追随の `@types/node` を T-30 として起票する | PR #3（`@types/node` 20→26）の「ランタイムのメジャーに合わせる／追い越さない」という判断が PR コメント1行にしか残らず docs に無かったため、`ci.yml` が Node 24 へ上がっても `@types/node` が `^20` に置き去りのまま拾われずにいた（＝知識を保管しなかったことの実害が既に出ていた）。保管先を検討/35 でなく 15 にしたのは、内容が方式の比較検討でなく**確定した技術契約**だから。書くのは各ファイルの値そのものではなく「**何が何に追随するか**」の対応表とし、値は各ファイル（compose・ci.yml・package.json・db-backup.sh）に置いたまま二重管理を避けた。あわせて「Dependabot は最新版しか PR にしないため、追随先に合わせた中間版は手動で上げる」を明記——最新版 PR のクローズだけでは追随が完了しないことが今回の見落としの構造的原因のため |
| 2026-07-24 | **T-29（postgres 18 系への追随）を取り下げ、バックログから削除する**（T-29 の番号は欠番として温存し再利用しない） | §11 を立てた時点で「ローカル/テストDB は本番 PostgreSQL に追随する・起点が動いていない片側だけの bump は閉じる」が原則として書かれ、T-29 が無くても次回以降の判断は導ける。加えて着手条件は「Neon が動き、かつ別件でDBを作り直す気になったら」で着手日も動機も無く、実施は数年先が濃厚＝**活動の台帳であるバックログに置くと塩漬け項目になる**（T-02 は「上流待ちで lint を上げられない」という現に効いている制約の追跡であり質が違う）。失われる実務情報のうち、移行が dump/restore になる点と `DATABASE_URL` 差し替えを伴う点は §11 へ移し、#29 のクローズ根拠と Neon 側の外部事情（preview 扱い・in-place 未対応・EOL 2029-11・出典リンク）は本ログの前掲エントリに残る |
| 2026-07-25 | T-30（`@types/node` を Node 24 系へ追随）を実施・完了。`^20`→`^24` に上げ lint/build/全テストで回帰なしを確認 | §11 の追随原則（型定義は本番ランタイムのメジャーに合わせ追い越さない）に沿った未消化分の解消。Dependabot が出す最新版（26）は本番 24 を追い越すため使えず、中間版 24 の手動 bump が唯一の追随手段だった（§11 明記の構造）。メジャー4つ分だが型エラーは出ず差分は package.json/lock のみ |
| 2026-07-25 | T-19（Shift+J/K のアーカイブ済み境界の巻き戻り）を調査のうえ対応し完了に移す。挙動判断はオーナーと決め log_01 に記録 | 調査で「視覚的巻き戻りのみ・データ不整合なし・エッジケース」と実害を確定。修正は挙動が絡む（本書の守備範囲外）ためオーナー判断「表示されているものは跨げる＝当日タスク付きアーカイブ済みも移動先に含む」を採り、docs 先行で画面定義書01 O-6 を更新（決定は log_01）。実装は移動先セクション順を表示順（`displaySectionOrder`＝`groupTasksBySection` 由来）に統一し、従来サーバのみ別だった `activeSections` 版を置換。着手前に要求軸を判断する T-19 の但し書きどおりの手順を踏んだ |
| 2026-07-25 | T-20（coverage の `.tmp` 競合）を `--no-file-parallelism` で解消。バックログの2案（出力先分離／逐次2回run）は採らない | 2案はいずれも合算カバレッジ像を失う——出力先分離は unit/integration が別レポートに割れ各々 domain=0%/infra=0% と誤読を招き、逐次2回run は後のrunが前のレポートを上書きする。`--no-file-parallelism` は全ファイル直列化で共有 `coverage/.tmp` への並行アクセス（競合の発生条件）を構造的に除去しつつ単一の合算レポートを保てる。coverage は随時実行の補助指標のため直列化による所要増（~11.7s）は許容でき、`test:unit`/`test:int`/`watch` は無影響。upstream 修正（vitest [PR #9378](https://github.com/vitest-dev/vitest/pull/9378)）は最新 4.1.10 に未取り込みで、追随を待たず設定側で閉じた。元の競合は間欠的でローカル再現しなかったため実証は競合条件の除去に留まる |
| 2026-07-25 | **T-26（git/ブランチ戦略）を T-06 Phase 1〜2 と一括実施し、T-26 を完了に移す**。設計は `34_AI並行開発体制検討.md` を `34_git運用と並行開発体制検討.md` に改称して統合。ブランチ命名は `<タスクID>-<slug>`（スラッシュなし）、TEST_DATABASE_URL の注入は「`vitest.config.ts` が worktree 直下の `.env.worktree` を読む（シェル環境変数優先）」を採用 | 両 T はマージ順序の調停・スキーマ変更の直列化で不可分のため一括で設計・実施。命名にスラッシュを使わないのは worktree ディレクトリ名・テストDB名へ機械的に変換するため（`feat/` 型 prefix は変換の手間だけ増える）。注入機構は test 系 npm スクリプト全部を1か所でカバーし CI・本体ワークツリーに無影響——npm ラッパー案（4スクリプト書き換え）・direnv 案（外部ツール依存）は過剰につき不採用。`wt-rm.sh` の y/N 確認は TTY 時のみ（AI 実行時は permissions の ask が確認を兼ねる）とし、dirty なら中断・`--force` なしで防護は git の安全装置に寄せた。検証で bash の「変数直後に全角文字が続くと変数名を誤解釈する」事象を踏み、スクリプト内の変数展開は `${name}` 形式に統一 |
| 2026-07-25 | T-06 の単独／並行モードの切替契機を「並行タスクがたまったとき（AI 判断）」から「**オーナーの明示指示のみ**」に変更。常時ヘッド＋ワーカー方式は不採用 | オーナー指摘「切替の判断基準が維持されなさそう」を受けた検討。常時ワーカー方式はフローが均一になる一方、チャット差分レビュー（規約「Edit/Write で変更」の存在理由）が消え、仕様質問が毎回「ワーカー停止→ヘッド→オーナー→SendMessage」の3ホップになり、対話往復の多い本プロジェクトの実装スタイルと合わない。切替をオーナー指示に一本化すれば維持すべき判断基準そのものが消え、統合経路（ブランチ＋PR＋CI）は両モード同一なので選択の重みも小さい |
| 2026-07-25 | **T-31・T-32・T-34・T-36 を一括で実施し完了へ移す**（1ブランチ・1PR。`scripts/` を共有するため分けない） | T-32（`wt:rm` の消し残り）と T-34（dev サーバの並列化）は同じ `wt-new.sh`／`wt-rm.sh` を触り、T-36（台帳の行操作）も同じ `scripts/` 配下。オーナー指示「まとめて着手」。T-31 は当初 `main` 直コミット予定だったが、**本ブランチのテスト実行中に実際の失敗を踏んだ**（11ms の逆行）ため、自分の PR を緑にする必要から同じブランチに含めた。1ブランチ＝1タスクの規約からは外れるが、いずれも挙動に触れないツール・テスト整備で PR の diff が `scripts/`＋テスト1行に収まるため可読性は落ちないと判断。着手時に既定として選んだ2点: ①dev サーバが向くDBは**本体と共有**（worktree ごとの複製は `createdb -T` が接続中に失敗し `pg_dump` は遅く、実データの厚みも失う。共有は従来と同じ挙動なので新しいリスクを持ち込まない）②`ledger-move` の対象は**台帳21 のみ**（3冊は構造が違い、破損3件はすべて21で発生。22・23 は1行が短く手編集で壊れにくい） |
| 2026-07-25 | **T-06 を完了に移す。検討ドキュメント（検討/34）を運用の一級文書 [16_git運用と並行開発体制定義書](../仕様/16_git運用と並行開発体制定義書.md)（仕様/16）へ昇格・改番し、当初構想の Phase 3（ヘッド運用の型化・Agent Teams 再検討）は設けない** | オーナー判断「検討ではなく運用の1級ドキュメントとしたい。Phase 3 は考えない」。1X=仕様ブロックはアーキテクチャ定義書と同じ「確定した契約」の置き場で、git 運用ルール・並行体制は日常運用の契約になったため（T-30 のとき「確定した技術契約は仕様へ」とした線引きと同じ）。Phase 表記の廃止は「完了した事項に予定表記を残さない」規約とも整合し、残作業が消えたことで T-06 は完了。本文書の経緯ログは log_16 へ引き継いだ |
| 2026-07-26 | **T-35 は方針②（稼働中ブランチの実差分の積集合）だけを実装し、方針①（着手前に変更予定ファイルを宣言させて交差を取る）は作らない**。代わりに「各ブランチに最初のコミットが乗った時点で②を掛け直して検算する」を §5 手順1 に足して完了とする | ①の入力は着手前の予測＝宣言で、外れれば判定も外れる。同じ交差判定を実差分（②）で早期に回せば、宣言を機械化しなくても見落としは同じだけ潰せる。実装で得た知見が2つある: (1) **ファイル名の積集合だけでは足りない**——一方が `tmp-dir` をファイル、他方が `tmp-dir/inner.txt` を作る形は積集合が空なのにマージが衝突する。§1.3 が `merge-tree` と積集合の両方を求めていた理由が実測で裏づけられたため、空のペアにのみ `merge-tree` を重ねる構成にした (2) **同じブランチが2回入ると総当たりがそのペアを飛ばし「重なりなし」＝最も危険な側へ倒れる**（レビュー指摘）。ヘッドがブランチ一覧をコマンド置換で組み立てる運用では現実に起きうるため、対象の組み立て時に重複を落とす。判定できなかった場合は終了コード 2 に分け、「重なりあり」（1）と取り違えられないようにした |
| 2026-07-26 | **T-38（FB-49）はカバレッジを「PR コメント＋ジョブサマリに層ごとの表」で見せる。README バッジ・外部サービス（Codecov 等）は採らない。数値の public 公開は可とする** | 公開の可否: カバレッジ数値は到達手段・実データにつながらないためリポジトリ public 化の禁止事項（CLAUDE.md）に触れない。**バッジを採らない理由**が本件の中心——全体値は 72.6% だが、内訳は `domain` 99〜100%・`usecases` 92〜99% に対し `src/app/**/actions.ts`（Server Actions＝合成ルート）・`proxy.ts`・hooks が 0%。この 0% は §8 が「presentation は自動テストの対象外・手動確認が担保」と決めた結果であって薄さではないため、単一の数値バッジは方針を誤って伝える。層ごとの表なら同じ数字がその実態ごと読める。外部サービスは差分カバレッジ等を得られるがシングルユーザーの本規模には過剰で、第三者へコードのカバレッジ情報を送る依存が増える。**出す場所を PR コメントにも広げた理由**: ジョブサマリは Actions 実行ページにあり PR から2クリック掛かるため、FB-49 の「GitHub 上で把握しづらい」を十分に解かない（オーナー指摘で判明）。ジョブサマリも残したのは main への push では PR が存在しないため。計測対象は変えていない——`src/app` を exclude すれば全体値は意味のある数字になるが、[T-33](./closed_23_技術改善バックログ.md)（`.tsx` を含めるか）と同じ論点に踏み込むので、可視化だけを独立に完了させた |
| 2026-07-26 | **T-38 に変更箇所カバレッジ（PR が変更した行の実行状況と未カバー行）を含める**。判定対象は `src/**/*.ts` に限り、対象が無い PR は「計測対象なし」と表示する | オーナー要望。全体値・層ごとの値は「いま全体がどうか」しか答えず、その PR で書いたコードがテストされたかは読めない。lcov の行データ（`lcovonly` reporter）と `base...HEAD` の差分の突き合わせで出せることを実測で確認した（`5ff8250^` を base に 20行/20行、`8b13548^` を base に 126行中111行・未カバー行の範囲圧縮まで動作）。**採用時に分かっていた弱点**: 直近40コミットのうち `src/**/*.ts` を含むのは7件で、`.tsx`・docs・scripts だけの PR では常に「対象なし」になる。これは `.tsx` を計測対象に含めるか（[T-33](./closed_23_技術改善バックログ.md)）と同じ論点だが、T-33 の判断（E2E 導入と併せて決める）を待たずとも `.ts` の変更に対しては即座に価値が出るため先行させた。`lcov` ではなく `lcovonly` を選んだのは前者が HTML ツリーも生成するため。CI は `actions/checkout` に `fetch-depth: 0` が要る（既定の浅い clone では base が存在しない） |
| 2026-07-26 | **presentation のテストを4段（ユニット／コンポーネント／ブラウザ／E2E）に分け、境界を「レイアウト幾何に依存するか」「async Server Component か」で引く。実装はコンポーネントテスト（jsdom）まで＝T-39。T-33 は T-03 待ちではなくなった** | オーナーの問い「`.tsx` のユニットテストは E2E しか選択肢が無いのか」を調べ、**無い**と分かった。T-33 が前提にしていた「E2E を入れない限り数字は埋まらない」は誤りで、実測では `.tsx` 35ファイル4,429行のうち Client Component が22ファイル・約3,600行を占め、これは jsdom + Testing Library で測れる。E2E が要るのは async Server Component（13ファイル・約450行、大半は薄い `page.tsx`）だけ。**段2と段3は同じ「コンポーネントテスト」で実行環境だけが違う**ため、オーナーが段2＝コンポーネントテスト／段3＝ブラウザテストと呼び分けを決めた（段2の呼称は業界でも揺れる——テストピラミッドでは unit 側、Testing Trophy〈Testing Library 作者〉では integration。本プロジェクトは §8 の既存基準「実DBを使うか」に従い DB を使わない段2をユニット側に置く）。**調査で見つかった2点**: ①「presentation は自動テスト対象外」としながら hooks は `.ts` なので既に計測対象の内側で 0% になっていた＝線引きが既に破れていた ②`use-flip-up.ts` は `offsetHeight`／`getBoundingClientRect()` で反転を決めるが jsdom はレイアウト計算をせず 0 を返すため、jsdom でテストを書くと「常に反転しない」という嘘の緑になる（T-37 が「Playwright で測って初めて分かった」と記録した FB-51 の件がまさにこれ）。逆に `ui.ts` は Tailwind クラス定数だけで、0% は正しく「テスト不要」を意味する——計測対象を機械的に広げると意味のない分母が増える証拠でもある。`.tsx` を計測対象に含めるかは T-39 でテストが積まれてから実数で判断する（薄いうちに含めると全体値が下がるだけ） |
| 2026-07-26 | **T-33 を「含める」で決着。「導入可能なコンポーネントテストを全部書いてから決める」は採らない** | オーナーの問い「T-39 を全部対応してから T-33 を決断すべきか」への検討。**依存関係が逆**という結論。①「全部」の終わりが定義できない——残る Client Component は20ファイル・約3,500行で、`icons.tsx`（SVG パスの羅列）のようにテストを書く意味がないものも混ざるため、線をどこに引くかの議論が T-33 と同じ形で再発する ②待つ間、まさに必要なフィードバックが得られない——各バッチの成果がカバレッジに現れず（`test-reviewer` が過去2回指摘したのと同じ状態が続く）、UI を触る PR の変更箇所カバレッジが永久に「対象なし」になる。**カバレッジは T-39 の成果を測る道具であって T-39 の結果ではない**ため、計器なしで作業を進めることになる ③「数字が低く見える」コストがこのプロジェクトでは小さい——数値ゲートが無く（§8）、バッジも意図的に採っていないため、落ちて困る CI も無い。層ごとの表があるので `src/domain` 99.69% は変わらず読める。実測は全体 75.76% → 50.55%、`src/app` 21.46% → 7.83%。折衷案（変更箇所カバレッジだけ `.tsx` を含め、全体・層は `.ts` のまま）は「今書いたコードがテストされたか」だけ見える利点があるが、分母が2種類になり §8 に説明が増えるので採らない。あわせて `test-reviewer` の観点に4段の線引き・jsdom の偽の緑（`offsetHeight` が常に 0）・00_共通 が条項の正であること・計測設定の非対称を追記した——今回の指摘はいずれも定型化できる種類で、同じ発見を毎回の推論に頼らせないため |
| 2026-07-26 | **コンポーネントテストで Server Action と `next/navigation` を偽物に差し替えることを許し、[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 に「偽物を置いてよい境界」として明文化する**。Server Action を props で注入する形へのリファクタは採らない | T-39 第2バッチの着手前調査で判明した構造。`daily-board.tsx` と masters の各テーブルは `actions.ts` を直 import し、その先は `@/infrastructure/db` の `pg.Pool` と `revalidatePath`（フレームワークの要求スコープ）に届くため、**素の jsdom では描画すらできない**。ここを塞いだままでは残り src/app の約7割（daily-board 198文・masters 216文）が段2に上がらない。§8 は「モック・スタブは原則使わない」と書く一方 usecases の Port については既に「本物と同じ契約を満たす偽物」を許していたため、**線引きを「プロセス外の依存か」に統一して3例（リポジトリ／Server Action／フレームワーク API）を列挙する**形に整えた——古典学派が禁じるのは内側の協力者の差し替えと「呼ばれ方の検証」で、プロセス外の境界を固定値に置くことは元から例外扱いだったという整理。差し替えの目的も「呼ばれたか」ではなく**成功・失敗のどちらを返すかを固定して楽観的更新（N-01）とロールバックの両分岐を通すこと**と明記した（N-01 の失敗時ロールバックは要件でありながら現在どの段でも未検証）。**props 注入案を採らない理由**: 6コンポーネント＋各 `page.tsx` の配線が冗長になり、テストのために本番コードの合成ルートを歪める。差し替えはテスト側だけで閉じる方を選んだ |
| 2026-07-26 | **T-39 の残りを6並行（ヘッド1＋ワーカー6）で進める。分割はディレクトリ境界＋「1ファイルに集約された巨大コンポーネントは単独ワーカー」** | オーナー指示「ワーカーを並列で起動しながら src/app のカバレッジを最大化」。分割の軸をディレクトリにしたのは、**新規テストファイルしか作らないため衝突面が実質ゼロ**になり `wt:overlap` の判定が自明になるから（`icons.tsx` のように複数バッチが import するファイルはあるが、編集するのは1バッチだけ）。`use-daily-shortcuts.ts`（93文・画面定義書01 §6 の20キー超）と `daily-board.tsx`（198文・N-01 の楽観更新と undo）は他と粒度が違うため単独ワーカーに割った。**共有ファイルの禁止事項をタスクカードに明記**: `src/app/_testing/setup.ts`・`vitest.config.ts`・`package.json`・台帳と log は誰も触らない（jsdom に無い API はテストファイル内で局所的に補い、全体で要るならヘッドがマージ後に集約する）。`@testing-library/user-event` の追加も禁じ既存の `fireEvent` に揃えた——依存追加は6ブランチが同じ lock を書き換えて必ず衝突する。`use-flip-up.ts` の判定式抽出（純関数 `shouldFlipUp` の切り出し＝唯一の本番コード変更）は共通コンポーネント担当の1ワーカーに限定し、フックの外形シグネチャは変えない条件を付けた（`select-popover` 担当のテストが同時に走るため） |
| 2026-07-26 | **T-39 を完了に移す。残作業は「カバレッジを埋め切る」ではなく独立した T として切り出す**（T-43・T-44・T-45・T-48・T-49・T-52〜T-62） | 完了の判断を「数値がいくつになったか」ではなく「**やり方が確立し、残りが独立して着手できる形になったか**」で行った。第2バッチ後の実測は全体 Stmts 86.39%・`src/app` 78.79%（テスト 752→1343件）だが、残る未カバーは①段外と決めた範囲（`actions.ts`・`page.tsx`）②別の T で扱う穴（`proxy.ts`＝T-59、リポジトリの単純経路＝T-60）③到達不能な防御コード、に分かれており、**T-39 に残すと「誰が何を判断するか」が曖昧になる**ため切り出した。切り出し先の着手条件はすべて「別件で触るとき」か具体的なトリガで書いてあり、塩漬けにならない形（T-29 を取り下げたときの基準と同じ）。**完了の根拠にレビュー5本（`verifier`・`test-reviewer`・`code-quality-reviewer`・`spec-reviewer`・`refactor-scout`）を統合後の main へリポジトリ全体で掛けた結果を採った**のは、per-branch レビューでは原理的に見えない統合起因の問題（同名別契約の `at()` 等）が残っていないことを確かめないと「完了」と言えないため——実際、6タイムゾーンでの全件緑まで確認して初めて取り違えなしと言えた。**副産物として T-39 自身の前提の誤りが2つ判明した**: (1) `proxy.ts` を「段外」としていたが**ユニットで測れる**（公開リポジトリの唯一のアクセス制御が 0% のまま残っていた＝T-59） (2) presentation を厚くしたことで、統合テストが「トランザクションを張る分岐だけを厚く検証し、**日常操作で最も通る単純 UPDATE 経路に実DBが一度も当たっていない**」ことが見えた（T-60）。**厚くした層そのものより、それによって見えるようになった隣の層のほうが収穫が大きい**という形 |
| 2026-07-26 | **台帳23 を台帳21 と同じ「一覧（索引）＋詳細節」書式へ揃える。ただし本書で足りていなかったのは詳細ではなく索引** | 台帳21 の再構成（[log_21](./log_21_ユーザーフィードバック.md)）からの横展開。**本書は既に `## T-XX` ＋構造化箇条書きで詳細の側が完成しており、作業は「詳細を分ける」ではなく「一覧表を足し、種別・優先度・状態を各節の1行目から索引へ引き上げる」**だった。索引が無いために、28件を俯瞰するには319行をスクロールし優先度を各エントリの箇条書きから拾う必要があり、`next-task` は毎回この全文を読んでいた。**見出しを `### T-XX` の ID のみに変えた**のは、全文スラッグのアンカーが壊れやすいことが実証されたため——`#t-33-カバレッジ計測に-tsx-を含めるかの検討` と `#t-39-…` を指す参照4か所が、T-33・T-39 を `closed_23` へ移した時点で既に切れていた（今回まとめて修正）。**台帳22 は横展開しない**——51行・2,544字・11エントリで1エントリ1行に収まり全体が1画面で読めるため、索引と本文に分ける利得が無く `ledger:move` の拡張コストだけが増える。あわせて**種別・優先度の語彙が実態と食い違っていた**ことが判明し、宣言側を実態へ寄せた（種別に `リファクタ`・`保守性`・`型安全`・`テスト補強`、優先度に `低` を追加。いずれも本文では既に使われていた）。**この宣言はその日のうちに作り直した**（次エントリ） |
| 2026-07-26 | **種別を「何に触るか＝着手時に何が網になるか」の6値（内部設計／型安全／テスト／ツール整備／依存追随／調査）へ作り直す。`負債返済`・`改善` は廃止** | 直前の追記で語彙を実態へ寄せた結果、**8値のうち機能していないものが露わになった**（オーナー指摘「負債返済や改善は23に起票されるなら当然であり種別の意味を為していない」）。実際、本書の守備範囲は「挙動を変えない技術活動」なので**全件が負債返済であり改善**で、この2語は何も区別しない。さらに `リファクタ`(6)・`保守性`(5)・`型安全`(3) の3分割も**着手時の行動が同じ**（既存テストを網に変異で振る舞い保存を確かめる）ため分ける意味がなかった。そこで軸を「何を良くするか」（主観・重複する）から「**何に触るか**」（客観・排他）へ変更。この軸なら各値が違う進め方を指す——`テスト`は本番コードに触れないので回帰の心配がなく、`ツール整備`は `scripts`/CI で**自動テストの網が無い**ので実行して確かめ、`依存追随`は上流待ちで自分では動かせず再確認の契機が要り、`調査`は成果物が決定そのもの。`型安全` だけは `内部設計` と行動が違う（コンパイラが確かめる／変異で確かめる）ため残した。28件を再分類した内訳は 内部設計11・型安全3・テスト5・ツール整備6・依存追随2・調査1。旧 `負債返済` の3件は T-02・T-05 が依存追随、T-49 が内部設計と**別物が同居していた**ことも分かった。あわせて `ledger:check` に種別・優先度の語彙検査を足し、廃止語が再び混入したら落ちるようにした |
| 2026-07-26 | **T-43 を2段に分けて進める。第1段（データ系＝`task()`・`routine()`・`at()`・基準日）を先に片付け、コンポーネントテストの作法系（`next/navigation` スタブ・`rowOf`・`hasClass`・jsdom スタブ・`clearAllMocks`）は第2段へ残す** | 着手時の再棚卸しで、9項目すべてを1PRでやると30ファイル超に触ることが判明した。分割の軸を「データ系／作法系」にしたのは、**両者で危険の質が違う**ため——データ系は既定値を変えるとテストの主張が静かに変わりうるので1件ずつ落ちる理由を追う必要があり（実際に隠れた依存が7件表面化した）、作法系は `vi.mock` のホイストという別の制約に律速される。段ごとにレビューを回した方が指摘が混ざらない。**起票時の一覧はマージ後に動いていた**——④`deferred()`（3→1）と⑤表操作ヘルパ（4→2）は並行ブランチ内で解消済み、`confirm` のスタブ手法も全4か所が `vi.spyOn` に揃っていた一方、⑥`hasClass` は 1→4、②`task()` は 22→24 に増えていた。「対象リストはマージ時に再棚卸しする」と起票時に書いておいたのが効いた |
| 2026-07-26 | **レビュー系サブエージェントを sonnet へ落とす案を見送り、`general-purpose` の使い方の方を正す（T-66）** | 利用枠の逼迫を受けて「opus 指定の3本（test-reviewer・code-quality-reviewer・refactor-scout）を sonnet へ」を検討したが、消費の内訳を実測したところレビュー系5本の合計が 11%、opus の3本で 9% しかなく、**節約が小さすぎて品質との取引が成立しない**と判明した。単独最大は `general-purpose` の 33%。あわせて、仮に落とすとしてもエージェントごとに向き不向きがあると整理した——**指摘がチェックリスト化で埋まる種類か否か**が分かれ目で、`spec-reviewer`（docs との突き合わせ）・`verifier`（コマンド実行）は条項として書けるので sonnet で足りる（実際に既に sonnet）。一方 `test-reviewer` の本体は「この検証は実装をどう壊しても緑のままではないか」という反実仮想の推論で、指示は §偽陽性防止 に書いてあっても*実行*が難しく、**見逃しが「指摘なし」として返るため正常な合格と区別できない**（沈黙する失敗）。T-43 で実際に出た指摘もこの形（既定値の中立化で異常系2本が同一入力になり主張が消えていた件）だった。よってサイズでも品質でも落とす先ではない |
| 2026-07-26 | **`verifier` から変異テストの責務を外す（T-67）。禁止条項の積み増しでは塞がないと判断** | 2026-07-26 に `verifier` が2度、作業ツリーを壊した（`git stash`＋`reset --hard` して戻さず終了 → その後スクリプトで変異を一括適用して戻さず終了）。**①のあとに「作業ツリーを変える git 操作をしない」をレビュー5本すべての定義へ足したうえで②が起きている**ため、条項を増やす対処は効いていないと見た。5本のうち書き込み権限を持つのは `verifier` だけで、その権限を要求しているのは変異テストだけ。責務ごと外せば、守られたかを監視する必要も消える。変異テストはメインセッションが Edit で行う——差分がチャットに出るので戻し忘れがオーナーから見える（スクリプト一括置換を禁じているのと同じ理由） |
| 2026-07-26 | **T-56 の見極めは「失敗を検出すべき」側に決まり、本書の守備範囲を外れるため [FB-70](./21_ユーザーフィードバック.md#fb-70) へ移管して T は完了にする** | T-56 は「型が存在しない失敗を宣言している（不要な抽象）」のか「存在しない id への更新を検出しないという設計判断が型に現れていない」のかを**着手時に見極める**チケットで、どちらに転ぶかで行き先が変わる作りだった。オーナーが後者と判断したため、対応は「戻り値の型を落とす」（挙動不変）ではなく「**存在しない id への更新を失敗として返す**」（挙動変更）になり、本書の守備範囲（挙動を変えない技術活動）を外れる。移管先を要件バックログ22 でなく FB管理簿21 にしたのは、①**詰める点が3つ残っており**（検出範囲・失敗の見え方・検出の実装層）分類 `改善（要仕様整理）` の定義そのものであること ②既存 F-XXX の範囲に収まるか新設かの判定自体が着手時の作業で、それを担う手順書が [guide_21](./guide_21_ユーザーフィードバック.md) にあること ③失敗の見え方が [FB-64](./21_ユーザーフィードバック.md#fb-64)（通信断で Server Action が失敗してもトーストもロールバックも出ない）と同じ論点で、**同じ簿の隣に置かないと「一緒に決める」判断が働かない**こと。「実運用の気づき」でなくレビュー由来だが、FB-58〜FB-69 も同じくレビュー・テスト作成由来で起票されており実態と整合する。**T-56 のような「見極めが先にあるチケット」は、見極めた瞬間に台帳をまたぐ**——起票時に行き先を1つに決めきれない場合、詳細節に分岐と移管先を書いておくと着手時に迷わない |
| 2026-07-26 | **エラー文言辞書の置き場を「画面配下か共有か」で分けるのをやめ、全部 `src/app/_lib/` へ寄せる（T-75 として起票）** | オーナー判断。[T-49](./closed_23_技術改善バックログ.md#t-49) は集約と同時に置き場の基準を「1画面でしか使わないものは画面配下・画面をまたぐものは共有」と置いたが、**その基準はファイル名から読み取れない**——`(daily)/_lib/error-messages.ts` と `_lib/routine-error-messages.ts` は「デイリー用とルーチン用」に見え、実際の軸（画面配下か共有か）と一致しない。さらに前者が後者を import しているため、ルーチン化の文言1つを追うのに2ファイルを行き来する。**基準そのものを捨てて1か所に集める方が、基準を名前に表す（`daily-error-messages.ts` へ改名する等）より安い**——文言辞書は presentation の表示都合そのもので画面をまたぐ流用が今後も起きるため、境界を引き直すたびにファイルが動く。着手を [T-74](./23_技術改善バックログ.md#t-74) の判断（辞書の選択をユースケース側へ寄せるか）に条件づけたのは、そちらで②を採ると**辞書の一部が presentation から消える**ため移動先が変わりうるから |
| 2026-07-26 | **T-74 は「辞書間の文言差を『複製して開始』専用辞書へ隔離する」で解く。①actions.ts をテストで固定・②辞書の選択をユースケースへ・③エラー型の直和化 はいずれも採らない** | 変異6（`startTaskAction` の辞書を `PUNCH_MESSAGES` → `OPERATION_MESSAGES` に差し替えても全段が緑）を精査すると、**2辞書の文言が食い違うのは `not_completed` の1コードだけ**（`OPERATION_MESSAGES` が F-208 向けに上書きしている）で、この差は「複製して開始」からのみ到達する。つまり差を当該アクション専用の辞書へ出せば、残る共有辞書は包含関係にあっても文言が一致し、**取り違えが観測可能な誤りでなくなる**（検出ではなく除去で解く）。①を採らなかったのは、`startTaskAction` の変異は差し替え先の文言が到達不能コードのものなので**そもそもテストで観測できない**——観測できるのは「複製して開始」側だけで、そこを固定するために §8 の境界を広げ DB 前提のテストを増やす代償が釣り合わない。②は表示文言（presentation の都合）を usecases 層へ持ち込むため層の責務に反する。③は意味的に共通な `task_not_found` / `already_started` を型の都合で分けることになり、全呼び出し元へ波及する割に不自然さが残る。再導入の検出は `error-messages.test.ts` に「共有コードで文言が一致する」不変条件テストを1本置いて担保する |
| 2026-07-26 | **T-74 の隔離では「専用辞書を打刻系で取り違える」1本が残ることを認め、それを塞ぐ案（操作種別→辞書の選択関数を `_lib` へ出して unit で固定）は T-76 として切り出す。T-74 は但し書き付きで閉じる** | 実装後の変異テストで、`DUPLICATE_AND_START_MESSAGES` を `undoCompleteAction` で誤って引いても `tsc --noEmit` も全テストも緑のまま文言が化けることが実測された（`PunchUsecaseError ⊆ TaskOperationError` の包含が専用辞書の側に残るため）。**T-74 が「構造的に消した」と言えるのは共有2辞書の間だけ**で、その範囲は元の変異6（`startTaskAction` の辞書差し替え）を無害化できている。残り1本まで同じ PR で塞ぐと方針の追加決定（選択関数の導入）になるため分けた。歯止めとして、取り違え先を1アクション専用の自己説明的な名前1つに限定し、辞書の doc に「型でもテストでも守れないため名前で示す」と明記してある。あわせて `ROUTINE_ERROR_MESSAGES` の命名軸のずれ（T-75 が公開名を変えない要件だったため据え置き）を T-77、`masters/_lib/action-result.ts` に残る同一文言の別辞書を T-78 として起票した。`usecases/task/operations.ts` への2件（`not_completed` を返すのは本ユースケースだけという前提のコメント・型の重複メンバーの畳み込み）は挙動不変かつ安いため T-74 の PR に含める |
| 2026-07-26 | **T-47 の範囲に `projection.ts` のローカル解釈（`dayStartAt`）を含める。`applyClockTime` だけに絞って残りを別 T にする案は採らない** | 台帳の背景が挙げていたのは `applyClockTime` だけだったが、実測すると `projection.ts` も `base.setHours(0,0,0,0)` / `now.getHours()` でローカル解釈しており、**セクション終了時刻・予想開始時刻という導出の土台がここにある**。片方だけ直すと「表示は JST 固定・入力解釈も JST 固定・導出だけローカル」というより読み取りにくい非対称になり、[T-62](./closed_23_技術改善バックログ.md#t-62)（CI の TZ 指定）が防ぎたい「手元で再現しない CI 落ち」の危険も導出側に残る。代償は `daily-list.test.tsx` の `atLocal` 9か所が `atJst` へ移ることで、**同ファイルを分割する [T-53](./23_技術改善バックログ.md#t-53) と衝突する**——4本並行の分配では T-47 を先にマージし T-53 を rebase する順序で調停する（ファイル集合が交わるので §1.3 の中間 rebase 省略は使わない） |
| 2026-07-27 | **T-37 の4論点を確定（判定対象＝機械判定のホワイトリスト／確認専用DBへ dev を向ける／T-03 とは環境だけ共有し T-37 を先に作る／起動はオーナー動作確認の直前）。あわせて `db:seed` は厚くせず確認用フィクスチャを別スクリプトで上乗せする** | 検討で前提が3つ動いた。①**Playwright はリポジトリの依存に入っていない**——FB-51 の実測は MCP のブラウザツールで行ったもので、T-37 は依存を1つも足さずに作れる。逆に T-03 と[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 が名前だけ予約している「ブラウザテスト段（`*.browser.test.tsx`）」は `@playwright/test` か vitest browser mode の導入が要る。したがって台帳が書いていた「片方を作ればもう片方がその上に乗る」重なりは、実際には**駆動方法ではなく向き先の環境（dev サーバとDB）だけ**だった ②台帳の論点は「実データを**壊さない**担保」だけを挙げていたが、開発DB（:5432）は `db:sync-masters` で本番のマスタ・ルーチンが入るため、**実名がエージェントの報告やスクリーンショット経由で public リポジトリへ流出する方が事故として重い**。破壊禁止の規約では防ぎきれないので、使い捨ての `db-test`（:5433・tmpfs）内に別DB名で確認用DBを作り dev をそちらへ向ける（T-34 の「どのDBを向けるか」もここで解ける）③その確認用DBのデータを `db:seed` で賄う案は採らない——`db:seed` は本番の初期投入にも使い[データモデル定義書](../仕様/14_データモデル定義書.md) §5 が正で、`projects` は空と定めている。確認用に要るデータ（画面下部までスクロールする程度のタスク量・候補の十分なマスタ）は**確認観点に従属して変わる**もので §5 の契約とは寿命が違い、分けておけばダミーが本番へ流れる事故も構造的に防げる。判定対象を機械判定に限るのは「確認したことになっている」状態を作らないため——知覚の判断（FB-52 の色・FB-51 のラベル折り返し）はオーナーの実機確認でしか決まらないので、**本エージェントはそれを置き換えず手前を潰す道具**と位置づけ、報告に「測っていないこと」を書かせる |
| 2026-07-27 | **T-03 の主題を「E2E 導入の検討」から「ブラウザテスト段（vitest browser mode）の導入」へ移し、E2E は T-85 として分ける。優先度も 様子見 → 中 へ上げる** | T-37 の完了後、オーナーの問い「MCP 経由と Playwright の依存追加はどちらが有用か」を整理して出た結論。**`@playwright/test` を足すこと＝E2E ではない**——[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §8 が予約している段3（`*.browser.test.tsx`）は vitest browser mode で部品を実ブラウザに描くだけで、**dev サーバもDBも要らない**。にもかかわらず T-03 が E2E と同じ器に入っていたため、格段に安い段3 の着手条件まで E2E の重さに引き上げられていた（「必要性を運用で見極めてから」）。実害の側から見ると逆で、**幾何・スクロールの不具合は気づきにくく既に FB-51 で1回出ている**のに対し、E2E が守る `page.tsx` は大半が薄く、壊れれば画面が出ないので手動確認で必ず気づく。したがって段3 を先に、E2E は後に置く。**T-37 との関係も書き換わる**——「T-03 と共有するのは環境だけ」と整理した確認用DB・dev サーバを実際に使うのは E2E（T-85）の方で、段3 は何も共有しない。MCP 経由（T-37）と自動テスト（T-03 / T-85）の違いは**探索か回帰か**で、前者は測る対象を決めずに始められる代わりに起動しなければ測られず、後者は逆。FB-51 で仮説3つが全部外れた場面に効いたのは前者、同じ不具合を二度出さないのは後者の仕事 |
| 2026-07-26 | **docs の品質維持を3層＋契機ベース監査で仕組み化する**（オーナー採用）。層1＝機械検査の拡張（T-86 起票。リンク・アンカー・台帳状態列・log_ の表構造を CI で常時検査）、層2＝`spec-reviewer` に docs 品質2項を追加（重複規定・経緯混入を差分時に止める）、層3＝再編時の参照追随を CLAUDE.md「書き方の規約」に1項追加。全docs監査は定常化せず**契機ベース**（再編直後・機能の節目・台帳肥大の兆候）で CLAUDE.md「起動の契機」に載せた | 2026-07-26 の品質監査（127件）の内訳で、機械判定できる劣化（アンカー切れ35件・移し忘れ・表構造）が量の過半かつ増殖性を持ち、「定期監査のみ」では次回の監査が今回より重くなると判断した。意味的な劣化（全文重複・矛盾・経緯混入）は差分時レビュー（層2）で発生時に止め、それでも漏れた分だけを監査が拾う分担にする。監査のコスト実測は Explore 3本並列で約39万トークン——毎週回す道具ではなく節目の道具 |
| 2026-07-27 | **`npm run typecheck`（`tsc --noEmit`）を足し CI と `verifier` の手順に載せる。既存の型エラー10件は `hold` の呼び出しへ明示型引数を与えて解消する（T-69）** | 検査を足すと CI が即赤になるため①（検査）と②（既存エラーの解消）を同じ変更に入れた。②の実装は台帳が併記していた2案のうち**明示型引数**を採った——もう一方の「`resolve` を広い型で受ける」は、狭まった `T` が `resolve` だけでなく `promise` の型でもある点を見落としている。`resolve` だけ広げると保留の promise が「成功しか返さない」と宣言したまま失敗値を流すことになり、**嘘の位置が変わるだけで消えない**。narrowing の原因（成功値定数が `const OK: DailyActionResult = { ok: true }` のように宣言型より狭い初期値を持ち、CFA が `T` を `{ ok: true }` へ狭める）は `hold` の doc に残し、型引数を省略しない規約を明記した。検査の置き場を `lint` と `build` の間にしたのは、`build` より速く落ちる方が待ち時間が短いため。**T-81（同ファイルのテスト作法）は巻き込まなかった**——当初 T-81 の着手条件は「T-69 と同時に」だったが、型は呼び出し側の型引数だけで通り、列位置の直参照・独自フィクスチャとは独立だったため、条件を書き換えて T-81 は据え置いた |
