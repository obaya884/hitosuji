# 要件定義書 決定ログ

方式・要件レベルの判断を日付順に残す。仕様そのものは[要件定義書](./要件定義書.md)が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-16 | 日界はカレンダー日付（0:00）固定とし、将来導入できるデータ設計だけ維持する | MVP のスコープを絞るため（データモデル定義書 §1） |
| 2026-07-16 | セクションの既定値を 朝/午前/午後/夜/深夜 の5区分とする | 汎用的な初期データとして投入（データモデル定義書 §5） |
| 2026-07-16 | 実行中タスクがある状態で別タスクを開始したら割り込みとして扱う | 同時実行を1件に保ちつつ、残りを後で再開できるようにするため（§5.2 F-201） |
| 2026-07-17 | 詳細要件書（詳細設計書）を作成しない | 本書＋データモデル定義書＋画面定義書を挙動の契約とし、実装の形はコードに委ねる |
| 2026-07-19 | ショートカット一覧を確定（画面定義書01 §6） | 設計方針＝選択行モデル＋単キーのニーモニック、修飾キーは Shift のみ |
| 2026-07-19 | コールドスタートは実測 1.4秒 / 0.2〜0.4秒で許容と判断 | N-01 は高頻度操作の即応が本質であり、初回1回の遅延は許容できるため |
| 2026-07-20 | S-02・S-03 は楽観的更新の対象外とする | 低頻度操作であり、保存完了を待って反映するほうが単純なため |
| 2026-07-21 | 全画面共通のUIを画面定義書00 に集約 | 要件定義書 §4 と画面定義書01 に分散し、S-04 追加時に再定義が要る状態だったため |
| 2026-07-21 | 先送り数（F-502）を「指定日に残っている未実行タスクの件数」と定義し、先送りの履歴は持たない | 他日へ移した分まで数えるには移動履歴のテーブルが必要になるが、過去分は遡れず、指標としては「やると決めてやらなかった数」が読めれば足りるため。データモデルを変えずに済む定義を選んだ |
| 2026-07-21 | `postponed_count` は記録のみとし、F-502 の集計には使わない（列は残す） | 先送り数の定義を「その日に残っている未実行タスク」にしたため参照先がなくなったが、通算の先送り回数は導出も遡及もできない事実であり、記録を止めると将来（常習的な先送りの可視化・F-505）に取り返せないため |
| 2026-07-21 | S-04 は日次を先に完成させ、週次・月次の切替は後続とする | 日次の振り返りが日課であり、週月の集計は実運用で必要性を確かめてからでよいため |
| 2026-07-21 | F-110 の時間合計の分子を「完了は実績・未完了は見積もり」の合算とし、ラベルを「合計」にする（実行中は見積もりで数え、完了時に実績へ切り替わる） | 残り時間表示で過去セクションも見出しが残るようになり、完了済みを「見積」で足すと実態とズレる。完了を実績にすれば「実際に使った時間＋これからの予定」を1数字で読める。詳細は画面定義書01 §3.2 |
| 2026-07-21 | 終了予定時刻（F-104）・セクション残り時間（F-110）の**式の1次情報をデータモデル定義書 §4.3 に一本化**し、要件定義書・画面定義書は要約＋参照に統一 | 同じ式が要件・データモデル・画面の3箇所に重複し、改訂のたび食い違う恐れがあった。書き方の規約「詳細は下位文書に1か所」に揃える。式は導出ロジックなので下位（データモデル）が本拠、表示の符号・色・表示条件は画面定義書が持つ |
| 2026-07-21 | S-04 の週・月切替（F-506 として分離）・N-08・Playwright による E2E 導入検討を Phase 3 へ移す | いずれも日々の体験に直結せず、必要性を運用で見極めてからでよいと判断したため（優先度変更の経緯は要求定義書 §10）。実運用テストは区切りのない継続活動のため実装計画での状況管理対象から外し、気づきはFB管理簿へ随時起票する |
| 2026-07-21 | F-210（開始打刻の取り消し）の `U` は「実行中タスクを選択中なら開始取消、それ以外は削除取消」で切り分ける（FB-37） | Undo の語感を保ちつつ削除の取り消し（O-8）を乗っ取らないため。実行中は最大1件で選択も容易。戻したタスクは開始前の位置を復元せず未実行として並べ直す（クライアントに状態を持たず再読込に強い）。割り込み時に直前タスク・再開タスクへ波及させないのは、開始の取り消しを1タスクの局所操作に留め実装と挙動を単純に保つため |
| 2026-07-21 | F-210 の戻し先を「今日は現在時刻を含むセクションの先頭、今日以外は並べ直さず打刻クリアのみ」と定める（FB-37 セルフレビュー） | 実行中を取り消すと現在位置が消え「これから領域の先頭」の参照が宙づりになるため、現在時刻のセクション先頭と明示。過去日・未来日は現在位置・これから領域が定義できないので並べ直しをしない |
| 2026-07-22 | 日界（F-116）を Phase 2 の Should として追加。影響範囲は「今日」の解決とセクション回転に閉じ、`task_date`（保存論理日付）は変えない | 従来「0:00 固定・データ設計だけ維持」の保留だった（本表 2026-07-16）。日跨ぎ挙動の確認を機に要求化（要求定義書 §10）。データ設計は導入済み（データモデル §1）なので、実装は表示日・展開対象日・終了予定対象日の解決とセクション表示順の起点付け替えに限定できる。設定UI・境界時刻の詳細は着手時に画面定義書へ |
| 2026-07-22 | F-116 の日界を「有効セクションのうち1つを日界セクションに指定」する方式に確定（日界時刻＝そのセクションの `start_time`）。既定は深夜(00:00)で現状の 0:00 固定を踏襲 | 日界を任意時刻にするとセクションの途中に境界が落ち、今日/前日の帰属や回転の起点が二重定義になり得た。セクション境界に一致させれば「有効セクションは24時間を敷き詰める」既存の枠組みに乗り、どのセクションも分割されない。オーナーもセクション選択方式を選好（データモデル §3.1 / 画面定義書03 §3.1・01 §3.1・§3.2） |
| 2026-07-22 | 日付表示クリックからのカレンダー日付ジャンプを F-117（Could）として F-106 から分離し §5.1・§3 を付け替え（FB-39） | datepicker は F-106 の要件（§5.1）・画面定義書01 §3.1 に含まれていたが未実装。基本ナビ（前後日・今日）は実装済みで Must のまま、頻度の低い遠距離ジャンプだけを Could として切り出し優先度を実態に合わせた。画面定義書01 §3.1 の対応要求も F-106 → F-117 へ付け替え済み。挙動そのものは既存仕様のままで変えていない |
| 2026-07-23 | 開発案件の管理単位をフェーズ（Phase 1〜3）から「決定状態＋着手トリガの台帳」へ移行する。§3「開発フェーズ」を「開発の現況」に縮約し、未実装要求のライブ台帳を新設の[要件バックログ](./要件バックログ.md)へ移す（旧 実装計画.md は MVP 期のフェーズ運用の史実アーカイブとして凍結）。決定状態は 確定・着手待ち／候補・見極め中／監視項目 の3つ、見送りの出口は要求定義書 §8・§10。あわせて技術軸のファイル名も 技術改善計画 → 技術改善バックログ に統一 | フェーズはリリース波（launch までに何をどの順で出すか）の抽象で、MVP へ向かう間は現実の作業順と一致していた。しかし 2026-07-20 の実運用開始後は作業が波でなく運用の気づきから随時流入するようになり、閉じたはずの Phase 2 に Should（F-118）が追加される・Phase 3 が「実施可否を運用で見極め中の候補プール」と化して番号が実態を表さない等、箱が現実と噛み合わなくなった。すでに機能している技術改善バックログ（T-XX）の非フェーズ型台帳と同型に揃える。ファイル名は launch 期の語彙「実装計画」を凍結名として残し、継続運用の語彙「バックログ」を新ファイルに与えることで役割の転換を名前にも表す。**MVP 期にフェーズ運用していた事実・各フェーズの対象要求と成果は実装計画「完了記録」に史実アーカイブとして残す**（フェーズ語はそこにのみ用いる） |
