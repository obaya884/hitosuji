# マスタ管理 決定ログ

本文に書かない「なぜそうしたか」を日付順に残す。仕様そのものは[マスタ管理](./03_マスタ管理.md)が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-20 | モード一覧のカラーバー横に色名を併記（FB-12） | 画面上では見分けにくい配色があり、どの色を使ったかを言葉で確認したいため |
| 2026-07-20 | 一覧の取得を名前順に揃える（FB-01） | S-01 のモード選択ポップオーバーだけ未ソートで並びが違っていたため |
| 2026-07-20 | 編集をセルのインライン編集に変更（FB-03） | 「編集」ボタンを都度押すのが面倒だったため |
| 2026-07-20 | カラーバー押下で色選択を開く（FB-03） | 名前の編集を経由せず色だけ変えられるようにするため |
| 2026-07-20 | 色の候補にホバーで色名の吹き出しを出す（FB-03） | 色だけでは選びにくく、ブラウザ標準の `title` は表示まで待たされるため |
| 2026-07-20 | 保存中は同じ行の他のセルを編集させない（FB-03 のレビュー指摘） | 更新は行の全項目を送るため、応答前に別のセルを触ると古い値で上書きしてしまう |
| 2026-07-22 | セクションタブに日界セクションのラジオ選択を追加し、日界セクションはアーカイブ不可にする（F-116） | 日界は「どのセクションが1日の開始か」の選択であり、セクション一覧に置くのが自然（オーナー選好）。ラジオで1件だけ選べるようにし、選択中セクションのアーカイブは「有効最低1件」と同種の破壊を招くため非活性にして先に付け替えさせる。マスタ画面は N-01 対象外のため保存待ち反映で扱う |
| 2026-07-22 | マスタ一覧のセクションは暦日 `start_time` 昇順のまま（デイリーの回転表示と分ける）（F-116） | マスタ設定はどのセクションが何時開始かを一望する場で、ここまで回転させると設定時に対応が分かりにくい。回転は利用の主画面であるデイリーだけに効かせる |
| 2026-07-30 | §3.1 の日界列から「行内に『1日の開始』等のラベルで現在の日界を示す」を外し、**ラジオの選択状態で示す**と実態どおりに改める（FB-62） | 日界は行ごとに1つのラジオで選ぶ排他選択であり、選択状態そのものが「どの行が1日の開始か」を表している。ラベル文字列を足しても伝える情報は同じで、列見出し「日界」・一覧上部の説明文（「先頭のラジオで『1日の開始（日界）』になるセクションを選べます」）・`aria-label` と合わせて四重になる。装飾を足さない方針（N-05）にも沿うため、実装を足す①ではなく docs を実態に合わせる②を採った |
| 2026-08-09 | 名前の**50文字上限を撤廃**し、§3.1〜§3.3 の各「名前」欄を §4「名前の入力」への参照に畳んだ（[FB-71](../../案件/closed_21_ユーザーフィードバック.md#fb-71)） | FB-71（ルーチン化だけ理由の分からない汎用メッセージが出る）を調べる過程で、**この上限に根拠が無いことが分かった**。DB は `text` で長さ制約を持たず、50 という数字の出所は本書 §3 の入力欄仕様だけで、決定ログにも由来が残っていない。単一ユーザーのアプリで長い名前を入れるのはオーナー自身であり、機械が止める理由がない——表示が破綻する長さは入力の制限ではなく表示側で吸収する。**ルーチン名も同時に上限が消える**（実装が `domain/shared/master-name.ts` を共有していたため、条項の無いルーチン側にも副作用で50文字が効いていた。画面定義書02 §4）。これでタスク名（制限なし）との非対称も消え、FB-71 の症状そのものが起きなくなる。空・空白のみを不可とする規則だけは残す（名前の無いマスタは選択肢として意味を成さないため）。**極端に長い名前を入れたときの一覧の見え方（折り返し・省略）は条項を起こさない**——`spec-reviewer` が「上限を外すなら表示側の受け皿が要るのでは」と指摘したが、オーナーが動作確認のうえ「実運用でとても長い名前は入れない」と判断した。実際に困ってから決める |
| 2026-08-30 | タブの並びを **セクション → プロジェクト → モード** に変え、§3 の節番号は据え置いた（[FB-110](../../案件/closed_21_ユーザーフィードバック.md#fb-110)） | 列の並びは デイリー（01 §3.3）・ルーチン一覧（02 §3）・レビュー（04 §3.3）で「何を／どの件で〈プロジェクト〉／どういう性質か〈モード〉」の順に揃えてある（[FB-102](../../案件/closed_21_ユーザーフィードバック.md#fb-102)）のに、マスタ画面のタブだけが逆向きに残っていた。マスタ画面は3つの分類軸そのものを並べる場なので、他画面で読む順と揃っている方が対応が取りやすい。**§3.2（モード）/ §3.3（プロジェクト）の節番号は入れ替えない**——§3.2 は docs・実装のコメント・テスト名から約30箇所で参照されており、タブの並びという表示上の理由で改番すると追随の量に見合わない。代わりに §3 のリード文で「節の並びはタブ順ではない」と明示した（オーナー裁定） |
