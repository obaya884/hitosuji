# レビュー 決定ログ

本文に書かない「なぜそうしたか」を日付順に残す。仕様そのものは[レビュー](./04_レビュー.md)が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-21 | 実績ログは実行済みタスクのみを時系列で並べ、セクションでグループ化しない | セクション区切りで未実行も含めると S-01 の過去日表示とほぼ同じ画面になり、別画面にする意味がなくなるため |
| 2026-07-21 | 先送り数は「表示日に残っている未実行タスクの件数」とする | 他日へ移した分まで数えるには移動履歴が要るが、過去は遡れない。理由は要件定義書 §8 決定ログ |
| 2026-07-21 | 今日以降の日付では先送り数を出さない | まだ実行されうるタスクを先送りと表示すると、指標として読めなくなるため |
| 2026-07-21 | 集計は数表のみとし、グラフを作らない | N-05。まず数字で足りるか運用で確かめる |
| 2026-07-21 | CSVエクスポート（F-504）を本画面の初期スコープから外す | 要求を Could へ引き下げたため（要求定義書 §10 決定ログ） |
| 2026-07-21 | 週・月の期間切替は日次の完成後に追加する | 日次の振り返りが日課であり、週月は必要性を運用で確かめてからでよいため |
| 2026-07-21 | 週・月の期間集計を F-506 として分離し Phase 3 へ移す | 日次が稼働して当面の振り返りは足りており、週月は Could として必要になってから着手する（要求定義書 §10・要件定義書 §8） |
| 2026-07-26 | **F-506（週・月の期間集計）の先行仕様を本文から外し、要件バックログ管理へ退避**（オーナー裁定）。退避した仕様案: 期間切替は日／週（月曜始まり）／月・日付ナビとショートカットは表示中の期間の単位で動く・実績ログの週・月表示は日付列を先頭に足し日をまたいで時系列・先送りの週・月は期間内の各日の合計で今日以降の日は含めない。着手時はこの仕様案を起点に本文へ再展開する | 未実装の詳細仕様が稼働中の記述と混ざると、実装との乖離をレビューで指摘できなくなる（guide_21 完了チェック3と同じ理屈）。要件バックログの F-506 は熟度を「設計済」に改めた |
| 2026-07-26 | §2 の「S-01 と同じ書式」に「列の構成・並びは §3.3 が正」と限定を付けた | 実績ログの列順（モード → プロジェクト）は実装・本書 §3.3 と一致しているが、S-01 のタスク行はプロジェクト → モード順（01 §3.3 に順序の理由あり）のため、「同じ書式」が列順まで同じと読める記述が食い違いに見えていた（品質監査で検出・オーナー確認済み） |
| 2026-08-01 | §3.3 の「見積もり未設定 ＝ S-01 と同じく薄色で `--:--`」を[共通](./00_共通.md) §2.4 への委譲に改め、隣接する「モード・プロジェクト未設定」の行と1行に束ねた（オーナー裁定） | 未設定表記の実体は 00_共通 §2.4 が持っており、同じ規則を §3.3 が二重に書いていた（**規則の内容は同一で挙動は変わらない**）。隣の行は既に委譲済みで、片方だけ実体を持つ非対称でもあった。発端は T-41（コード内コメントの参照先を実体へ向け直す作業）で、`review-board.tsx` のコメントが指す 04 §3.3 が「完全な委譲スタブではない」ため向け先を決められなかったこと——**コメントを直す前に docs 側の重複を解消する**順序を採った。なお 01 §3.3 の見積もり列にも同種の記述が残るが、そちらは「終了予定時刻の計算に含まれない」という画面固有の理由を伴うため、今回は触っていない |
| 2026-08-01 | 実績ログのコメント（F-206）は**列にせず行の下に全文表示**する（§3.3）。従来の予約案「コメント列を追加する」を差し替えた | コメントに長さの上限を設けない（要求定義書 F-206）と決めたため、固定幅の列では大半が切れて読めない。この画面はコメントを読み返すための場所（用途は「実施の記録・気づき」）なので、切り詰まる形にする意味がない。既に7列あるところへ8列目を足すと横も苦しい。デイリー（画面定義書01 §3.3）の選択行と同じ見せ方に揃うので、書いたときと読むときで見え方が変わらない利点もある |
| 2026-08-01 | コメントの**折り返し幅をタスク名列に揃える**（当初は行の全幅）。判断の理由はデイリー側の[決定ログ](./log_01_デイリーリスト.md)（同日）が持つ | オーナーの動作確認を受けてデイリーを直したのに合わせた。この画面だけ全幅のままにすると、コメントの長さで右側の列（モード・プロジェクト・時間）の見え方が変わるという同じ問題が残る。**揃えたのは規則（タスク名列に収める）であって実測の幅ではない**——2画面は列構成が違い、タスク名列はどちらも「残りを取る可変列」なので幅は一致しない。幅まで合わせるには両テーブルの固定列の合計を揃える必要があるが、そこまでする理由は無いと判断した |
