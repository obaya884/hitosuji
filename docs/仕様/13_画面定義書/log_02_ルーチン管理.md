# ルーチン管理 決定ログ

本文に書かない「なぜそうしたか」を日付順に残す。仕様そのものは[ルーチン管理](./02_ルーチン管理.md)が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-20 | 一覧にモード・プロジェクト列を追加し、列順を 名前/モード/プロジェクト/見積/繰り返し/開始想定/有効/操作 とする（FB-09 / FB-13） | 展開後にどう分類されるかを一覧で確認したい。チェックボックスは属性を読んだ後に触る列として右端側へ寄せる |
| 2026-07-20 | ルーチンの見積もりは1分以上を必須とする | S-01 のタスクは0分＝未設定を許容するが、ルーチンは雛形であり見積もりのないタスクを生み続けてしまうため |
| 2026-07-20 | 列見出しクリックでの並べ替えを追加（FB-10 / F-306） | ルーチンが増えると開始想定時刻順だけでは棚卸しできないため |
| 2026-07-20 | 並び順を記憶しない | 画面を開き直したときに意図せず並びが変わっている事故を避けるため |
| 2026-07-20 | 一覧の表示にはアーカイブ済みマスタも使う（FB-19） | 参照中のモード・プロジェクト名が空欄になっていたため（編集フォームの選択肢は有効なマスタのみ） |
| 2026-07-25 | 週次フォームに「週間隔」入力を追加、要約表示を 1=週次 / 2=隔週 / n=n週ごと に出し分け（FB-44 / F-301） | 隔週の曜日ルーチンを表現するため。曜日チェックの直下に週間隔を置き、週次のときだけ出す。詳細な起算・判定はデータモデル定義書 §3.4・§4.1 が正 |
| 2026-07-25 | 週間隔の入力上限を 53 にし、数値入力をスピナー（type=number・クリックで±1）に統一（FB-44） | 上限53はデータモデル定義書 §3.4 の値域に合わせる。数値入力の操作感をルーチン化モーダル（画面定義書01 §4.1）と揃えるオーナー要望 |
| 2026-07-25 | 週次の曜日入力に「平日」「土日」のプリセットボタンを置き、要約表示は曜日がプリセットとちょうど一致するときだけ「週次(平日)」等に置き換える（FB-53 / F-301） | 平日ルーチンのたびに曜日を5つ手で選ぶのが手間というオーナーの気づき。**繰り返し種別に「平日」を足す案は採らない**——`recurrence_type` に新値が増えて展開判定・要約表示・マイグレーションに波及し、`weekdays` ビットマスクで表せるものをデータ構造の分岐で表すことになるため。プリセットは入力補助に徹し、押したあとに個別チェックで微調整できる利点も残る（データモデル定義書 §3.4 は無変更）。要約を「ちょうど一致のときだけ」に限ったのは、月〜金＋土のような場合に「平日＋土」等の合成表記を作ると規則が増えるため。土日プリセットは平日と対で置くオーナー判断（使用実績は未知だが、対になっていないほうが不自然）。曜日チェックが出る場所には同じ補助を置く方針とし、デイリーのルーチン化ポップオーバー（画面定義書01 §4.1）にも同じボタンを置く |
| 2026-08-01 | 要約表示から「全曜日は列挙する」という例外を外し、正規化の結果に従わせた。§4 の曜日欄の入力仕様にも「7つ選ぶと『毎日』として保存される」挙動を書き足した（画面に注記文言を出すのではなく、条項として記述する）（FB-56 / F-301） | 週間隔1の全曜日は保存時に `daily` へ寄る（データモデル定義書 §3.4）ため、要約表示側に分岐を足す必要がなくなった（`describeRecurrence` は無変更で、正規化後の値を素直に「毎日」と出す）。週間隔2以上の全曜日は正規化されず weekly のまま残るので列挙が続く。フォームに注記を置いたのは、保存後に編集フォームを開き直すと種別が「毎日」に変わって曜日チェックが消えるため——入力中に種別を切り替えない選択の裏返しで、驚きを文書側で引き受ける。正規化そのものの判断はデータモデル定義書の決定ログが正 |
| 2026-08-08 | 一覧の列順を **名前 / モード / プロジェクト / 繰り返し / 見積 / 開始想定 / 有効 / 操作** に変更（見積と繰り返しを入れ替え）。並べ替えの対象列（F-306）と有効トグルの位置は変えない（FB-92） | [FB-91](../../案件/closed_21_ユーザーフィードバック.md#fb-91) で本文に主従の大小がついた結果、この画面だけ **主・従・従・主・従・主** と交互になり、視線が段差を何度も越える形になった（オーナーが動作確認中に気づいた）。入れ替えると **主・従・従・従・主・主** で、文言の塊と数値の塊に分かれる。**列順は 2026-07-20 の FB-09 / FB-13 で「展開後にどう分類されるかを一覧で確認したい」として決めたもの**で、その意図（分類の3列を名前の直後に置く）は入れ替え後も保たれる——動くのは見積が分類の右へ回る点だけ。**見積を右端側へ寄せる案は採らない**（有効トグルと操作列の手前に数値が2つ並ぶ形は維持する。開始想定は展開時刻としてセクション名の併記を持ち、見積と近い位置で読めるほうが棚卸ししやすい） |
| 2026-08-09 | §4 の名前欄に「文字数の上限は設けない」と明記した（[FB-71](../../案件/closed_21_ユーザーフィードバック.md#fb-71)） | 本書はもともと文字数を規定していなかったが、実装がマスタ共通の名前検証（`domain/shared/master-name.ts`）を流用していたため、**条項の無い50文字上限が事実上効いていた**。上限の撤廃そのものの理由は[log_03_マスタ管理](./log_03_マスタ管理.md)（2026-08-09）が正。ここで明記するのは、規定が無いままだと同じ流用が再び起きるため——ルーチン名は生成タスクのタスク名になるので、制限を持たないタスク名（画面定義書01 §8）と揃うことが要件 |
| 2026-08-09 | 一覧に**バンドル列を名前の直後**に追加し、並べ替え（F-306）の対象にも入れた。編集フォームにもバンドルのセレクトを足した（F-119） | 列順は 2026-08-08 で決めた「文言の塊と数値の塊に分ける」原則に従う——バンドルは分類の列なので、モード・プロジェクト・繰り返しと連続する位置に入れる。名前の直後にしたのは、束ねの所属が分類の中でも最も粗い括りだから。並べ替えに入れたのは、バンドル順に並べて繰り返し列を見れば運用ルール（同じ繰り返しで揃える）の点検になるため。**編集フォームのセレクトは他バンドル所属からの付け替えを許す**——[05](./05_バンドル管理.md) O-5 が候補を未所属に絞るのは「バンドルから見ると相手が黙って抜ける」ためで、ルーチン1件の所属を決めているこの場では当のルーチンが対象だと分かっている |
| 2026-08-14 | 一覧の列順を **名前 / プロジェクト / モード / 繰り返し / 開始想定 / 見積 / バンドル / 有効 / 操作** に変更し、§3 のリード文の根拠を「文言の列と数値の列をそれぞれ隣り合わせる」から「**ルーチンを読む順**」へ差し替えた。§3.1 の対象列の記述順・§4 の項目順も同じ順に合わせ、有効トグルの位置は「開始想定の右」から「**操作列の左**」という相対の書き方へ直した（[FB-102](../../案件/closed_21_ユーザーフィードバック.md#fb-102)） | オーナーが実運用で「読む順と合わず、バンドルが名前の直後を占めている」と判定した。動いたのは3点——①バンドルが名前の直後から数値の右へ ②プロジェクトとモードが入れ替わる ③開始想定と見積が入れ替わる。**2026-08-08（FB-92）の「文言の塊と数値の塊に分ける」原則は落とす**（オーナー裁定）——バンドルを例外として残す書き方も検討したが、原則と例外の2段構えにするより、列順の根拠を「何を〈名前〉／どの件で〈プロジェクト〉／どういう性質か〈モード〉／どの頻度で〈繰り返し〉／いつ〈開始想定〉／どれだけ〈見積〉」という読む順1本に統一するほうが、以後の列の追加でも判断が割れない。前半3列はデイリー（[01](./01_デイリーリスト.md) §3.3）と同じ並びになり、**同日にレビュー（[04](./04_レビュー.md) §3.3）も揃えた**ので3画面が一致する。バンドルを末尾へ回したのは、この列が「ルーチンから見たバンドル」の確認用（束ね方の編集は [05](./05_バンドル管理.md)）で、読む順の流れに乗らないため。**§4 の項目順も列順に合わせる**のはオーナー裁定——同じ画面で一覧とフォームが違う順に並ぶ理由がない。開始日・終了日は列を持たないが繰り返しの期間なので、繰り返しの入力群に続けたまま置いた |
