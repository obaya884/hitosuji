# デイリーリスト 決定ログ

本文に書かない「なぜそうしたか」を日付順に残す。仕様そのものは[デイリーリスト](./01_デイリーリスト.md)が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-25 | 現在セクションの強調色（`band-now` / F-121）を紺藍寄りの淡色 `#dde5f2` から、`band` を暖色側へ一段濃くした `#ead9d0`（粘土寄り）へ変更（FB-52 動作確認） | 当初は「アクセント＝紺藍」の体系に沿えて淡い寒色を採ったが、実機でオーナー判定が「思ったより見づらい」。淡い寒色は `paper`・`band`・`accent-weak` がいずれも高明度で密集する帯域に入り、面として沈まないため差が読み取れなかった。**濃さ（明度差）で沈ませ、暖色側へ振って `band` との同系性を保つ**方針に変えた。紅系統はパレット上 `danger` に予約されており、同じ見出し行の残り時間がマイナス時に警告色になるため、`danger-weak`（`#f7ece8`）とは明度・彩度で離して警告と読めない位置に置く。`accent-weak` を避ける理由（選択行の面色と同色になる）は変わらない |
| 2026-07-25 | 保留1スロットの「後から来た操作が前の保留を置き換える」が**新たな削除（`D`）のときだけ到達可能**であることを O-13 に明記した（F-212 動作確認） | 保留があるあいだ `U` はその解決に吸われる（保留優先）ため、新たな完了の取り消しは発生しえず、`U` 経由で保留が置き換わることはない。置き換えの記述だけを読むと「完了取消でも置き換わる」と取れ、実際にヘッドが到達不能な動作確認手順を書いた。オーナー判断は「トースト表示中は `U` が保留の解決にロックされるのが自然」で挙動は変えず、記述の精度だけを上げた |
| 2026-07-25 | 打刻の取り消し（O-13 / O-15、要件定義書 F-210 / F-212）の戻し位置の表現を「現在時刻を含むセクションの先頭」から「**現在位置**（§4.2 の定義）」へ統一した | 下位のデータモデル定義書 §4.5/§4.7 は当初から「§4.2 の現在位置。実行中タスクが別にあればその直後」と書いていたのに対し、上位の要約が「セクションの先頭」と読める書き方で、F-212 の実装時に「どちらに従うか」の判断点として浮上した（実装は §4.7 に従っている）。開始の取り消し（F-210）では対象自身が唯一の実行中タスクなので両者の差が出ず、緩い要約でも動いていた。完了の取り消し（F-212）は他に実行中タスクがありうるため差が顕在化する——先頭に置くと §4.2-b の繰り下げが次の表示で同じ位置へ動かし、行が跳ねて見える。O-13 / F-210 側の同じ表現も同時に直したのは、同じ規則が2通りの書き方で並ぶ状態を残さないため |
| 2026-07-19 | 行に余白を確保し、詰めすぎない | 視認性（N-05） |
| 2026-07-19 | 列見出し行を置く | どの数字が何かを一目で分かるようにするため |
| 2026-07-19 | モード色をカラーバーではなく行全体の文字色に反映 | カラーバーより情報密度を上げられるため |
| 2026-07-19 | 日付表示は表示日によらず同じ見た目 | 日付自体を着色すると視線が散るため |
| 2026-07-19 | 有効なセクション・未分類は0件でも常に表示 | 1日の枠組みを俯瞰でき、移動先としても要るため |
| 2026-07-21 | 並び替えのドラッグ＆ドロップを廃止し、`Shift+J/K` に一本化（FB-27） | 実運用でまったく使わなかった。キーボード完結（N-04）で足り、行のドラッグ属性・ドロップ判定はインライン編集や選択と干渉しうるため |
| 2026-07-21 | 選択移動・並び替えの矢印キー（`↑↓`・`Shift+↑↓`）を廃止し、`J/K`・`Shift+J/K` に限定（FB-33） | HJKL 体系に統一する。矢印との二本立ては覚える対象を増やすだけで、片方しか使っていなかったため |
| 2026-07-21 | タスク0件のグループは見出しを時間帯までに切り詰め、「タスクなし」行も置かない（FB-25・FB-26） | `0/0` や `見積 --:--` は情報がないのに視線を取る。空セクションは「枠だけがある」と分かれば足りるため |
| 2026-07-21 | セクション見出しの残り時間を静的な過不足から「現在時刻から見た残り」へ再設計。溢れは `-`・警告色、表示は今日かつ終了前のみ（FB-34・FB-31・FB-32） | 静的な過不足はプラン時の目安にすぎず、日中は「今からこの枠のタスクをこなして間に合うか」を知りたい。終了予定時刻（F-104）のセクション版。過去セクションで残りを出しても行動につながらないため終了前のみ表示する |
| 2026-07-21 | 時間合計の分子を「完了は実績・未完了は見積もり」の合算とし、ラベルを「見積」→「合計」に改める | 残り時間の再設計で過去セクションも見出しが残るようになり、完了済みを「見積」で足すと実態とズレる。完了を実績にすれば「実際に使った時間＋これからの予定」を1数字で読め、ラベルも見積専用でなくなるため「合計」が正確 |
| 2026-07-21 | クイック追加は Enter 後に欄フォーカスを保つ連続追加をやめ、追加したタスクを選択してリストへ移す（FB-29） | 実運用では1件足したらすぐ見積もり入力・並べ替え・開始打刻へ進むことが多く、連続入力は少数派だった。追加物を即操作できる方が流れに合う。連続追加は `A` で欄へ戻れば従来どおり |
| 2026-07-21 | クイック追加欄へフォーカスするキーを `N`（New）から `A`（Add）へ変更（FB-36） | 操作の呼称を「追加（Add）」で統一する。`A` は未使用で競合がなく、由来も直感的なため |
| 2026-07-19 | 打刻時刻の入力は区切り文字なしも受け付ける | 数字だけで打てるほうが速い |
| 2026-07-19 | 表示日によらず打刻できる | 深夜作業で前日のタスクを開始・終了する運用があるため |
| 2026-07-19 | ショートカット体系を vim の HJKL に統一（オーナーレビューで確定） | 覚え直しの負荷を下げるため |
| 2026-07-19 | 中断・複製・先送りは楽観的更新の対象外 | 生成されるタスクの `sort_order` をサーバが全タスクを見て決めるため、クライアントで予測できない |
| 2026-07-19 | 複製後は選択行を複製したタスクへ移す | 続けて見積もり編集や開始打刻へ進めるため |
| 2026-07-20 | 本文の文字サイズを1段階拡大（行高は維持）（FB-05） | 常用画面のため読みやすさを優先（**2026-08-08 に廃止**。下の FB-91 のエントリを参照） |
| 2026-07-20 | 「今日へ」ボタンから強調色をやめ、今日以外のときだけ表示（FB-07） | 強調色が「表示日＝今日」の状態表示に見え、逆に誤認を招いていた |
| 2026-07-20 | 列見出しを行メニュー列の幅まで含めて整列（FB-14） | 見出しと数値がずれて見えたため |
| 2026-07-20 | ヘルプにニーモニック由来を併記（FB-04） | 覚え直しの助けになるため |
| 2026-07-20 | 先送り・ルーチン化にショートカットを割り当てない | 誤操作で実行されると後片付けが要る操作のため |
| 2026-07-20 | タスクからのルーチン化を追加（FB-11 / F-305） | ルーチンへの登録し直しが重く、デイリーの文脈を離れずに作れる導線が要るため |
| 2026-07-20 | ルーチン化の作成は即座に閉じ、結果はトーストで示す | 操作感を軽くするため（ローディング表示を置かない） |
| 2026-07-20 | 自動セクション移動を追加（FB-02 + FB-16 / F-113） | 実行中タスクより上に未実行タスクが残る／未分類のまま開始したタスクが時間帯とずれる、が毎日発生していた |
| 2026-07-20 | 自動移動の失敗で打刻を巻き添えにしない | 並びの整形は補助的な処理であり、記録は成立させるべきため |
| 2026-07-20 | 自動移動に取り消しUIを設けない | 並びの整形でありデータの欠損ではないため |
| 2026-07-20 | 選択行のスクロール追従を追加（FB-20） | J/K の移動や自動移動でフォーカス行が画面外に出ていたため |
| 2026-07-20 | セクション見出しにタスク進捗を追加し、左寄せ1行に変更（FB-06 / F-114） | 時間の過不足だけでは消化度合いが読めず、左右分離は視線が往復するため |
| 2026-07-21 | 画面上部を sticky にし、下端に罫線（FB-22） | スクロール中も進捗・残時間を見失わず、クイック追加も使えるようにするため。罫線がないと境界が判別できない |
| 2026-07-21 | サマリを日付の直後へ左寄せ・1段階拡大（FB-22） | 画面右端に離すと日付と見比べるのに視線が往復するため |
| 2026-07-21 | 1日全体の進捗と現在時刻を追加（FB-22 / F-114 拡張） | 今日の総量と消化、終了予定までの残りを引き算せずに読めるようにするため |
| 2026-07-21 | 「残」を「残作業」へ改称（FB-22） | 「残りの空き時間」と誤読しうるため |
| 2026-07-21 | 開始時刻の修正でもセクションを移す規則 c を追加（FB-24 / F-113） | 開始打刻の瞬間しか見ておらず、時刻を直しても時間帯とずれたままだった |
| 2026-07-21 | 規則 c の対象に完了タスクも含める（FB-24） | 「記録だから動かさない」は時刻が正しい場合の話であり、時刻自体を直したなら位置も直すべきため |
| 2026-07-21 | 終了時刻だけの修正では移動しない（FB-24） | セクションの帰属は「いつ始めたか」で決める。深夜まで延びたタスクが深夜セクションへ飛ぶのを避けるため |
| 2026-07-21 | 規則 c の移動先での位置は開始時刻順（規則 a の末尾追加と異なる） | 修正はログの訂正であり、打刻済みタスクの時刻順に並んでいないと記録として読めないため |
| 2026-07-21 | §4.2 の目的を「プラン側＋ログ側」の両面で書き直す（C-7 を新設） | リストはプランであると同時にその日のログでもある、という前提が明文化されておらず、規則 c や「完了タスクを動かさない」の根拠を説明できなかったため |
| 2026-07-21 | 打刻時刻の編集は既存値を全選択して始める（FB-23） | 打ち直しが前提の項目であり、毎回消してから入力するのが手数だったため。タスク名・見積もりは部分修正もあるため対象にしない |
| 2026-07-21 | 開始の取り消し（O-13 / F-210）を追加し、`U` を「実行中選択中は開始取消・それ以外は削除取消」で切り分ける（FB-37） | 誤って開始したタスクを未実行へ戻す手段がなかった。Undo の語感を保ちつつ削除取消（O-8）を乗っ取らない切り分けにした。実行中は最大1件で `C` から選択も容易 |
| 2026-07-22 | 完了タスクの `Enter` を「複製して開始」（O-14 / F-208）に割り当てる。複製（O-11）＋開始（O-2）の合成とし、専用の生成・取消規則は増やさない。取り消しは F-210 に委ね（複製は未実行で残る）、打刻ボタンには割り当てず `Enter` 限定にする | 「もう一回」の Y→Enter の2操作が毎日煩わしかった（要求 §10 で Should へ繰り上げ）。既存規則の合成で足り、専用 Undo やボタン割り当ては誤操作の後片付けを増やすため避けた |
| 2026-07-21 | 取り消したタスクは開始前の位置を復元せず未実行として並べ直す（FB-37） | 開始後に他操作を挟んでも取り消せるようにするため、開始前位置のスナップショットをクライアントに持たない。サーバ側で未実行の位置を導出すれば再読込にも強い |
| 2026-07-21 | 割り込みで開始した場合も取り消すのは当該タスクのみ（直前タスク・再開タスクへ波及させない）（FB-37） | 開始の取り消しを1タスクの局所操作に留め、挙動と実装を単純に保つため。オーナーも再開タスクは残ってよいと確認 |
| 2026-07-22 | `U` は「直前の削除の取り消しが保留中ならそれを優先」する（保留がないときだけ実行中選択中で開始取消）（FB-37 動作確認） | 他タスクを `D` で削除すると選択が現在地（実行中タスク）へ自動で移るため、削除直後の `U` で開始が取り消され、ユーザーの「削除を取り消したい」期待と食い違った。Undoトースト表示中は U をその取り消しに対応させる |
| 2026-07-22 | 複製して開始（O-14 / F-208）の「表示日が今日でないときは複製元と同じセクションへ置く」挙動を O-14・データモデル定義書 §4.6 の本文に明記する | 実装済み（F-201・F-210 と同じ「今日でなければ動かさない」慣習）だが本文に配置分岐が書かれておらず、§4.5 は書き分けているのに §4.6 だけ今日前提の記述になっていた。挙動は変えずドキュメントの欠落を埋めた |
| 2026-07-22 | 選択ポップオーバーの候補移動を矢印キーではなく `J`/`K` にする（F-112） | 要求文は「矢印キー」だったが、FB-33 で矢印を廃止し行選択を `J`/`K`（vim HJKL）に統一済み。ポップオーバー内だけ矢印を復活させると体系が二本立てになるため、`J`/`K`＋Enter に揃える |
| 2026-07-22 | §3.1「日付指定（日付表示クリックでdatepicker）」の対応要求を F-106 → F-117（新規 Could）へ付け替え、（未実装）を明記（FB-39） | datepicker は未実装のまま F-106（Must）に紐づいていた。基本の日付ナビ（前後日・今日）と分け、遠距離ジャンプを Could として切り出したため（要求定義書 §10・要件定義書 §8）。UI 仕様そのものは変えていない |
| 2026-07-22 | デイリーのセクション表示順を日界セクション起点の回転にする（F-116） | 日界を変えても `task_date` は不変で、変化は表示順の起点と「今日」の解決に閉じる。回転は表示だけの並べ替えで、配置先導出（`sectionAt`・24時間敷き詰め）には手を触れない。既定の深夜 00:00 では通常の `start_time` 昇順と一致し現状を壊さない |
| 2026-07-22 | 「今日」を日界で解決する（現在時刻が日界時刻より前なら前の暦日を今日とする）（F-116） | 初期表示日・今日判定・自動セクション移動（§4.2）の発火条件を日界に合わせるため。深夜作業が前の論理日に収まる。既定の 00:00 では暦日と一致する |
| 2026-07-22 | セクション選択ポップオーバー（O-5）の候補順も見出しと同じ日界回転にする（F-116。spec-reviewer 指摘） | 見出し行は回転するのに選択肢だけ暦日順だと、日界を 00:00 以外にしたとき並びがずれて選びにくい。`listDailyList` が返すセクションを回転済みにして両者を一致させた（既定 00:00 では従来どおり start_time 昇順） |
| 2026-07-25 | datepicker（F-117）を実装。開き方は日付表示クリック＋ショートカット `G`（Go to date）の2系統、週の起点は月曜、月移動は前月/翌月のみ（年ジャンプなし）、カレンダー内は矢印/HJKL＋Enter/Esc で操作（FB-39） | オーナー確定。キーボード完結（N-04）を重視し開くキーも新設。離れた月へのジャンプは実運用で数か月程度が主のため年移動は持たず ◀▶ に絞って UI を軽くした。カレンダーは2次元グリッドのため、行操作では割り当てない矢印キーをこの中だけ例外的に受ける（§6） |
| 2026-07-25 | ルーチン化ポップオーバー（O-12 / §4.1）に週間隔入力を追加（FB-44） | 隔週などを S-02 だけでなくデイリーからのルーチン化でも設定できるようにするオーナー要望。既定1（毎週）で、週次選択時に曜日の下へ出す。週間隔の仕様本体は画面定義書02 §4・データモデル定義書 §4.1 が正 |
| 2026-07-25 | 並び替え（Shift+J/K）は移動タスクへ選択を固定し追従させる（FB-50） | 未選択（一度も J/K 等で選んでいない）状態で Shift+J すると、移動後に選択が「現在地（先頭の未実行）」へ再導出され別タスクへ飛び、上位2件が無限スワップ・空セクションへ移しても選択が先頭へ戻り動かないように見えた。移動自体（楽観更新・サーバ確定）は正しく、原因は `moveByStep` が選択を確定しなかったこと。J/K ナビが `setSelectedId` で選択を確定するのと揃え、並び替えも移動タスクを選択したまま追従させる（§5）。T-19 とは独立の既存バグ（クライアント選択ロジックは main でも同挙動） |
| 2026-07-25 | セクション選択ポップオーバー（O-5）の各選択肢に時間帯を付記する。粒度は「開始のみ」ではなく「範囲（開始〜終了）」を採る（FB-46） | 名前だけでは時間帯が思い出せず選びにくいというオーナーの気づき。「時間帯が思い出せない」の解消には枠の始点だけでなく終点まで見える範囲表示が直接効くため。終了時刻はセクションが24時間を敷き詰める性質から次セクション開始の導出（`sectionRanges`）で得る。候補順は F-116 の日界回転を保つ必要があるため、順序は `listDailyList` が返す回転済みセクションのまま維持し、`sectionRanges`（start_time 昇順）からは endTime のみ引く。表記は見出し §3.2 と揃える（`font-mono`・弱色）。既存のポップオーバー要求 F-112 の範囲内の表示詳細のため要求定義書は変更せず（F-105＝見出し表示とは別条項）、要件定義書 F-112 に1行・詳細は本書 O-5 に置く |
| 2026-07-25 | Shift+J/K（O-6）の移動先を「表示中のセクション順」に一致させ、当日タスクが属するアーカイブ済みセクションも移動先に含める（T-19） | 従来サーバ側の移動先算出は `activeSections`（アーカイブ済み除外）で、表示・楽観更新（`optimisticGroups` 由来でアーカイブ済みを含む）と食い違い、当日タスク付きアーカイブ済みセクション境界を跨ぐ Shift+J/K で楽観更新が巻き戻っていた（データ不整合はなく視覚的な巻き戻りのみ・エッジケース）。「表示されているものは跨げる」を採り、client/server とも移動先セクション順を表示順（`groupTasksBySection` 由来）に統一。`S`（O-5）の割り当てがアーカイブ済みを除外するのとは役割が異なる（O-6＝表示上の並べ替え／O-5＝割り当て先選択）ため両者の対象差は許容する |
| 2026-07-25 | 完了の取り消し（O-15）を `U` に載せ、取り消しの保留を削除（O-8）と共通の1スロットにする（FB-41 / F-212） | 戻し先は「実行中へ（`ended_at` のみクリア）」ではなく**未実行**をオーナーが確定。要求の本体が「実施していなかったと直す」ことであり、実行中へ戻す案は実行中1件制約に触れて割り込みを誘発するため。`U` に載せた理由: 現行の `U` は「保留なし・実行中でない」とき `undoDelete()` に落ちて**何も起きない**ため、そこへ完了取消を割り当てても新しいキーを増やさずに済む。ただしその空き分岐は、削除 Undo を狙って遅れて押した `U`（トースト5秒消滅後）が完了タスクの巻き戻しに化ける経路でもあるため、Undoトーストによる復帰を必須とした。O-13（開始の取り消し）がトーストを持たないのは失う情報が `started_at` だけで小さいのに対し、O-15 は開始・終了の両方＝実績そのものを破棄するので、行が消える削除（O-8）と同等に扱うのが釣り合う。保留を種類別に持たず1スロットへ寄せたのは、`U` の切り分けを現行の2段（保留優先→選択状態で分岐）のまま保つため。競合は「削除の5秒以内に別タスクを巻き戻す」ときだけで、後から来た操作を優先するのは直感に沿う |
| 2026-07-25 | 未実行タスクの予想開始時刻（F-120）を、新列ではなく実施時間列への併記（`HH:MM-`・弱色）で出す（FB-47） | 未実行行では実施時間列が空いており、そこへ入れると実打刻と同じ位置に縦へ並ぶため「上から下へ時間が流れる」読み方がそのまま成立する。独立列を立てる案は完了行で常に空になり、テーブル型リスト（N-05）の列数も増える。行頭（タスク名の左）案はタスク名の左端がずれ、モード色の行先頭とも競合するため退けた。実績との区別は弱色が担う。対象を未実行行のみに絞ったのは、実行中・完了は実打刻を持つため予想が要らず、「予想が出ている行＝これからやる行」という見分けにもなるから。表示日が今日のときだけ出すのは終了予定（F-104）・セクション残り時間（F-110）と同じ規律（現在時刻起点の導出値を別日に当てても意味をなさない）。行単位では 24:00 超過の警告色を出さない（行数が多く騒がしくなるため、超過警告はサマリ行の終了予定に集約する） |
| 2026-07-25 | プロジェクトをタスク名への併記から独立列へ移し（位置はタスク名の直後・モードの左）、未設定表記をモード列とともに薄色の `-` に揃える（FB-48） | 併記だとタスク名に埋もれて視認しづらいというオーナーの気づき。位置をモードの左にしたのは、併記時の並び（タスク名の右隣）をそのまま列へ昇格させる形で目の移動が小さく、「何を〈タスク〉／どの件で〈プロジェクト〉／どういう性質か〈モード〉」の読み順にもなるため。未設定表記は従来の薄色プレースホルダ（「プロジェクト」「モード」という語をそのまま出す）をやめ、記号 `-` に統一した——未設定行が多いと列が同じ語で埋まって視線のノイズになり、見積もり未設定の `--:--`（F-103）と規律も揃わないため。割り当てできることはホバーとキー（`P`/`M`）が担う。セクションの併記はタスク名セルに残す（見出し行でグループ化されており行ごとの値を読む必要が薄く、FB-48 の範囲外）。列が1つ増えるがテーブル型リスト（N-05）の範囲内と判断し、幅は固定＋切り詰めで行高を変えない |
| 2026-07-25 | セクション選択（O-5）の候補先頭に「現在のセクションへ」を固定で置く。「現在」は**現在時刻を含むセクション**とし §4.2 の「現在位置」とは揃えない（FB-51） | 候補がセクション名の一覧だけだと、いまどの時間帯にいるかを頭で照合してから選ぶ必要があるというオーナーの気づき。FB-46 の時間帯付記は照合の材料を増やしたが、照合そのものは残っていた。先頭固定の項目にしたのは `S` → Enter で確定でき手数が最小になるため（該当セクションに印を付けるだけの案は、印を探して `J/K` で移る手数が残る）。「現在」を現在位置（実行中タスクのセクション）に揃えなかったのは、利用者が知りたいのは「いまの時間帯」であり、実行中タスクがその時間帯をすぎていると時刻とずれて期待を外すため。挿入位置は移動先セクション末尾のままとし候補によって規則を変えない（`S` の結果が予測できなくなるのを避ける）。表示日が今日でなければ出さないのは現在時刻起点の値に共通の規律（F-104・F-110・F-120） |
| 2026-07-25 | 現在セクションの見出しは**背景色**で強調し、そのために `band-now` トークンを新設する（FB-52 / F-121） | オーナー確定で手段は背景色（左端の縦罫・文字色＋太字・罫線の各案より一目で分かるため）。ただし既存の `accent-weak` は選択行の面色で、見出しの直下の行が選択されていると2行が同じ色でつながって見えるため流用せず、**`accent-weak` より一段濃い**新トークンを起こす（オーナー指示）。`band` と同じ「見出しの帯」の役割を保ったまま紺藍へ寄せる位置づけで、命名も `band` 系に揃えた。開始値は `#dde5f2` を提案（`band` #f0eee7 / `accent-weak` #eef1f7 との差が視認でき、`ink` の本文とも十分なコントラストが取れる）。実装時の実機確認で調整してよい。対象外を未分類・アーカイブ済みセクションとしたのは、前者が時間帯を持たず、後者が24時間の敷き詰め（データモデル定義書 §3.1）から外れて「現在」を定義できないため |
| 2026-07-25 | `band-now` の値を提案の `#dde5f2`（紺藍寄りの淡色）から **`#ead9d0`（`band` を暖色側へ一段濃くした粘土寄り）** へ変更（F-121） | 実機確認でオーナーが「思ったより見づらい」と判断。原因は明度で、`paper` #fafaf8（L\* 98.7）・`band` #f0eee7（93.7）・`accent-weak` #eef1f7 がいずれも高明度に固まっており、そこへ淡い寒色を置いても**面として沈まない**——「帯」の役割は色相ではなく明度が担っていた。系統的な一貫性（強調＝紺藍）よりも帯として沈むことを優先し、`band` と同じ暖色系のまま明度を落とす方向へ切り替えた（#ead9d0 は L\* 87.8 で `band` より 5.9 低い）。紅系統は警告の `danger` 族と衝突しうるため、`danger-weak` #f7ece8 とは明度・彩度の両方で離し、「このセクションは危ない」と読めない位置に置いた |
| 2026-07-25 | プログレスバーの未実施部分（トラック）を不透明な `line` から**半透明トークン `track`（`ink` を 6% 重ねる）** へ変更（FB-54） | `band-now`（L\* 87.8）の導入で、トラック `line` #e8e6e0（L\* 91.3）が**地色より明るく**なり、他の地色では「沈んだ溝」に見えていたものが現在セクションだけ「浮いた帯」に裏返った。コントラスト比自体は落ちておらず（`line` 対 `band-now` = 1.097 で `line` 対 `band` の 1.064 より高い）、**極性の反転**と、中性グレーのトラックが暖色の地に載って境界の手がかりが色相だけになったことが原因。したがって要件は「濃くする」ではなく**「地色が何であれトラックは地より暗い側に居る」**と定めた。不透明色の付け替え（`line-strong` へ変更／地色ごとに切り替え）でも現時点は解けるが、地色は `paper`・`band`・`band-now` と既に3つあり F-118 のハイライトも控えているため、地色が増えるたびに見直しが必要になる。半透明なら地色に依らず一定量だけ沈み、構造的に二度と反転しない。アルファのトークンは `scrim` に先例がある。トラックに罫線を足す案は、バーの高さが 6px で 1px の罫線が高さの3分の1を占め角丸でにじむため退けた |
| 2026-07-25 | §2 の列見出しの列挙を「名前」→「タスク」に直し、モード色の反映範囲は §3.3 だけに書いて §2 は参照に縮める | 前者は docs が「名前」・実装の `<th>` が「タスク」で FB-48 以前から食い違っていたもの。**実装側を変えず docs を寄せた**——列見出しの語を変えると画面の文字が変わり、オーナーが運用で見慣れた表示を理由なく動かすことになる。後者は同じ規則が §2 と §3.3 に全文で並んでおり、FB-48 で反映範囲にプロジェクトを足したときに片方だけ直す事故が実際に起きかけた（spec-reviewer が検出）。「同じ規則を2か所に書きたくなったら下位に寄せる」（CLAUDE.md 書き方の規約）に従い、列ごとの詳細を持つ §3.3 を正とした |
| 2026-07-26 | §6 の `?` の説明を「ショートカット一覧の表示」から「表示・非表示（トグル）」へ直す（FB-61） | 実装と画面に出る文言（`shortcut-help.tsx` の「この一覧の表示・非表示」）はトグルで一致しており、§6 の表だけが片方向に読めた。**実装側を変えず docs を寄せた**——画面の文言を変えるとオーナーが運用で見慣れた表示を理由なく動かすことになるため（§2 の列見出しを「タスク」へ寄せたのと同じ扱い） |
| 2026-07-26 | §3.3 に「モード未設定行の色」を条項として起こす（**タスク名と見積は既定の文字色・他は副次情報の色**）。T-53 | 従来の §3.3 は「モード未設定時は既定の文字色」としか書いておらず、実装が未設定行のモード・プロジェクト・セクション併記・実績・実施時間を副次情報の色（`text-ink-muted`）にしていることも、見積とタスク名を対象外にしていることも条項になかった（T-53 の分割で `test-reviewer` が「テストが挙げる条項が docs に実在しない」と指摘）。**実装を正として条項を起こした**——未設定行を本文色のまま並べると色付きの行より目立つという理由が実装の意図として読み取れ、除外2つにもそれぞれ理由がある（見積は §2.4 の専用表記を持つ／タスク名は主情報）。挙動は変えていない |
| 2026-07-27 | §5 に「**終了打刻がサーバで拒まれたら選択も元の行へ戻す**」を明記する（F-211 / N-01） | §5 は「終了打刻でタスクを**完了したら**選択を現在地へ送る」としか書いておらず、サーバが失敗を返したときのことが条項になかった。実装（`daily-board.tsx` の `punch()`）は `setSelectedId` を楽観的更新の `run()` の**外**に同期で置いているため、失敗すると行だけ実行中へ巻き戻り選択は次のタスクへ進んだままになる。同じ §5 の並び替えが失敗時も「選択は移動対象に残す」と定めているのと非対称で、N-01（失敗時はトースト＋ロールバック）の趣旨からも選択の送りはロールバック対象に含まれるのが素直、というオーナー判断。**条項を起こしてから実装を直す**（実装は要件バックログ F-211 の行で管理）。PR #74（T-69）のテスト観点レビューで `test-reviewer` が検出——従来は `hold` の型推論が狭まって失敗値を `resolve` できず、この経路のテスト自体が書けなかった |
| 2026-07-27 | `B`/`F`（§6）と時刻セルのクリック（§3.3）が開けるのは**そのフィールドが打刻済みの行だけ**と定める（FB-68 / FB-58 / F-203） | FB-68 は未打刻タスクの `B`/`F` でキーボードが固定される不具合で、原因は「`editing` を立てる側が打刻を見ないのに、入力欄を描く側は見ている」という非対称。**片側だけ直すのでなく、キーとクリックの受け付け条件を同じ規則に揃えて**塞いだ——描画側のガードは既に「開始時刻は開始済み・終了時刻は完了済み」で、これがそのまま条項になる。同時に FB-58（実行中に `F` で空欄の終了時刻欄が開き、終了時刻だけを先に決められる）も解消する。実行中に終了時刻を入れたら完了扱いにする案（FB-58 ①の別案）はオーナー判断で採らず、別 FB として起票——終了打刻の入口が `Enter`・打刻ボタン以外に増え、打刻から状態を導出する設計（データモデル定義書 §3.5）への影響が大きいため、切り離して検討する。運用上は「`Enter` で終了 → `F` で時刻を修正」で同じことができる |
| 2026-07-28 | 「現在地」（§5）の未実行探索を**現在セクション → 未分類 → 表示順全体**の順にし、`C`・初期選択・終了打刻後の送り（F-211）で共通の規則として1か所に書く（FB-78） | 未分類はリスト先頭（§3.2）に置かれるため、フラットな「表示順で最初の未実行」だとインボックスに1件でも未実行があれば常にそこへ当たり、打刻ループの最中に `C` を押しても「いまの時間帯の次にやるタスク」へ戻れなかった。§4.2 規則 b は現在位置より前の未実行を繰り下げるが**未分類はその対象外**（トリアージ前の受け皿を時刻経過で動かさない）ので、未分類の未実行は構造的に現在セクションより前に居座り続ける。未分類を探索から外さず2番手に残したのはオーナー判断——インボックスは「見落としたくない」置き場でもあり、現在セクションが空のときはそこへ寄せたい。**「現在セクション」は §4.3 の定義（現在時刻を含むセクション）を引く**——§4.2 の「現在位置」（実行中があればその直後）は `C` では実行中が最優先で選ばれるため差が出ず、差が出るのは F-211 の送り先（終了直後のスナップショットでは終了対象がまだ実行中に見える）だけで、そこでは §4.2-b がサーバ側で未実行を繰り下げる先＝**現在時刻を含むセクション**と一致する方が並びと選択がずれない。3段で決まらないときに全体探索へ落とす（選択を動かさない案を採らない）のは、現在セクションより前のセクションに未実行が残る状態が規則 b の発火前に起こりうるためで、従来挙動の上位互換として安全側に倒した。表示日が今日でないときに規則2 を飛ばすのは現在時刻起点の値に共通の規律（F-104・F-110・F-120・F-121）。**規則3（未分類）に実装上の分岐を持たせていない**のは、未分類が表示順の先頭（§3.2）にある以上、規則4 の全体探索がそのまま未分類を先に見るため——専用分岐を書いても変異テストで落とせない（＝観測できない）コードになる。条項側は探索順を明示したまま、3・4 が自動的にこの順になる旨を1行添えて実装と対応づけた。要求軸は既存に収めた（F-211 の要求文が既に「次にやるタスクへ自動で送り、打刻ループを止めない」で、今回は『次にやるタスク』の定義の是正。`C` 側は N-04 の範囲） |
| 2026-08-01 | **`C` をコメント編集（O-16）へ譲り、現在地ジャンプは `N`（Now）へ移した**（F-206 の着手）。あわせて予約してあった `Shift+C` を解除した。**このログの過去エントリに出てくる `C` は当時の表記のまま残す**（現在地ジャンプを指す） | コメントは日々書く操作なので修飾キーなしの1打鍵を当てたい、というオーナー判断。`C` は Comment の頭文字で最も素直な当て先で、玉突きになる現在地ジャンプも `N`（Now）で同じ概念を指せる。`N` は FB-36 でクイック追加を `N`（New）→ `A`（Add）へ移して以来の未使用キーで、競合しない。ショートカットの割り当ては N-04 の範囲なので要求軸は起こしていない |
| 2026-08-01 | コメント（F-206）は**印＋選択行での2行目展開**とし、コメントの無い行には印を出さない（O-16 / §3.3） | 用途は「実施の記録・気づき」（完了後に書き、レビューで読み返す）とオーナーが定めたため、デイリーでは常時全文が見えている必要がない。一方で「どの行にコメントがあるか」は一覧で分かってほしいので、印だけを常時出し、全文は選択行のときに開く。**未設定行に印を出さない**のは、[共通](./00_共通.md) §2.4（未設定を空欄にしない）が「クリックして設定できると伝える」ための規約であり、対象は値の列だから——コメントは列ではなく行の付属表示で、印を出すと全行に薄い印が並んで N-05（装飾を排する）と衝突する。マウスだけで新規コメントを書けなくなるが、キーボード操作が主（N-04）の画面なので許容する、というオーナー判断。**入力欄で `Enter` を改行にしなかった**のは、他のインライン編集（名前・見積・時刻）と確定操作を揃えるため（改行は `Shift+Enter`） |
| 2026-08-01 | コメント全文の**折り返し幅をタスク名列に揃える**（行いっぱいに広げない。§3.3）。実装当初は行の全幅で折り返していた | オーナーの動作確認で「プロジェクト列の手前で折り返すイメージ」と指摘。行いっぱいだと、コメントが伸びるたびに右側の列（プロジェクト・モード・時間）の下まで文字が回り込み、**列の見え方がコメントの長さで変わる**。タスク名列に収めれば、コメントが何行になっても表として読む縦の並びが崩れない。レビュー（画面定義書04 §3.3）も同じ理由で揃えた——同じものが画面ごとに違う幅で折り返すと、書いたときと読み返すときで印象が変わる |
| 2026-08-01 | ハイライト（F-118）の見せ方を**淡黄の専用トークン `highlight` の地色 ＋ タスク名セル右端の⭐**とし、**左端の縦帯は使わない**（§3.3 / O-17） | 「行の色ごと変える」がオーナーの当初イメージだったが、地色は既に**選択行の `accent-weak`（青）**が占めており、ハイライト行を選択した瞬間に印が読めなくなる。当初はこれを**左端の縦帯**で解けると考え、地色と縦帯の役割を分ける案で合意しかけた。その後オーナーから「⭐のクリックを付け外しの入口にしたい」という指定が出たことで、**⭐が選択中も完了後も行に残る**——つまり縦帯と同じ役目をすでに果たす——と分かり、縦帯を落とした。同じことを3通り（地色・縦帯・⭐）で言う状態を避け、手段を2つに畳んでいる。色相の選定は、既存の面色が**選択＝青（`accent-weak`）／警告＝赤茶（`danger-weak`）／現在セクション＝粘土（`band-now`）**と埋まっているため、どれとも読み違えない淡黄（付箋・マーカーの語彙）を採った |
| 2026-08-01 | ⭐は**タスク名の直後ではなくタスク名セルの右端**に置き、**OFF のアウトラインは選択行とマウスを乗せた行にだけ出す**（他の未ハイライト行は場所だけ空ける。§3.3） | ON/OFF の両方を⭐で見せる以上、未ハイライト行にも☆が並ぶ。全行に常時出すと行が騒がしくなる（N-05）ので、押せることを伝える必要がある行——選択行とホバー行——にだけ出し、他は**場所だけ空ける**ことで列の縦の揃いを保った。位置をタスク名の直後にしなかったのは、名前の長さで⭐の横位置が行ごとにずれ、「常に同じ場所を押す」という空きスロットの利点が消えるため。セル右端なら⭐が縦一列に揃う。**タスク名の編集中も⭐を隠さない**のは、列位置が固定で入力欄と場所を取り合わないから——セクション併記・コメント印は名前に続けて流し込まれるため隠す必要があったが、事情が違う |
| 2026-08-01 | **完了した行はハイライトの地色を出さず⭐だけ残す**（§3.3）。「完了してもハイライトは外れない」という寿命の規則自体は変えていない | 上限を設けない（下記）以上、一日が進むほど淡黄の行が増える。全部を同じ強さで残すと、夕方には**まだ手を付けていない本丸が、片付いた本丸と同じ色の中に埋もれる**。地色＝視線を集める力は済んだものから引き、「今日の本丸だった」という記録は⭐で残す、と役割を分けた。規則がひとつ増えるが、実物を並べて見比べたオーナー判断で採用 |
| 2026-08-01 | ⭐の置き場所を**タスク名セルの右端から「コメント印の右」へ移し**、**OFF のアウトラインをホバーでは出さず選択行だけに出す**（場所の確保もやめる。§3.3。同日の実装の動作確認でオーナーが判断し、上の「⭐はタスク名の直後ではなく…」エントリの決定を覆す） | 実機で見たオーナーの判定は「**ホバーで☆が出るのはかなり煩い**」「アイコンの位置のまとまりが悪い」。⭐を縦一列に揃える案は「押す位置が常に同じ」を狙ったものだったが、そのために**セル右端＝コメント印から離れた位置**へ置くことになり、行に付属する印が2か所に散った。加えて場所を空ける方式は、未ハイライト行にも常時スロットが残るため、ホバーで☆が現れる頻度が高くなる。コメント印の隣へ寄せると印がひとまとまりになり、**流し込みの末尾**に置くので選択で⭐が現れても左の表記は動かない（＝場所を空ける必要がなくなる）。左でなく右にしたのはこのため（左だとコメント印が押し出される）。**タスク名の編集中に⭐を隠す**方針へ変えたのも同じ帰結——列位置が固定でなくなり、出したままだと入力欄と場所を取り合う。押す位置が行ごとにずれる不便は残るが、`H` キーと行メニューという位置に依らない入口が2つあるため許容した |
| 2026-08-01 | レビュー（画面定義書04 §3.3）の⭐は**タスク名の先頭**に置く（デイリーは末尾なので位置が揃わない） | 同じ動作確認でのオーナー判断。デイリーの⭐は**付け外しの入口**なので操作対象（コメント印）の隣にあるのが自然だが、レビューの⭐は読み取り専用で**その行が本丸だったことを示す見出し**として働く。見出しは行の頭にある方が、実績ログを上から時系列で読むときに拾いやすい。**同じ印が画面ごとに違う位置に出る**ことになるが、役割が違う（入口 / 見出し）以上は揃える方が不自然と判断した |
| 2026-08-01 | ハイライトの**本数に上限を設けず**、付け外しは `H`・⭐クリック・行メニューの3経路、**`U`（取り消し）の対象にはしない**（O-17 / §6） | 上限は「本丸は3本まで」という規律を仕組みで担保できるが、単一ユーザーのアプリで、地色が変わる印は付けすぎれば自分で気づける。上限を設けると超過時の挙動（弾く／古いものを外す）とその文言・エラー経路が要り、得るものに対して仕様が重くなる。`H` は単独では未使用（`Shift+H` が前日移動）で Highlight のニーモニックがそのまま使える。**先送り（O-7）・ルーチン化（O-12）にショートカットを当てない規律の対象外**としたのは、あの2つがメニュー限定なのは「誤操作で実行されると後片付けが要る」ためで、同じキーで即座に戻せるトグルには当てはまらないから。同じ理由で `U` の対象にもせず、削除（O-8）・完了の取り消し（O-15）と保留1スロットを奪い合わせない |
| 2026-08-02 | セクション残り時間が 0 のとき**符号なしの `残り 0:00`** で出す（§3.2）。従来は `+0:00`（既存テストが固定していた挙動）（オーナー裁定） | レビュー画面の差異 0 を符号なしに変えた（[04 の決定ログ](./log_04_レビュー.md) 同日）ときに、同じ「符号＋H:MM」の形で 0 の扱いだけが違うことに気づいた。当初は「差異＝差の向き／残り＝枠の余裕」で意味が違うため揃えない判断だったが、**オーナーが実機で見て揃える方を選んだ**——読み手にとっては同じ画面群に並ぶ同じ形の数字だから。**先に決めた 04 §3.3 の規則をデイリーへ広げた形**なので、規則の本体は 04 側に置いたまま §3.2 から参照する。実装も共有ヘルパ（`formatSignedDuration`）へ寄せ、2画面が同じ実体を引くようにした |
| 2026-08-02 | 終了打刻で**送り先の未実行が無いとき、打刻した行（＝完了行）を選択する**と確定した（§5 / F-211。要件定義書も同時に更新）。従来の実装は「選択に触れない」で、打刻前に別の行を選んでいたらそこに残っていた（オーナー裁定） | 打刻ボタンは行ごとにあるので、**選択している行と打刻する行は食い違いうる**（キーボード経由では常に一致する）。§5 は失敗経路について「打刻前に選んでいた別の行ではなく打刻した行へ戻す」と明記していたのに、成功経路は「完了行に据え置く」としか書いておらず、選択が最初から完了行にある前提の言い方だった。そのため第3の行を選んでいた場合の正が読めない。裁定は**打刻した行へ送る**——失敗経路の戻し先と揃い、打刻直後はその行に用がある（`U` で完了の取り消し）ため。実装は `setSelectedId(next ?? task.id)` の1行で、分岐はむしろ1つ減った。**発見は T-96**（テストのクリック道具の統一）——`await` を足したことで、この状態を見ていたテストが「据え置いた」と「選択を捨てて現在地から採り直した」を区別できなくなり、穴が表に出た |
| 2026-08-02 | **`F2`（タスク名編集）の割り当てをやめ、`R` 1つにする**（§6。オーナー判断「実際使っていない」） | `F2` は多くのアプリで rename に当たる慣習キーとして `R` と併記していたが、単一ユーザーのアプリで**本人が使っていない**以上、慣習に合わせる利得が無い。一方でキーが1つ増えるたびに、ディスパッチャ・表・除外規則の検証対象が連動して増える（[T-98](../../案件/closed_23_技術改善バックログ.md#t-98) で1つの表へ集約した後もこれは変わらない）。ニーモニック `Rename` は `R` が担うので、失われる手掛かりも無い |
| 2026-08-02 | **`?` のヘルプ一覧末尾から「先送りはショートカットを割り当てていない」の注記を落とす**（オーナー判断） | §6 の条項（先送り O-7・ルーチン化 O-12 にキーを割り当てない）自体は変えない。**一覧に載っていない操作にキーが無いことは一覧を見れば分かる**ので、注記は同じことを二度言っている。実際その注記は条項の半分（先送り）しか書いておらず、ルーチン化を足して条項と揃えるか落とすかの二択になっていた。ヘルプは操作を思い出すための最小の一覧という位置づけを採り、落とす方を選んだ |
| 2026-08-03 | 打刻修正（F-203）で入力した `HH:MM` を**そのタスクが属する論理日の時刻として読む**と定める（§3.3。開始＝論理日の24時間の中の一意な時刻／終了＝開始以降で最も早い候補）。あわせて**未来の時刻への修正を禁じる**（§8）（FB-83） | 従来は入力を「元の打刻の暦日」へ当てはめるだけで、日界（F-116）を見ていなかった。日界が 00:00 以外だと論理日が暦日をまたぐため、またいだ側の時刻（深夜に開いたまま前の晩の `23:40` を入れる等）が約24時間先の未来に落ち、**終了打刻の段になって初めて「終了時刻は開始時刻より後にしてください」で弾かれる**（オーナーが実運用で踏んだ）。行の表示は入力どおりに見え、経過も 0:00 に丸まる（FB-28）ので途中で気づけない。開始と終了で規則を分けたのは、**終了だけが論理日の外へ出うる**ため——日をまたいで走り続けたタスクの終了（§8）は次の日界より後になり、開始と同じ「論理日の中の一意な時刻」では表せない。逆に開始は必ず論理日の中にあるので一意に決まり、`task_date` と日界だけで落とし先が決まる（現在時刻に依存しないので、同じ入力がいつ確定しても同じ結果になる）。候補のうち元の打刻に**最も近いもの**を採る案（日界を見ずに済む）は採らなかった——12時間以上ずらす修正（朝の打刻を夜へ直す等の打刻し忘れ）が前日側へ吸われるため。未来の禁止は、①打刻修正が「既に打刻した時刻を直す」操作である以上あり得ない値であること、②本件の事故を**終了打刻の段でなく修正の段で**見せられることの2点から入れた。判定を分単位にしたのはクライアント時計のズレを踏まないため（FB-28 と同種の配慮）。表示を折返し（`25:40`）にしない判断もここに含む——入力欄と表示の書式が割れるうえ、上の規則では `HH:MM` のままで一意に読めるため |
| 2026-08-03 | 上の規則から派生した3点を §3.3 に明記する（FB-83 のレビュー）。①**未来日のタスクに付いた打刻の開始時刻は修正できない**（O-13 で取り消す）②終了に**開始と同じ分**を入れたときは開始時刻そのものへ寄せる ③**未来の検査は画面側だけ**でサーバは再検証しない | ①は「開始は論理日の中の一意な時刻」の帰結——未来日は論理日ごと未来にあるので、どの `HH:MM` も未来判定に掛かる。未来日での打刻自体が誤操作（画面定義書01 §7 が表示日によらず打刻を許すことの副作用）で、戻す道は打刻の取り消しがすでに持っているため、修正手段を別に用意せず「できない」を条項にした。②は `test-reviewer`・`code-quality-reviewer` が拾った実バグの是正——打刻は秒を持つ（`new Date()`）ので、終了を分の頭に落とすと絶対時刻では開始より前になり、domain は通すのにサーバの再検証と `ck_tasks_time` が弾く＝インラインで止まらずトースト＋ロールバックへ落ちていた。分単位で「同じ」と見た以上、寄せ先は開始時刻そのものが最も素直（実績 0:00 は §3.3 の既存規則で表示できる）。③は `spec-reviewer` の指摘への回答——サーバ側の再検証は DB の整合性制約と対になっている（`ck_tasks_time` に対応する 開始 ≦ 終了）ものだけを持つ方針を採った。未来の禁止に対応する制約は DB に無く、`PunchError` へコードを足すと打刻・中断・複製・先送りの辞書すべてが到達しないコードを抱える（`TaskOperationError ⊇ PunchUsecaseError`）。単一利用者・単一クライアントであることも踏まえ、非対称は残したうえで条項に書いた |
| 2026-08-06 | **§4.2 の規則 a を規則 c と同じ「開始時刻順の位置」へ統一する**（既にそのセクションにいる場合も置き直す）。挿入位置の定義は表の外に1つ置き、規則 a / c の両方がそれを参照する形にした（FB-86） | 規則 a の「そのセクションの末尾へ積む／既にいるなら何もしない」は、**ルーチンから展開されたタスクが最初から所属セクションにいる**ため事実上ほぼ発火せず、並びがルーチンの `sort_order` のまま残っていた。同じ §4.2 が掲げる C-7（過去側は上から順に実行したログとして読める）に、規則 c だけが届いて規則 a が届いていない状態。**基準を「打刻した順（末尾）」ではなく「開始時刻順」にした**のは、規則 c と同じ規則にすれば §4.2 が持つ位置の定義が1つで済み、打刻修正済みのタスクが混じったときも2つの規則が食い違わないため。通常の打刻では実時刻が最も遅いので結果は「末尾へ積む」と一致し、差が出るのは時刻を直した行が混じるときだけ。**副次的な挙動変化**として、開始したタスクが同セクションの未実行タスクより**前**に入るようになる（従来の末尾＝未実行の後ろは C-7 の並び順と逆だった）。目の前で行が動く点は、打刻が利用者自身の操作である以上許容とオーナー判断（毎分の自動並び替えを避けている §4.2 の方針とは別物）。終了打刻へ広げる案は採らない——セクションの帰属は「いつ始めたか」で決める既存の規則（対象外表）と衝突するため |
| 2026-08-06 | **規則 c（開始時刻の修正）から対象日の制限を外し、過去日・未来日でも移す**。規則 a / b は今日限定のまま残す（FB-87） | 対象日を今日に限る条項が全規則へ一律に掛かっており、**規則 c が働くのは「今日のログの訂正」だけ**になっていた。訂正が最も起きるのは昨日以前なので、最も要る場面で効かない状態。規則 a / b は「現在位置」＝現在時刻を基準にするため別の日に当てても意味をなさないが、**規則 c は修正後の開始時刻だけで移動先も挿入位置も決まり、現在時刻を一切使わない**（実装も同じ形で、対象日の判定は呼び出し側の1条件だけだった）ため、日付を問わず同じ意味を持つ。未来日も含めたのは、未来日の「計画として尊重する」が守るのは未打刻の予定であり、そこに打刻がある時点で例外的な状態だから（[FB-85](../../案件/closed_21_ユーザーフィードバック.md#fb-85) と地続き）——時刻があるなら位置も揃う方が一貫する。過去日では規則 b の繰り下げが働かないが、規則 c の挿入位置が「最初の未実行タスクの直前」であるため打刻済みと未実行の前後関係は崩れない。あわせて「過去日はログとして保全する」が守るのは**打刻の記録そのものであって並びの凍結ではない**ことを対象外表に明記した |
| 2026-08-06 | **O-14「複製して開始」の配置も規則 a に揃える**（データモデル定義書 §4.6 の「末尾配置」を「開始時刻順の位置」へ改め、実装も規則 a と同じ計算へ寄せた。FB-86 の派生） | 規則 a が「末尾」だったあいだは O-14 の独自実装（`appendSortOrder`）と一致しており、O-14 の条項も「規則 a で入る」と書いて済んでいた。規則 a を開始時刻順に変えたことで**同じ条項を指す2つの実装が別の位置を返す**状態になり、`spec-reviewer` が検出した。揃える側を選んだのは、複製行が**即座に実行中になる**以上、未実行タスクの下に置くのは §4.2 冒頭の C-7（上から ログ → 実行中 → 予定）と逆向きだから。オーナー確定。実装では、複製行と割り込みの再開タスクという**まだ id を持たない2行を同時に置く**必要があるため `placeNewPair`（データモデル定義書 §3.5 の採番を2席ぶんに広げたもの）を新設した——`placeSortOrder` を2回呼ぶ形だと、1回目の振り直しを2回目が見ないまま採番してしまう |
| 2026-08-08 | §2 の「本文の文字サイズは1段階大きくする」を落とし、[共通](./00_共通.md) §1.1 の3段スケールへの参照に置き換えた。§3.1 のサマリも「1段階拡大して本文と揃える」から、数値が主・ラベルが従という書き方へ言い換えた（FB-91） | **この画面の階層がそのまま全画面の基準になった**ため、1画面だけ大きいことを定めた条項は役目を終えた。実際の見た目はこの画面についてはほぼ変わらない（変わるのは列見出しがメタから従へ1段上がる点だけ）——他の3画面がここへ合流する形の改訂だから。行高を文字サイズに追随させない点は本画面固有の判断なので §2 に残した——余白で吸収して1タスク=1行の高さを保つのはリストの見通しを守るためで、文字サイズの規則とは別の話。スケールを決めた経緯は[log_00_共通.md](./log_00_共通.md) 2026-08-08 が正 |
| 2026-08-08 | **割り込み（O-2）で、相手タスクの終了までを楽観的更新の即時反映に含める**（従来は開始タスクの `started_at` だけを即時に反映し、相手の終了はサーバ確定後だった）。あわせて O-2 に「どこまでが即時か」を明記した（FB-69） | 非同期区間のあいだ画面上に**実行中タスクが2件並んで見える**状態があり、「実行中は全体で最大1件」という不変条件と食い違って見えていた（サーバ側では守られている）。docs だけを実態に合わせる案（除外に O-2 を足す）も採れたが、**相手の終了はサーバの確定結果と必ず一致する**ので先取りしない理由が無い——終了時刻は開始と同じクライアント時刻を使い（要件定義書 F-201）、完了タスクは §4.2 の対象外表により並びが動かない。つまり「サーバが採番・配置を決めるから待つ」という除外の根拠が、相手の終了という部分には当たっていなかった。再開タスクの生成と開始タスクの自動セクション移動（§4.2-a）は採番・配置をサーバが決めるため確定後のまま残し、その線引きを O-2 に書いた（従来はコード内コメントだけが根拠だった）。上位の N-01 側は個別の操作を足すのでなく**除外の理由（サーバが採番・配置を決める副作用）を書く形へ改めた**（[log_12](../log_12_要件定義書.md) 2026-08-08）。**表示日に居ない実行中タスク**（前日以前。F-209 のバナーで気づく状態）を割り込んだ場合は画面上に相手が無く、先取りする対象そのものが無い |
| 2026-08-09 | §4.1 のルーチン化ポップオーバーで、**事前検証を残したまま検証の入口を domain の `validateRoutineSchedule` に一本化し、範囲を開始想定時刻から予定全体へ広げる**（曜日の未選択・週間隔・日・間隔もポップオーバー内でエラーになる）。文言は共有辞書から引く（FB-75） | ポップオーバーが開始想定時刻の検証だけを自前に持ち、しかも文言を直書きしていたため、[FB-72](../../案件/closed_21_ユーザーフィードバック.md#fb-72) ② で寄せたはずの文言が**この経路だけ届いていなかった**（辞書の食い違いではなく辞書の迂回。T-49 の `test-reviewer` が検出）。事前検証をやめてサーバへ一本化する案も採れたが、§4.1 が既に「ポップオーバー内にエラーを表示する」を定めており、押下と同時にポップオーバーを閉じる設計上、サーバ一本化はエラーの出し場所をトーストへ移す＝条項の書き換えになる。**残すなら中途半端に残さない**方を採り、検証の入口をルーチン管理と共有の `validateRoutineSchedule` に揃えた——同じ規則が2か所に写る状態そのものを無くすため。副次的に、曜日未選択などが**往復を待たずポップオーバー内**で分かるようになる（従来はサーバ確定後のエラートーストで、ポップオーバーは既に閉じていた）。文言の一覧を画面定義書に書かない方針は FB-72 のまま |
| 2026-08-09 | 同じ §4.1「入力の検証」に**エラーは入力を直した時点で消す**を加えた（FB-75 の対応中にオーナー確定） | 従来はエラー文が「作成」を押し直すまで残り、直した後も古い指摘が出ている状態になっていた。検証範囲が予定全体へ広がったことで、曜日を選び直しても「曜日を1つ以上選んでください」が残る、という場面が増えるため一緒に閉じた。先例はあるが粒度が違う——マスタ管理・ルーチン管理はセルやフォームを**開き直した時点**で落とし（[共通](./00_共通.md) §2.3）、デイリーの他の入力はトーストなので時間で消える（同 §2.2）。**入力を直すたびに落とすのはここが最初**で、送信前のクライアント検証を持つのがこのポップオーバーだけだから。同じ規則が他画面にも要ると分かった時点で[共通](./00_共通.md)へ寄せる（今1か所しかない規則を先に共通へ上げない）。**受理範囲は旧実装のまま保つ**ことも確認した——`validateRoutineSchedule` が使う正規化は DB の `HH:MM:SS` を吸収する切り出しなので、任せきりにすると `09:05x` のような余剰が黙って `09:05` に切り詰められる。区切りなし入力（`0805`）を受け付けるのはこのポップオーバーだけ（ルーチン管理の入力欄は `type="time"` でブラウザが書式を強制する）なので、余剰を判別できるのもここだけになる |
| 2026-08-09 | §7「打刻できる日付」を「表示日によらず打刻できる」から「**今日以前の表示日でだけ打刻できる**」へ狭め、未来日では打刻ボタンを出さず `Enter` も効かないようにした（FB-85 の詰める点④の裁定） | FB-85 は「未来日のタスクの開始時刻を直そうとすると『現在時刻までの時刻を入力してください』と出るが、論理日ごと未来なのでどの `HH:MM` を入れても通らない」という**指示どおりにしても出口に着かない文言**の話として起票された。文言を分岐させる案（`future_task_date` を起こして取り消し O-13 を案内する）と比べて経路封鎖を採ったのは、**§7 が「表示日によらず」を許していた理由が過去日にしか掛かっていなかった**から——条項に書かれた根拠は深夜作業で前日を打刻する運用だけで、未来日側には積極的な理由が無い。打刻は実時刻で入る以上、未来日での打刻は論理日の外の記録になり、[FB-83](../../案件/closed_21_ユーザーフィードバック.md#fb-83) で入れた未来禁止と噛み合って**作った瞬間に直せない状態**になる。塞げば「修正できるのは打刻済みの時刻だけ」（§3.3）により時刻修正の入口ごと消えるので、新しいエラーコードも文言も要らない。未来日のリストは空ではない（ルーチンは当日以降に展開され、先送り O-7 も翌日へ積む）ため `Enter` の暴発は起こりやすく、塞ぐ実益もある。[FB-87](../../案件/closed_21_ユーザーフィードバック.md#fb-87) で「未来日に打刻がある時点で例外的な状態」と判断した線とも揃う。**中断（`I` / O-4）も同じガードに載せる**——現在時刻での終了打刻を含むので打刻操作の一部であり、行メニューの項目は「実行中のみ」で非活性になるが `I` キーは状態を見ずに発火するため、盤面側で止める必要がある。**検査は画面側だけ**とし、サーバ側の再検証は増やさない（F-203 の未来禁止で決めた線に揃える。§3.3 末尾）。代償として「明日のタスクをいま始めたい」の入口が無くなるが、現状その操作の結果は壊れた記録であり、正しい出口は「タスクを今日へ移す」——それが無いこと自体を [FB-98](../../案件/21_ユーザーフィードバック.md#fb-98) へ分離した |
| 2026-08-09 | O-7（先送り）に「**ルーチン由来のタスクは紐付けが外れ、翌日にはその日の展開分と同名で2件並ぶ**」を明記した（[FB-99](../../案件/closed_21_ユーザーフィードバック.md#fb-99)） | 画面の側に現れるのは「翌日に同名が2件並ぶ」ことなので、操作表には結果だけを書いた。決定そのもの（なぜ紐付けを外すか・退けた代替案）は [log_14](../log_14_データモデル定義書.md) 2026-08-09 が持つ |
| 2026-08-09 | O-4（中断）・O-2（割り込み）の操作表に「**生成される行の名前は元タスク名＋「（再開）」**」を書いた（[FB-95](../../案件/closed_21_ユーザーフィードバック.md#fb-95)） | 画面の側に現れるのは「再開分の行だけ名前の末尾が違う」ことなので、操作表には結果と、接尾辞が保存値なのでリネーム（O-5）で直せることだけを書いた。接尾辞を保存する判断と退けた代替案は [log_14](../log_14_データモデル定義書.md) 2026-08-09 が持つ |
| 2026-08-09 | バンドルの道を**行の最左端・太さ6pxの縦帯**とし、別バンドルが隣接しても隙間を空けず、孤立したメンバーにも出すことにした（F-119 / §3.3） | 打刻ボタンを貫く案（ボタン同士を線でつなぐ数珠つなぎ）は、未来日でボタンが消える・実行中は `accent` 塗りつぶしと色がぶつかる、と**状態によって見え方が変わる**。最左端なら打刻ボタン・完了の✔・選択（`accent-weak`）やハイライト（`highlight`）の地色のどれとも場所を争わない（F-118 が左端の縦帯を使わないと決めていたので空いていた）。太さはオーナーが 2/3/4/6px を実トークンのモックで見比べて 6px を選んだ。隙間を空けないのは、6px あれば色の変わり目で分かれて読め、隙間を入れると道が破線に見えるため。孤立メンバーにも出すのは、帯の意味を「つながり」ではなく**「この行はこのバンドルの一員」**に置いたから——割り込みで散った日ほど所属を知りたい。バンドル名を常設せずホバーに寄せたのは、追加する装飾を縦帯1本に限る方針（N-05）を守るため |
| 2026-08-10 | §4.1 の「引き継がない値」に**バンドル（F-119）**を明記した | 実装は最初からバンドルを引き継がない形になっていたが、条項の一覧に無いため「書かれていない挙動」になっていた（レビューで露見）。入力させるのを繰り返し種別と開始想定時刻だけに絞る §4.1 の方針の裏返しで、束ねに入れるかは後から S-05 で決める。**元タスクの当日の所属を雛形へ持ち越さない**——ルーチン化は「別の日に繰り返す雛形を作る」操作であり、その日たまたま組んだ流れを繰り返しの定義に固定してしまわないため（オーナー裁定・2026-08-10） |
| 2026-08-12 | **選択行が消えたら、消える前の並びで直後の行（無ければ直前）を選ぶ**と定めた（§5。対象は削除 O-8・先送り O-7。[FB-106](../../案件/closed_21_ユーザーフィードバック.md#fb-106)） | それまで**条項が無く、実装が事実上決めていた**——`keepSelection` が「選択IDが実在しなければ現在地を採り直す」形だったため、実行中が無く現在セクションに未実行も無いと、リスト先頭の未分類（§3.2）に探索が当たって**選択が一番上へ飛んでいた**。現在地の探索順そのものは [FB-78](../../案件/closed_21_ユーザーフィードバック.md#fb-78) の意図どおりで誤りではなく、**削除の送り先に現在地を流用していたことが問題**だった。位置基準（直後の行）を採ったのは、削除は「いま見ているあたりを整理する」操作で、続けて同じ場所で `J`/`K` を使うため——打刻ループへ戻す F-211 の送り（現在地）とは目的が違う。**状態を問わない**（完了行でも選ぶ）のは、位置を保つことが主眼で、未実行に限ると「そのセクションに未実行が無ければ結局飛ぶ」形に戻るため。先送り（O-7）も同じ規則にしたのは、行がその日から消えるという点で削除と同型で、実装も送り先の計算1か所で済むから。**取り消し（`U`）で行が戻っても選択を動かさない**のは、送り先で作業を続けている最中に選択を奪い返さないため（オーナー確定）。要求軸は既存に収めた——選択の送り先は N-04（キーボード完結）の範囲で、F-211 は終了打刻の送りに限った条項のまま動かさない |
| 2026-08-13 | **複製（O-11）の挿入先を「1日のリスト全体の未実施のトップ」から「複製元の直下」へ変えた**（[FB-103](../../案件/closed_21_ユーザーフィードバック.md#fb-103)。要件定義書 F-111 も同時に更新） | 過去の時間帯のタスクを複製すると複製行が元の行から離れた「今の位置」に現れ、どれを複製したのか読めなかった。**セクションを合わせるだけでは足りず、隣に並ぶことを採った**——セクション内の「未実施のトップ」に置く案（打刻済みの後ろ・未実行の先頭。§4.2 の並びの規律と整合する）も検討したが、セクション内に行が多いと元の行との対応がやはり読めない。**複製元が打刻済みなら未実行の複製行が打刻済みの並びに割り込む**が、これは §4.2 の規律より「どれを複製したのかが読めること」を優先した結果として O-11 に明記した（オーナー確定）。**規則 b（未実行の繰り下げ）には例外を設けない**——例外にするには「やり残しではない未実行タスク」を表す列が要り（打刻からは導出できない）、「上から順に実行すればよい状態を保つ」規則 b の意図とも衝突する。この結果、**表示日が今日で複製元が現在位置より前にある場合は、直下に置いた状態が画面に出ないまま現在位置へ繰り下がる**（複製は楽観的更新の対象外で、行が画面に出るのは規則 b を通したサーバ描画の後。決定の過程で確認した）——そこでは「未実施のトップへ置く」案と結果が一致するため、**2案の差が出るのは規則 b が効かない場面**（過去日・未来日、現在位置以降のセクション、未分類）だけ。オーナーが最も気にしたのは今日の現在以降のセクション（打刻済みが無いので割り込みも起きず、直下に留まる）で、そこを最良にする案として直下を採った。**複製して開始（O-14）は据え置き**——あちらは即座に実行中になるので規則 a（開始時刻を含むセクション）に従うのが筋で、配置の根拠は「開始したかどうか」で説明できる（2026-08-06 の決定を動かさない）。選択行の移動（複製行へ移す）も据え置き（§5 の追従で画面内に保たれる） |
| 2026-08-18 | **`?` のヘルプ一覧のニーモニック由来を、操作の説明側からキーの側へ移した**（§6 の条項に置き場を明記。[FB-108](../../案件/closed_21_ユーザーフィードバック.md#fb-108)） | 由来（Rename・Go to date 等）はキーそのものの説明なのに、説明文を読み切った先に置かれていたため視線で結びつかなかった。とくに説明が2行に折り返す行（`N`・`C`・`U`・`G`）では由来が次行の末尾へ流れ、対応が読めない。**キーと同じ行に続けて薄字で添える**形にしたのはオーナー裁定——キーの下に改行して積む案は行高が増えて一覧が縦に伸び、括弧を半角にして等幅で一体化する案は由来とキーの区別がつきにくくなる。**併記の表記（全角括弧＋薄色）は説明側にあったときのまま動かさない**（移すのは場所だけ、という差分に留める）。左列が長くなるぶんは**パネルの幅ごと広げて**吸収する（オーナー裁定）——左列だけ広げると説明側が痩せて今度は説明の折り返しが増え、幅を据え置いて折り返しを許すとキー列が2行になり、由来をキーへ寄せた意味が薄れるため |
| 2026-08-18 | **由来を持つキーを5つ増やした**（`I` = Interrupt / `M` = Mode / `P` = Project / `D` = Delete / `T` = Today。§6 の表と条項の括弧書きも同時に更新。[FB-108](../../案件/closed_21_ユーザーフィードバック.md#fb-108) の追加要望） | 由来をキーの側へ寄せたことで**由来のある行と無い行の差が目立つ**ようになり、書けるものは書いておきたいというオーナーの判断。**綴りは `Interrupt`**（要望時の表記は `Interupt` だったが UI に出る文字列なので正しい綴りにした）。`Shift+H` / `Shift+L` / `T` の行は**由来があるのは末尾の `T` だけ**なので括弧書きも `Today` 1語に留める——`Shift+H` / `Shift+L` は vim の左右移動から来ており日本語の語との対応が無い（§6 の表側には注記を書いたが、ヘルプ一覧は1語で足りる）。`J` / `K`・`Enter`・`Shift+J` / `Shift+K`・`?` は**引き続き由来を持たない**（vim 由来・キー名そのもの・記号なので、無理に英単語を当てると創作になる） |
| 2026-08-23 | 終了予定（F-104）・予想開始（F-120）の**折返し表記（`25:30`）をやめ、暦日をまたぐ側に「翌」を前置する**形にした（§3.1「日またぎの時刻表記」を新設。要件定義書 F-104・データモデル定義書 §4.3 も同時に更新。[FB-84](../../案件/closed_21_ユーザーフィードバック.md#fb-84)） | 折返しの起点が論理日の暦日 0:00 だったため、日界（F-116）が 00:00 以外だと論理日の後半が 24:00 超で表され、日界 05:00 なら `29:00` まで伸びる。二桁の大きい数字は時計として読み取れない（オーナーが実運用で気づいた）。**折返しの起点を日界へ移す案は採らなかった**——日界 05:00 で朝の 06:00 が `01:00` と出て、**暦日の壁時計と数字が一致しなくなる**（現行が暦日起点を採っていた理由がこれで、そこは動かしたくない）。前置なら桁が増えず、数字はそのまま壁時計として読める。**記号（`+2:00`）ではなく「翌」の語にした**のは、セクション見出しの残り時間が `残り +0:30`（余裕の符号）を使っており、同じ画面で `+` が二義になるため。**警告色との非対称は残す**——「翌」は暦日をまたいだこと、警告色は論理日の区切り（次の日界）を越えたことを表し、日界 05:00 では `翌 2:00`（警告なし）と `翌 7:00`（警告あり）が別物として読める。もともと非対称だったものが、前置で見えるようになったぶん説明しやすくなった。**2日以上先は `+2日 3:00`** とする（見積もりが積み上がって当日中に終わらないとき。「翌」のままだと表示が嘘になる。「翌々」は3日目以降に続けられないので使わない）。**打刻修正（F-203）の表示は `HH:MM` のまま**（2026-08-03 の決定を維持。前置も付けない——入力欄と表示の書式を割らないため） |
| 2026-08-25 | セクション見出しの残り時間（F-110 / §3.2）の表示条件を「**表示日が今日**」から「**表示日が過去でない**」へ広げ、未来日にも出すことにした（[FB-104](../../案件/closed_21_ユーザーフィードバック.md#fb-104)） | 未来日を開いても残り時間が出ず、**その日の枠に予定が収まるか**を計画の段階で読めなかった（オーナーが実運用で気づいた）。**過去日には広げない**（オーナー判断）——過去の残り時間は「これから収まるか」ではなく「やり残しが枠のどれだけを食っていたか」という別の読み物になり、その日を語る役目は時間合計（実績）が既に果たしている。**今日の過ぎたセクション（現在時刻が枠の終了を過ぎたもの）は据え置きで出さない**（オーナー裁定）——過去日を出さない方針と筋が揃う。結果として条項は「過去でない」＋「今日はさらに now < 枠の終了」の2条件になるが、後者は今日だけに掛かる追加条件だと読めるよう書き分けた。**警告色（マイナス＝超過）は表示日を問わず適用する**（オーナー要望）——未来日で「収まらない」と読めることこそがこの拡張の目的なので、色を今日だけに絞る理由がない。**F-110 だけ「今日だけ」の規律から外れる**ので、その性質の違い（未来日では `max` が常に枠の頭を返し、静的な〈枠 ⇔ 予定〉の対比になる）を §3.2 に明記した。終了予定（F-104）・予想開始（F-120）・現在セクションの強調（F-121）・日付ピッカーの「今日」候補・§5 の現在地の探索は now 起点の値なので今日限定のまま——これらの条項が「F-110 と同じ規律」と引いていた参照は、F-110 を外して F-104 / F-120 / F-121 を引く形へ直した |
| 2026-08-26 | 「現在地」（§5）の未実行探索に**規則3「現在セクションより後ろのセクション」を挿入**し、未分類を3番手から4番手へ下げた（要件定義書 F-211 も同時に更新。[FB-109](../../案件/closed_21_ユーザーフィードバック.md#fb-109)） | [FB-78](../../案件/closed_21_ユーザーフィードバック.md#fb-78) で足した規則2（現在セクション優先）は「打刻ループの最中にトリアージ前のインボックスへ飛ばない」ためのものだったが、**現在セクションの未実行を打ち終えた瞬間にその穴が開く**——次点が未分類だったため、`N` も終了打刻後の送りもリスト先頭のインボックスへ飛び、その日の続き（後ろのセクション）へ進めなかった。オーナーの実運用での気づき。**「続き」は現在セクションより後ろのセクション**と定めた（§3.2 の回転順で後に来るもの。未分類は回転の対象外なので含まない）。**未分類を順路から外さず4番手に残した**のはオーナー確定——FB-78 で「見落としたくない置き場」として残した判断を引き継ぎ、クイック追加した直後のタスクへ `N` で戻れる道も保つ。**やり残し（現在セクションより前に残った未実行）を未分類より後ろに置いた**のは、今日の表示では §4.2 規則 b がそれらを現在位置の直後へ繰り下げるため前のセクションに居座り続けないのに対し、未分類は規則 b の対象外で構造的に残るから（＝実際に拾う必要があるのは未分類の側）。実装上は**規則4・5 に専用の分岐を持たせない**（未分類がリスト先頭にある以上、全体探索がそのまま未分類を先に見る）——FB-78 で規則3 に分岐を書かなかったのと同じ理由で、観測できない分岐を作らない。**3か所（初期選択・`N`・終了打刻後の送り F-211）すべてに適用**したのもオーナー確定で、F-211 は「上から順にこなす打刻ループを保つ」ためのものだから続きへ進む方が趣旨に合う（FB-78 で3か所を揃えた判断も維持）。**表示日が今日でないときは規則2 と同じく規則3 も飛ばす**（現在セクションが定義できない）。要求軸は既存に収めた——F-211 の要求文「次にやるタスクへ自動で送り、打刻ループを止めない」の『次にやるタスク』の定義の是正であり、`N` 側は N-04 の範囲（FB-78 と同じ判定） |
