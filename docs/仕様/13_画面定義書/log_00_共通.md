# 共通 決定ログ

本文に書かない「なぜそうしたか」を日付順に残す。仕様そのものは[共通](./00_共通.md)が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-19 | 修飾キーは Shift のみ、preventDefault は最小限 | ブラウザ既定動作と衝突させないため |
| 2026-07-20 | グローバルナビを上部の横タブから左サイドの Navigation Rail へ変更（FB-08） | 項目が4つに増えても縦なら伸ばせる。本文の縦方向をヘッダに取られない |
| 2026-07-20 | レールはラベルのみとし、アイコンを使わない（FB-08） | 意味を覚える必要がなく装飾も増えない。N-05 との整合をこの形で取る |
| 2026-07-20 | 各画面に h1 を置く（FB-08） | S-01 だけ見出しがなく、文書構造としても抜けていた |
| 2026-07-20 | レールを画面に固定（FB-08） | 下へスクロールした状態からでも他画面へ移れるようにするため |
| 2026-07-20 | すべてのトーストを自動消去＋×で手動クローズ（FB-15） | エラートーストが残り続けて画面を塞いでいたため |
| 2026-07-20 | マスタ編集をセルのインライン編集へ（FB-03） | 編集ボタンを都度押すのが面倒だったため |
| 2026-07-20 | 保存中は同じ行の他のセルを編集させない（FB-03 のレビュー指摘） | 行全体を送る実装のため、古い値での上書きが起きうる |
| 2026-07-20 | ポップオーバーは下に入りきらなければ上向きに反転（FB-21） | 画面下部の行で選択肢が画面外へはみ出し、選べなかった |
| 2026-07-24 | レール上部のアプリ名を今日のデイリー（`/`）へのリンクにする（FB-45） | ロゴ＝ホームへ戻る一般的なパターン。日付を離れた表示日から今日へ一手で戻れる |
| 2026-07-25 | 未設定の表記を §2.4 として本書に新設し、各画面（S-01 §3.3・S-02 §3・S-04 §3.3）はそこを参照する形にする（FB-55） | FB-48 でデイリーのモード列・プロジェクト列を薄色の `-` に統一した結果、ルーチン一覧とレビュー画面の同じ列が空欄のまま残り、同じ「未設定」が画面によって分かれた。オーナー判断は「すべての空欄は `-` で揃える」。**同じ規則を3つの画面定義書に書き足す形は採らなかった**——過去に同じ挙動が複数文書に全文で書かれて改訂のたびに食い違う事故が起きており（CLAUDE.md 書き方の規約）、3か所に並べれば同じことが起きる。空欄をやめる理由は、空欄が「そこに表示する余地がない」と読めてクリックで設定できることが伝わらないため。**`--:--` を統合しなかった**のは、時間の値には「未設定（0分）」と「未確定（実行中で実績が出ていない）」の2種があり、桁の形を保ったまま不在を示す役目が `-` と別だから。集計の「（未設定）」グループ行（S-04 §3.5）も、セルの不在ではなく行のラベルなので対象外とした |
| 2026-07-26 | ボタンにフォーカスがある間の `Enter` を画面のショートカットから除外する規約を §3 に明記（FB-60） | 実装（S-01 の打刻）は以前からこの除外を持っていたが、根拠がコードコメントにしかなく、§3 の除外規則は「テキスト入力中」までしか書いていなかった。**挙動は変えず条項だけ足した**——ブラウザがフォーカス中のボタンを押す以上ショートカットも走れば同じ操作が必ず二重に発火するため、除外は妥当。特定画面の事情ではなくフォーカスとキー入力の一般的な衝突なので S-01 §6 ではなく共通規約に置き、§6 からは参照させた |
| 2026-07-26 | Undo の有効期限を「そのトーストが出ているあいだだけ」と §2.2 に明記（FB-65） | 実装は削除（S-01 O-8）・完了の取り消し（O-15）の保留を、トーストが消える（5秒の自動消去・×）のと同時に破棄しており妥当（保留を無期限に持つと何を戻す操作なのかが読めなくなる）だが、O-8・O-15 は「トーストのボタン、または続けての `U`」としか書かず期限が読み取れなかった。期限はトースト部品の寿命そのものなので規則は §2.2 に1か所だけ置き、O-8・O-15 からは参照させた |
| 2026-07-26 | §3 の4条の**かかり方**を明記する（箇条1・2 はキー操作すべて／箇条3・4 は画面全体のショートカットで、オーバーレイの内側は対象外）。T-45 | 共通ガードを `keyboard.ts` へ集約する作業で、**4か所の実装が同一ではなかった**ことが判明した——`use-daily-shortcuts.ts` と `review-board.tsx` は入力欄フォーカスを見るが、`date-picker.tsx` と `select-popover.tsx` は見ない。**単純に1本化すればオーバーレイ2つの挙動が変わる**ところだった。`spec-reviewer` の検算では、§3 の箇条3・4 が文中で「画面のショートカット」と限定しており箇条1・2 が無限定なので、**この差は元から条項が書き分けていたもの**で実装は正しかった。ただし読み手がそこまで辿れる保証がなく、コード側が2関数（`isOperableKeyEvent` / `isGlobalShortcutEvent`）に分かれて**この区切りに依存するようになった**ため、暗黙のままにせず1段落として立てた。**挙動は変えていない**。オーバーレイが対象外である理由（開いている間はオーバーレイが入力の主体で、背後の画面のショートカットは別途止まっている）も添えて、次に画面を足す人が判断できる形にした |
| 2026-07-26 | §2.3 の「保存中」の適用範囲を**確定・取消・新規追加行にも広げる**（応答待ちの入力欄からの再確定・新規追加行の `Enter` 連打・保存中の取消を止める）。T-44 | それまでの条項は「同じ行は編集できない」＝**開き直しだけ**を止める書き方で、①開いたままの入力欄で値を変えて blur すると2件目の UPDATE が飛ぶ ②新規追加行で `Enter` を連打すると同じ行が二重に作られる ③保存中に取消すと行が閉じて失敗のメッセージだけが残る、の3つが残っていた（T-44 で編集セル・新規行を部品へ抽出した際に `test-reviewer` が指摘。**いずれもマスタ管理3表すべてに元からあった持ち越し**で、抽出そのものが作った欠陥ではない）。条項の狙いが「古い値での上書きを防ぐ」だけなら①②は範囲外とも読めるが、**二重送信の防止も同じ狙いに含める**とオーナーが判断した——一意制約に当たらない名前で二重に作られると、利用者から見て取り消し方が分からない状態になる。部品が1か所に集まった直後が最も安く塞げるタイミングなので、条項改訂と実装を T-44 の PR に含める |
| 2026-07-26 | §2.3 の「保存中」をさらに**新規作成フォーム**（インライン編集のセルに限らない）と**保存中に始める操作**（一覧の「新規追加」ボタン・送信せず表示だけ変えるその場の選択）へ広げる。T-44 | 1つ前の改訂を実装した直後のレビューで、同じ穴が条項の外側に3つ残っていることが分かった。①**ルーチン管理の新規作成フォーム**の保存・取消が保存中も押せる（`spec-reviewer`・`code-quality-reviewer`・`test-reviewer` が独立に「高」で指摘）——改訂した §2.3 は「インライン編集」節にあるためフォーム部品まで及ぶかが読みで割れ、**及ぶ**とオーナーが判断した（同じ二重送信・同じ「失敗メッセージだけ残る」が起きる以上、部品の形で線を引く理由がない） ②一覧の「新規追加」ボタンが保存中も押せ、押すと編集中のセルが閉じる——**実質「保存中の取消」**なので同じ扱い ③モードの色の選択が保存中も押せる——送信はされないため二重送信は起きないが、保存中だけ送信する値（旧色）と表示（新色）が食い違う。①〜③はいずれも**元からあった持ち越し**で、T-44 の抽出が作ったものではない。あわせて、前の改訂で「失敗のトースト」と書いた箇所を「失敗のメッセージ」に訂正した——マスタ管理3表の失敗表示はトースト（§2.2）ではなくインラインの帯である |
| 2026-07-30 | §2.1 の表に「適用」列を足し、**表示位置（上向き反転）・候補選択（`J`/`K`＋Enter・現在値ハイライト）・候補の折り返しはデイリーの候補選択ポップオーバー（F-112 / N-04）に限る**と明記する。閉じ方・文字色は全画面のまま（FB-59） | §2.1 が候補選択の詳細を S-01 §6 へ委譲していたため、**マスタ管理のカラーピッカー（§3.2）にも同じ規約が及ぶのかが読み取れなかった**のが問題の本体。オーナー判断は「限る」——上位文書（要求 F-112・要件 F-112 / N-04）が元から「モード/プロジェクト/セクションの選択ポップオーバー」「S-01 の全主要操作」とデイリーを名指ししており、共通側だけが無限定に読める状態だった。カラーピッカーは候補が**色の丸の2次元グリッド**で、`J`/`K` の1次元移動も「1行に収める」制約も意味を持たない（1次元移動を当てると2列目以降への移動規則を別途決めることになる）。**閉じ方と文字色を全画面に残した**のは、実装が既に全画面で `Esc`／外側クリックを守っており、部品の形によらず成り立つ規約だから。実装は変更なし（カラーピッカーへ `J`/`K` と反転を足す②は採らない）。**「適用」列には対象のポップオーバーまで書いた**——S-01 の中でも項目ごとに対象が違い（表示位置＝反転は行メニュー・ルーチン化・日付ピッカーにも及ぶが、`J`/`K` の候補移動は属性選択 O-5 だけで、行メニューは実装が持たず S-01 §7 も表示位置と閉じ方しか委譲していない）、「S-01」とだけ書くと行メニューにも `J`/`K` が要ると読めてしまうため（`spec-reviewer` 指摘） |
| 2026-08-01 | 永続化の失敗を §4.1 として立て、失敗を**結果が届かなかった / サーバが失敗を返した**の2種に分け、**後者だけ表示データを取り直す**。あわせて「すでに存在しない対象への更新は失敗として返す」を条項化した（FB-64 / FB-70） | §4 は「失敗時はトースト＋ロールバック」としか書いておらず、実装が扱えていたのはサーバが `{ok:false}` を返す失敗だけだった——Server Action の呼び出し境界6か所のいずれにも `catch` が無く、**通信断では拒否が transition の外へ抜けて何も表示されない**（画面だけ成功したように見えたまま保存されていない状態になりうる）。楽観的更新は「失敗したら戻す」が対で初めて成立するため、まず失敗を1本の経路に集める形にした。**2種に分けた**のは画面の直し方が逆になるから——届かなかった失敗にサーバへの取り直しを投げても必ずまた失敗し、新しい例外源を増やすだけになる。逆にサーバが失敗を返したなら通信は生きているので取り直せて、**削除済みの行が画面に残り触るたび同じ失敗を繰り返す**状態を断てる。**存在しない対象への更新を失敗として返す**規則を共通条項にしたのは、実装がこれを打刻・並び替えでは検出しインライン編集では検出しない非対称を持っており（同じ O-5 の中でもセクションは検出し、モード・プロジェクトは検出しない）、その分かれ目が「ドメイン判断に現在値が要るか」という実装都合＝**存在検査が副産物としてしか行われていなかった**ため。仕様として決めた覚えのない振る舞いだったので、ここで決める。**S-03 マスタ管理も同じ差分で揃えた**（`spec-reviewer` 指摘）——条項を画面横断で立てた以上、S-01 だけ直すと非対称が別の場所へ移るだけになる。S-02 ルーチンは以前から検出済みで、残っていたのはマスタ3種の更新・アーカイブ切替・復元・日界切替（セクションの復元と日界切替は「見つからなければ成功」を返していた）。コードは物理削除が既に持つ `not_found` を再利用し、**文言からは「画面を再読み込みしてください」を落として「すでに削除されている可能性があります」に改めた**——取り直しは実装が自動で行うようになったので、手作業を促す指示が実装と重複するため。**一律に error boundary で受ける案は採らなかった**——打刻1回の通信断で画面全体がエラー表示に置き換わり、§4.1 が定めるトースト＋ロールバックを満たさない（保険としては別途有効だが、これ単独では答えにならない）。S-01 §8 には行を残して §4.1 を参照させた——削除済みタスクへの操作が同じ表の「別PCとの同時操作の競合（後勝ちを許容）」の対象外であることは、画面側で読めないと誤解を招くため |
| 2026-08-08 | §1.1「文字サイズ」を新設し、**全画面で共通の3段スケール（主 / 従 / メタ）**を定めた。主＝行の名前と時間の値すべて、従＝分類・コメント・列見出し・フォームの入力行、メタ＝セクション見出しの数値とフォームの項目ラベル。**主従を色で表さない**。あわせて**デイリーの「本文を1段階拡大する」例外（S-01 §2・FB-05）を廃止**し、デイリーの階層を全画面の基準に据えた（FB-91） | 発端はルーチン一覧が小さく見えるというオーナーの気づき。棚卸しすると**ルーチン固有ではなく**、レビューも同型で、サイズの絶対値は画面ごとにバラバラだった（デイリー16/14、ルーチン14/12、レビュー14/12）。ところが**主従の切り方は3画面で完全に一致していた**——名前と時間の数値が主、分類が従。つまり壊れていたのは階層ではなく画面間の絶対値で、**直すべきは「揃える対象」の取り違えだった**。最初に「本文は全画面で同じサイズ・列ごとに変えない」と条項化して実装まで通したが、それは**画面間の不揃いを消すために行内の階層まで潰す**もので、オーナーの「行の主要素とそれ以外に差があるほうが視認性が高い」で撤回した。**主従を色で表さない**のは、モード色（F-401）が文字色を占有しているため——従列を薄色にすると、モード付きの行とそうでない行で主従の見え方が変わる。この制約があるためサイズが唯一の軸になり、3段が要る。**主と従の境界は「時間の値か、それ以外か」**。ここは2度動かした——最初は「名前だけが主」、次に「名前と時間の量・記録が主（予定の設定値は従）」、最後にオーナーの「ルーチンは開始想定も主が良い」で**時間の値をすべて主**へ寄せた。記録と予定で分ける線は、条項としては書けても**同じ `HH:MM` が画面によって大小に割れる**（デイリーの実施時間は主・レビューの時刻列は従、という食い違いが実際に残っていた）。**このアプリが読ませたいのは時間そのもの**なので、時刻・所要時間をまとめて主に置き、従は分類とコメントだけにするのが最も単純で、セクションのマスタ画面で時間帯（従）と長さ（主）が隣り合って割れる問題も同時に消えた。**列見出しは従に合わせた**——見出しが指している中身が従なので、そこに揃えるのが素直（主に合わせると見出しがデータと同じ強さで主張する）。**ボタン・浮遊面・ナビを対象外にした**のは、それらが本文の一部ではなく部品で、独自のスケールを持ってよいから |
| 2026-08-08 | §2.4 の対象を**セルの記号だけから、選択肢・読み上げ・集計の行ラベルまで**へ広げ、**語を「未設定」1つに揃える**。セクションの `null`（「未分類」）は対象外と明記した（FB-57） | FB-55 で §2.4 を新設したときに規定したのは**セルの表記だけ**で、選択肢・読み上げ・行ラベルの語は無規定のまま残った。その結果、同じ「未設定」が**5通り**に割れていた——セルは `-`、デイリーの選択ポップオーバーは「モードなし」「プロジェクトなし」、ルーチン編集フォームのセレクトは「なし」、読み上げ（`aria-label`）は「モード（未設定）」、レビュー集計のグループ行は「（未設定）」。**規定していない場所が割れる**という FB-55 とまったく同じ壊れ方なので、対処も同じ形（上位文書で確定 → 各画面は参照）を取り、**今度は「語を当てる場所」を数え上げて表に載せた**（範囲を狭めればまた同じことが起きる）。オーナー判断は**「未設定」に統一**——採ると語が1つになり、残るバリエーションは「記号 `-`（セル）／語『未設定』（それ以外）」の2つだけで、§2.4 の見出しとも一致する。**「なし」は採らなかった**：選択肢としては最も自然な日本語だが、集計の行ラベルが「（なし）」になってラベルとして弱い。**属性名を冠する案（「モードなし」）も採らなかった**：ポップオーバー単体で何の選択か読めるという利点はあるが、ルーチン編集フォームでは直上の項目ラベルと重複し、かつ語が「〜なし」と「未設定」の2種のまま残って FB の主旨が半分しか解けない。**集計行の括弧は残した**——語は同じで、括弧は「マスタの名前ではなく行のラベル」を示す記法として働く（マスタ名が「未設定」でも区別が付く）。**セクションの `null` を対象外にした**のは、「未分類」が値の不在ではなく**0件でも常に表示するインボックスグループの名前**だから（S-01 §3.2）。選択肢だけ「未設定」に変えるとデイリーのグループ見出し「未分類」と食い違うため、対象外であることを表に1行として残し、次に読む人が「揃え忘れ」と誤認しないようにした。あわせて、**ポップオーバーの未設定候補を薄色にする**規則も §2.4 へ書いた——実装（`select-popover.tsx`）とテストは以前から薄色にしていたが**どの文書にも書かれていなかった**もので、これも「無規定だから割れる」の同類。実装は変えていない。**語彙の直書きを ESLint（`no-restricted-syntax`）で禁止した**のは、FB-55・FB-57 の壊れ方がどちらも「新しい箇所が自分で語を書いた」であり、既存の使用箇所をテストで固定するだけでは同じ乖離が戻るため（`test-reviewer` 指摘）。文字列リテラルと **JSX の子テキスト**の両方を見る——FB-57 の実際の犯人が `<option value="">なし</option>` で、これは文字列リテラルではないため片方だけでは捕まらない |
| 2026-08-08 | §2.4 の**「空欄にしない」の適用範囲を「クリックしてその場で値を入れられるセル」に限る**と明記し、記録・導出のセル（実績・打刻時刻・差異）は空欄でよいとした。あわせて**`--:--` を出すところでは必ず薄色**を条項化した（FB-93） | FB-57 で語を揃えたあと、**時間の値は記号と色でまだ割れていた**——デイリーの未実行行は実績列を空欄にし、レビューは実行中行に `--:--` を出し、その `--:--` の薄色は見積もり列にだけ付いていた（`test-reviewer` と `code-quality-reviewer` が独立に指摘）。§2.4 の「時間の値（見積もり・実績・打刻時刻）」という列挙は FB-55 で**見積もり列を念頭に**書かれたもので、各画面の条項と突き合わせられていなかった。オーナー判断は**「設定できるセルに限る」**——§2.4 が空欄をやめる理由は「クリックして設定できることが伝わらない」であり、**その理由は設定できるセルにしか掛からない**。実績・打刻時刻は打刻の結果で、未実行のあいだは設定する対象ではないので空欄で困らない。**実装を条項に寄せる案（デイリーの未実行行にも `--:--` を出す）は採らなかった**——朝は未実行行が多く `--:--` が2列に並んで横の縞模様が出るうえ、時刻列は予想開始時刻（F-120）と場所を争う。**記号を各画面に委ねたまま色だけ揃えた**のは、記号の要否は「その画面が何を読ませたいか」で決まる（デイリーは今日これからやることの一覧、レビューは終わった記録の読み返しなので、後者では未確定の行だけが桁の形で浮くほうが読みやすい）が、**同じ記号の濃さが画面・列で変わる理由は無い**——色の差があると意味の差だと読んでしまう。実装では `UnsetTimeMark` を立てて**薄色を部品側に持たせた**（`UnsetMark` と対）——条項を呼び出し側の付け忘れで破れる形にしておいたのが、そもそもこの割れの原因だったため。レビューの `開始–終了` は確定値と記号が1セルに同居するので、記号側だけを弱める。**薄色の適用は「セル」に限らない**——最初はレビューの2列だけを直したが、レビュー3本（`code-quality-reviewer`・`test-reviewer`・`spec-reviewer`）が独立に**セクション見出しの時間合計**（S-01 §3.2）の抜けを指摘した。見積もり0のタスクだけのセクション＝**クイック追加の直後**に `合計 --:--` が出るのに、そこはメタ段の濃さのまま確定値の分母と並んでいた。原因は `formatEstimate`（文字列を返す整形関数）を呼ぶ4か所が各自で薄色を付ける形だったこと——`UnsetTimeMark` で塞いだのは記号を直接置く経路だけで、**整形関数の経路には同じ穴が残っていた**。オーナー判断は「メタ段も薄色に揃え、機構を1本化する」で、`formatEstimate` を廃止して `DurationValue`（0分を未設定として扱う表示部品）に置き換え、4か所すべてがそこを通る形にした。これで「記号を出すところでは必ず薄色」を**全経路で部品が保証する**。テスト側も、セル自身が薄い場合と記号側の span だけが薄い場合を同じ観測レベルで主張できるよう `faintTextOf`（`_testing/dom.ts`）を置いた——機構の書き方にテストを縛ると、等価な整理でも赤くなる（`test-reviewer` 指摘）。**記号の定数を直に import する経路も ESLint（`no-restricted-imports`）で閉じた**——`--:--` の直書きは FB-57 のルールが止めるが、`UNSET_TIME_MARK` を import して自前で span を書けば薄色を付け忘れられる。記号は「必ず薄色」が対になるので描画を部品に閉じ、**語（「未設定」等）は色を伴わないため対象外**とした（各画面が定数を直に使ってよい） |
| 2026-08-09 | **§4.2「確定を待つ操作」を新設**し、進行中の合図（0.5秒を超えたときだけ出す）と再発火の抑止を、画面ではなく**操作の性質**で定めた（FB-76 / N-12） | 起票時の FB-76 は「保存中に無効化されるボタンが無効に見えない」で、対処案は `ui.ts` に無効時の見た目を1つ定めることだった。**着手時にオーナーの実運用の体感を確かめて立て方を取り替えた**——本番で困っているのは Neon 無料枠の寝起きで保存が長引く場面であり、そこで足りないのは「押せない」ことではなく**進行中だと分かる合図**の方だった。無効の見た目を揃える案は、①遅いときに伝わる情報が少ない ②速いときに点滅する（避けるには結局遅延が要り、手間が変わらない）③恒久的な無効（ルーチン由来はルーチン化不可・日界セクションは削除不可）と保存中が同じ見え方になり意味の差が消える、の3点で採らなかった。**遅延を条項の中心に据えた**のは、窓の長さが環境で桁違いに変わるため——ローカルの実測は数十ms（[FB-74](../../案件/closed_21_ユーザーフィードバック.md#fb-74) の計測）で、そこに何かを出しても点滅にしかならない。**画面ではなく操作の性質で定めた**のは、画面で切ると FB-94（複製・先送りを楽観的更新にする）が通った瞬間に条項が実態とずれるため。あわせて**再発火の抑止**を同じ節に入れたのは、§2.3 をデイリーへ適用外とした 2026-07-26 の根拠（楽観的更新なら別セルを開いた時点で画面の値がすでに新しい）が**確定を待つ操作には当たらない**と着手時に判明したから——画面が変わらないまま待つので、遅い状態で先送りを2回押すと2発目がすでに先送り済みのタスクをもう1日ずらす（Server Action は直列化されるので同時ではなく順に適用される。[アーキテクチャ定義書](../15_アーキテクチャ定義書.md) §7）。抑止の範囲を「確定を待つ操作すべて」とし楽観的更新の操作を通したのは、N-01 の体感を損なわないため。起票時の主題だった**無効の見た目の不統一は別エントリへ分離**した（現状 `disabled:text-ink-faint` / `opacity-40` / `opacity-60` / 無指定の4通りが混在しており、恒久的な無効の可読性と合わせて別に詰める） |
| 2026-08-09 | Navigation Rail に「バンドル」を追加して項目を5つにし、**ルーチンの直後**に置いた（F-119 / S-05） | バンドルの管理を独立画面にした判断そのものは[log_05](./log_05_バンドル管理.md) 2026-08-09 が持つ。ここで決めたのは並び順だけ——束ねる対象がルーチンで、S-02 と S-05 を往復して整えるため隣り合わせる。マスタ（S-03）の隣に置かないのは、バンドルがラベルの台帳ではなく編成だから |
| 2026-08-10 | §4.1 に「**保存完了を待つ画面（S-02 / S-03 / S-05）の失敗はトーストでなく表の上部の帯**で出し、**完了の通知は出さない**」を明記した | 条項は「知らせ方はトースト」と一律に書いていたが、S-02 / S-03 は最初から `TableFrame` のエラー帯で通しており、**条項と実装が食い違ったまま3画面が動いていた**。F-119 で S-05 を起こす際にレビューで露見し、オーナー裁定で**実態を正とした**（S-05 だけトーストにすると同じ性質の画面で知らせ方が2通りになり、揃えるなら既存3画面を直す作業が F-119 のスコープに入ってくる）。**なぜ画面で分かれるか**も残した——S-01 でトーストが要るのは楽観的更新で画面が先に変わり確定したか見た目から分からないからで、保存完了を待つ画面は一覧がその結果に変わること自体が合図になる |
