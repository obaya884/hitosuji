# テスト戦略定義書 決定ログ

本文（[17_テスト戦略定義書.md](./17_テスト戦略定義書.md)）に関する判断を日付順に残す。本書の分離（2026-07-26）以前のテスト戦略の判断は[アーキテクチャ定義書の決定ログ](./log_15_アーキテクチャ定義書.md)にある（そのまま動かさない）。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-08-01 | CI のテスト実行タイムゾーンを **UTC と JST の2本**にし、§9 に「実行タイムゾーン」を新設した（T-62） | 論点は「検出網をどう張るか」ではなく「**本番環境をなぞれているか**」だと整理し直した。当初は「ローカル TZ 依存を再導入したら落ちる網」として、JST とも UTC とも暦日がズレる極端なゾーン（`Pacific/Kiritimati` = +14）1本を検討したが、**本番に存在しない環境なので赤くなったときに「本番で困るのか」を判断できない**ため捨てた。代わりにアプリが実際に走る環境を数えると2つ——Vercel 上の Node（UTC）とオーナーの端末のブラウザ（JST）——で、テストの段もそれに対応する（unit・integration が Node 段、component が jsdom ＝ ブラウザ段）。**JST のみ**（CI を手元に揃える案）は、本番のサーバ側を一度も試さないため退けた——オフセットを書かない `new Date("...T10:00")` をサーバ側に書くと手元でも CI でも緑のまま Vercel でだけ日付がズレ、気づく手段が無くなる。**UTC のみ明示**（現状の追認）も、ランナー既定が UTC で実質いまと同じでブラウザ側を試さないため退けたが、「既定に頼らず明示する」こと自体には意味があるので基準側の `TZ: UTC` として残した。着手時点で TZ 依存は残っておらず（UTC / +14 / −11 / New_York の4ゾーンで unit・component 1566 本と integration 63 本が全件緑）、足したのは網だけ |
| 2026-08-01 | 上の実測の過程で見つかったフレークテスト1本を、`waitFor` で待つ形ではなく **`deferredAction` で応答を自分で解く形**へ直した（T-62 の副次。根本側は [T-92](../案件/closed_23_技術改善バックログ.md#t-92)） | `modes-table.test.tsx` の「追加の失敗はメッセージで知らせ、新規行を残す」が20回中2回落ちていた。TZ とは無関係で、エラーメッセージの描画と `isPending` の解除が同じコミットで届くとは限らないのに同期で `disabled` を読んでいたのが原因。最初は `projects-table.test.tsx` の先例に倣い `waitFor` で包んだが、`test-reviewer` が「**即解決のモックでは抑止が掛かった瞬間を観測できず、`disabled={false}` の変異が生き残る**」と指摘した——「解けた」と「そもそも抑止されなかった」を区別できていなかった。応答を `act` の中で解く形に変えると両側の変異で落ち、かつ待ちに依存しないのでフレークが構造的に消える。unit 15回・component 20回・integration 8回を回して、フレークはこの1本だけだと確かめている |
| 2026-08-01 | **§10「変異テスト」を新設**し、発火条件を差分ベースで置いた（①テストを新規に足した差分で1本選んで壊す ②種別「内部設計」の T-XX で網が効くことを1点確認）。節は §9 の後・決定ログの前に足し、既存の §1〜§9 は動かしていない（T-87） | 明文化されていたのは**手続きと担当者だけ**で、いつ実施するかがどこにも無かった。実務では定着していた（FB-68 では2回実施）が、いずれも判断ベースで条項に従ったものではなく、**怠っても緑のまま**なので誰も気づかない。粒度は T-87 の対応方針案（「ガード・分岐・early return の追加/変更を主目的とする差分」）を採らなかった——本番コード側の性質で引くと該当判定が主観になり、レビュー4本の起動条件（CLAUDE.md「起動の契機」＝差分に何があるか）と引き方が揃わないため。「テストファイルを足したか」なら `git diff --name-only` で決まる。結果の残し方はコミットメッセージ本文1行（現状慣習の追認）に留め、台帳・本ログには積まない——頻度が高く1件ずつ記録する値打ちがない。節番号は**新設を末尾寄り（§10）に置いて §1〜§9 を据え置いた**——カバレッジ（§7）の隣が内容的には自然だが、§8・§9 への外部参照が CLAUDE.md・`verifier`・`test-reviewer`・アーキテクチャ定義書・完了記録に散っており、改番の巻き添えが本文の得より大きい |
| 2026-08-01 | 変異テスト**専用のサブエージェントは置かない**と決め、[T-68](../案件/23_技術改善バックログ.md#t-68) を「網羅的な変異テストの道具（Stryker）を評価する」へ書き換えた | T-68 の動機は「走らせる部分の出力が長くメインセッションのコンテキストを圧迫する」だったが、上の §10 で実施が「差分あたり1回」に定まると、対象ファイルだけを回せば出力は数行で、長ければ**実行だけ `verifier` に頼める**（変異そのものの委譲だけが禁止されている）。エージェント1本の起動コスト（T-66）に見合わない。当時の論点①（`isolation: worktree` で事故を防ぐ）③（`verifier` と別に立てる意味）も前提が消えて同時に不要になった。残るのは論点②「**どこを壊せば意味があるかごと委譲する**」＝網羅的な変異だが、これはエージェントより Stryker Mutator の領分——決定論的に動くので T-67 のような戻し忘れが構造的に起きず、判断の当たり外れも無い。ただし変異体ごとにテスト一式を回すため遅く、`src/domain` に絞る前提でなければ実用にならないので、着手条件付きの様子見に置いた |
| 2026-08-01 | **クリックを `src/app/_testing/interactions.ts` の `click` に通す**条項を §4 に足し、適用範囲を「**Server Action を呼ぶコンポーネントのテスト**」に絞った（[T-92](../案件/closed_23_技術改善バックログ.md#t-92) / [T-93](../案件/closed_23_技術改善バックログ.md#t-93)） | 置き場を新ファイルにしたのは、`dom.ts` が読み取り・`actions.ts` が偽物の置き場で、どちらも操作ではないため（同じ切り分けを `masters/_testing/table-helpers.ts` の冒頭コメントが既に書いている）。適用範囲を全コンポーネントテストへ広げなかったのは、`fireEvent.click` が 250 箇所超あり、その大半が **Server Action を持たない部品**（`date-picker`・`row-menu`・`toast` 等）だから——待つ相手が無いところに `act` 包みを課しても罠は消えず、差分だけが膨らむ。`vi.mock("./actions")` を持つファイルは5つで、そこだけが「押した後に応答が返る」テストになる。**blur・Enter 起点の確定は対象外**にした（T-92 の対応方針が click に限られており、確定経路まで広げると差分が倍になる）ので、それらの `waitFor` は残っている。**`daily-board.test.tsx` は同じコミットに入れず [T-96](../案件/closed_23_技術改善バックログ.md#t-96) へ分けた**——1881行あり楽観的更新のロールバックが絡むうえ、既にローカルで act 包みの道具（`clickAndSettle`）を使い分けていて危険が小さい |
| 2026-08-02 | **セクション枠の終了時刻をフィクスチャ内で導出に倒し**、§4 に「同じフィクスチャ内の別の値から一意に決まる従属値は導出する」を1行足した（[T-82](../案件/closed_23_技術改善バックログ.md#t-82)） | §4 は「テストが依拠する値は**呼び出し側に書く**」と「基準日は `TEST_DATE` 1つ（リテラルで書くと手動同期になる）」を併せ持ち、時間帯がどちら側かは条項から決まらなかった（T-53 のワーカーは「導出にすると時間帯が呼び出し側から読めなくなる」として見送っている）。オーナー裁定は**導出**——時間帯そのものは `SECTIONS` の `startTime` に書かれたまま残るのでフィクスチャを開けば追え、失われるのは「手写しの写し」だけだと判断した。そこで切り分けの基準を「**テストが前提として選ぶ値か、別の値から一意に決まる従属値か**」に置き、後者は「呼び出し側に書く」の射程外だと1行の中で明示した（`spec-reviewer` が「どちらに属するかの基準が本文から読めない」と指摘したのを、本文の追記ではなく同じ1行に畳んで解いた）。枠は**セクション名**で引く（添字だと `SECTIONS` の並び替えで別セクションになる）。副作用として、フィクスチャが production と同じ `sectionRanges` を使うようになったため `groupTasksBySection`・`toSectionOptions` の出力と突き合わせる期待値には枠の導出を使えない（トートロジーになる）——その旨を `factories.ts` のヘッダに明記した |
| 2026-08-02 | **クリックの道具を2つに分け**、Server Action に届かないクリックは `clickWithoutServer`（素の `fireEvent.click`）へ通す条項を §4 に足した（T-96）。適用範囲は `click` を使う5ファイル＋共有ヘルパすべて | T-96 の対応方針は「素の `fireEvent.click` も同じ道具に通す」＝単一の道具への統一だったが、実際に当てると `selectRow`（行の選択・76箇所）のような**待つ相手が無いクリック**まで `act` に包むことになり、`await` 76箇所と `it` 70個の `async` 化が付随する。これは 2026-08-01 に T-92 が適用範囲を絞った理由（「待つ相手が無いところに `act` 包みを課しても罠は消えず、差分だけが膨らむ」）がそのままファイルの内側で当てはまる形なので、**その理屈をクリック単位へ拡張して2つに分けた**（オーナー裁定）。素の `fireEvent.click` を残さないのは、残すと「判断した結果」なのか「`click` に通し忘れた」のかが読めなくなるため。**daily-board だけに留めなかった**のは、他4ファイルが届かないクリックも `click` に通しており、そのままだと「`clickWithoutServer` が付いていない＝サーバに届く」という読みがリポジトリ全体で成立しないから。**disabled のボタン・インライン編集中の行外クリック・`confirm` を false にした削除は `click` のまま残した**——1つ目は名前が「届かない」という主張の結論を先取りしてしまい、2つ目は実ブラウザでは blur 確定が届き、3つ目は同じボタンが他所では届くため |
| 2026-08-02 | **§2 に「複数のテストファイルで同じコンポーネントを描くときは前後処理を登録関数へ畳んでよい」を足した**（T-101。条件は「契約が同一のものだけ」「doc に積むものを列挙する」の2つ） | もとの §2 は詰め物について「共通化せず各ファイルに置く」としか書いておらず、1つのコンポーネントを6ファイルで描く形が想定外だった。**条項の趣旨は「何を差し替えたかが読めていること」**で、置き場が各ファイルであることはその手段にすぎない——登録関数に畳んでも doc に列挙すれば趣旨は満たせる。逆に「契約が同一のものだけ」を外すと、`scrollIntoView` の空実装と `vi.fn` のように一方へ寄せた瞬間にもう一方が嘘の緑になるので、そこは条件として残した。**条項の置き場を「詰め物」の項目の下から §2 の末尾へ移した**のは、`setupBoard` が畳んでいるのが詰め物だけでなく固定時計・Server Action の既定値・保留の後始末も含むため（`spec-reviewer` 指摘。詰め物の下に置いたままだと、実装が条項の射程を超えているように読める） |
| 2026-08-02 | **§3 にテストファイル分割の命名規則（`<対象>.<主題>.test.tsx`・対象は1つに保つ）を足した**（T-101） | コンポーネント段のファイル分割は T-101 が初めてで、規則がどこにも無かった。**「対象は1つに保つ」を明記した**のは、主題で割るつもりが対象で割れると（例: board と list を1ファイルに混ぜる）コロケーションの前提が崩れるため。主題名を「その名前で説明できる範囲」に絞る条項も同時に置いた——実際 T-101 では `shortcuts` に表示の配線が、`row-menu` に盤面全体の警告バナーが混ざっており、名前から外れるものが同居すると**次に読む人がその主題の全量をそのファイルだと誤読する** |
| 2026-08-02 | **§4 に「行の取り方は共有しない」を足し、共有 `dom.ts` の `rowOf` を廃してデイリー（`taskRow`）とマスタ（`rowOf`）へ分けた**（T-101。オーナー裁定） | 同じ「行を引く」でも**表の作りが画面ごとに違う**——デイリーは名前セルが必ずボタン（クリックで rename に入る入口）なので役割で引け、マスタは有効行がボタン・アーカイブ済み行が素のテキストなので文言でしか引けない。共有側に1つ置くと引き方の前提が合わない画面で静かに取り違えるうえ、デイリーでは実際に2つの入口（`row` と `rowOf`）が併存していた。**デイリーの `rowOf` 71箇所は `taskRow` へ寄せきれた**ので、文言ベースの入口はマスタだけに残った。寄せ替えで「同じ文言が画面に1つだけ」という暗黙の主張は失うが、代わりに「名前セルがクリック入口である」を全体で主張する形になる（意図した交換。名前セルの `button` を `span` に壊すと169件が赤くなることで確認した） |
| 2026-08-02 | **§7 から全体カバレッジの水準（「5割前後」）を落とし、本書は全体値を書かないことにした**（T-95） | 起票時の乖離は「5割前後」に対し実測 87.83%、着手時に測り直すと 88.52% で**数日でさらに動いていた**。数字を実測へ書き換えても同じことが繰り返される。そもそも §7 自身が「全体値ではなく層ごとの数字で読む」「全体値はテスト密度の指標にならない」と定めており、**本文が全体値の水準を持つ理由が無い**。数値ゲート・合格基準を設けない方針は変えていない（水準を書かないことと、ゲートを置かないことは同じ判断の表裏） |
| 2026-08-08 | **§6 の presentation に「共通部品へ抽出したら、観点も部品段へ移す」を足した**（T-100。画面段が持つのは ①その画面固有の列・操作 ②部品から Server Action への配線 ③その画面固有の失敗・制約、の3つ） | §6 は層の分担しか書いておらず、**コンポーネント段の中の粒度**を定めていなかった。そのため T-44 / T-79 でマスタ管理3表の編集セル・新規行・外枠を `_components/` へ抽出したあとも、3表のテストが抽出前の観点をそのまま保持し、同一のテスト名が17件（うち12件は3ファイル全部）残っていた。**部品を抽出しても観点を移さないと「部品段と画面段のどちらが正か」が読めなくなる**のが本質で、保守コスト（1つの規則を変えると4ファイルが赤くなる）はその症状にすぎない。境界は**「その画面しか持たないもの」**に引いた（オーナー裁定）——より緩い「テスト名が完全一致するものだけ落とす」案は、名前が違うだけで中身が同じもの（「変更なしの確定は…」と「名前の変更なしは…」）が残るので、機械的に見えて基準にならない。より強い「合わせ技も1表だけに代表させる」案は、表ごとに持つテストが非対称になり「なぜこの表にだけあるのか」が読めなくなるため退けた。落とす前に部品段にその観点があることを確かめる条件を付けたのは、`TableFrame` と `useServerAction` にもテストがある（＝画面段の重複の受け皿が `_components/` だけではない）ことが着手時に分かったため——受け皿を取り違えると観点ごと消える |
| 2026-08-08 | 上の②を「部品から Server Action への配線」から**「部品との配線（3方向すべて）」へ広げた**（同日中の修正。T-100 / `test-reviewer` の指摘） | 最初の書き方だと、**画面 → 部品へ渡す値**（`isPending`）と**部品のコールバック → 画面の状態**（`onClose` でセルが閉じる・`onCancel` で新規行が消える）が①②③のどこにも属さず、条項に従うと落ちてしまう。実際いったん落とし、`isPending={isPending}` を `isPending={false}` に変える変異でマスタ3表が**全緑のまま通る**ことを実測した。落とした側の理由付け（「必須 props だから型が担保している」）が誤りで、**型が防ぐのは省略だけ**であり誤った値は通る。より根本には、**配線は部品段では原理的に測れない**——部品のテストは「受け取った値どおりに振る舞う」ことしか主張できず、「渡していない／間違った値を渡している」は観測できない——という非対称があり、これを条項に書いていなかった。`lcov` でも `onClose` / `onCancel` の行が一度も実行されなくなっていたことが裏付けになった（削除前後で比較）。10本を戻して削減は33本→23本になったが、**測れないものを画面段に残す**という切り分けのほうが条項として長持ちする |
| 2026-08-29 | **ブラウザ段（§3）を導入し、設定（`vitest.browser.config.mts`）・実行（`npm run test:browser`）・CI ジョブ（`browser`）とも他の3段から分離した**。`vitest.config.mts` に project として並べる案は採らない（T-03） | 段そのものが Chromium バイナリを前提とし、他の3段と前提が違うため。並べると `npm test` と `test:coverage` の**双方**に「browser を除く」指定が要り（vitest の否定フィルタ `--project '!browser'` は integration の globalSetup を止められず使えなかった）、足し忘れると clean clone や `wt:new` した worktree で全テストがこける。分離すれば `npm test` 側に除外の記述が1つも要らない。CI も別ジョブなら `verify` の所要（3分台。[T-118](../案件/23_技術改善バックログ.md#t-118)）に足さずに済む |
| 2026-08-29 | **ブラウザ段の CI は JST の1本だけ**とし、§9 の UTC / JST 2本回し（2026-08-01 の決定）の対象にしない（T-03） | 2本回す理由は「本番が2つある」こと（Node 段は Vercel の UTC、クライアント側はオーナー端末の JST）。ブラウザ段の本番はオーナーの端末しかなく、UTC 側に対応する本番が存在しない |
| 2026-08-29 | **§3 に「ブラウザ段のキー操作は `userEvent` を使い、`fireEvent` を使わない」を明記した**（T-03） | [FB-42](../案件/closed_21_ユーザーフィードバック.md) の再現条件を実測して分かったのが「**本物のキー入力であること**」の1点だったため。実ブラウザでも `fireEvent`（`dispatchEvent` の同期呼び出し）では `stopPropagation` を外しても緑のままで、`userEvent`（CDP 経由の trusted event）でだけ赤くなった。ネイティブの discrete event では React が state を同期フラッシュするので「リスナが確定 → 再レンダー → 別ターゲットへ新しいクロージャでリスナ再登録 → その後そこへ伝播」の順になり、これが FB-42 の言う「隙間」だった。段の道具を取り違えると**この段を作った意味そのものが消える**ので、書き方の契約として明記した |
| 2026-08-29 | **§3 に「ブラウザ段には §2 の jsdom 詰め物を持ち込まない」を明記し、盤面の前後処理を `setupBoard`（詰め物あり）と `setupBoardInBrowser`（詰め物なし）に分けた**（T-03） | 実ブラウザには本物の `ResizeObserver` と `scrollIntoView` があり、偽物で潰すと**この段で測りたい幾何そのものが動かなくなる**。最初の題材（FB-42）は幾何を見ないので実害は出なかったが、そのまま置くと次に幾何を測るテストが嘘の緑になる。§2 の他の偽物（Server Action の差し替え・`next/navigation`）は段に依らず要るのでブラウザ段でもそのまま使う |
| 2026-08-30 | **ブラウザ段の依存を `optimizeDeps.include` に明示列挙することを §3 の契約に加えた**（T-03 の対応中に発覚） | 列挙しないと vite が実行の最中に依存を見つけて再最適化し、ページのリロードでテストの import が落ちる。**オーナーの手元（依存最適化のキャッシュが無い状態）で実際に失敗し、キャッシュの温まった作業環境では緑だった**——この非対称がそのまま「CI と clean clone だけが落ちる」形になるため、契約として書いた。列挙に drizzle-orm・pg が並ぶのは、盤面のテストが辿る `../actions`（`vi.mock` で差し替える側）が静的にはサーバの実装まで繋がっているため |
