# データモデル定義書 決定ログ

データ構造・主要ロジックの「なぜそうしたか」を日付順に残す。仕様そのものは[データモデル定義書](./14_データモデル定義書.md)が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-25 | §4.3 の予想開始時刻（F-120）で、実行中タスクの残りは**表示順の位置に依らず**常に起点へ含めることを本文に明記した | 式は元から「実行中の残り」を無条件、「未実行の見積もり」だけを「それより前」と限定して書いていたが、実行中タスクより上に未実行行が並ぶ場合（並び替え後・未分類の残置など）にどう読むかが式だけでは判じにくく、実装時に判断点として浮上した。位置で差し引く読みを採らないのは、そうすると「最後の未実行タスクの予想開始 ＋ その見積もり ＝ 終了予定時刻（F-104）」の恒等式が崩れ、同じ積み上げの途中経過という F-120 の定義（サマリの終了予定と行の予想開始が別勘定になる）と矛盾するため。実装ではこの恒等式をテストで固定した |
| 2026-07-22 | 日界（F-116）を専用の設定テーブルではなく `sections.is_day_start`（有効セクション内でちょうど1件）で表す | 日界は「どのセクションが1日の開始か」の1点であり、設定操作も「セクションを選ぶ」形（画面定義書03 §3.1）。1値のために新テーブル＋リポジトリ＋ポート＋ユースケースを増やすより、既にアプリ層の不変条件（有効最低1件・start_time 重複禁止）を持つ sections にフラグを足す方が構成が小さく、オーナーの捉え方とも一致する。有効内の重複は部分ユニーク索引、最低1件はアプリ層、日界セクションのアーカイブ禁止で担保する |
| 2026-07-25 | weekly に `week_interval`（n週おき。NULL/1=毎週）を追加し、隔週などを表現できるようにした（FB-44 / F-301） | 曜日を持つ weekly と n日ごとの interval の隙間（「特定曜日を n週間隔で」）を埋める。新 recurrence_type を足すと weekly と機能重複するため weekly を拡張。起算は start_date を含む週を第0週とし月曜始まり（datepicker と週の起点を揃える）。既存 weekly 行は NULL=毎週で無変更、次回保存時に 1 が入る |
| 2026-07-25 | `week_interval` の値域を 1〜53 に定め、CHECK を `BETWEEN 1 AND 53` にした（FB-44） | weekly は本質的にサブ年のリズムを表す道具で、意味のある最大間隔は「約年1回」。1年が触れうる最大週数=53を上限とし、それ以上（数年に一度）は weekly では表さない。上限は誤入力（530 等）の防御も兼ねる。0003 適用後の追い CHECK 変更のため migration を分けた（0004） |
| 2026-07-26 | §3.5・§4.4 の振り直しの範囲を「その日の中で全体／当日分」から「**挿入先（移動先）のセクション内**」へ訂正した（T-50） | `sort_order` の集約作業で `spec-reviewer` が実装との齟齬として報告し、検算したところ**文言のほうが不正確**だった。根拠3点: ①`daily-list.ts` は `tasks.filter(t => t.sectionId === section.id)` してから `sortOrder` で並べており、**`sort_order` はセクション内でしか比較されない**（表示順は「セクション（start_time 順）→ `sort_order`」） ②§3.5 自身が「末尾追加・ルーチン展開: **セクション内**最大値 +1000」と書いており、採番がセクション単位なのに振り直しだけ日単位というのは内部矛盾 ③スキーマに `sort_order` の一意制約が無いので、セクションをまたいで値が重なっても害がない。日単位で振り直しても意味がなく書き込みが増えるだけなので、**実装（セクション単位）が正しい**。なお「その日の中で」という表現は、おそらく「日をまたがない」（`sort_order` は `task_date` ごとに独立）を意図した書き方が、範囲の限定と読めてしまったもの。**挙動は変えていない**——実装は旧来どおりで、条項の記述だけを実態へ寄せた |
| 2026-07-28 | §4.3 のセクション残り時間（F-110）を `(セクション終了 − now) − 未完了見積もり` から**セクションを表示順に積み上げる形**へ改め、各セクションの作業は `max(到達時刻, セクション開始)` から始まるものとした（FB-80） | 旧式は `(セクション終了 − now)` に**そのセクションが始まる前の時間**まで空きとして数えており、まだ始まっていないセクションは常に「開始時刻 − 現在時刻」のぶん過大だった（本番でオーナーが検知）。有効セクションが24時間を敷き詰める（§3.1）ため最後の枠は次の日界まで伸びており、そこで過大分と枠の長さが同時に最大になって最も壊れて見えた（現在 10:30・枠17:00 の午後に対し残り +18:30 と、枠より大きい値すら出る）。積み上げ（F-104 / F-120）を使う形に寄せたのは、行に出ている予想開始時刻と同じ「先の予定に押される」感覚を見出しにも通すため。**`max` を取ってセクション開始まで待たせる**のは、素の積み上げ（§4.3 が F-120 に定める「セクションをまたいでもリセットしない」）だけだと、早く終わる日に「セクション開始前に積み上げが到達する」状態が残り、依然として残りが枠を超えるため——今回の不具合の症状（枠より大きい残り）が消えない。結果として F-110 は F-104 / F-120 と計算が分かれるが、これは問う内容が違うからで、F-104/F-120 が「詰めてやったらいつ終わる・いつ着手できるか」を答えるのに対し F-110 は「**この枠に収まるか**」を答える。未分類を積みの対象から外したのは、時間帯の枠を持たず、まだどの枠でやるか決めていないタスクの置き場だから（F-104 は1日の総量を見るので未分類も含める。両者で対象が違うことを条項に明記した） |
| 2026-07-28 | §4.3 のセクション残り時間（F-110）から**セクション間の波及をやめ、各セクションが自分の枠と自分のタスクだけで決まる形**にした（FB-81。同日の FB-80 で入れた積み上げの一部を撤回する） | FB-80 では「前のセクションが溢れたら先のセクションの残りも縮む」を採ったが、本番で見たオーナーの判断は「見出しの数字はその時間帯の予定と枠の対比として**独立に読めるほうがよい**」。波及があると、ある見出しの数字が別の時間帯のやり残しで動くため、値の変化を見たときに原因がその行の外にあり読み解きが要る。1日全体の見通し（遅れが後ろへ押していく様子）は終了予定時刻 F-104 と行ごとの予想開始時刻 F-120 が既に担っており、見出しでも二重に表現する必要がなかった。**FB-80 の核心だった `max(now, セクション開始)` は残る**——まだ始まっていないセクションを現在時刻起点で測ると残りが枠の長さを超える、という不具合の本体はこちらが直しているため。結果として式は「枠の終わり − max(now, 枠の頭) − そのセクションの未完了見積もり」となり、現在のセクションでは FB-80 以前と同じ値、将来のセクションでは「枠 − 見積もり」になる。波及をやめたことで、アーカイブ済み・過去セクションを「残りは出さないが積む」と特別扱いする条項も不要になり消した |
| 2026-08-01 | §3.4 に**繰り返しの正規化**を定め、`weekly` かつ全曜日（127）かつ `week_interval` が NULL/1 の入力を、保存時に `daily`（`weekdays` / `week_interval` は NULL）へ書き換えることにした（FB-56 / F-301） | 同じ展開結果になるルーチンの作り方が2通りあり、一覧の要約表示も「毎日」と「週次(月・火…日)」に割れていた（FB-53 の動作確認でオーナーが気づいた）。**正規化を `week_interval` ≤ 1 に限った**のは、隔週の全曜日が「n週おきに、その週だけ7日連続」という daily に間隔の概念がないため表せない別物だから（入力自体は禁じない——意味が表現できるものを入口で塞ぐと機能が後退する）。**値を残さず NULL に落とす**のは既存の daily 行の持ち方（daily は両列 NULL）に揃えるため。元が全曜日だったという情報は失われるが、daily と区別して読む用途がない。**書き換えの場所を domain の入力検証**（作成・更新・ルーチン化が共通で通る `validateRoutineInput`）にしたのは、リポジトリが受け取った値をそのまま書くため DB の値も要約表示も自動で揃い、UI 側の変更が要らないから。**UI で7つめのチェックが入った瞬間に種別を切り替える案は採らない**——state 構造の異なる2つのフォーム（`routine-form` は分割 state・`routinize-popover` は単一オブジェクト）へ同じ制御を入れる割に、得られるのは「保存後に編集フォームを開き直したときの驚き」の解消だけで、Server Action を直接叩く経路には効かない。**既存行のバックフィルはしない**（該当行があっても展開結果は同じで害がなく、次にそのルーチンを保存した時点で寄る。空振りするかもしれないマイグレーションを履歴に残さない） |
| 2026-08-01 | ハイライト（F-118）を **`tasks.highlighted boolean NOT NULL DEFAULT false`** で持つ（§3.5）。別テーブル（`task_highlights` 等）は作らない | §1 の「導出できる値はカラムに持たない」に照らして検討したが、ハイライトは打刻や並びから導出できる値ではなく**ユーザーの宣言そのもの**なので、保存する以外にない。別テーブルにする動機は多重度（1タスクに複数）か履歴（いつ付けたか）だが、どちらも要求に無い——本数の上限を設けないのは「1タスクに何個も付く」意味ではなく「ハイライトされたタスクが何件あってもよい」意味であり、1タスクに対しては真偽値1つで足りる。`DEFAULT false` にしたことで、ルーチン展開（§4.1）で生成されるタスクが常に OFF になるという要件（要件定義書 F-118）も既定値だけで満たされ、展開側に条項が増えない |
| 2026-08-01 | ハイライトを**引き継ぐのは中断・割り込みの再開タスクだけ**とし、複製（§4.6 / F-111・F-208）では引き継がない（§4.2 / §4.6） | 「同じ仕事の続きか、別の実施か」で切った。中断で生まれる残りタスクは元タスクと同じ仕事の続きなので、印が消えると中断のたびに付け直しになる。複製は「もう一回」＝別の実施で、コメントを引き継がないのと同じ扱いにした。先送り（F-107）で持ち越されるのは規則を足したからではなく、`task_date` の付け替えだけで同じ行が翌日へ移るため自動的にそうなる——「翌日は改めて選び直す」案（先送り時に印を落とす）も検討したが、実装差が `postpone` の1か所に留まる一方で「先送り＝翌日やる」ものから印が消えるのは意図と逆に見えるため退けた。後から必要になれば同じ1か所で倒せる |
| 2026-08-09 | **先送り（F-107）で `routine_id` を NULL に落とす**ことにした（§3.5。[FB-99](../案件/closed_21_ユーザーフィードバック.md#fb-99)） | ルーチン由来のタスクの先送りは、移動先の日に同じルーチンの行があると `uq_tasks_routine_date` に衝突して「保存に失敗しました」で止まっていた（展開は表示のたびに走る §4.1 ので、翌日を一度でも開いていれば必ず居る）。衝突しない場合も無事ではなく、先送りされた行が移動先の展開を `ON CONFLICT DO NOTHING` で吸い、**その日のぶんが無音で欠ける**。両方の根が「同じ行が `routine_id` を持ったまま日付を跨ぐ」ことなので、跨ぐ時点で紐付けを切った。**問うているのは行の意味の続きではなく「その行がどの日の展開の実体か」**で、日付が変われば実体も別になる——ハイライトの引き継ぎ（2026-08-01）で使った「同じ仕事の続きか、別の実施か」の軸とはここが違い、先送りで印が移るのと `routine_id` が外れるのは両立する。**移動先に同ルーチンがあれば弾く案**は、毎日のルーチンが事実上ずっと先送りできなくなる（翌日を開けば必ず衝突する）うえ、無音の欠けが残るため退けた。**一意制約を落として同日に複数を許す案**は、その制約が §4.1 の冪等 INSERT の土台そのもので、展開の重複防止を別の仕組みへ作り替える対価に対し、得られるのは「先送り後もルーチン由来のままにする」ことだけなので採らない。代償は2つあり、いずれもオーナー確定で許容した: ①移動先には**先送り分とその日の展開分が同名で2件並ぶ**（「今日やらなかった実施」と「その日の実施」で意味は別）②先送りされた行は以後ルーチン由来として扱われない（ルーチン化 O-12 が可能になり、削除しても §3.6 のスキップを記録しない——スキップは「その日の展開を止める」記録なので、他日へ移った行が持たないのは筋が通る）。**既に欠けた展開は遡って戻せない**（展開の履歴を持たず、§4.1 はバックフィルをしない） |
| 2026-08-09 | あわせて**先送りでも元の日のスキップ（§3.6）を記録する**ことにした（[FB-99](../案件/closed_21_ユーザーフィードバック.md#fb-99)。`test-reviewer` の指摘から判明） | 紐付けを外すだけだと `(routine_id, 元の日)` が空くので、**その日を再表示した時点で §4.1 が同じタスクをもう一度展開する**——先送りしたはずのものが元の日に戻る（先送りの Server Action は `revalidatePath("/")` を呼ぶため、今日を見ながら先送りするとその場の再描画で戻りうる）。統合テストで再現を確認した。これは紐付け外しで入った不具合ではなく**元からあった**もので、先送りが `task_date` を書き換えるだけだった時点から `(routine_id, 元の日)` は空いていた。スキップは削除（O-8 / F-301）が既に持っている「**その日はこのルーチンを展開しない**」の記録で、先送りも「その日から取り除く」操作である以上、同じ記録を残すのが筋が通る。**解除の経路は持たない**——削除にはトーストの Undo があるが先送りに取り消しは無く、戻す道は「元の日へ動かす」操作（[FB-98](../案件/21_ユーザーフィードバック.md#fb-98)）が入ったときに、その操作の側で考えればよい。スキップの記録はタスクの移動と同じトランザクションで行う（片方だけ残ると再展開が復活する。削除 O-8 と同じ理由） |
| 2026-08-09 | §3.1〜§3.3 の `name` から「50文字以内（アプリ層で検証）」を外した（[FB-71](../案件/closed_21_ユーザーフィードバック.md#fb-71)） | 上限そのものを撤廃したため（理由は[log_03_マスタ管理](./13_画面定義書/log_03_マスタ管理.md) 2026-08-09 が正）。**列の定義は変わらない**——もともと `text` で長さ制約を持たず、注記は入力仕様の転記だった。DDL・マイグレーションは無変更 |
| 2026-08-09 | §4.2 の再開タスクの `name` を「T と同値」から「**T の名前＋接尾辞「（再開）」**」へ改めた（[FB-95](../案件/closed_21_ユーザーフィードバック.md#fb-95)） | 同名の行が並ぶとリスト・レビューでどれが再開分か名前だけでは読めない、というオーナーの実運用の気づき。**表示だけで導出する案（`split_parent_id` が入っている行に画面側で「（再開）」を添える）と比べたうえで、保存する側をオーナーが選んだ**——保存すればデイリー・レビューに限らず名前が出るところすべてで一様に読め、将来の出力先でも同じになる。代償として `split_parent_id` との二重表現・リネームで消せる・複製とルーチン化に接尾辞が付いて回るの3点を受け入れる（再開分の判定に使うのは引き続き `split_parent_id` で、名前の側は表示のためだけの値と位置づける）。中断を重ねたときは `〜（再開）（再開）` と積み上げる（オーナー判断「単純に後ろに付け加えて良い」）——回数表記（`（再開2）`）は、割り込みで日をまたぐと親が別日のリストに居て連鎖をたどれず表記が揺れるため採らない |
| 2026-08-09 | `bundles`（§3.7）に `color` を持たせ、モードのプリセット13色を共有することにした（F-119） | 2026-07-22 の設計は名前だけのマスタ（単色の道）を前提にしていたが、道が複数あると単色では見分けられない。新しいパレットを起こさずモードのプリセットを使い回すのは、色の意味は行ごとの文脈で決まるので、選べる色を2系統に増やすほうが混乱するため |
| 2026-08-09 | メンバーシップを `tasks.bundle_id` として**タスク側にも持ち**、`tasks.routine_id → routines.bundle_id` の join で導出しないことを再確認した（F-119） | 導出にすると、ルーチンの所属を後から変えたときに**過去日の道が遡って変わる**——ログの不変を原則とする方針（§1）に反する。あわせて、先送りやルーチン削除で `routine_id` が外れても所属を保てる。将来ルーチン以外の供給元から `bundle_id` を付ける余地も、タスク側に持っていれば開いたままになる（[31_バンドル検討.md](../検討/31_バンドル検討.md) §7） |
| 2026-08-09 | `bundle_id` の伝播を **§4.8 に一覧化**した（複製は引き継がない・先送りは外す。中断と割り込みの再開タスクだけ引き継ぐ） | 契機ごとの扱いを §4.1〜§4.7 に散らすと、新しい操作を足すたびに拾い漏れる。複製で引き継がない判断は `routine_id`・コメント・`highlighted` と揃えたもので、あわせて**複製するたびに未完了メンバーが増えてバンドルが進行中のままになり、割り込み警告が鳴り続ける**事故も防ぐ。先送りで外すのは `routine_id` を外す扱いと揃えたもので、付けたまま移すと移動先で改めて展開されるぶんと同じバンドルに同名が2件並ぶ |
