# 要求定義書 決定ログ

要求の追加・優先度変更の経緯を日付順に残す。要求そのものは[要求定義書](./11_要求定義書.md) §6・§7 が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-16 | 操作感の基準を「機能の豊富さより、リストの簡素さと操作の速さ」とする | オーナー確認（§5 C-1〜C-3 の出所） |
| 2026-07-16 | 既存データ移行を不要とする（F-601 対象外） | ゼロからのスタートで良い |
| 2026-07-16 | プラン制限を考慮しない（すべて無制限） | 自作のため |
| 2026-07-16 | 中断時に残り見積もり分のタスクを自動生成する（F-204） | 中断した残りを後で再開できるようにするため |
| 2026-07-16 | オフライン対応を対象外とする（N-10） | Webアプリ前提であり、常時接続の環境でしか使わないため |
| 2026-07-16 | 認証はシングルユーザー前提の簡易なもので足りるとする（N-03） | 利用者がオーナー本人だけであり、ログイン画面を作る価値がないため |
| 2026-07-19 | 必須ショートカット一覧をオーナーが作成し、体系の基準とする（N-04） | 主要操作がキーボードで完結するかを一覧で検証できるようにするため（一覧は画面定義書01 §6 が正） |
| 2026-07-17 | 独立した詳細要件書を作らず、要件定義書群を挙動の契約とする | 個人開発として維持できる文書量にとどめるため |
| 2026-07-19 | F-112（ポップオーバーのキーボード操作）・F-208（完了タスクの再実行1操作化）を Could として追加 | 実装中に気づいた改善余地。運用で必要性を見極めてから着手する |
| 2026-07-20 | F-304 のうちスキップを Must へ（Phase 1 で実装） | 展開タスクを削除しても再表示で復活し、当日やらない選択ができなかったため |
| 2026-07-20 | F-305 を Could → Should へ繰り上げ（FB-11） | 日々のタスクをルーチンへ登録し直す作業が重く、ルーチン整備の律速になっていたため |
| 2026-07-20 | F-108（入力補完）・F-206（コメント）を Should → Could へ引き下げ | 日々のタスクはルーチン展開でほぼ生成され、手入力・記録の場面が想定より少なかったため |
| 2026-07-20 | F-113 を Should として追加（FB-02 + FB-16） | 実行中タスクより上に未実行タスクが残る／未分類のまま開始したタスクが時間帯とずれる、が毎日発生していたため |
| 2026-07-20 | F-306 を Should として追加（FB-10） | ルーチンが増えると開始想定時刻順だけでは棚卸しできないため |
| 2026-07-20 | F-114 を Should として追加（FB-06） | 時間の過不足（F-110）だけではセクション内の消化度合いが読み取れないため |
| 2026-07-21 | F-114 に「1日全体」を追加（FB-22） | 画面上部で今日の総量と消化を把握したいため |
| 2026-07-21 | 実装済みだが要求IDのなかった機能を遡及登録: F-115（削除のUndo）・F-209（放置された実行中タスクの警告）・F-405（参照0件マスタの物理削除）・N-11（操作結果の通知） | 文書の再整理でトレーサビリティの穴として発見したため。いずれも Phase 1 で実装済みであり、要求の内容や優先度を変えるものではない |
| 2026-07-21 | F-113 に「打刻時刻を後から修正した場合も追随する」を明記（FB-24） | 実装が開始打刻の瞬間しか見ておらず、時刻を直しても時間帯とずれたままだった。要求文の「実際の時間の流れに追随する」に本来含まれる挙動であり、新規要求ではなく穴の明示として扱う |
| 2026-07-21 | F-504（CSVエクスポート）を Should → Could へ引き下げ | 実運用でログを外部へ持ち出して分析する場面がまだ生じていないため。必要になってから作る |
| 2026-07-21 | C-7「リストはプランとログを兼ねる」を価値として追加し、F-113 を両面の表現に書き直す | それまで F-113 を未来側（これから何をやるか）だけで書いていたため、完了タスクの扱いやログの訂正（規則 c）の根拠を要求から導けなかった。S-04 レビューの設計にも効く前提として、要求の層に置く |
| 2026-07-21 | F-112（ポップオーバーのキー操作）・F-208（完了タスクの再実行1操作化）を Could → Should へ繰り上げ | 実運用で毎日引っかかる操作だと分かったため。どちらもキーボード完結（C-3 / N-04）の穴で、ポップオーバーはクリックに手が落ち、完了タスクの「もう一回」は Y→Enter の2操作が煩わしい |
| 2026-07-21 | F-109（カレンダー俯瞰）・F-207（長文ノート）・F-602（外部カレンダー連携）を Could → 対象外 | 数週間の運用で必要性を感じなかったため。俯瞰は日付ナビ（F-106）で足り、長文はタスク管理の範囲外、外部連携は本アプリを一本化先とする方針（成功基準）と逆行する |
| 2026-07-21 | F-110 のセクション評価を「見積合計 − 枠」の静的な過不足から「現在時刻から見た残り時間」へ変更（FB-34） | 静的な過不足はプラン時の目安にすぎず、日中は「今からこの時間帯のタスクをこなして間に合うか」を知りたい。終了予定時刻（F-104）のセクション版として、実行の判断に直結させる |
| 2026-07-21 | F-110 の分子（時間合計）を「見積もり合計」から「完了は実績・未完了は見積もり」の合算に変更し、ラベルを「見積」→「合計」に改める | 残り時間表示（FB-34）を入れて過去セクションでも見出しが残るようになり、完了済みでも「見積」表記だと実態とズレる。完了は実績を反映すれば「そのセクションに実際どれだけ使ったか＋これからの予定」を1つの数字で読める。ラベルも見積専用でなくなるため「合計」が正確 |
| 2026-07-21 | F-503 を「日次の集計」に絞り、週次・月次の期間集計を F-506（Could）として分離 | F-503 は日次・週次・月次を1要求に束ねていたが、日次のみ実運用で日課になり実装済み。週次・月次は必要性を運用で確かめてからでよいため、優先度を分けられるよう別IDに切り出した |
| 2026-07-21 | N-08（性能劣化しないこと）を Should → Could へ引き下げ | 数年分のログ蓄積が前提の確認であり、当面は問題が顕在化しない。ログが溜まってから実効性を確認すれば足りるため |
| 2026-07-21 | F-210（開始打刻の取り消し）を Should として追加（FB-37） | 誤って開始したタスクを未実行へ戻す手段がなく、時刻修正（F-203）でも `started_at` を null 化できなかった。日々の誤操作からの復帰として必要と判断。既存の打刻（F-201）や削除の取り消し（F-115）に収まらないため別IDを起こした |
| 2026-07-22 | 日界（1日の開始時刻）の設定を F-116（Should）として要求化し Phase 2 対象とする | これまで「0:00 固定・データ設計だけ維持」の保留事項（要件定義書 §8 決定ログ 2026-07-16）に留めていたが、日をまたいで残る実行中タスクの扱い（日跨ぎ挙動）を確認する中で、生活リズムに日界を合わせられないと1日の区切りが実態とずれると判断した。`task_date` を保存論理日付にしてある（データモデル §1）ため既存データへの影響なく導入できる |
| 2026-07-22 | F-208（完了タスクの「複製して開始」）を実装。既存の複製（F-111）＋開始（F-201）の合成とし、新規の挙動規則は起こさない。Undo は F-208 専用にせず F-210（当該複製タスクの開始取消のみ・複製は未実行で残す）に委ねる。完了タスクの打刻ボタンは操作なしのままとし `Enter` 限定にする | 「もう一回」の高速化は既存規則の合成で足り、専用の生成・取消規則を増やすと打刻導出と割り込みの一貫性を崩す。ボタンにも割り当てると完了行のクリックが破壊的操作になり誤操作の後片付けが増えるため、キーボード完結（C-3）の `Enter` に絞った |
| 2026-07-22 | 日付表示クリックからのカレンダー日付ジャンプを F-117（Could）として F-106 から分離（FB-39）。基本の日付ナビ（前後日・今日）は F-106（Must・実装済み）に残す | datepicker は要件 §5.1・画面定義書01 §3.1 に F-106 の一部として書かれていたが未実装で、基本ナビが実装済みなのに全体が Must のままだと優先度の実態とずれる。離れた日付への移動は日々の頻度が低く、必要性を運用で見極めてよいため Could として切り出した。F-109（カレンダー俯瞰＝対象外）とは別物で、こちらは表示日をジャンプする移動手段であり期間の俯瞰表示ではない |
| 2026-07-22 | F-118（ハイライト）を Should として追加 | その日注力したいタスクを見失わないよう強調したいというオーナーの要望。TCC のハイライトに相当する。モード色（F-401）や消化進捗（F-114）とは別軸の「今日ここに注力する」印で、複数可。安価で日々の集中に効くため Should とした。あわせて検討したバンドル（関連ルーチンを束ねて視覚化する機能。当初「コンボ」と呼んでいたが順序を持たない集合モデルに確定し改称）は仕様の自由度が高く運用しながら詰める必要があるため、要求化を保留し `docs/検討/31_バンドル検討.md` で検分する |
| 2026-07-22 | F-119（バンドル）を Could として追加。関連ルーチンを束ね、デイリー上で1つの流れ（左ガターの縦線＝"道"）として視覚化し、割り込み（束ねの最中に非メンバーを開始）を検知して知らせる機能。設計は対話で確定し `docs/検討/31_バンドル検討.md` が正（集合モデル／案Y展開／オーバーレイ／警告B案／`bundles`＋`routines.bundle_id`＋`tasks.bundle_id`）。要件定義書・データモデル定義書・画面定義書への詳細展開は**実装時に行う**（データモデル定義書 §3 は Drizzle スキーマと1:1 のため、テーブル未作成の段階で書くと乖離する）。アドホック束ね・順序モデルは当面スコープ外 |
| 2026-07-24 | F-211（終了打刻後に選択行を次へ自動送り）を Should として追加（FB-40） | 選択モデル（画面定義書01 §5）は選択タスクが存在する限り据え置くため、終了打刻しても完了タスクに選択が残り、次の未実行タスクへ毎回手で移す必要があった。上から順にこなす打刻ループを止めない値。既存の追加後・複製後（O-11）・削除取消後の「操作後に選択を送る」前例と一貫。リストの並び追随（F-113）は並び順の要求で選択行の追従は含まないため別IDを起こした。送り先は現在地規則（C・初期選択と同じ）、対象は終了打刻のみ（複製開始 F-208 は既に生成物へ選択が移る）、送り先が無ければ完了行に据え置き、をオーナーと確定 |
| 2026-07-25 | F-212（完了タスクを未実行へ戻す）を Should として追加（FB-41） | 完了タスクを未実施に戻す手段が無かった。F-210 は実行中→未実行のみを対象とし完了は含まないため別IDを起こした。戻し先はオーナー確定で**未実行**（`started_at`・`ended_at` をともにクリア）とし、「実行中へ戻す（`ended_at` のみクリア）」は採らない。理由は要求の本体が「実施していなかったと直す」ことであり、実行中へ戻す案は実行中1件制約に触れて割り込みを誘発するため。あわせて**戻しの取り消し**を要求に含める。F-210 が取り消しを持たないのは失う情報が開始時刻のみで小さいのに対し、F-212 は開始・終了の両方＝実績そのものを破棄するため、行が消える削除（F-115）と同等に扱うのが釣り合う。加えて現行 `U` は「保留なし・実行中でない」とき何も起きないため、そこへ完了取消を割り当てると削除 Undo を狙って遅れて押した `U`（トースト消滅後）が巻き戻しに化ける経路が生まれる |
| 2026-07-25 | F-120（未実行タスクの予想開始時刻）を Should として追加（FB-47） | F-104 は1日全体の終了予定時刻という単一の値で、行ごとに「これはいつ着手できるか」は読めない。同じ積み上げ（現在時刻＋実行中の残り＋先行する未実行の見積もり）の途中経過を行に出すだけで、新しい計算規則は起こさない。オーナー確定: 表示は実施時間列への併記（列を増やさない。N-05）、対象は未実行行のみ（実行中・完了は実打刻を持つ）、表示日が今日でなければ出さない（終了予定 F-104・セクション残り時間 F-110 と同じ規律） |
