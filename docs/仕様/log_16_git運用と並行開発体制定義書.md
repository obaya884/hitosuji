# git 運用と並行開発体制 定義書 決定ログ

本文（[16_git運用と並行開発体制定義書.md](./16_git運用と並行開発体制定義書.md)）に関する判断を日付順に残す。検討ドキュメント34時代（〜2026-07-25）の検討ログも本書へ引き継いだ。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-23 | T-06 の体制を検討。ヘッド1＋ワーカーN の2階層とし、worktree 自体より (1) 共有リソースの分離 (2) 通信と調停、が本質的難所と整理。DB 分離は `TEST_DATABASE_URL` が既に env 化されているため「同一 db-test 内に worktree ごとの DB」で足りる。スキーマ変更タスクはジャーナル衝突のため常に最大1本。共有 docs の記入はヘッドに一元化（T-06 起票の動機となった WIP 交錯の再発防止）。本番反映はオーナー合図の規律を維持しヘッドにも自動マージ権を与えない |
| 2026-07-23 | 通信機構を比較し**サブエージェント方式（worktree 分離＋SendMessage 継続）を採用、Agent Teams は見送り**。決め手は機能差でなく「通信を最小化する体制設計」が本命であること: ワーカー間通信は設計上不要（発生したら分割ミスのシグナル）、必要能力（worktree 分離・入れ子で verifier 起動・往復継続）はサブエージェントで揃う、Agent Teams は実験的・CLI 限定・in-process メイトがバックグラウンド起動不可で CLAUDE.md の並列レビューフローと衝突（`/resume` でメイトが復元されない既知問題も運用と相性が悪い）。通信は「タスクカード／完了報告・仕様質問・中断報告」の定型に限定し、仕様質問は必ずヘッド経由でオーナー対話に上げる |
| 2026-07-25 | **T-26 の git 運用ルールを統合して実施**（当初の文書名を「34_git運用と並行開発体制検討」に改称）。(1) ブランチ命名は `<タスクID>-<slug>`（スラッシュなし・49文字以内）——worktree ディレクトリ名・テストDB名へ機械的に変換するため。`feat/` 型 prefix は変換の手間が増えるため不採用。1ブランチ1PR・マージ後削除を規約化（`claude/*` 使い回し・残置の反省）。(2) `TEST_DATABASE_URL` の注入は「`vitest.config.ts` が worktree 直下の `.env.worktree` を読む（シェル環境変数優先）」を採用——test 系 npm スクリプト全部を1か所でカバーし CI・本体ワークツリーに無影響。npm ラッパー案・direnv 案は過剰につき不採用。(3) `wt-rm.sh` の確認は TTY 時のみ y/N（AI 実行時は permissions の ask が確認を兼ねる）。dirty なら中断し `--force` は設けず、防護は git の安全装置に寄せる |
| 2026-07-25 | 並行モードへの切替契機を「並行タスクがたまったとき（AI 判断）」から「**オーナーの明示指示のみ**」に変更。常時ヘッド＋ワーカー方式は不採用——オーナー指摘「切替の判断基準が維持されなさそう」を受けた検討で、常時ワーカー方式はチャット差分レビュー（規約「Edit/Write で変更」の存在理由）が消え、仕様質問が毎回3ホップ（ワーカー停止→ヘッド→オーナー→SendMessage）になり、対話往復の多い本プロジェクトの実装スタイルと合わないため。切替をオーナー指示に一本化すれば維持すべき判断基準そのものが消える |
| 2026-07-25 | **ワーカーのモデル選択の基準を §2 に追加**（既定は継承、4条件をすべて満たすときだけ `sonnet`）。weaker model の実リスクは「コードが書けない」ことではなく **docs の隙間を質問せず推測で埋める**ことで、§4-2 の「手を止める」が最も省略されやすい振る舞いだと整理した。spec-reviewer と PR diff は検出であって予防ではないため、条件②（docs に書き切られている）で隙間のないタスクに限る形に寄せた。前提として `.claude/agents/*.md` の5体すべてが `model:` フロントマターを持つことを確認（verifier・spec-reviewer = sonnet / code-quality-reviewer・test-reviewer・refactor-scout = opus）——フロントマターは親からの継承より優先されるため、安全網はワーカーのモデルに依存しない。初適用の判定: F-212 は4レイヤ縦断・既存 Undo スロットの一般化・楽観的更新の意味論を含むため継承（opus）を維持、F-120 はデータモデル定義書 §4.3 に式があり純粋な追加なので `sonnet` 相当と判定（ただし起動済み・実装中だったため切替はせず、基準は次の並行バッチから適用） |
| 2026-07-25 | **検討ドキュメント（検討/34）から運用の一級文書（仕様/16）へ昇格・改番**し、当初構想の Phase 3（ヘッド運用の型化・Agent Teams 再検討）は設けないことにした。オーナー判断「検討ではなく運用の1級ドキュメントとしたい。Phase 3 は考えない」による。1X=仕様ブロックはアーキテクチャ定義書と同じ「確定した契約」の置き場で、本書の内容（git 運用ルール・並行体制）は日常運用の契約になったため。Phase 表記の廃止は「完了した事項に予定表記を残さない」規約とも整合し、これにより T-06 は完了（Agent Teams を将来再検討する場合は本ログ 2026-07-23 の選定理由を起点にする） |
| 2026-07-25 | **rebase 後の force push はオーナーが実行する**と §1.3 に明記。あわせて §3.2（dev サーバを分離しない方針）に、並行度が上がると動作確認が直列化する旨と T-34 への参照を追記 | force push については前セッションで「`.claude/settings.json` に feature ブランチ限定の allow ルール（`git push --force-with-lease origin f-*` / `fb-*` / `t-*` / `n-*`）を足せば次からワーカー自身が押せるはず」と見込んで案Bを採ったが、**今回の並行バッチで実測したところ効いていなかった**——ヘッドが allow ルールに完全一致する形で叩いても拒否され、`cd` の前置なし・リダイレクトなしでも同じだった。原因を全階層の設定を読んで特定したところ、**ユーザーグローバル設定 `~/.claude/settings.json` の deny に `Bash(git push --force*)` があり、`--force-with-lease` が文字列として `--force` から始まるため一致していた**。deny は allow より優先されるので、プロジェクト側の allow では原理的に覆せず、案Bは最初から成立しない施策だった（当初「permission ルールより手前で止められている」と書いたが誤りで、permission ルールそのもので止まっていた）。解消にはグローバル deny を狭める必要があるが、AI 自身の制約を緩める変更なのでオーナーの判断に委ねる。通常の push はワーカーもヘッドも通る（FB-48・FB-53 のワーカーはどちらも自分で push し PR を作成できた）ので、制限は force push に限る。保留していた「§4 に代行を書くべきか」は実測で「書くべき」と確定した。dev サーバの直列化は 2026-07-25 の並行4本（FB-48・FB-53・FB-51・FB-54）で顕在化し、確認のたびにヘッドが checkout を切り替える運用が明確なボトルネックになった。あわせて、AI のバックグラウンドから立てた dev サーバが**30分前後で回収される**ことも実測（3回連続。約32分・約29分・約29分でいずれもクリーン終了）ため、T-34 では「起動はオーナーのターミナルが持ち、`wt:new` は環境を用意するまで」を論点に含めた |
| 2026-07-25 | **force push の代行は不要になった**。オーナーがグローバル deny を `Bash(git push --force*)` → **`Bash(git push --force *)`（`--force` の後に空白）** へ狭めたため、`--force-with-lease` が一致しなくなり通るようになった（ヘッドから実測して確認）。素の `git push --force` / `-f` は塞がれたままで防護は落ちていない。§1.3 を「ヘッドが実行する」に改め、**lease なしの force は使わない**（リモートに他者の更新があれば失敗して気づけることが防護の本体なので迂回しない）ことを明記した |
| 2026-07-25 | **並行度の目標を「2〜3で十分」から「4本以上」に変更**し、そのための条件を §2 に4項目として明記。オーナー判断「2までだと並列の意味があまりないので、4やそれ以上で問題ないようにフローや構造を作り込んでいく方向にしたい」による | 同日の並行4本（FB-48・FB-53・FB-51・FB-54）を1日回した実測評価が根拠。**実装＋レビューの工程は明確に速くなった**——2機能が同時に実装され各4体のレビュアーを通って PR に到達し、ヘッドはその間コードを書かずに FB-55・FB-56・T-34 の起票と §2.4 の共通規約化を進められた（直列なら実装の後ろに積まれる作業）。レビューの質も落ちておらず、test-reviewer が自己参照テストと未検証のクランプ経路を、spec-reviewer が §3.3 の列挙漏れを捕まえている。**一方でエンドツーエンドの吐き出し速度はほとんど変わらなかった**——律速が「AI がコードを書く時間」から「オーナーの動作確認＋git の儀式」へ移っただけで、確認のたびにヘッドが checkout を切り替え dev サーバを立て直す往復が発生した（T-34 の直接の動機）。さらに、4本のうち FB-48 と FB-51 が `daily-list.tsx` の同じ関数で衝突したため**実際には2＋2しか並ばず**、逐次 rebase の CI 再走もコストとして見えた。手戻りも増えた（モード列の切り詰めは仕様がプロジェクト列しか書いておらず2周目に、ラベル折り返しと反転バグはどちらも「実装完了」後のオーナー確認で判明）。**ヘッド自身の記入事故も同日に3件**（FB台帳の表を2回破損・テーブル内に空行混入・`.gitignore` を detached HEAD にコミット）で、文脈切替の増加と無関係とは考えにくい。したがって「上限を絞る」ではなく「非実装工程を作り込む」方向に倒し、①確認の並行化 ②CI 再走の削減 ③衝突の機械判定 ④ヘッドの記入ミスの機械検出、の順で潰す方針とした |
| 2026-07-25 | **§3.4 を「記入はヘッドに一元化」から「文案はワーカー・記入はヘッド（スクリプト経由）」へ細分**。オーナー提起「台帳更新済みの変更（PR）をワーカーに作らせてマージした方が操作が委譲できて無駄が少ないのでは」に対する検討の結果 | 提起の前半は正しい——ヘッドがワーカーの報告から完了記録を再構成すると二次情報になり、同日に実際の誤り（レビュアーの指摘とワーカー自身の判断を取り違えて中継）が出た。よって**文案は本人に書かせる**ことにした。一方で記入までワーカーに委ねる案は採らない: ライブ台帳から `closed_*` への移動は「一方から行を削除し、他方へ**同じ挿入位置に追記**する」操作で、削除は各ワーカーが別々の行を触るため綺麗に merge できても**追記は全員が同じアンカーに集まり N 本で N−1 回衝突する**（衝突対象は1行が千文字超の markdown テーブル行という最悪の形）。加えて `対応済み` にはマージコミットが要り PR 内では確定できず、他タスクのレビューから派生した新規 FB・並行運用そのものから出た T-XX・オーナー対話を記録する `log_*` にはワーカーの持ち主がいない。さらに**同日の破損3件は判断ミスではなく「千文字超の1行を手編集した」ことが原因**で、書き手を替えても危険は移動するだけで消えない。したがって誤りの入り口そのものを消す方向（T-36 を検査から**行操作のスクリプト化**へ拡張）に倒し、ヘッドの記入はそのスクリプト経由と定めた |
| 2026-07-26 | **dev ポートの割り当てを「使用中を避ける」から「割り当て済み＋使用中を避ける」に変えた**（T-40）。`wt-new.sh` が `../hitosuji-wt/*/.env.worktree` の `DEV_PORT` を読み、3001 から重ならない番号を選ぶ | 起票時の候補はブランチ名のハッシュ（`3001 + hash % 90`）と起動時探索の2案だったが、どちらも採らなかった。**割り当て済みポートは既に `.env.worktree` に書いてあり、それを読めば衝突を確率ではなく事実として避けられる**——ハッシュは作成順に依存しない代わりに衝突確率が残り、起動時探索は `wt:new` が案内した URL と実際のポートが食い違う。読む対象は「listen しているか」ではなく「他の worktree に割り当てたか」であるべきで、T-34 の実装はそこを取り違えていた（dev サーバを立てないまま6つ作った T-39 第2バッチで全部 3001 になった）。使用中の検査も残してあるのは、worktree 外のプロセスが 3001 番台を掴んでいる場合に備えるため。空きが尽きた（3100 到達）ときは黙って本体と取り合わずエラーで止める |
| 2026-07-26 | **`wt:new` が起点の未 push を検査して中断する**（`--allow-unpushed` で明示的に上書き）。あわせて既定で `git fetch origin` を1回打つ（`--no-fetch` で省略可）（T-46） | 「実行前に fetch / pull しておくこと」というスクリプト冒頭のコメントは人間向けの注意で機械的な担保が無く、T-39 第2バッチで実際に踏んだ（未 push の docs コミット1本が6ブランチの起点に入り、6つの PR すべてに写り込んだ）。ローカルで `main...branch` を見ても差が出ないためヘッドの検査では気づけず、オーナーが PR 画面で発見している。**fetch を既定にしたのは、`origin/<起点>` 自体が古いと検査が意味を失うため**——検査の前提を人間の手順に委ねると同じ失敗の再演になる。所要は `npm ci` に比べれば無視できる。逃げ道を2つとも残したのは、オフラインや意図的に未 push の起点から派生させる場合に手詰まりにしないため。**遅れている方向（ローカルの起点が `origin` より古い）は警告のみで続行**とした——`fetch` はリモート追跡 ref しか動かさないので古い起点から生えうるが、①意図して古い起点から派生させることはある ②解消には `git pull` でオーナーのローカル `main` を動かす必要があり、それは `wt:new` が黙って代行してよい操作ではない、の2点による。中断する（進んでいる）方と非対称なのは、**写り込みは他人のレビュー体験を壊すのに対し、古い起点は自分が rebase すれば済む**という実害の差 |
| 2026-07-26 | **並行時のレビューは「起動と対処はワーカー・裁定はヘッド」に分ける**（T-70）。§2 に2項、§4-1 の完了報告に4点の書式を追加し、CLAUDE.md の「指摘への対処はメインセッションが行う」は単独モードの規定だと明記した | オーナー提起「並行開発の場合レビューサブエージェントはワーカーが自身で起動、修正を実施するワークフローでルール化できないか」による。**発端は新しい要望ではなく既存の食い違い**——§2 は当初からワーカーについて「worktree の内側では CLAUDE.md の運用をそのまま実行する（… → verifier・code-quality-reviewer → spec-reviewer・test-reviewer）」と書いていたのに、CLAUDE.md の「指摘への対処はメインセッションが行う」が並行モードにもそのまま読め、ヘッドのタスクカードが後者に従っていた。同日の並行4本で実害が3つ出た: ①T-51 のワーカーが「レビューはヘッド側で起動してほしい」と書いて完了状態のまま待機し並行性が失われた ②T-42 では指摘への対処にヘッドが全文を読み要約し直す往復が入った（実装文脈を持つのはワーカー側なのに） ③4ブランチ × 最大4本の報告がヘッドへ集まり、最も希少なヘッドのコンテキストを食う（T-66 の実測で消費の7割が 150k 超のコンテキスト由来）。**委譲の範囲を「対処」までに留め「裁定」を残したのが要**——書いた本人が採否も決めると自己レビューの利益相反になるため、見送りだけはオーナーが決める形にした。生のレビュー本文をヘッドへ流さない書式にしたのは、ヘッドのコンテキストを裁定と共有 docs のために温存するのが目的（③への直接の対処）。**この案が今日まで採れなかったのは T-67 の前だから**——`verifier` が変異テストのために書き込み権限を要求し、実際に2度作業ツリーを壊していた。責務を外して初めてワーカーに検証系を任せられるようになった。残る弱点は `test-reviewer` の沈黙する失敗（T-66）で、起動側と受領側が同一になると範囲の妥当性を誰も確認しない。件数を必ず書かせる以上の担保は置かず、運用で見ることにした |
| 2026-07-26 | **T-70 の初適用（並行4本 = T-42・T-51・T-57・T-59）の結果。ルールは維持し、追加の担保は置かない** | 見送り希望は計13件上がり、**独断で処理されたものはゼロ**。うち3件を裁定で却下（Port のコメント誤り・追跡 ID の付記・tuple 境界検査の後退）でいずれもワーカーが直した。**懸念していた「沈黙する失敗」（T-66）は、むしろワーカー自身のレビューが拾った**——T-59 では当初の変異7件を*生き延びる*変異が2件見つかり（素通しガードをヘッダ非存在で条件付ける／`startsWith("Basic ")` の末尾空白落ち）、「全部撃墜＝網が完全」ではないことが実証された。T-57 では2人のレビュアーの見解が割れた恒真アサーションの扱いを、**権威でなく変異による実測で決着**させている（擁護側の「並び順の保存を守る」が成り立たないことを確認して削除）。T-51 では実DBを一度も通っていない guard を3つ特定し、「すべて T-51 以前からの穴で本差分が開けたものはゼロ」まで**起点と照合して**切り分けた。**ヘッドのコンテキストには生のレビュー本文が一度も流れていない**（4点の報告書式が効いた）。副作用として、ワーカーが衝突可能性を**未確認のまま**留保の理由に使う事例が1件出た（T-57 の tuple 件）——ヘッドが `git diff --name-only main..<ブランチ>` で否定して差し戻した。報告書式に「留保の根拠は機械的に確認したものか」を足すことも考えたが、**却下の1往復で当のワーカーが自ら手順を修正した**ため、条項の積み増しはしない（T-67 で「禁止条項の積み増しでは塞がらない」と学んだのと同じ判断） |
| 2026-07-26 | **本文に混在していた判断の経緯・実測の記述を本ログ参照へ寄せて圧縮**（§2 のレビュー集約の実害3点と並行4本の実測・対応状況、§3.2 のポート選定の理由と dev サーバ回収の実測、§3.4 の文案/記入分離の理由2点、§4 の T-66 の趣旨、§5 手順2 の T-46 の経緯）。仕様の内容は変えていない。オーナー指示「1級ドキュメントとして肥大したので整理し直したい」による。経緯はすべて本ログの既存エントリ（2026-07-23〜26）が持っており本文と二重記載になっていた——「本文は現在の仕様だけを書き、経緯は log_ へ」の規約（CLAUDE.md）を適用した |
| 2026-07-26 | **§1.3・§2・§3 の構成を再整理**（オーナー指摘「§2 の箇条書きが雑然／§3 が共有リソースの全体像を示していない／§1.3 も雑然」による。規則の内容は不変）。§1.3 は「合図と権限／マージキューの調停（中間 rebase の省略と `wt:overlap` を配下へ）／force push」の3本柱に束ね直し、§2 はフラットな9項目を §2.1 役割と分担・§2.2 レビューの分担・§2.3 ワーカーのモデル選択・§2.4 並行度の目標 の小節へ分割、§3 は冒頭に「共有リソース×扱い×対応節」の一覧表を追加した |
| 2026-07-26 | **CLAUDE.md「サブエージェント運用」に単独／並行のモード読み替え規定を一本化**（並行モードでは各ワーカーが worktree の内側で「メインセッション」を自分に読み替えて同じ運用を実行する、を節冒頭で1回宣言）し、箇所ごとの但し書き（「これは単独モードの規定——並行では…」）を削った。本書 §2.2 も CLAUDE.md の記述を訂正調で引用する形（「CLAUDE.md の『…』は単独モードの規定であり」）から読み替え規定への参照に改めた | T-70 反映時は CLAUDE.md 側の規定文を残したまま但し書きを重ねたため、下位文書（本書）が上位ガイドの読み方を訂正する逆転が生じていた（オーナー指摘）。読み替えを冒頭の1規則にすれば、個別規定は素のまま両モードで成立し、見送りの裁定（オーナー）だけが真の並行専用例外として残る |
| 2026-07-26 | **並行時のオーナー動作確認を「ヘッド側でブランチを checkout」から「各 worktree の dev サーバで行う」に改めた**（§2.1・§5。CLAUDE.md の要約も追随） | T-34 で worktree ごとの固有ポートが入り、§2.4 も「動作確認の並行化は §3.2 で手当て済み」としていたのに、確認手順の記述だけが T-34 以前のまま残り矛盾していた（品質監査で検出・オーナー裁定）。checkout 切替はヘッドのブランチ状態を動かし確認の直列化も招くため、dev サーバ側へ一本化した |
| 2026-08-01 | **§2.2 に「対処後の再検証は同じレビュアーを resume せず新しく起動する」を追加** | 種別「内部設計」×優先度「低」の並行4本（T-41 / T-63・T-89 / T-77〜79 / T-80・T-83）で、1本が**約20分、返らない返事を待って停止した**。原因は、ワーカーが起動したレビュアー（ヘッドから見て孫）が呼び出し元へメッセージを返す手段を持たないこと——`SendMessage` で resume して再検証を頼むと、その結果はワーカーではなく**ヘッドへ届く**。ヘッドが内容を転送して解消した。**他3本では起きていない**ので構造的な欠陥ではなく、再検証を新しいエージェントの起動として行えば結果は起動元へ返る（3本はそうしていた）。なお「resume で頼んだ」ことはヘッドに届いた孫の但し書き（「SendMessage が使えないため呼び出し元へ転送してほしい」）からの推定で、ワーカーのトランスクリプトを読んで確かめてはいない（読むとヘッドのコンテキストが溢れるため）。条項は1行に留め、経緯は本ログへ置く |
| 2026-08-01 | **§1 のマージ方式（squash）は変更しない**。ただし同日の PR #82・#83・#84・#85・#90・#91・#92・#93 の**8本が squash ではなくマージコミットでマージされた**事実を記録する（履歴はそのまま残す） | オーナーが #93 のマージ後に「方針が変わっていそう」と気づいて発覚した。履歴を調べると、リポジトリ全体でマージコミットは同日の8件だけで、#81 以前はすべて squash。方針変更の判断があったのではなく、**AI が §1 の条項を確認せず直前の履歴に合わせて `--merge` を使い続けた**取りこぼしが8本連鎖したもの。オーナー裁定は「やり直しは不要、以後 squash に戻す」。§1 の意図（main のログを線形に保つ／squash ゆえ `git branch -d` が効かず `-D` で消す）はいまも有効なので本文は変更しない。**再発防止として、マージの実行前に §1 のマージ方式を読む**——直前の履歴を規範として扱わない（この8本のように、履歴の方が条項から外れていることがある） |
| 2026-08-01 | 上の再発防止を規律だけに頼らず、**リポジトリ設定で squash 以外を塞いだ**（Allow merge commits・Allow rebase merging をいずれも無効化。§1 に併記） | 「毎回 docs を読む」は8本連続で破られた実績があり、規律だけでは同じ取りこぼしが再発する。GitHub は最低1つのマージ方式を要求するので、squash だけを残せば**方式の取り違えが構造的に起きなくなる**（`gh pr merge --merge` はリポジトリ側で弾かれる）。rebase も塞いだのは、選択肢が1つなら迷いようがないため——将来コミットを分けたまま線形に入れたくなったら設定を戻す。**この方式で縛れるのはマージ方式だけ**で、ブランチ名・コミット規約・push の合図は引き続き条項と規律が担う |
