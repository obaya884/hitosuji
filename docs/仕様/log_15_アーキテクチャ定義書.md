# アーキテクチャ定義書 決定ログ

レイヤー構成・DDD規約の「なぜそうしたか」を日付順に残す。仕様そのものは[アーキテクチャ定義書](./15_アーキテクチャ定義書.md)が正。2026-07-26 の分離までのテスト戦略の判断も本ログにある（以後のテスト戦略の判断は `log_17_テスト戦略定義書.md` へ）。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-26 | §8 に「テスト補助の置き場」を追加し、ドメインの型を組み立てるフィクスチャ（`task()` / `routine()` / `atJst()` / `atLocal()`）は `src/domain/<集約>/testing/` に1本だけ置いて全層から使うと定めた（T-43） | T-39 の6ワーカー並行実装で同じ道具が別々の流儀で増え、`task()` が24本・`at()` が6定義（3シグネチャ）まで分裂した。集約先の候補は「domain 配下」と「`src/app/_testing/`」の二択だったが、後者にすると **domain のテスト（node 環境）が presentation 配下を参照する形になり依存方向が逆に見える**。domain は最下層なのでどの層から参照しても方向に反さず、`src/usecases/*/testing/in-memory-repository.ts` という前例とも揃う |
| 2026-07-26 | 共有ファクトリの**既定値は中立**（`estimateMinutes: 0`・`sectionId: null`・未打刻）に固定し、テストが依拠する値は呼び出し側へ書く規約にした（T-43） | 統合前は既定が `estimateMinutes: 30` / `sectionId: 1` のファイルがあり、**本文のどこにも 30 や 1 が現れないままテストがその既定に寄りかかっていた**（読んでも何を主張しているか分からない）。中立既定へ寄せた結果、実際に「見積もり0では手前の `estimate_required` で落ちて曜日検証に届かない」等の隠れた依存が7件表面化し、すべて呼び出し側へ明示した。テスト名から仕様条項が読めることが現在の最大の美点なので、既定値でそれを損なわない |
| 2026-07-26 | 既定値の中立化には**「異常系の判別が消える」という副作用**があると分かり、§8 の規約を「呼び出し側に書く」だけでなく**「そのテストが通す前提の値も書く」**まで含めて読むことにした（T-43） | `test-reviewer` が実際の劣化を1件見つけた——`from-task.test.ts` の「ルーチン由来からは作成できない」と「ルーチン由来の判定は見積もりチェックより優先する」が、`estimateMinutes` の既定を 0 にした結果**完全に同じ入力・同じ期待値**になり、後者が「優先する」を証明できなくなっていた（実装を `routineId !== null && estimateMinutes === 0` に壊しても両方緑）。中立既定は「暗黙の依存を炙り出す」効果と引き換えに「**手前のガードで落ちて本題に届かないテスト**」を作りうる。落ちたテストを直すだけでは足りず、**落ちなかったテストが依然として別物を主張しているか**を見る必要がある |
| 2026-07-26 | 打刻時刻の組み立てを `atJst()` と `atLocal()` に**名前で分けた**（同じ `HH:MM` 文字列を取る同一シグネチャ）（T-43 / T-47） | 表示側の `format.ts` は `APP_TIME_ZONE = Asia/Tokyo` 固定、入力の解釈側 `applyClockTime` は `Date#setHours` ＝ローカル、という非対称が現にある（根の解消は T-47）。統合前は同名 `at()` が JST 版とローカル版の**2つの契約で並存**しており、素朴に一方へ寄せると JST の手元では通り UTC の CI だけが落ちる。シグネチャを揃えたのは、テストを書くとき選ぶべきは「タイムゾーンの意味」だけで、書式まで変わると呼び分けの判断が濁るため |
| 2026-07-26 | 基準日を `TEST_DATE = 2026-07-26`（日曜）1つに統一し、曜日そのものに依拠するテストだけが自前の日付を持つことにした（T-43） | 共有ファクトリの `taskDate` 既定と、テストがユースケースへ渡す表示日が食い違うと、各呼び出しで `taskDate` を上書きする羽目になり集約の意味が消える。統合前は層ごとに 07-19 / 07-20 / 07-26 の3つがあったので、既存の app 層に合わせて 07-26 に寄せ、domain・usecases 側は**曜日を保ったまま +7日**ずらした（07-19 も 07-26 も日曜） |
| 2026-07-26 | マスタ固定値（`MODES` / `PROJECTS` / `SECTIONS`）は**統合しない**と決め、プリセット外の色を `MODE_COLORS` 参照へ直すだけに留めた（T-43） | 4セットの中身が違うのは不揃いではなく、各シナリオが必要とする条件（アーカイブ済みを含むか・id 体系・時間帯の並び）そのもの。1セットへ寄せると「このテストは何に依拠しているか」が本文から消え、上の中立既定の規約と同じ失敗を別の形で作る。一方で色だけは**プリセット外の hex（`#ff0000` 等）が使われていて実データと乖離**しており、`modeColorName` のフォールバック経路を無自覚に踏んでいたため直した（T-57 と同じ根） |
| 2026-07-26 | `next/navigation` の差し替えを**各テストファイルではなく `setup.ts` へ寄せ**、コンポーネント段の既定にした（T-43 第2段） | 6ファイルが4流儀（`vi.hoisted` で `push` だけ／`usePathname` を `vi.fn` 化／`AppRouterInstance` 全6メソッド／`vi.hoisted` を使わない素の `vi.fn`）に割れていた。**共有モジュールへ関数として切り出す案は原理的に不可能**——`vi.mock` はそれが書かれたファイルの先頭へホイストされるため、`import` した値を工場関数の中から参照できない。残る選択は「`setup.ts` へ寄せる」か「書き方だけ揃える」の二択で、前者を採った。決め手は**これが画面ごとの判断ではなく段そのものの前提**だという点——`next/navigation` はアプリのルータ文脈の外では常に動かず、「この画面ではルータを偽物にする」という選択の余地がない（`cleanup()` や `useRealTimers()` を `setup.ts` に置いているのと同じ性質）。実験で、`setup.ts` の `vi.mock` が全コンポーネントテストに効くこと、かつ**ファイル側で `vi.mock` を書けばそちらが優先される**こと（＝別の契約が要るテストの逃げ道が残る）を確認してから決めた |
| 2026-07-26 | jsdom の詰め物（`scrollIntoView`・`ResizeObserver`）は**共通化せず各ファイルに残す**と決めた（T-43 第2段） | ルータと同じ「段の前提」に見えるが扱いが逆になる。`scrollIntoView` は `daily-board` では**描画を通すだけの空実装**、`daily-list` では**追従したかを検証する `vi.fn`** として使われており、**同じ API でファイルごとに要る契約が違う**。一方に寄せると他方が嘘の緑になる（§8 が詰め物について警告しているとおり）。`ResizeObserver` は1ファイルにしかなく、集約の利得が無い。**「重複しているから寄せる」ではなく「契約が同じか」で判断する**という線引きがここで確定した |
| 2026-07-26 | `vi.clearAllMocks()` を7ファイルの `beforeEach` から `setup.ts` の `afterEach` へ移した（T-43 第2段） | `clearAllMocks` は**呼び出し記録だけ**を消して実装は残すため、各ファイルが `beforeEach` で組む `mockResolvedValue` の既定と衝突しない。`beforeEach` と `afterEach` のどちらに置いても各テストから見た状態は同じ（前者は直前に、後者は直前のテストの直後に消える）ので、後始末を担う `setup.ts` 側へ寄せた。**どの Server Action が成功既定かは各ファイルに残す**——それはファイルの前提として見えていた方が読めるため（起票時の「集約してはいけないもの」のとおり） |
| 2026-07-26 | §8 に「カバレッジ計測は同一ワークツリーで同時に1本まで」を追加し、`npm run test:coverage` を排他ロック付きのラッパ（`scripts/test-coverage.sh`）に替えた（T-58） | 起票時の見立て（前回実行が残した `coverage/` を後続が引き継ごうとする）は**実測で否定された**——vitest は起動時に `coverage/` を消して `.tmp` を作り直す（`coverage.clean` の既定 true）ため、残骸は元から掃除されている。`npm test`（カバレッジなし）の直後に `test:coverage` を回しても再現せず、**2本のカバレッジ計測を重ねたときだけ**1本目が `coverage/.tmp/coverage-N.json` の ENOENT で落ちることを再現できた（vitest 自身のエラー文言も「同じ `reportsDirectory` で複数の Vitest を同時に走らせていないか」と言っている）。したがって掃除を足しても placebo にしかならず、**同時実行そのものを止める**方向へ倒した。この形が要るのはメインセッションと `verifier` が別々にテストを叩く運用があるため。ロックは `coverage/` の外（`node_modules/.cache`）に置く——中に置くと vitest 自身の掃除で消える |
| 2026-07-26 | **§8 のテスト戦略を [17_テスト戦略定義書](./17_テスト戦略定義書.md) へ分離**し、§8 は要点と参照のスタブにした。節名（「偽物を置いてよい境界」「段の呼び名と境界」「テスト補助の置き場」等）は 17 側でそのまま維持する | 7/26 までの追記（T-39・T-43・T-47・T-58・T-37）で §8 が本文の3割超・入れ子3段の詳細規約になり、1級ドキュメントとして肥大した（オーナー指示「適切に情報を整理し直したい」による整理）。「詳細は下位文書に1か所だけ書く。上位文書は要約＋参照」の規約に従い下位文書を新設。「アーキテクチャ定義書 §8」への参照はコードコメント・台帳・完了記録に30箇所以上あるため、§8 を削除せず参照スタブとして残し、同名の節で読み替えられるようにした（コードコメント28箇所は続くオーナー指示により同日中に 17 直指しへ書き換えた。台帳・完了記録はスタブ経由のまま）。本ログのテスト戦略の既存エントリも移動しない（closed_23 等からのリンクを保つ。以後の判断は log_17 へ） |
| 2026-07-26 | 時刻の解釈・導出・表示を **`APP_TIME_ZONE`（Asia/Tokyo）固定**にそろえ、テストの日時ヘルパを `atJst` 1本にした（`atLocal` を削除）。タイムゾーンは domain が**引数で受け取り既定値を持たない**（定数の置き場は `src/domain/shared/time-zone.ts`、束ねるのは usecases / presentation）（T-47 / T-55） | 表示だけ JST 固定で解釈・導出がローカルという非対称は、コードから前提が読めず、UTC で走る CI だけが落ちる回帰を生んでいた。定数を presentation（`format.ts`）に置いたままでは `resolveToday` を usecases へ下ろせない（依存方向が逆になる）ため、置き場を `domain/shared` へ移すことが T-55 の前提条件でもあった。夏時間に耐えるのは `fromZonedClock` 単体の契約で、上位の導出（折返し・セクション終了時刻）は「1日=24時間」前提のまま——Asia/Tokyo に夏時間が無いので成り立つ前提として明記した |
| 2026-08-02 | **§4 に「Server Action の形」を追加し、40本を早期 return から `if (result.ok) { … } else { return failure(…) }` へ揃えた**（T-107。オーナー提案） | T-107 で失敗の返し方を `failure(文言)` の1流儀にまとめた副作用として、**`return` を落としても誰も気づけない状態**が生まれた——変更前の `return { ok: false, … };` は `return` を外すとオブジェクトリテラルが文の位置に来て構文エラーになったが、関数呼び出しは式文として合法なため、tsc も lint も全1866テストも緑のまま通る（実測）。失敗が成功に化けるので、楽観的更新が巻き戻らず「保存したのに次のリロードで消える」出方をする。40本中35本がこの状態だった（残り5本は後段で `result.value` を使うため絞り込みで落ちる）。当初はスクリプトによる形の検査を CI へ足す案だったが、**早期 return をやめれば型が塞ぐ**というオーナーの指摘で不要になった——if/else はどちらの枝が欠けても値を返さない経路ができ、明示した戻り値型に `undefined` が無いので `TS2366` で落ちる（失敗側・成功側の両方向で実測）。`noImplicitReturns` の追加も要らない。代償はネストが1段深くなることと2行増えることだが、**`revalidatePath` が成功枝の内側に入って「成功したときだけキャッシュを捨てる」が構造として読める**ようになる副次的な利得がある。条項として残したのは、この形が非自明で「早期 return に整理」されると穴が再び開くため |
| 2026-08-02 | **§2 に「コメントの仕様参照」を足し、裸の `§` の解釈をレイヤーで分けた**（T-102）——`src/app` 配下は自画面の定義書とみなし、下位3層では裸の `§` を禁じる | 起票時の調査で**同じ `§4.2-a` がデータモデル定義書と画面定義書01 で別条項を指している**ことが分かっており、しかも両方が裸で書かれていた。**全参照に文書名を付ける**案は採らなかった——`src/app` 配下は圧倒的多数が自画面を指しており、そこへ機械的に文書名を足すと冗長さのほうが害になる。逆に下位3層は**属する画面が無いので既定の解釈が置けない**ので、そこだけ裸を禁じれば曖昧さは消える。`F-XXX` / `N-XX` / `O-XX` を対象外にしたのは採番が全文書で一意だから（[T-41](../案件/closed_23_技術改善バックログ.md#t-41) の追随作業で実在性は確認済み）。この規約は grep での追随可能性を守るためのもので、次に節を移動・改番したときに旧参照を洗い出せることが目的（CLAUDE.md「書き方の規約」が前提にしている作法） |
| 2026-08-02 | **§2「コメントの仕様参照」は「下位3層で裸の `§` を一律に禁じる」案を採らず、「1ファイルが2つ以上の文書を指すときだけ各参照に文書名を書く」にした**（T-102。オーナー裁定） | 着手時に全88件を特定したところ、**下位3層の76件のうち大半は「1ファイル＝1文書」**だった（`sort-order.ts` の5件は全部データモデル §3.5、`review.ts` の6件は全部 画面定義書04）。そこへ文書名を足しても曖昧さは1つも減らず、台帳が禁じた「全483箇所の機械的な書き換え」に近づくだけになる。**危険なのは1ファイルが2文書を指す9ファイル**で、そこでは同じ字面が別条項を指す——`daily-list.ts` の `§3.1`（21行＝データモデル / 76行＝画面定義書01）、`operations.ts` の `§4.2-a`（42行＝データモデル / 104・105・124行＝画面定義書01）、`drizzle-task-repository.ts:40` と `in-memory-repository.ts:123` に至っては**1行の中に2文書が混在**していた。`src/app` 側の既定を画面ディレクトリで決め、その配下の `_components` / `_lib` / `_testing` に継がせたのも同じ理由（`masters/_components/master-editable-cell.tsx` は画面定義書03 の話と 00_共通 §2.3 の話を両方持つので、後者にだけ文書名が要る） |
| 2026-08-08 | **§7 に「境界はその呼び出しが複数行を書くかで決まる」を足し、`drizzle-task-repository.ts` の分岐8か所は残した**（T-105。オーナー裁定） | 起票時の見立ては「分岐は挙動を変えず往復1回ぶんの最適化にしかならないのに、1メソッドが2経路に割れて統合テストを恒久的に2本要求する」だった。実際に**全部トランザクションへ寄せる形を作って**確かめたところ、その見立ては当たっていなかった——テストは1本も減らず（96→99 で増えた）、代わりに**DB操作の解像度が落ちた**（オーナー指摘）。畳むと、`suspend` や割り込みの `start` のように**操作として本質的に不可分なもの**と、`move` や `delete` のように**付帯更新が付いてくるときだけ複数行になるもの**が同じ形に見え、「なぜ原子性が要るのか」がコードから読めなくなる。仕様条項との対応が取れることを美点にしているコードベースでは、往復1回の節約よりこの情報のほうが価値が高い。ただし**元の分岐に戻すだけでも解像度は戻らない**——元の書き方は「空なら単文、あればトランザクション」という最適化の顔をしており、起票時に「性能上の最適化にしかならない」と読まれたのはコードがそう書いてあったから。そこで分岐は残したまま、各枝に「何行書くか」と「途中が見えると何が壊れるか」を書く形へ直し、その書き方を §7 の条項にした。オーナーが挙げた別案（単文版とトランザクション版をメソッドで分け、ユースケースで呼び分ける）は採らなかった——Port が10→20、偽物も同数、そして**ユースケース15か所に分岐が生まれてユニットテストの対象になる**ので、数えていた重複が上の層へ増えて引っ越すだけになる。§7「ユースケース層にトランザクション抽象を導入しない」・§6「メソッド粒度はユースケースが必要とする操作単位」とも衝突し、単文版を非空の付帯更新で呼ぶ取り違えが `sort_order` の重複として静かに出る経路も新しくできる。**この作業で唯一残った実利はテスト側**で、`test-reviewer` が `delete` / `restore` にロールバックを見るテストが1本も無いこと（「削除すると同じトランザクションでスキップが記録される」という名前が誰も観測していない性質を名乗っていた）と、`move` が「振り直しに移動対象自身が入る」実運用の形を一度も通していないことを見つけ、3本足した |
| 2026-08-09 | **§7 に「同時に複数の更新が届く前提を置かない」を足し、楽観ロック・バージョン列を置かない根拠を明文化した**（FB-74。オーナー裁定: 対応不要で記録に留め、根拠だけ条項化する） | FB-74 の見立ては「セクション変更（O-5）と打刻修正（F-203）が同じ2列（`section_id` / `sort_order`）を書くので、両方が飛んでいる間に後着が先着を消しうる」だった。**実測で否定された**——Next.js のクライアント側アクションキュー（`app-call-server.js` の `callServer` → `dispatchAppRouterAction(ACTION_SERVER_ACTION)` → `app-router-instance.js` の `actionQueue.pending` チェーン）が Server Action を直列化するため、**同一の JS 実行単位で2つ発火させても POST が重ならない**（確認環境で計測: 1本目 33548–33658ms / 2本目 33665–33702ms）。しかも2件とも正しく適用された（割り込みが2連鎖して完了2件＋実行中1件）——両ユースケースは実行時に DB を読み直す（`setTaskSection` は `listByDate`、`updateTaskPunch` は `findById` ＋ `listByDate`）ので、直列に届く限り失われる更新は原理的に出ない。**人間の操作速度の問題ではなく構造的に起きない**ので、起票時の対処案（画面側でこの2操作だけ排他にする／サーバ側で楽観ロック）はどちらも要らない——どちらも N-01 の体感0msを削るだけになる。条項として残したのは、根拠が**フレームワークのクライアント実装**にあってコードのどこを読んでも出てこないためで、書いておかないと同種の疑問が出るたびに検証し直しになる。**再確認を条項に含めた**のは、Next の別ドキュメント（`01-getting-started/07-mutating-data.md`）がこの直列化を "an implementation detail and may change" と断っているため。保証の範囲をクライアント1つぶんと書いたのはタブを2つ開けばディスパッチャが別になるからだが、人間が100ms以内に両タブを操作することはないので実害としては数えない |
