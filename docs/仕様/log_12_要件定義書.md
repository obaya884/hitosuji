# 要件定義書 決定ログ

方式・要件レベルの判断を日付順に残す。仕様そのものは[要件定義書](./12_要件定義書.md)が正。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-07-16 | 日界はカレンダー日付（0:00）固定とし、将来導入できるデータ設計だけ維持する | MVP のスコープを絞るため（データモデル定義書 §1） |
| 2026-07-16 | セクションの既定値を 朝/午前/午後/夜/深夜 の5区分とする | 汎用的な初期データとして投入（データモデル定義書 §5） |
| 2026-07-16 | 実行中タスクがある状態で別タスクを開始したら割り込みとして扱う | 同時実行を1件に保ちつつ、残りを後で再開できるようにするため（§5.2 F-201） |
| 2026-07-17 | 詳細要件書（詳細設計書）を作成しない | 本書＋データモデル定義書＋画面定義書を挙動の契約とし、実装の形はコードに委ねる |
| 2026-07-19 | ショートカット一覧を確定（画面定義書01 §6） | 設計方針＝選択行モデル＋単キーのニーモニック、修飾キーは Shift のみ |
| 2026-07-19 | コールドスタートは実測 1.4秒 / 0.2〜0.4秒で許容と判断 | N-01 は高頻度操作の即応が本質であり、初回1回の遅延は許容できるため |
| 2026-07-20 | S-02・S-03 は楽観的更新の対象外とする | 低頻度操作であり、保存完了を待って反映するほうが単純なため |
| 2026-07-21 | 全画面共通のUIを画面定義書00 に集約 | 要件定義書 §4 と画面定義書01 に分散し、S-04 追加時に再定義が要る状態だったため |
| 2026-07-21 | 先送り数（F-502）を「指定日に残っている未実行タスクの件数」と定義し、先送りの履歴は持たない | 他日へ移した分まで数えるには移動履歴のテーブルが必要になるが、過去分は遡れず、指標としては「やると決めてやらなかった数」が読めれば足りるため。データモデルを変えずに済む定義を選んだ |
| 2026-07-21 | `postponed_count` は記録のみとし、F-502 の集計には使わない（列は残す） | 先送り数の定義を「その日に残っている未実行タスク」にしたため参照先がなくなったが、通算の先送り回数は導出も遡及もできない事実であり、記録を止めると将来（常習的な先送りの可視化・F-505）に取り返せないため |
| 2026-07-21 | S-04 は日次を先に完成させ、週次・月次の切替は後続とする | 日次の振り返りが日課であり、週月の集計は実運用で必要性を確かめてからでよいため |
| 2026-07-21 | F-110 の時間合計の分子を「完了は実績・未完了は見積もり」の合算とし、ラベルを「合計」にする（実行中は見積もりで数え、完了時に実績へ切り替わる） | 残り時間表示で過去セクションも見出しが残るようになり、完了済みを「見積」で足すと実態とズレる。完了を実績にすれば「実際に使った時間＋これからの予定」を1数字で読める。詳細は画面定義書01 §3.2 |
| 2026-07-21 | 終了予定時刻（F-104）・セクション残り時間（F-110）の**式の1次情報をデータモデル定義書 §4.3 に一本化**し、要件定義書・画面定義書は要約＋参照に統一 | 同じ式が要件・データモデル・画面の3箇所に重複し、改訂のたび食い違う恐れがあった。書き方の規約「詳細は下位文書に1か所」に揃える。式は導出ロジックなので下位（データモデル）が本拠、表示の符号・色・表示条件は画面定義書が持つ |
| 2026-07-21 | S-04 の週・月切替（F-506 として分離）・N-08・Playwright による E2E 導入検討を Phase 3 へ移す | いずれも日々の体験に直結せず、必要性を運用で見極めてからでよいと判断したため（優先度変更の経緯は要求定義書 §10）。実運用テストは区切りのない継続活動のため実装計画での状況管理対象から外し、気づきはFB管理簿へ随時起票する |
| 2026-07-21 | F-210（開始打刻の取り消し）の `U` は「実行中タスクを選択中なら開始取消、それ以外は削除取消」で切り分ける（FB-37） | Undo の語感を保ちつつ削除の取り消し（O-8）を乗っ取らないため。実行中は最大1件で選択も容易。戻したタスクは開始前の位置を復元せず未実行として並べ直す（クライアントに状態を持たず再読込に強い）。割り込み時に直前タスク・再開タスクへ波及させないのは、開始の取り消しを1タスクの局所操作に留め実装と挙動を単純に保つため |
| 2026-07-21 | F-210 の戻し先を「今日は現在時刻を含むセクションの先頭、今日以外は並べ直さず打刻クリアのみ」と定める（FB-37 セルフレビュー） | 実行中を取り消すと現在位置が消え「これから領域の先頭」の参照が宙づりになるため、現在時刻のセクション先頭と明示。過去日・未来日は現在位置・これから領域が定義できないので並べ直しをしない |
| 2026-07-22 | 日界（F-116）を Phase 2 の Should として追加。影響範囲は「今日」の解決とセクション回転に閉じ、`task_date`（保存論理日付）は変えない | 従来「0:00 固定・データ設計だけ維持」の保留だった（本表 2026-07-16）。日跨ぎ挙動の確認を機に要求化（要求定義書 §10）。データ設計は導入済み（データモデル §1）なので、実装は表示日・展開対象日・終了予定対象日の解決とセクション表示順の起点付け替えに限定できる。設定UI・境界時刻の詳細は着手時に画面定義書へ |
| 2026-07-22 | F-116 の日界を「有効セクションのうち1つを日界セクションに指定」する方式に確定（日界時刻＝そのセクションの `start_time`）。既定は深夜(00:00)で現状の 0:00 固定を踏襲 | 日界を任意時刻にするとセクションの途中に境界が落ち、今日/前日の帰属や回転の起点が二重定義になり得た。セクション境界に一致させれば「有効セクションは24時間を敷き詰める」既存の枠組みに乗り、どのセクションも分割されない。オーナーもセクション選択方式を選好（データモデル §3.1 / 画面定義書03 §3.1・01 §3.1・§3.2） |
| 2026-07-22 | 日付表示クリックからのカレンダー日付ジャンプを F-117（Could）として F-106 から分離し §5.1・§3 を付け替え（FB-39） | datepicker は F-106 の要件（§5.1）・画面定義書01 §3.1 に含まれていたが未実装。基本ナビ（前後日・今日）は実装済みで Must のまま、頻度の低い遠距離ジャンプだけを Could として切り出し優先度を実態に合わせた。画面定義書01 §3.1 の対応要求も F-106 → F-117 へ付け替え済み。挙動そのものは既存仕様のままで変えていない |
| 2026-07-23 | 開発案件の管理単位をフェーズ（Phase 1〜3）から「決定状態＋着手トリガの台帳」へ移行する。§3「開発フェーズ」を「開発の現況」に縮約し、未実装要求のライブ台帳を新設の[要件バックログ](../案件/22_要件バックログ.md)へ移す（旧 実装計画.md＝現 archive_24_実装計画.md は MVP 期のフェーズ運用の史実アーカイブとして凍結）。決定状態は 確定・着手待ち／候補・見極め中／監視項目 の3つ、見送りの出口は要求定義書 §8・§10。あわせて技術軸のファイル名も 技術改善計画 → 技術改善バックログ に統一 | フェーズはリリース波（launch までに何をどの順で出すか）の抽象で、MVP へ向かう間は現実の作業順と一致していた。しかし 2026-07-20 の実運用開始後は作業が波でなく運用の気づきから随時流入するようになり、閉じたはずの Phase 2 に Should（F-118）が追加される・Phase 3 が「実施可否を運用で見極め中の候補プール」と化して番号が実態を表さない等、箱が現実と噛み合わなくなった。すでに機能している技術改善バックログ（T-XX）の非フェーズ型台帳と同型に揃える。ファイル名は launch 期の語彙「実装計画」を凍結名として残し、継続運用の語彙「バックログ」を新ファイルに与えることで役割の転換を名前にも表す。**MVP 期にフェーズ運用していた事実・各フェーズの対象要求と成果は実装計画「完了記録」に史実アーカイブとして残す**（フェーズ語はそこにのみ用いる） |
| 2026-07-25 | F-401 のモード色の表現を「左端のカラーバー等」から「行全体のテキスト色」へ改める | 実装・画面定義書01（§2・§3.3）はいずれも行全体のテキスト色で、要件定義書だけが初期案のカラーバー表記のまま残っていた。実装を正として要件側を合わせる（挙動の変更ではなく記述の是正）。FB-48 のプロジェクト列を追加する作業中に発見 |
| 2026-07-26 | **運用タイムゾーンが Asia/Tokyo 固定であることを §2.2 に明記する**（T-47） | この前提はコード定数（`APP_TIME_ZONE`）と技術改善バックログの決定ログにしか無く、**どの仕様文書にも書かれていなかった**（T-47 のレビューで `spec-reviewer` が指摘）。F-116 の日界・F-203 の `HH:MM` 解釈・F-104/F-120 の導出がすべてこの1点に依拠しており、明記が無いと「実行環境のローカル時刻に従う」という別の読み方が常に残る。置き場を要件定義書 §2.2（システム方式）にしたのは、画面や集約に属する規則ではなくアプリ全体の方式だから。**海外で開いたときに現地時刻で表示する案は挙動の変更**なので、本文には「必要になれば要求として新規に起こす」までを1行残し、経緯（②案を採らなかった理由）は[技術改善バックログの決定ログ](../案件/log_23_技術改善バックログ.md) T-47 が持つ |
| 2026-07-26 | 品質監査に伴う整理（オーナー裁定）: §5.2 の F-208・F-210・F-212 を要約＋参照へ圧縮（詳細は画面定義書01 O-13〜O-15 とデータモデル定義書 §4.5〜§4.7 が正）。F-204 に見積もり未設定（0分）の例外を明記しデータモデル定義書 §4.2 と揃えた。F-107 から指定日移動を落とした（要求定義書側の決定に追随）。コールドスタートの実測・タイムゾーンの理由展開・N-03/N-06 の経緯記述を決定ログ・台帳参照へ圧縮し、§3 の MVP 期 blockquote をアーカイブ参照1行に畳んだ | 「詳細は下位文書に1か所」「本文は現在の仕様のみ」の規約適用。同じ挙動が2文書に全文で書かれ、片方だけの改訂で食い違う状態を解消するため。コールドスタートの実測 1.4秒/0.2〜0.4秒は本表 2026-07-19 のエントリが正。悪化時の対処案（本文から本エントリへ退避）: ①Neon の autosuspend 時間延長 ②cron ping ③別構成への移行、の順 |
| 2026-08-01 | **N-03 の Basic認証を「資格情報の設定状態で切り替える」方式から「デプロイ環境で切り替える」方式へ改める**（`VERCEL_ENV` が `production` / `preview` なら常に認証を要求し、ローカル開発は常に素通し。認証を要求する環境で資格情報が欠けていれば 500）。FB-73 の 2026-07-26 の判断（環境の概念を持ち込まず、空文字だけ 401 に倒す）を置き換える | FB-73 の実装中にオーナーが「値の状態で分岐するのは要件として過剰で、環境で分岐する方が分かりやすい」と判断した。この方式なら FB-73 の問題（空文字で認証が黙って外れる）は**構造的に消える**——本番は環境変数の中身によらず認証を要求するため、素通しへ落ちる経路自体が無くなる。条件分岐も「空文字か／未設定か／片方だけか」の3軸から環境の1軸へ減る。判定に `NODE_ENV` ではなく `VERCEL_ENV` を使うのは、`npm run build` がローカルでも `NODE_ENV=production` で走る（CLAUDE.md「環境変数」節）ため、手元のビルド検証で認証が有効化される事故を避けるから。**Preview も認証対象**にしたのは、資格情報が未設定の Preview は事実上封鎖され、公開 URL が意図せず見られる余地を残さないため。**資格情報が欠けているときを 401 でなく 500 にした**のは、利用者が何を送っても解決できない状態で「資格情報を出せば解決しうる」と伝える 401 は嘘になり、オーナー自身も設定ミスとパスワード間違いを見分けられなくなるから（起動時に落とす案を採らない理由は FB-73 の詳細節が持つ。本方式は起動失敗と違いデプロイは生きたままログに理由を出せる） |
| 2026-08-08 | **N-01 の除外の書き方を「操作の一覧」から「副作用の性質」へ改めた**（サーバが採番・配置を決める副作用だけがサーバ確定後になる、と書き、どこまでが即時かは画面定義書01 §4 が正とした。FB-69） | 従来の N-01 は対象外を「中断 O-4・複製 O-11・先送り O-7」の3操作で列挙しており、**操作まるごと対象か対象外か**の二値にしか読めなかった。実際には開始（O-2）のように**打刻は即時・生成タスクの採番と配置だけサーバ確定後**という部分的な操作があり、その事実がコード内コメントにしか無かった（開始取消 O-13・完了取消 O-15 の並べ直しも同型で、こちらは画面定義書01 側にだけ書かれていた）。列挙を足すより**除外される理由（サーバが採番・配置を決める）を書く**方が、同型の操作が増えたときに自動的に説明が届く。詳細な線引きは画面定義書01 O-2 が持つ（[log_01](./13_画面定義書/log_01_デイリーリスト.md) 2026-08-08） |
| 2026-08-01 | F-118（ハイライト）の仕様を確定し §5.1 を「（未実装）」のまま実装内容へ書き換え、§7 の実現先を `S-01` から **`S-01 + S-04 + データモデル tasks.highlighted`** へ広げた | 要求（§6.1 F-118）は「その日注力したいタスクを強調表示できる」までで、寿命・引き継ぎ・データ表現が未定だった。オーナーとの対話で、使い方は「朝に本丸を数本立て、日中に増減もする」・**完了しても外れない**・**本数の上限は設けない**と定めた。**表示日を問わない**（過去日・未来日でも付け外しできる）としたのは、終了予定（F-104）・予想開始時刻（F-120）が「今日だけ」なのは**現在時刻から導出する値だから**であって、保存された宣言であるハイライトにはその理由が当たらないため。レビュー（S-04）への展開はオーナーの追加要望で、「今日の本丸をやれたか」を振り返る用途に当たる。見え方・操作の詳細は画面定義書01・04 が、データ表現はデータモデル定義書 §3.5 が持つ |
| 2026-08-09 | **N-01 の「S-01 の対象外」から操作の個別列挙を落とし、画面定義書01 §4 への参照に寄せた**（FB-76 / N-12。`spec-reviewer` 指摘） | 従来の N-01 は「中断・複製・先送りは**生成タスクの採番をサーバが決めるため**対象外」と3操作だけを名指ししていたが、実際の対象外はルーチン化（O-12）・複製して開始（O-14）・保留中の取り消しの実行（O-8 / O-15）を含む6操作で、**理由も操作ごとに違う**（採番／画面が変わらない／並びが戻る）。2026-08-08 に「操作の一覧から副作用の性質へ」改めた（FB-69）ときに書き換えたのは後半の1文だけで、前半の列挙が残っていた。N-12 を新設したことでこの列挙が**適用範囲を決める一次情報として読まれる**ようになり、不完全さが実害を持った——00_共通 §4.2 が「N-01 の対象外がそのまま該当する」と書いているため。同じ理由で §4.2 側の再列挙も落とし、**一覧を持つのは画面定義書01 §4 の1か所だけ**にした（CLAUDE.md「同じ規則を2か所に書きたくなったら下位に寄せる」）。実際、今回の着手中に O-14 を 01 §4 にだけ足してしまい、§4.2 の列挙が古いまま残る事故が起きている |
| 2026-08-09 | F-107 に「**ルーチン由来のタスクは移動と同時に紐付けを外す**」を足した（[FB-99](../案件/closed_21_ユーザーフィードバック.md#fb-99)） | ルーチン由来のタスクの先送りが展開の一意制約と衝突して失敗し、衝突しない日は移動先の展開が無音で欠けていた不具合の是正。決定の理由と退けた代替案は [log_14](./log_14_データモデル定義書.md) 2026-08-09 が持つ |
| 2026-08-09 | F-204 の生成規則から「同名」を外し、**再開タスクの名前は元タスク名＋「（再開）」**と定めた（[FB-95](../案件/closed_21_ユーザーフィードバック.md#fb-95)） | 要求（F-204 = 中断したぶんを後で再開できる）は変わらないため要求定義書は据え置き、生成規則の側だけを直した。中断（O-4）と割り込み（F-201）は同じ生成規則を通るので**両方に掛かる**——片方だけにすると同じ「再開タスク」が2通りの名前で生まれる。接尾辞を保存する判断と退けた代替案（表示だけで導出する案・回数表記）は [log_14](./log_14_データモデル定義書.md) 2026-08-09 が持つ |
| 2026-08-09 | バンドル（F-119）の挙動を **§5.6 として独立した節**に置いた（ルーチン §5.3 の中に入れなかった）。あわせて §4 画面一覧に S-05 を追加し、ナビの項目を5つにした | バンドルはルーチンに属する機能ではなく、`tasks.bundle_id` を持つタスク群に掛かる仕組みで、供給元はルーチン以外にも増えうる（[31_バンドル検討.md](../検討/31_バンドル検討.md) §7）。§5.3 に入れると「ルーチンの一機能」という誤った位置づけが条項の場所として固定される |
| 2026-08-09 | 割り込みの検知を**事後の情報トーストのみ**とし、①事前の確認で開始を止める案 ②トーストに取り消しボタンを添える案 の両方を退けた（F-119） | ①は打刻の体感即応（N-01・C-2）をこの操作だけ壊す。加えて正当な割り込み（急な用事）でも毎回止まるので、反射で消すようになって効かなくなる。②を置かないのは、警告が出た瞬間は開始したタスクが実行中かつ選択中で、**`U`（画面定義書01 O-13）がそのまま開始の取り消しに当たる**ため——同じことができる導線を2つ持たない。オーナーが理想として挙げた「やめて本来の次タスクへ戻る」は、この `U` と道の表示で足りると判断した。ボタンを置かない判断がこの接続に依存することは §5.6 の本文に残した |
| 2026-08-10 | N-11 の実現方式を「操作の結果はトーストで通知する」から**画面の性質で分岐する形**（S-01 はトースト／保存完了を待つ画面は表の上部の帯・完了通知なし）へ書き換えた。あわせて §5.6 の運用ルールから「揃っていないメンバーは S-05 が印で知らせる」を落とし、§7 の F-405 に S-05 と `bundles` を足した | F-119 のマージ前レビューで、**条項と実装が食い違っている箇所が3つ**見つかった。①N-11 は一律トーストと書いていたが S-02 / S-03 は最初から帯で、画面定義書05 も 2026-08-09 に帯へ揃えたので、実現方式だけが古いまま残っていた（画面定義書00 §4.1 は同日に直してあり、上位の実現方式が追随していなかった）。②⚠印は[31_バンドル検討.md](../検討/31_バンドル検討.md) §7.4 で保留にしたのに、要件側だけ「印で知らせる」と残り、**未実装の条項があるように読めた**。③F-405 は画面定義書05 §5 でバンドルにも及んだのにトレーサビリティが S-03 のままだった。いずれも下位文書が先に動いて上位が取り残された形で、**上位は要約に留める規約の裏返しの事故**（要約ゆえに気づきにくい） |
| 2026-08-13 | F-111（複製）の挿入先を「未実施のトップ」から**複製元の直下**へ変えた（[FB-103](../案件/closed_21_ユーザーフィードバック.md#fb-103)） | 要求（F-111 = ワンアクションで複製し未実行タスクとして追加）は挿入位置を持たないため要求定義書は据え置き、要件の側だけを直した。規則 b（F-113）に例外を設けない判断と、複製して開始（F-208）を据え置いた理由は [log_01](./13_画面定義書/log_01_デイリーリスト.md) 2026-08-13 が持つ |
| 2026-08-24 | N-05 の実現方式に**押せること・押せないことの示し方**（詳細は画面定義書00 §2.5）を1行足した（[FB-100](../案件/closed_21_ユーザーフィードバック.md#fb-100) / [FB-97](../案件/closed_21_ユーザーフィードバック.md#fb-97)） | 要求（N-05 = 装飾を排したシンプルなUI）は変えていない——ホバーと無効の見せ方は N-05 の表示詳細で、[FB-57](../案件/closed_21_ユーザーフィードバック.md#fb-57)（未設定の語）・[FB-93](../案件/closed_21_ユーザーフィードバック.md#fb-93)（未設定の記号と色）を要求定義書に触れず画面定義書へ収めたのと同じ扱いにした。**それでも N-05 に1行を足した**のは、「見せ方が割れる」という同型の事故が3回続いており（FB-55 / FB-57・FB-93 / 今回）、要件の側に「形の種類で1つに決める」という原則が無いと**次に部品が増えたときも同じように割れる**ため。詳細は画面定義書00 §2.5 へ委譲し、本文は原則の1行に留めた |
