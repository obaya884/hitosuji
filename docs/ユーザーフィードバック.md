# ユーザーフィードバック管理簿

- 作成日: 2026-07-20
- 目的: 実運用（TCC2 との並行運用〜本運用）で得たオーナーの気づきを一元管理し、今後の開発の入力にする

## 運用ルール

1. 気づきはまず本書に**起票**する（粒度はバラバラで良い。会話で出たものは AI が転記する）
2. 起票時に**分類**する: `不具合` / `改善（軽微）` / `改善（要仕様整理）` / `要求（優先度変更含む）` / `開発環境`
3. 対応するときは通常の開発ルールに従う — **挙動レベルの変更は該当仕様書（要求定義書・要件定義書・データモデル定義書・画面定義書）を先に更新**し、本書には対応結果と参照先だけを残す
4. 状態は `未対応` / `対応中` / `対応済み` / `見送り` の4つ。対応済み・見送りには日付と理由（または反映先）を書く

## 一覧

### 不具合

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-01 | 2026-07-20 | デイリー画面のモード選択ポップオーバーの並び順が名前順になっていない（マスタ画面は名前順。デイリー用データ取得が未ソートの `listAll()` を返している） | 未対応 |
| FB-14 | 2026-07-20 | デイリーの列見出しが行の3点メニュー分の幅を考慮しておらず、「実施時間」の位置が少しずれている | 未対応 |

### 改善（軽微 — 仕様判断がほぼ不要で、画面定義書の更新＋実装で完結する）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-04 | 2026-07-20 | キーボードショートカット一覧（ヘルプ）に各キーのニーモニック由来を記載する（例: B = Begin） | 未対応 |
| FB-05 | 2026-07-20 | デイリーの行の高さはそのままで、文字サイズをもう少し大きくして読みやすくする | 未対応 |
| FB-07 | 2026-07-20 | 今日以外の日付を表示中に「今日」ボタンが強く表示され、表示日が今日であるかのように誤認する。強調の意味を整理する | 未対応 |
| FB-09 | 2026-07-20 | ルーチン一覧にプロジェクトとモードの列を表示する | 未対応 |
| FB-12 | 2026-07-20 | モード一覧で色に名前を表示する（画面上では見分けにくい配色があるため） | 未対応 |
| FB-13 | 2026-07-20 | ルーチン一覧の「有効」チェックボックス列は「開始想定」の右に置く | 未対応 |
| FB-15 | 2026-07-20 | エラートーストを一定秒で自動的に消す、または手動で閉じられるようにする | 未対応 |

### 改善（要仕様整理 — 挙動・デザインの方針をオーナーと決めてから docs 反映 → 実装）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-02 | 2026-07-20 | セクション未設定（未分類）のタスクを開始したら、開始時刻が含まれるセクションへ自動で移動させたい（FB-16 と関連） | 未対応 |
| FB-03 | 2026-07-20 | マスタの変更で編集ボタンを都度押すのが面倒。常時インライン編集などの操作方式を検討する | 未対応 |
| FB-06 | 2026-07-20 | セクション見出しの情報量を TCC を参考に増やしたい（何を表示するかの整理が必要） | 未対応 |
| FB-08 | 2026-07-20 | グローバルナビゲーション（デイリー／ルーチン／マスタ）を左サイドの Navigation Rail にする（全画面のレイアウトに影響。N-05「シンプルなUI」との整合を確認して決める） | 未対応 |
| FB-10 | 2026-07-20 | ルーチン一覧のソート軸を選べるようにする（開始想定・プロジェクト・モード・名前・繰り返しタイプ） | 未対応 |
| FB-16 | 2026-07-20 | 実行中タスクの前に未実施タスクが並んでいる状態はおかしいので、未実施タスクを自動でセクション移動させたい。**要求レベルからの整理が必要**（自動で動かすことの是非・タイミング・対象。FB-02 と合わせて1つの要求として検討する） | 未対応 |

### 要求（新規・優先度変更）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-11 | 2026-07-20 | タスクリストからのルーチン化（= F-305、現在 Could / Phase 3）が意外と重要。ルーチンへの改めての登録作業が重たいため、優先度の繰り上げを検討する | 未対応 |

### 開発環境

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-17 | 2026-07-20 | 本番と開発で異なるファビコンを設定し、タブで見分けられるようにする（現状は create-next-app 既定のままという未対応事項もある） | 未対応 |
| FB-18 | 2026-07-20 | 本番DBのデータを過剰にならない範囲（マスタ・ルーチンなどタスク以外）で開発DBへ流し込めるようにする。シングルユーザーなので問題なく、タスクはルーチン展開で生成される | 未対応 |
