# ユーザーフィードバック管理簿

- 作成日: 2026-07-20
- 目的: 実運用（TCC2 との並行運用〜本運用）で得たオーナーの気づきを一元管理し、今後の開発の入力にする

## 運用ルール

1. 気づきはまず本書に**起票**する（粒度はバラバラで良い。会話で出たものは AI が転記する）
2. 起票時に**分類**する: `不具合` / `改善（軽微）` / `改善（要仕様整理）` / `要求（優先度変更含む）` / `開発環境`
3. 対応するときは通常の開発ルールに従う — **挙動レベルの変更は該当仕様書を先に更新**し（下記「着手手順」）、本書には対応結果と参照先だけを残す
4. 状態は `未対応` / `対応中` / `対応済み` / `見送り` の4つ。対応済み・見送りには日付と理由（または反映先）を書く

## 着手手順（FB → 仕様書 → 実装）

FB は「画面の話」として書かれるため、**画面定義書だけ直して上位文書に反映し忘れる**事故が起きやすい（2026-07-20 に F-306 で発生。指摘を受けて是正）。着手時は必ず上から順に判定する。

### 手順1: 既存の要求（F-XXX / N-XX）に当てはまるか判定する

`docs/要求定義書.md` §6 を検索し、この FB が実現しようとしていることが**既存要求の範囲に収まるか**を確かめる。

- **収まる** → 手順2へ（要求定義書の変更は不要）
- **収まらない** → **要求定義書に新しい ID を起こす**（要求文＋優先度＋起票理由と FB 番号を書く）。例: FB-10「ルーチン一覧のソート軸選択」は F-303「一覧・編集・有効/無効切替」に含まれないため F-306 を新設した
- **既存要求の優先度を上げ下げする** → 要求定義書の優先度列を変更し、変更理由と日付を残す（例: F-305 の Could → Should）

### 手順2: 要件定義書へ反映する

- 要求IDを**新設・優先度変更**したなら §3 のフェーズ表（Phase 1/2/3 の要求ID一覧）を必ず直す
- §5 の該当節に、挙動の要件を1行以上書く（画面の詳細ではなく「何がどうなるか」）

### 手順3: 画面定義書へ反映する

- 対象画面の本文（表示要素・操作仕様）に詳細を書く
- ヘッダの「対応要求」行に要求IDを追記する
- 判断が分かれた点は**理由と日付**を添える（後から読んで再現できるように）

### 手順4: データモデル定義書（列やテーブルが増減する場合のみ）

### 手順5: 実装 → 本書の状態を更新

対応済みにするとき、**どの文書のどこに反映したか**を状態欄に書く。

> チェック: 実装に入る前に「要求定義書 → 要件定義書 → 画面定義書」の3階層すべてを見たか？ 画面定義書しか触っていないなら、手順1の判定を飛ばしていないか確認する。

## 一覧

### 不具合

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-01 | 2026-07-20 | デイリー画面のモード選択ポップオーバーの並び順が名前順になっていない（マスタ画面は名前順。デイリー用データ取得が未ソートの `listAll()` を返している） | 対応済み（2026-07-20。画面定義書03 §4 のとおり `listDailyList` で name / start_time 昇順に整列。セクションの並び漏れも併せて修正） |
| FB-14 | 2026-07-20 | デイリーの列見出しが行の3点メニュー分の幅を考慮しておらず、「実施時間」の位置が少しずれている | 対応済み（2026-07-20。画面定義書01 §2 に整列の要件を追記し、`table-fixed` ＋ `colgroup` で列幅を一元化） |
| FB-19 | 2026-07-20 | ルーチン一覧（S-02）はモード・プロジェクトを**有効なマスタのみ**受け取っているため、アーカイブ済みマスタを参照するルーチンの新設列が空欄になる。画面定義書03 §4「参照中の表示には従来どおり使われる」に反する（FB-09 の実装レビューで発見。修正には `src/app/routines/page.tsx` で全件を渡す変更が必要） | 対応済み（2026-07-20。表示用に全件、編集フォームの選択肢には有効分のみを渡すよう props を分離） |

### 改善（軽微 — 仕様判断がほぼ不要で、画面定義書の更新＋実装で完結する）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-04 | 2026-07-20 | キーボードショートカット一覧（ヘルプ）に各キーのニーモニック由来を記載する（例: B = Begin） | 対応済み（2026-07-20。画面定義書01 §6 に併記の方針を追記し、ヘルプへ反映） |
| FB-05 | 2026-07-20 | デイリーの行の高さはそのままで、文字サイズをもう少し大きくして読みやすくする | 対応済み（2026-07-20。画面定義書01 §2 に追記。本文を1段階拡大し、縦余白で行高44pxを維持） |
| FB-07 | 2026-07-20 | 今日以外の日付を表示中に「今日」ボタンが強く表示され、表示日が今日であるかのように誤認する。強調の意味を整理する | 対応済み（2026-07-20 オーナー判断。画面定義書01 §3.1 を改訂し、強調色をやめてラベルを「今日へ」に。ボタンの有無で今日以外を示す） |
| FB-09 | 2026-07-20 | ルーチン一覧にプロジェクトとモードの列を表示する | 対応済み（2026-07-20。画面定義書02 §2/§3 に列構成を明記して実装。ただし FB-19 の積み残しあり） |
| FB-12 | 2026-07-20 | モード一覧で色に名前を表示する（画面上では見分けにくい配色があるため） | 対応済み（2026-07-20。画面定義書03 §3.2 にプリセット13色と色名の対応表を追加し、一覧のバー横へ併記） |
| FB-13 | 2026-07-20 | ルーチン一覧の「有効」チェックボックス列は「開始想定」の右に置く | 対応済み（2026-07-20。画面定義書02 §3 の列順に反映） |
| FB-15 | 2026-07-20 | エラートーストを一定秒で自動的に消す、または手動で閉じられるようにする | 対応済み（2026-07-20。画面定義書01 §8 に「Undo 5秒 / エラー8秒＋×で手動クローズ」を明記。エラー表示も帯からトーストへ統一） |

### 改善（要仕様整理 — 挙動・デザインの方針をオーナーと決めてから docs 反映 → 実装）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-02 | 2026-07-20 | セクション未設定（未分類）のタスクを開始したら、開始時刻が含まれるセクションへ自動で移動させたい（FB-16 と関連） | 対応済み（2026-07-20: FB-16 と合わせて F-113 として要求化し実装。要求定義書 §6.1・要件定義書 §3/§5.1・画面定義書01 §4.2・データモデル定義書 §4.4） |
| FB-03 | 2026-07-20 | マスタの変更で編集ボタンを都度押すのが面倒。常時インライン編集などの操作方式を検討する | 対応済み（2026-07-20。画面定義書03 §4「編集方式」を新設し、3タブとも値のセルをクリックしてその場編集へ。モードの色はカラーバー押下で選択が開き、候補のホバーで色名を吹き出し表示） |
| FB-06 | 2026-07-20 | セクション見出しの情報量を TCC を参考に増やしたい（何を表示するかの整理が必要） | 未対応 |
| FB-08 | 2026-07-20 | グローバルナビゲーション（デイリー／ルーチン／マスタ）を左サイドの Navigation Rail にする（全画面のレイアウトに影響。N-05「シンプルなUI」との整合を確認して決める） | 対応済み（2026-07-20。要件定義書 §4 に共通レイアウトとして方針を記載（ラベルのみ・アイコンなしで N-05 と整合、本文は中央寄せ維持、レールは画面固定）。画面定義書01〜04 のレイアウト図も更新。あわせてデイリーに h1 を追加） |
| FB-10 | 2026-07-20 | ルーチン一覧のソート軸を選べるようにする（開始想定・プロジェクト・モード・名前・繰り返しタイプ） | 対応済み（2026-07-20。要求定義書に F-306 を新設 → 要件定義書 §3・§5.3 → 画面定義書02 §3.1 の順に反映して実装） |
| FB-16 | 2026-07-20 | 実行中タスクの前に未実施タスクが並んでいる状態はおかしいので、未実施タスクを自動でセクション移動させたい。**要求レベルからの整理が必要**（自動で動かすことの是非・タイミング・対象。FB-02 と合わせて1つの要求として検討する） | 対応済み（2026-07-20: FB-02 と合わせて F-113 として要求化し実装。反映先は FB-02 の欄を参照） |

### 要求（新規・優先度変更）

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-11 | 2026-07-20 | タスクリストからのルーチン化（= F-305、現在 Could / Phase 3）が意外と重要。ルーチンへの改めての登録作業が重たいため、優先度の繰り上げを検討する | 対応済み（2026-07-20。要求定義書で F-305 を Should へ、要件定義書 §3 で Phase 2 へ変更のうえ実装。挙動は画面定義書01 §4.1・O-12） |

### 開発環境

| ID | 起票日 | 内容 | 状態 |
|---|---|---|---|
| FB-17 | 2026-07-20 | 本番と開発で異なるファビコンを設定し、タブで見分けられるようにする（現状は create-next-app 既定のままという未対応事項もある） | 対応済み（2026-07-20。要件定義書 §2.3 に方針を追記し、`src/app/icon.tsx` で生成。本番=紺 / ローカル=琥珀。create-next-app 既定の favicon.ico は削除） |
| FB-18 | 2026-07-20 | 本番DBのデータを過剰にならない範囲（マスタ・ルーチンなどタスク以外）で開発DBへ流し込めるようにする。シングルユーザーなので問題なく、タスクはルーチン展開で生成される | 対応済み（2026-07-20。要件定義書 §2.3 に方針を追記し、`npm run db:sync-masters` を用意。本番ダンプから sections/modes/projects/routines のみを開発DBへ流し込む。実データで検証済み） |
